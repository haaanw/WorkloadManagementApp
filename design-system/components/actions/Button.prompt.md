One-sentence: Tuwa's button — primary is the single ink-filled pill per screen (never accent), secondary a hairline rectangle, quiet a bare text action.

```jsx
<Button onClick={start}>Start session</Button>
<Button variant="secondary">Adjust load</Button>
<Button variant="quiet">Not now</Button>
```

Press inverts relief (sinks into a pocket). Sentence-case labels, 11px/500.