One-sentence: the verdict decision row — butted equal-weight cells inside one 12px container (nocebo guard: never pressure the athlete toward modifying).

```jsx
<KeyRow keys={[{title:'Adjust load'},{title:'Keep plan',role:'cta'}]} />
```