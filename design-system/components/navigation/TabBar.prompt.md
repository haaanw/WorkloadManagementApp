One-sentence: the Console tab bar — text-only labels, faint selection well, 1.5px accent tick above the active tab.

```jsx
<TabBar items={['Today','Workload','Recovery','Log','Profile']} active={tab} onSelect={setTab} />
```