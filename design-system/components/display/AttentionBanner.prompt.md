One-sentence: the shared attention banner (PR, load spike, fatigue, cycle) — card plane with a 2px zone rule inside the left edge.

```jsx
<AttentionBanner zone="caution" title="Load spike" message="Weekly load is 32% above your 4-week average." action="Review workload" />
```