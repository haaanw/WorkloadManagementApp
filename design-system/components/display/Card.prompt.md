One-sentence: the card plane — warm stone, hairline border, 12px corners, relief instead of shadow.

```jsx
<Card>…</Card>
<Card variant="raised">hero surface</Card>
<Card variant="debossed" padding="8px 12px">a pocket</Card>
```