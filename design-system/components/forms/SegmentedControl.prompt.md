One-sentence: butted hairline segmented control (time ranges, view switches) — active cell brightens with a 2px ink top-rule.

```jsx
<SegmentedControl options={['7D','28D','90D']} value={range} onChange={setRange} />
```