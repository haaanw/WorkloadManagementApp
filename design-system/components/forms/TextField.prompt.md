One-sentence: text input whose focus state thickens the ink hairline and debosses the field (accent never marks focus).

```jsx
<TextField label="Email" value={v} onChange={setV} placeholder="you@example.com" />
```