One-sentence: detent stepper — raised − / ＋ keys flanking a fixed-width debossed readout well (digits change, the stone never resizes).

```jsx
<Stepper value={80} onChange={setKg} step={2.5} unit="KG" />
```