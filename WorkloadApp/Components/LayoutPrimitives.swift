import SwiftUI

// v4.1 layout-recomposition primitives (owned by workstream WS2 — see
// .planning/orchestration/2026-07-20-v41-handoff.md). Pre-registered stub:
// the orchestrator created this file so parallel sessions never edit pbxproj.
//
// WS2 builds here: MetricCell (unit-superscript numeral cell with delta line,
// fixed-width dial values), RuledSectionHeader (micro-caps + trailing hairline
// rule), and any other composition primitives the recomposed screens share.
// All values from ColorTokens / Font.Tokens / Spacing / CornerTokens only.
