import SwiftUI

// v4.2 "Machined" form system (owned by workstream WS-B — see
// .planning/orchestration/2026-07-20-v42-handoff.md). Pre-registered stub: the
// orchestrator created this file so parallel sessions never edit pbxproj.
//
// WS-B builds here, on the `.raised` / `.debossed` Relief primitives (CardStyle.swift):
// InstrumentFormRow (56pt press-well row, drawn 1.5px tick-chevron that rotates on the
// spring), ReadoutWell (fixed-width debossed value pocket, mono + micro-caps unit),
// InlineOptionList (debossed channel holding butted raised option cells with drilled
// selection dots — replaces every stock iOS Menu), FormField (focus grows a debossed
// well + ink border), MachinedToggle (round polished knob in a debossed channel that
// turns ink when on, engraved index tick), and the quiet destructive row.
// All values from ColorTokens / Font.Tokens / Spacing / CornerTokens; motion via Motion.
