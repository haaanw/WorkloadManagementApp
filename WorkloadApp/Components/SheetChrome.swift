import SwiftUI

// v4.1 sheet-chrome primitives (owned by workstream WS3 — see
// .planning/orchestration/2026-07-20-v41-handoff.md). Pre-registered stub:
// the orchestrator created this file so parallel sessions never edit pbxproj.
//
// WS3 builds here: the instrument sheet header (micro-caps title bar + quiet
// caps Cancel/action slots replacing stock navigationTitle + glass buttons),
// applied across the app's sheets. All values from ColorTokens / Font.Tokens /
// Spacing / CornerTokens only; motion via Motion tokens.
