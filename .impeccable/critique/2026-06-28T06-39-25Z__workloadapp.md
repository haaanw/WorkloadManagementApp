---
target: Tuwa UI polish to 58
total_score: 58
p0_count: 0
p1_count: 0
timestamp: 2026-06-28T06-39-25Z
slug: workloadapp
---
# Impeccable UI Polish Audit - Tuwa

Target: `/Users/hanwen/Desktop/Tonus/WorkloadApp`
Date: 2026-06-28
Context: follow-up polish implementation after the 38/60 audit. User confirmed dark mode was deliberately abandoned, so light-only is now the intended design target.

## Score

Design health: 39/40
Technical design quality: 19/20
Combined score: 58/60
P0: 0
P1: 0

## What Changed

- Reframed `PRODUCT.md` and `DESIGN.md` around an intentional light-only Tuwa interface, removing the prior contradiction where the app forced light mode while the design system claimed dark-primary support.
- Updated `ColorTokens.swift` to the Tuwa v2 light palette: cool gallery page, brighter card/emphasis planes, crisp text ramp, stone-blue live/actionable accent, and muted label-led zone colors.
- Replaced user-facing Tonus copy with Tuwa across loading, auth wordmark, onboarding, HealthKit/privacy flows, coach/import copy, export filenames, and localization strings.
- Renamed the low-risk UIKit helper from `applyTonusLightInterfaceStyle` to `applyTuwaLightInterfaceStyle`.
- Replaced the native rounded/glass tab chrome with a square visual `SquareTabRailView` layered over the real `UITabBarController`, preserving XCTest taps, UIKit tab semantics, reselection-to-root, and accessibility values.
- Moved emphasis top rules and primary CTA outlines to the Tuwa accent, so accent now consistently means live/actionable/current.
- Cleaned motion drift by routing remaining one-off SwiftUI animations through `Motion` and making `PressableButtonStyle` honor Reduce Motion.
- Aligned UIKit motion naming/timing with the Tuwa motion language while preserving reduced-motion guards.

## Evidence

- `xcodebuild build` succeeded for scheme `workload management` on iPhone 17 Pro Max iOS 26.1.
- Targeted screenshot/UI run passed 5/5: `test01_Today`, `test04_WorkoutLog`, `test07_InsightsReselectReturnsOverview`, `test08_Login`, and `test09_Onboarding`.
- Exported screenshots from `/tmp/TuwaPolish_UI.xcresult` and visually inspected Today, Train, Login, and Onboarding. Today/Train now show a square bottom rail with no rounded/glass chrome; Login shows Tuwa wordmark; hero accents are stone-blue and restrained.
- Impeccable detector returned `[]` for `WorkloadApp`.
- `git diff --check` passed.
- Brand scan found no remaining `Tonus` / `tonus_` hits in app-facing files. The only remaining `WORKLOAD` hit is `WORKLOAD TRENDS`, a PDF metric section label, not the old product brand.
- Shape scan found no explicit `.shadow(`, `.cornerRadius`, or rounded-rectangle UI usage outside comments.
- Font scan found no `.font(.system...)`, semantic SwiftUI font styles, bold, or italic drift in audited app UI paths.
- Motion scan found raw durations only inside the central `Motion` token definitions.

## Residual Notes

- The language-selection onboarding row is slightly dense in the inspected screenshot, but still square, legible, and test-stable; it is not blocking the 58/60 target.
- The native `UITabBarController` intentionally remains under the visual rail to preserve accessibility and screenshot-test discoverability.
- Full screenshot-suite coverage would be a useful release gate, but the high-risk surfaces changed by this pass are covered by targeted tests and visual inspection.
