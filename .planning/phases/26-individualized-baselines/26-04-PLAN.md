---
phase: 26-individualized-baselines
plan: 04
type: execute
wave: 4
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - WorkloadAppTests/BaselineConvergenceReportTests.swift
  - .planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md
autonomous: false
requirements: [P26-REPORT, P26-CHECKPOINT]
must_haves:
  truths:
    - "D-04 / §7: a deterministic seeded convergence-report generator lives in the TEST target (BaselineConvergenceReportTests) — it drives the Plan-02 engine + Plan-03 bucketer over synthetic + realistic traces and emits a markdown artifact"
    - "Determinism (§7.2): NO Date.now (dates from a fixed anchor + day offsets), NO system RNG — noise uses the existing `ShadowMetrics.SplitMix64(seed:)` verbatim; a hash-equality test proves two runs with the same seed produce byte-identical markdown"
    - "§7.3 traces: stable, step-change, outlier-injection, gap-stretch, stale-repeat, rising-instability, realistic-HRV — each with KNOWN ground truth so the report shows tracking error (robust EWMA vs the incumbent 7-day mean)"
    - "§7.4 report panels: per-scenario series (day | raw y | μ EWMA | 7-day-mean incumbent | z | σ | cvRatio | cvLevel | confidence) + summary stats (tracking error robust-vs-incumbent, z mean/SD, day CV first fires, day confidence crosses 0.5/0.9) + a PASS/FAIL line per invariant"
    - "Invariants are also XCTAsserted (not just printed) so CI catches regressions: outlier barely moves μ, CV fires on rising-instability + stays quiet on clean, z~N(0,1) on stable, confidence ramps 14→60, gap/stale erode confidence without corrupting μ"
    - "§7.5 the artifact is written to .planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md (FileManager + String.write), with a temp-dir fallback if the repo path is unwritable (never hard-fails in sandboxed CI)"
    - "Scope fence: this plan registers NO predicting shadow arm and applies NO z→recovery mapping (D-01) — the report judges engine BEHAVIOR (convergence/robustness), not accuracy"
    - "Build green via real xcodebuild; the report artifact is committed/regenerable; HUMAN REVIEWS the report as the Phase 26 result checkpoint (D-04)"
  artifacts:
    - path: "WorkloadAppTests/BaselineConvergenceReportTests.swift"
      provides: "seeded deterministic generator: trace fixtures + prequential score/step loop + markdown emit + invariant asserts + hash-equality"
      contains: "SplitMix64"
    - path: ".planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md"
      provides: "the D-04 markdown result-checkpoint artifact (baseline-vs-raw tracking, z, CV firing, confidence ramp, robustness panels)"
      contains: "tracking"
  key_links:
    - from: "BaselineConvergenceReportTests"
      to: "BaselineEngine.score/step + DayBucketer (prequential loop)"
      via: "per-day score-then-step over seeded traces"
      pattern: "\\.score\\(|\\.step\\("
    - from: "BaselineConvergenceReportTests"
      to: "26-convergence-report.md artifact"
      via: "FileManager + String.write(to:atomically:encoding:)"
      pattern: "write\\(to:"
---

<objective>
Produce the Phase-26 result checkpoint (D-04): a **deterministic, seeded convergence-report generator** in the test target that drives the robust baseline engine (Plan 02) + day-bucketer (Plan 03) through synthetic and realistic signal traces with known ground truth, and emits a markdown artifact showing baseline-vs-raw tracking (robust EWMA vs the incumbent flat 7-day mean), prequential z behavior, Altini CV early-warning firing, the confidence ramp, and robustness to injected outliers / gaps / stale samples. The same invariants the report displays are also XCTAsserted so CI catches regressions, and a hash-equality test proves byte-reproducibility.

Purpose: give the user a reproducible, no-device artifact to JUDGE the substrate's BEHAVIOR (convergence + robustness, NOT accuracy — no z→recovery mapping, no shadow arm; D-01). This is the user's gated result-checkpoint for the phase, so the final task is `autonomous: false` and pauses for human review of the report.

Output: `BaselineConvergenceReportTests.swift` (generator + invariant asserts + hash-equality) and the generated `26-convergence-report.md` artifact — build-green via real xcodebuild, then a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-individualized-baselines/26-CONTEXT.md
@.planning/phases/26-individualized-baselines/26-RESEARCH.md
@CLAUDE.md
@.planning/phases/26-individualized-baselines/26-02-SUMMARY.md
@.planning/phases/26-individualized-baselines/26-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + prior plans — do not re-explore. -->

Seeded PRNG to reuse VERBATIM (the only allowed randomness source):
- WorkloadApp/Services/ShadowMetrics.swift:127 `ShadowMetrics.SplitMix64(seed: UInt64)` — a nested `struct SplitMix64: RandomNumberGenerator` with `mutating func next() -> UInt64`. Use it via Swift's RNG-consuming APIs (e.g. `Double.random(in:using:&rng)`) or a Box–Muller transform over `next()` for Gaussian noise. Each scenario uses a FIXED seed so the report is byte-reproducible. NEVER `SystemRandomNumberGenerator`.

Engine API (from Plan 02 — see 26-02-SUMMARY for final signatures):
- `BaselineEngine.SignalState` value mirror, `BaselineEngine.CVWarning`, `BaselineConstants` (half-lives, σ floors, etc.).
- `BaselineEngine.lambda(halfLifeDays:)`, `.score(state:observation:...)->(z:Double?, innovation:Double)`, `.step(state:observation:lambda:floor:huberK:)->SignalState`, `.cvUpdate(...)`, `.confidence(state:daysSinceLastBucket:)`.
- PREQUENTIAL LOOP (the discipline the report demonstrates): per day t ascending — score(state_{t-1}, y_t) → read z/cv → step(state_{t-1}, y_t) → state_t. score BEFORE step, always (§2.1).

Bucketer API (from Plan 03 — see 26-03-SUMMARY): `DayBucketer.BucketedDay`, `DayBucketer.bucketMorningWindow(...)`, `DayBucketer.bucketSleep(...)`. GAP days (value nil) are SKIPPED in the fold but advance daysSinceLastBucket.

Incumbent comparator (for the tracking-error panel): the flat 7-day mean = `RecoveryScoreEngine.computeBaseline(values:)` (:243) — call it READ-ONLY over the trailing raw window to produce the "incumbent" column. Do NOT modify it.

Trace fixtures (RESEARCH §7.3 — build deterministically, seeded):
  stable | step-change(+Δ at day k) | outlier(+6σ single spike) | gap-stretch(days k..k+5 GAP) | stale-repeat(same sample 4 days) | rising-instability(variance ramps, mean flat) | realistic-HRV(mean~50ms, weekly rhythm, occasional bad night).

Determinism rules (§7.2): synthetic dates from a fixed anchor `Date(timeIntervalSince1970: 0)` + `i*86400` (or Calendar day-add with a fixed UTC calendar); engine/bucketer already contain no time/RNG, so determinism is structural; the only RNG is seeded SplitMix64.

Artifact path (§7.5): `.planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md`. Resolve the repo root from `#filePath` parent traversal (or a `BASELINE_REPORT_DIR` env override); fallback to the test bundle's temp dir if unwritable so CI never hard-fails (print the resolved path).

This plan adds NO app-target file (test-only) → NO pbxproj edit (WorkloadAppTests is synchronized).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Seeded convergence-report generator — trace fixtures, prequential loop, markdown emit, invariant asserts, hash-equality</name>
  <files>WorkloadAppTests/BaselineConvergenceReportTests.swift, .planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md</files>
  <read_first>
    - WorkloadApp/Services/ShadowMetrics.swift (lines 124–185 — SplitMix64 usage + the existing seeded block-bootstrap as a determinism exemplar)
    - WorkloadApp/Services/BaselineEngine.swift (Plan 02 — the exact score/step/cvUpdate/confidence signatures + BaselineConstants)
    - WorkloadApp/Services/DayBucketer.swift (Plan 03 — BucketedDay + bucket functions)
    - WorkloadApp/Services/RecoveryScoreEngine.swift (lines 243–247 — computeBaseline incumbent comparator, READ-ONLY)
    - .planning/phases/26-individualized-baselines/26-RESEARCH.md §7 (lines 484–517 — fixtures, panels, artifact path, determinism)
  </read_first>
  <action>
    Create `WorkloadAppTests/BaselineConvergenceReportTests.swift` (`final class BaselineConvergenceReportTests: XCTestCase`, `@testable import workload_management`). It is a "test" only as a deterministic execution harness: it both ASSERTS invariants and WRITES the markdown report. Implement:
    (1) Trace fixtures (§7.3): for each scenario build a deterministic day-indexed `[(date: Date, value: Double?)]` series — dates from a fixed anchor (`Date(timeIntervalSince1970: 0)` + day offsets via a fixed UTC `Calendar`), values from a constant ground-truth + seeded Gaussian noise via `ShadowMetrics.SplitMix64(seed:)` (Box–Muller over `next()` or `Double.random(in:using:&rng)`). Scenarios: stable, step-change, outlier(+6σ), gap-stretch (insert nil/GAP days), stale-repeat (same value/date → buckets once, rest GAP), rising-instability (variance ramps, mean flat), realistic-HRV (mean ~50ms + weekly rhythm + occasional bad night). Where a scenario exercises bucketing, run the raw samples through `DayBucketer` first; otherwise feed day buckets directly.
    (2) Prequential loop: for each scenario, walk days ascending; on a valued day call `BaselineEngine.score(state_{t-1}, y_t)` then `BaselineEngine.step(...)` (score BEFORE step); on a GAP day skip the fold but advance daysSinceLastBucket; record per-day `raw y, μ(EWMA), incumbent 7-day-mean (RecoveryScoreEngine.computeBaseline over trailing window), z, σ, cvRatio, cvLevel, confidence`.
    (3) Markdown emit (§7.4): build a String with, per scenario, a series table of those columns + summary stats (mean|μ−truth| robust vs mean|7day−truth| incumbent to show the robustness win; z mean/SD on stable ≈0/≈1; day CV first reaches .elevated/.high; day confidence first crosses 0.5 and 0.9) + a `PASS/FAIL` line per invariant. Write it to `.planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md` via `FileManager`/`String.write(to:atomically:encoding:.utf8)`; resolve the repo path from `#filePath` parent traversal (or `ProcessInfo` `BASELINE_REPORT_DIR`), fallback to `NSTemporaryDirectory()` if unwritable (print the path either way). Create the `artifacts/` dir if missing.
    (4) Invariant XCTAsserts (CI regression guard — NOT just printed): outlier scenario μ-move ≤ k·σ-bounded (robust); rising-instability cvLevel reaches `.high` while clean stays `.normal`; stable-scenario z mean≈0 / SD≈1 (within tolerance); confidence ≈0 at count≤14 and ≈1 by count≥60; gap/stale scenarios DON'T corrupt μ but DO erode confidence; robust tracking error ≤ incumbent on the step-change/outlier scenarios.
    (5) Hash-equality (§7.2 determinism): a test that generates the report String TWICE with the same seeds and asserts the two Strings are byte-identical (e.g. equal `.hashValue` or full `==`), proving reproducibility.
    NO predicting shadow arm, NO z→recovery mapping (D-01 — behavior only). NO pbxproj edit (test-only, synchronized group). All randomness via seeded SplitMix64; no `.now`, no system RNG.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' -only-testing:WorkloadAppTests/BaselineConvergenceReportTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `BaselineConvergenceReportTests` builds the seven §7.3 traces deterministically (fixed-anchor dates + seeded SplitMix64), runs the prequential score-then-step loop, and emits the markdown report.
    - The report contains, per scenario, the §7.4 series table + summary stats + per-invariant PASS/FAIL; the robust-vs-incumbent tracking-error comparison is present.
    - Invariants are XCTAsserted (outlier robustness, CV fires/quiet, stable z~N(0,1), confidence ramp 14→60, gap/stale erode confidence without corrupting μ, robust tracking ≤ incumbent).
    - A hash-equality test proves two same-seed runs are byte-identical.
    - `grep -nE "\.now|SystemRandomNumberGenerator" WorkloadAppTests/BaselineConvergenceReportTests.swift` returns nothing; `grep -n "SplitMix64" ...` returns a match.
    - The artifact `.planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md` is written (or temp-dir fallback printed); xcodebuild test exits 0.
  </acceptance_criteria>
  <done>A deterministic seeded generator drives the engine + bucketer through synthetic and realistic traces, emits the D-04 markdown convergence report, asserts all behavior invariants for CI, and proves byte-reproducibility — with no shadow arm, no z→recovery mapping, and no live behavior change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 26 result checkpoint — human reviews the convergence report (D-04)</name>
  <what-built>
    A robust individualized-baseline SUBSTRATE, parallel and gated OFF (the live flat 7-day-mean recovery score is unchanged): a local-only `BaselineState` @Model (Plan 01), a pure `BaselineEngine` with EWMA/Welford/MAD/Huber + prequential no-leak z + Altini CV + 0–1 confidence (Plan 02), a pure `DayBucketer` + additive RHR history fetch + a machine-enforced tier fence (Plan 03), and a deterministic seeded convergence-report generator (this plan). All unit tests + the tier-fence + hash-equality tests are green via real xcodebuild. No predicting shadow arm and no z→recovery mapping were added (D-01 substrate-only) — Phase 28 fusion / Phase 29 shadow-run own that.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md`.
    2. Confirm the ROBUSTNESS story across scenarios:
       - Stable: robust μ tracks ground truth; z mean ≈ 0, SD ≈ 1; CV stays `.normal`; confidence ramps from ~0 (≤14 days) toward ~1 (~60 days).
       - Step-change: baseline re-centers within ~the half-life; z spikes then settles; no overshoot.
       - Outlier (+6σ): Huber barely moves μ (compare the robust column vs the incumbent 7-day-mean column, which jumps more) — this is the headline robustness win.
       - Gap-stretch & stale-repeat: μ is NOT corrupted across gaps/stale, but confidence drops (no carry-forward, no fake stability).
       - Rising-instability: CV escalates `.elevated`→`.high` even though the mean is flat (the Altini "variability in variability" demonstration).
       - Realistic-HRV: end-to-end sanity (tracking + z distribution + CV + confidence together look reasonable).
    3. Confirm every per-invariant line reads PASS.
    4. Sanity-check the tunable constants used (half-lives 7/10/7, huberK 1.5, σ floors, CV thresholds, confidence 14→60/τ≈2) — if the report shows the baseline chasing outliers, flag dropping huberK to 1.345; if CV over/under-fires, flag the threshold tweak. These are tunable named constants the report exists to calibrate.
    5. Confirm the live recovery score is unchanged (BaselineTierFenceTests is green — the flat 7-day mean is still the live source; the substrate is gated OFF).
    Approve to mark Phase 26 done, or describe any constant re-tune / scenario you want added (a follow-up gap-closure plan can adjust constants and regenerate the deterministic report).
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 26, or describe the constant adjustments / additional scenarios you want before approving.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report file write → filesystem | The generator writes a markdown file under the repo `.planning/` path with a temp-dir fallback; no network, no secrets, no untrusted input. |
| Determinism / reproducibility | The only randomness is seeded SplitMix64; no Date.now / system RNG — the report is byte-reproducible (a falsified "it converged" cannot hide behind nondeterminism). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-26-09 | Repudiation | Non-reproducible / cherry-picked report | mitigate | Seeded SplitMix64 + fixed-anchor dates + hash-equality test ⇒ byte-reproducible; engine/bucketer have zero time/RNG. |
| T-26-10 | Tampering | Report-generator accidentally enabling live behavior / a shadow arm | mitigate | Test-target only; no app file added; NO shadow-arm registration, NO z→recovery mapping (D-01); tier fence (Plan 03) still green. |
| T-26-11 | Denial of Service | Unwritable artifact path in sandboxed CI | accept/mitigate | FileManager write falls back to NSTemporaryDirectory() (prints path); test never hard-fails on path alone. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (Foundation/XCTest + in-repo SplitMix64 only). |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/BaselineConvergenceReportTests` exits 0 on the iPhone 17 Pro Max sim (id=8E872500-703D-4292-9758-38ADFCCFB126).
- Determinism: `grep -nE "\.now|SystemRandomNumberGenerator" WorkloadAppTests/BaselineConvergenceReportTests.swift` returns nothing; SplitMix64 is used; the hash-equality test passes.
- Artifact exists at `.planning/phases/26-individualized-baselines/artifacts/26-convergence-report.md` (or temp-dir path printed) and contains the per-scenario panels + PASS/FAIL invariants.
- Scope: no shadow arm registered (ShadowPredictor.registeredArms unchanged), no z→recovery mapping; the BaselineTierFenceTests (Plan 03) remain green.
- The human-verify checkpoint is the Phase 26 result gate (D-04) — `autonomous: false`.
</verification>

<success_criteria>
- A deterministic, seeded convergence report (markdown artifact) demonstrates baseline-vs-raw tracking, prequential z behavior, Altini CV firing, the confidence ramp, and robustness to outliers/gaps/stale — judged on BEHAVIOR, not accuracy (D-01/D-04).
- All behavior invariants are XCTAsserted (CI-regressable) and the report is byte-reproducible (hash-equality).
- No predicting shadow arm and no z→recovery mapping were added; the live recovery score is unchanged.
- The human reviews the report as the Phase 26 result checkpoint and approves (or requests a constant re-tune + regenerate).
</success_criteria>

<output>
Create `.planning/phases/26-individualized-baselines/26-04-SUMMARY.md` when done (record the seeds used per scenario, the resolved artifact path, the headline robust-vs-incumbent tracking-error numbers, which invariants passed, and any constant the human asked to re-tune).
</output>
</content>
