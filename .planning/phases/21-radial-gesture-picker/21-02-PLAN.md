---
phase: 21-radial-gesture-picker
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift
  - WorkloadApp/Views/Coach/TemplateEditorSheet.swift
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "C-06a: ActiveWorkoutSheet's sport-type segmented Picker is replaced by RadialPicker(selection: $sportType, ...) and its session-type segmented Picker by RadialPicker(selection: $sessionType, ...)"
    - "C-06a-onchange: Changing sportType via the radial picker still resets sessionType via defaultSessionType(for:) (the existing .onChange behavior is preserved)"
    - "C-06b: Coach TemplateEditorSheet's sport + session segmented Pickers are replaced by RadialPicker instances bound to the same @State sportType/sessionType"
    - "No data flow change: bindings, save logic, prefill (loadTemplate/loadPrescription), and ExercisePickerView wiring are untouched"
    - "Both sheets retain VStack(spacing: 16) layout and 16pt padding; no segmented .pickerStyle remains for sport/session in either file"
    - "The stale duplicate Views/TemplateEditorSheet.swift is NOT modified (only the in-build Coach copy is edited)"
  artifacts:
    - path: "WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift"
      provides: "Sport + session RadialPicker integration; preserved defaultSessionType onChange"
      contains: "RadialPicker"
    - path: "WorkloadApp/Views/Coach/TemplateEditorSheet.swift"
      provides: "Sport + session RadialPicker integration"
      contains: "RadialPicker"
  key_links:
    - from: "ActiveWorkoutSheet.$sportType"
      to: "RadialPicker(selection:)"
      via: "drop-in binding replacement of the segmented Picker; .onChange(of: sportType) re-attached to keep defaultSessionType reset"
      pattern: "RadialPicker.*sportType"
    - from: "TemplateEditorSheet.$sportType / $sessionType"
      to: "RadialPicker(selection:)"
      via: "drop-in binding replacement of the two segmented Pickers"
      pattern: "RadialPicker"
---

<objective>
Wire the `RadialPicker` from Plan 01 into the two target sheets, replacing the four `.pickerStyle(.segmented)` selectors (sport + session in each) with `RadialPicker` instances bound to the existing `@State` properties. This satisfies ROADMAP criterion 6 ("Works in both TemplateEditorSheet and ActiveWorkoutSheet"). The integration is a pure view-layer swap: bindings, save/prefill logic, and the `defaultSessionType(for:)` reset behavior are preserved exactly. Only the in-build Coach `TemplateEditorSheet.swift` is edited; the stale duplicate `Views/TemplateEditorSheet.swift` is left untouched.

Output: edited `ActiveWorkoutSheet.swift` and `Views/Coach/TemplateEditorSheet.swift`, building and running with the radial picker live in both sheets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-radial-gesture-picker/21-CONTEXT.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Exact integration sites. Extracted from codebase — do not re-explore. -->

From WorkloadApp/Components/RadialPicker.swift (Plan 01):
- `RadialPicker<Option: RadialSelectable>(selection: Binding<Option>, title: LocalizedStringKey, diameter: CGFloat = 240)`. `SportType` and `SessionType` both conform to `RadialSelectable`.

From WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift:
- `@State private var sportType: SportType = .lifting` (line 14), `@State private var sessionType: SessionType = .strength` (line 15)
- Lines 51-66: the two segmented Pickers inside `VStack(spacing: 16)` (line 47) with `.padding(.horizontal, 16)` / `.padding(.vertical, 16)` (lines 75-76), `.background(ColorTokens.surface)`:
  - Sport Picker (51-56) has `.onChange(of: sportType) { _, newSport in sessionType = defaultSessionType(for: newSport) }` (57-59)
  - Session Picker (61-66)
- `private func defaultSessionType(for sport: SportType) -> SessionType` (line 331) — MUST keep being called when sport changes.
- Bindings flow into the save path (lines 155-156, 462-465) and prefill (loadTemplate ~379, loadPrescription ~345) — DO NOT touch these.

From WorkloadApp/Views/Coach/TemplateEditorSheet.swift (the IN-BUILD copy, referenced in project.pbxproj `Coach` group):
- `@State private var sportType: SportType = .lifting` (line 13), `@State private var sessionType: SessionType = .strength` (line 14)
- Lines 36-48: two segmented Pickers inside `VStack(spacing: 16)` (line 30), `.padding(.horizontal, 16)` / `.padding(.vertical, 16)` (56-57), `.background(ColorTokens.surface)`. NO onChange. Bindings flow into save (163-164, 174-175) and loadTemplate (131-132) — DO NOT touch.

WARNING: `WorkloadApp/Views/TemplateEditorSheet.swift` is a STALE duplicate NOT in the build target. Do NOT edit it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sport + session segmented Pickers in ActiveWorkoutSheet, preserving the defaultSessionType reset</name>
  <files>WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift</files>
  <read_first>
    - WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift lines 45-76 (the VStack with both Pickers + padding/background) and line 331 (defaultSessionType)
    - WorkloadApp/Components/RadialPicker.swift (init signature, RadialSelectable conformance)
    - 21-CONTEXT.md Integration Points
  </read_first>
  <behavior>
    - The sport-type segmented Picker (lines 51-56) is replaced by `RadialPicker(selection: $sportType, title: "Sport Type")`
    - The session-type segmented Picker (lines 61-66) is replaced by `RadialPicker(selection: $sessionType, title: "Session Type")`
    - Changing the sport via the radial picker still resets `sessionType` to `defaultSessionType(for: newSport)` — the `.onChange(of: sportType)` is retained (attached to the sport RadialPicker or kept on the enclosing VStack)
    - No `.pickerStyle(.segmented)` for sport/session remains in the file
    - The enclosing `VStack(spacing: 16)`, 16pt padding, and `ColorTokens.surface` background are unchanged; all other view content (TextField, TimelineView timer, exercise list) is unchanged
    - Save/prefill bindings are byte-for-byte unchanged
  </behavior>
  <action>
    Replace the two `Picker(...) { ForEach... }.pickerStyle(.segmented)` blocks with the two `RadialPicker(selection:title:)` calls. Keep the `.onChange(of: sportType) { _, newSport in sessionType = defaultSessionType(for: newSport) }` — attach it to the sport `RadialPicker` (or leave it on the VStack) so the reset still fires. Use `LocalizedStringKey` titles matching the originals ("Sport Type", "Session Type"). Do not change `defaultSessionType`, the save block, or the prefill methods.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && grep -c "RadialPicker" "../WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift" && ! grep -q "pickerStyle(.segmented)" "../WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift" && echo "no-segmented-OK" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ActiveWorkoutSheet.swift contains two `RadialPicker` instances bound to `$sportType` and `$sessionType`
    - No `.pickerStyle(.segmented)` remains in the file
    - The `defaultSessionType(for:)` reset still runs when sport changes (onChange retained)
    - `xcodebuild build` succeeds
  </acceptance_criteria>
  <done>ActiveWorkoutSheet uses the radial picker for sport + session; sport->session reset preserved; builds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace sport + session segmented Pickers in the in-build Coach TemplateEditorSheet</name>
  <files>WorkloadApp/Views/Coach/TemplateEditorSheet.swift</files>
  <read_first>
    - WorkloadApp/Views/Coach/TemplateEditorSheet.swift lines 30-58 (VStack with both Pickers + padding/background)
    - WorkloadApp/Components/RadialPicker.swift (init signature)
    - 21-CONTEXT.md Integration Points + Assumptions #2 (this is the in-build copy)
  </read_first>
  <behavior>
    - The sport segmented Picker (lines 36-41) is replaced by `RadialPicker(selection: $sportType, title: "Sport")`
    - The session segmented Picker (lines 43-48) is replaced by `RadialPicker(selection: $sessionType, title: "Type")`
    - No onChange existed here, so it is a straight swap; the enclosing `VStack(spacing: 16)`, padding, background, TextFields, and notes field are unchanged
    - Save (163-164, 174-175) and loadTemplate (131-132) bindings unchanged
    - The stale `Views/TemplateEditorSheet.swift` is NOT touched
  </behavior>
  <action>
    Replace the two segmented `Picker` blocks in `Views/Coach/TemplateEditorSheet.swift` with `RadialPicker(selection: $sportType, title: "Sport")` and `RadialPicker(selection: $sessionType, title: "Type")`. Do not alter the template name TextField, notes TextField, save logic, or loadTemplate. Confirm the file edited is the Coach copy (path contains `/Views/Coach/`).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && grep -c "RadialPicker" "../WorkloadApp/Views/Coach/TemplateEditorSheet.swift" && ! grep -q "pickerStyle(.segmented)" "../WorkloadApp/Views/Coach/TemplateEditorSheet.swift" && echo "no-segmented-OK" && git -C "/Users/hanwen/Desktop/Tonus" diff --name-only -- "WorkloadApp/Views/TemplateEditorSheet.swift" | grep -q . && echo "STALE-DUP-MODIFIED-FAIL" || echo "stale-dup-untouched-OK" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Views/Coach/TemplateEditorSheet.swift contains two `RadialPicker` instances bound to `$sportType` and `$sessionType`
    - No `.pickerStyle(.segmented)` remains in that file
    - `Views/TemplateEditorSheet.swift` (stale duplicate) is unmodified
    - `xcodebuild build` succeeds
  </acceptance_criteria>
  <done>Coach TemplateEditorSheet uses the radial picker for sport + session; stale duplicate untouched; builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Picker selection -> existing save path | The radial picker writes the same `@State` enums the segmented pickers wrote; the downstream save/sync path is unchanged. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | Editing the wrong (stale) TemplateEditorSheet | mitigate | Plan edits only `Views/Coach/TemplateEditorSheet.swift` (the .pbxproj-referenced copy); a verify step asserts `Views/TemplateEditorSheet.swift` is untouched in git diff. |
| T-21-05 | Repudiation | Lost defaultSessionType reset behavior | mitigate | The `.onChange(of: sportType)` reset is explicitly retained; acceptance criteria assert it still fires when sport changes. |
</threat_model>

<verification>
- `xcodebuild build` exits 0 after both integrations.
- `grep "pickerStyle(.segmented)"` returns nothing in either edited file (for sport/session).
- `git diff --name-only` does NOT include `WorkloadApp/Views/TemplateEditorSheet.swift` (stale duplicate untouched).
- Manual smoke (run skill / simulator): long-press the sport tile in ActiveWorkoutSheet and in Coach TemplateEditorSheet -> ring opens, drag highlights with haptic, release selects; changing sport in ActiveWorkoutSheet resets session type.
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- RadialPicker is live in BOTH ActiveWorkoutSheet and the in-build Coach TemplateEditorSheet for sport + session type (ROADMAP criterion 6).
- The `defaultSessionType(for:)` reset behavior in ActiveWorkoutSheet is preserved.
- No data-flow, save, prefill, or schema change; stale duplicate sheet untouched.
- Both files build clean.
</success_criteria>

<output>
Create `.planning/phases/21-radial-gesture-picker/21-02-SUMMARY.md` when done.
</output>
