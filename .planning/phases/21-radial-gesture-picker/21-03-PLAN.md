---
phase: 21-radial-gesture-picker
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - WorkloadAppTests/RadialPickerInteractionTests.swift
  - workload management/workload management.xcodeproj/project.pbxproj
  - DESIGN.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "C-08-verify: An automated check confirms RadialPicker.swift contains no RoundedRectangle, no .shadow(, no ColorTokens.accent, no .system( font"
    - "C-01-verify: Behavior-level tests assert commit-on-highlight sets the binding and commit-in-deadzone/outside-cancel leaves the binding unchanged (driving RadialRingGeometry + a small selection-resolver), proving criteria 4 + 5 without UI automation"
    - "C-genericity: A test instantiates the selection logic for BOTH SportType (7 cases) and SessionType (5 cases), proving the component is generic over any RadialSelectable (criterion 1 + 7)"
    - "Font discrepancy closed in docs: DESIGN.md and ROADMAP Phase 21 criterion 8 reference General Sans (not Alpino), with a note that the Alpino decision was superseded 2026-05-11"
    - "ROADMAP Phase 21 plan list reflects the three real Phase 21 plans (currently it erroneously lists 17-xx plans)"
  artifacts:
    - path: "WorkloadAppTests/RadialPickerInteractionTests.swift"
      provides: "Behavior tests for commit/cancel selection resolution across both enums; DESIGN.md source-compliance assertions"
      contains: "SessionType"
    - path: "DESIGN.md"
      provides: "Corrected font reference (General Sans, Alpino superseded) in any Phase-21-relevant wording"
      contains: "General Sans"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 criterion 8 says General Sans; Phase 21 Plans list corrected to 21-01/02/03"
      contains: "21-01-PLAN.md"
  key_links:
    - from: "RadialPickerInteractionTests"
      to: "RadialRingGeometry + selection resolution"
      via: "drives highlightIndex(for:) with crafted points to assert commit vs cancel outcomes for both enums"
      pattern: "highlightIndex|commit|cancel"
---

<objective>
Lock in Phase 21 with behavior-level tests that prove the commit/cancel selection contract (criteria 4, 5) and genericity (criteria 1, 7) without flaky UI automation, add an automated DESIGN.md-compliance assertion over the component source, and close the documentation debt: correct the stale "Alpino" font reference to "General Sans" in DESIGN.md and ROADMAP criterion 8, and fix the ROADMAP Phase 21 Plans list (which currently mislabels the plans as 17-xx). This is the verification + docs-hygiene plan.

Output: `WorkloadAppTests/RadialPickerInteractionTests.swift` (registered in the test target), corrected `DESIGN.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-radial-gesture-picker/21-CONTEXT.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Contracts to verify against. Extracted from codebase / Plan 01. -->

From WorkloadApp/Components/RadialPicker.swift (Plan 01):
- `protocol RadialSelectable: CaseIterable, Hashable, Identifiable { var displayName: String { get }; var radialIcon: String { get } }`
- `private struct RadialRingGeometry { let count; let diameter; let deadZoneRadius; func angle(forIndex:); func point(forIndex:radius:); func highlightIndex(for point: CGPoint) -> Int? }` — returns nil for dead zone / beyond cancel radius. (If declared `private`, Plan 03 Task 1 must relax it to internal or `@testable`-accessible so the test target can reach it. Mirror however Plan 01 exposed it to RadialPickerGeometryTests.)
- `RadialPicker<Option: RadialSelectable>(selection:title:diameter:)`.

From Plan 01 test file:
- `WorkloadAppTests/RadialPickerGeometryTests.swift` — XCTest, `@testable import workload_management`, geometry-only assertions. Mirror its import + style.

Docs to correct:
- `DESIGN.md` — Decisions Log rows: 2026-05-10 "Migrated from DM Sans to Alpino", 2026-05-11 "Migrated from Alpino to General Sans" (the LATER one is authoritative). Typography section already says "General Sans". The font is currently correct in the system body; the fix is to ensure no Phase-21-facing wording implies Alpino is current, and (optional) annotate the 2026-05-10 row as superseded.
- `.planning/ROADMAP.md` — Phase 21 block: success criterion 8 reads "...Alpino font, 8pt grid" (line ~182); the Plans list under Phase 21 erroneously contains `17-01-PLAN.md ... 17-03-PLAN.md` (copy-paste from Phase 17, lines ~188-190).

Verified font reality (from code): `App/WorkloadApp.swift` registers GeneralSans-Regular/-Medium; `Utilities/FontTokens.swift` builds every Font.Tokens.* from GeneralSans-* (Noto Sans SC cascade); no Alpino asset exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Behavior + genericity + source-compliance tests for RadialPicker</name>
  <files>WorkloadAppTests/RadialPickerInteractionTests.swift, WorkloadApp/Components/RadialPicker.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - WorkloadApp/Components/RadialPicker.swift (RadialRingGeometry signature + access level; RadialSelectable; how Plan 01 exposed geometry to tests)
    - WorkloadAppTests/RadialPickerGeometryTests.swift (import + XCTest style to mirror)
    - workload management/workload management.xcodeproj/project.pbxproj (RadialPickerGeometryTests.swift entries as the template for adding the new test file to WorkloadAppTests)
    - 21-CONTEXT.md D-07 (commit vs cancel rule), D-01/D-04 (genericity over both enums)
  </read_first>
  <behavior>
    - A test resolves selection for a point inside a known sector at a valid radius -> highlightIndex returns that index -> mapping `Array(Option.allCases)[index]` yields the expected option (a "commit" path)
    - A test for a point inside the dead-zone radius -> highlightIndex == nil -> "cancel" (no commit)
    - A test for a point beyond the cancel radius -> highlightIndex == nil -> "cancel"
    - The above are run for BOTH `SportType` (count 7) and `SessionType` (count 5), proving the geometry + selection works for any RadialSelectable count (criteria 1, 7)
    - A source-compliance test asserts the RadialPicker.swift source string contains none of: "RoundedRectangle", ".shadow(", "ColorTokens.accent", ".system(" (DESIGN.md criterion 8). (Read the file via its bundle path or a relative path resolved from #filePath; if file-read in the test bundle is impractical, implement this as a verify-block grep instead — see verify.)
    - If Plan 01 left RadialRingGeometry `private`, this task relaxes it to internal (or adds a test seam) so the test target compiles
  </behavior>
  <action>
    Add `WorkloadAppTests/RadialPickerInteractionTests.swift` (`@testable import workload_management`, XCTest). Write parameterized-style tests (helper taking `count` + expected option array) exercising `RadialRingGeometry.highlightIndex(for:)` for both `SportType.allCases` (7) and `SessionType.allCases` (5): assert a crafted in-sector point at a mid radius resolves to the expected index/option, a center point (within deadZoneRadius) resolves to nil (cancel), and a far point (beyond cancel radius) resolves to nil (cancel). Add a genericity assertion that `RadialPicker<SportType>` and `RadialPicker<SessionType>` both type-check (a compile-time witness: a function returning each, or `_ = RadialPicker(selection: .constant(.lifting), title: "x")`). For the source-compliance check, prefer a grep in the verify block (most robust); only embed an in-test file read if straightforward. If `RadialRingGeometry` is `private` in Plan 01, change it to internal in RadialPicker.swift (minimal edit) so the test can reach it — keep all other access levels. Register the new test file in the WorkloadAppTests target in project.pbxproj (mirror RadialPickerGeometryTests.swift entries).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && grep -c "RadialPickerInteractionTests.swift" "workload management.xcodeproj/project.pbxproj" && grep -Lq "RoundedRectangle\|\.shadow(\|ColorTokens.accent\|\.system(" "../WorkloadApp/Components/RadialPicker.swift" && echo "DESIGN-COMPLIANT-OK" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/RadialPickerInteractionTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `RadialPickerInteractionTests` asserts commit (in-sector -> correct option) and cancel (dead-zone -> nil, beyond-cancel -> nil) for both SportType and SessionType
    - A genericity witness for `RadialPicker<SportType>` and `RadialPicker<SessionType>` compiles
    - `RadialPicker.swift` source contains none of RoundedRectangle / .shadow( / ColorTokens.accent / .system( (grep clean)
    - New test file registered in the WorkloadAppTests target; `xcodebuild test` exits 0
  </acceptance_criteria>
  <done>Commit/cancel behavior and genericity are verified for both enums via fast pure tests; component source is asserted DESIGN.md-compliant.</done>
</task>

<task type="auto">
  <name>Task 2: Close the Alpino->General Sans font discrepancy and fix the ROADMAP Phase 21 plan list</name>
  <files>DESIGN.md, .planning/ROADMAP.md</files>
  <read_first>
    - DESIGN.md Decisions Log (last two rows: 2026-05-10 Alpino, 2026-05-11 General Sans) + Typography section
    - .planning/ROADMAP.md Phase 21 block: success criterion 8 (~line 182) and the Plans list (~lines 188-190, currently 17-xx)
    - 21-CONTEXT.md "Font Discrepancy Finding"
  </read_first>
  <behavior>
    - ROADMAP Phase 21 success criterion 8 reads "...0pt corners, no shadows, General Sans font, 8pt grid" (Alpino -> General Sans)
    - ROADMAP Phase 21 Plans list reflects the three actual plans: 21-01-PLAN.md (component + protocol + enum conformance + pbxproj), 21-02-PLAN.md (integration into both sheets), 21-03-PLAN.md (tests + docs) — replacing the erroneous 17-xx entries
    - DESIGN.md makes clear General Sans is current and the Alpino step was superseded (annotate the 2026-05-10 row as "superseded 2026-05-11" or leave the authoritative 2026-05-11 row intact and ensure no body wording implies Alpino is live)
    - No other DESIGN.md or ROADMAP content is altered
  </behavior>
  <action>
    In `.planning/ROADMAP.md`: change Phase 21 criterion 8 "Alpino font" to "General Sans font". Replace the mistaken `17-01-PLAN.md ... 17-03-PLAN.md` lines in the Phase 21 Plans list with the three Phase 21 plan descriptions above (use `- [ ]` checkboxes, matching the list style). In `DESIGN.md`: verify the Typography section already says General Sans (it does) and add a short superseded note to the 2026-05-10 Alpino decisions-log row (e.g. append "(superseded 2026-05-11 -> General Sans)") so the discrepancy can never be misread as current. Make only these targeted edits.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus" && (grep -n "Alpino font" .planning/ROADMAP.md && echo "ALPINO-STILL-PRESENT-FAIL" || echo "no-alpino-font-OK") && grep -n "21-01-PLAN.md\|21-02-PLAN.md\|21-03-PLAN.md" .planning/ROADMAP.md && grep -n "General Sans font" .planning/ROADMAP.md && grep -n "superseded" DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 21 criterion 8 says "General Sans font"; no "Alpino font" remains in the Phase 21 block
    - The Phase 21 Plans list lists 21-01-PLAN.md, 21-02-PLAN.md, 21-03-PLAN.md (not 17-xx)
    - `DESIGN.md` annotates the 2026-05-10 Alpino row as superseded; Typography section still says General Sans
    - No unrelated content in either doc is changed
  </acceptance_criteria>
  <done>Font discrepancy closed in docs (General Sans is unambiguously current); ROADMAP Phase 21 plan list corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> component internals | Tests use `@testable import` to reach `RadialRingGeometry`; this is a test-only seam, not a production surface. |
| Docs edits | DESIGN.md / ROADMAP.md are planning docs, not shipped code; edits carry no runtime risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | Relaxing RadialRingGeometry access for tests | accept | At most a `private`->internal change within the module; no public API exposed; no new target/dependency. |
| T-21-07 | Repudiation | Doc/code font drift | mitigate | This plan removes the Alpino/General Sans ambiguity so future agents cannot act on the stale reference; the source compliance test pins the real font usage via Font.Tokens. |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/RadialPickerInteractionTests` exits 0 (commit/cancel + genericity for both enums).
- `grep` confirms RadialPicker.swift has no RoundedRectangle / .shadow( / ColorTokens.accent / .system(.
- `.planning/ROADMAP.md` Phase 21 block contains "General Sans font" and the 21-0x plan list, and no "Alpino font".
- `DESIGN.md` marks the 2026-05-10 Alpino decision superseded.
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- Commit-on-highlight and cancel-on-deadzone/outside behavior (criteria 4, 5) verified by fast pure tests for both SportType and SessionType (criteria 1, 7).
- Component source asserted DESIGN.md-compliant (criterion 8).
- The Alpino vs General Sans discrepancy is closed in DESIGN.md and ROADMAP; ROADMAP Phase 21 plan list fixed.
</success_criteria>

<output>
Create `.planning/phases/21-radial-gesture-picker/21-03-SUMMARY.md` when done.
</output>