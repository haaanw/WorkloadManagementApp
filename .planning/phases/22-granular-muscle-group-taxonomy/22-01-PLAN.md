---
phase: 22-granular-muscle-group-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/Enums.swift
  - WorkloadAppTests/MuscleGroupTaxonomyTests.swift
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "MuscleGroup gains ~26 anatomically specific cases covering every ROADMAP-named muscle, each with a stable lowerCamelCase rawValue"
    - "All 7 original cases (chest, back, legs, shoulders, arms, core, fullBody) are RETAINED with their exact original rawValues — MuscleGroup(rawValue: \"legs\") still decodes (non-destructive backward compat)"
    - "A BodyRegion enum exists (legs, back, chest, shoulders, arms, core, fullBody) conforming String/Codable/CaseIterable/Identifiable with displayName + systemImage"
    - "MuscleGroup has a computed `var region: BodyRegion` mapping EVERY case (old + new) to exactly one region"
    - "MuscleGroup gains `var systemImage: String` (satisfies CLAUDE.md enum convention, currently absent)"
    - "A pure helper `MuscleGroup.suggestedSpecific(for coarse: MuscleGroup) -> MuscleGroup` returns the documented default specific value per region (legs->quads, chest->pecsLower, back->lats, shoulders->lateralDelts, arms->biceps, core->rectusAbdominis, fullBody->fullBody)"
    - "MuscleGroup still conforms to String, Codable, CaseIterable, Identifiable with `id == rawValue`; every case has a displayName via String(localized: \"muscleGroup.<case>\")"
    - "No SwiftData VersionedSchema/SchemaMigrationPlan is introduced — change is purely additive enum cases over a String rawValue"
  artifacts:
    - path: "WorkloadApp/Models/Enums.swift"
      provides: "Expanded MuscleGroup enum, BodyRegion enum, region mapping, systemImage, suggestedSpecific helper"
      contains: "enum BodyRegion"
    - path: "WorkloadAppTests/MuscleGroupTaxonomyTests.swift"
      provides: "Tests for rawValue stability, region totality, suggestedSpecific mapping, old-value decode"
      contains: "suggestedSpecific"
  key_links:
    - from: "MuscleGroup.region"
      to: "BodyRegion"
      via: "computed property switch over all cases"
      pattern: "var region: BodyRegion"
---

<objective>
Expand the `MuscleGroup` enum in `WorkloadApp/Models/Enums.swift` from 7 coarse values to a ~33-value taxonomy (~26 new anatomically specific muscles + all 7 original coarse values RETAINED as decodable aliases), organized by a new `BodyRegion` enum. Add the `systemImage` property the CLAUDE.md convention requires (currently missing), a computed `region` mapping, and a pure `suggestedSpecific(for:)` default-mapping helper used later by the picker's re-specify UX.

This plan is **pure model + tests only** — no UI, no SwiftData migration, no sync. It is the foundation Plans 02 (UI) and 03 (serialization/localization) build on. The single most important guarantee: every original rawValue (`chest`, `back`, `legs`, `shoulders`, `arms`, `core`, `fullBody`) remains a valid case, so every existing SwiftData row and `groupsJson` blob continues to decode unchanged — backward compatibility comes from retention, not rewriting.

Output: modified `Enums.swift` (`MuscleGroup` expanded; new `BodyRegion`; `region`, `systemImage`, `suggestedSpecific`) plus a new test file asserting rawValue stability, region totality, the default mapping, and old-value decode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-granular-muscle-group-taxonomy/22-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Current contracts the executor extends. Extracted from codebase — do not re-explore. -->

From WorkloadApp/Models/Enums.swift (line 169) — CURRENT:
```swift
enum MuscleGroup: String, Codable, CaseIterable, Identifiable {
    case chest, back, legs, shoulders, arms, core, fullBody
    var id: String { rawValue }
    var displayName: String {
        switch self {
        case .chest: String(localized: "muscleGroup.chest", defaultValue: "Chest")
        // ... one per case ...
        }
    }
}
```
- Pattern to replicate exactly: `String, Codable, CaseIterable, Identifiable`; `id { rawValue }`; `displayName` via `String(localized: "muscleGroup.<case>", defaultValue: "...")` (NO `return` keyword — this codebase uses implicit-return single-expression switch arms, see existing displayName).
- Sibling enums in this file (e.g. `ExerciseCategory`, `SportType`) include `systemImage` — mirror their `var systemImage: String { switch self { case ...: "sf.symbol.name" } }` style.

Locked decisions (from 22-CONTEXT.md):
- D-01: new cases use lowerCamelCase rawValues; rawValues are permanent.
- D-02: BodyRegion = {legs, back, chest, shoulders, arms, core, fullBody}; fullBody region RETAINED.
- D-03: all 7 old cases retained with exact original rawValues (non-destructive).
- D-04: retained coarse cases keep region-name displayName.
- D-05: suggestedSpecific(for:) default map (legs->quads, chest->pecsLower, back->lats, shoulders->lateralDelts, arms->biceps, core->rectusAbdominis, fullBody->fullBody).
- D-06: no VersionedSchema — additive only.

Full case list + region grouping + default mapping: see the "Full Migration Mapping" table in 22-CONTEXT.md (authoritative).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand MuscleGroup + add BodyRegion, region mapping, systemImage, suggestedSpecific</name>
  <files>WorkloadApp/Models/Enums.swift, WorkloadAppTests/MuscleGroupTaxonomyTests.swift</files>
  <read_first>
    - WorkloadApp/Models/Enums.swift lines 169-191 (current MuscleGroup) AND an enum in the same file that already defines `systemImage` (e.g. SportType / ExerciseCategory) to copy the exact style and implicit-return convention
    - .planning/phases/22-granular-muscle-group-taxonomy/22-CONTEXT.md — the full migration mapping table (case list, regions, defaults) and decisions D-01..D-06
    - WorkloadAppTests/ (any existing *Tests.swift, e.g. RecoveryScoreEngineTests.swift) for XCTest style: `@testable import workload_management`, `func test_...`, XCTAssert patterns
  </read_first>
  <behavior>
    - MuscleGroup.allCases includes all 7 original cases AND the new specific cases from the mapping table (quads, hamstrings, glutes, calves, hipFlexors, psoas, adductors, hipRotators, tibialisAnterior, lats, trapsUpper, trapsMid, trapsLower, rhomboids, erectors, pecsUpper, pecsLower, anteriorDelts, lateralDelts, posteriorDelts, biceps, triceps, forearms, rectusAbdominis, obliques, transverseAbdominis)
    - MuscleGroup(rawValue: "legs") != nil and == .legs (old value still decodes — backward compat); same for chest/back/shoulders/arms/core/fullBody
    - .legs.region == .legs ; .quads.region == .legs ; .lats.region == .back ; .pecsUpper.region == .chest ; .lateralDelts.region == .shoulders ; .triceps.region == .arms ; .obliques.region == .core ; .fullBody.region == .fullBody — and EVERY case returns a region (totality: iterate allCases, none traps)
    - suggestedSpecific(for: .legs) == .quads ; (.chest) == .pecsLower ; (.back) == .lats ; (.shoulders) == .lateralDelts ; (.arms) == .biceps ; (.core) == .rectusAbdominis ; (.fullBody) == .fullBody
    - suggestedSpecific(for:) for an already-specific value returns that same value unchanged (idempotent)
    - Every MuscleGroup case has a non-empty displayName and a non-empty systemImage; BodyRegion.allCases each have non-empty displayName + systemImage
    - id == rawValue for every case
  </behavior>
  <action>
    In `WorkloadApp/Models/Enums.swift`, replace the current 7-case `MuscleGroup` with the full taxonomy from the 22-CONTEXT mapping table: KEEP the 7 original cases (exact rawValues unchanged) and ADD the ~26 new specific cases (lowerCamelCase rawValues per D-01). Preserve `String, Codable, CaseIterable, Identifiable` conformance and `var id: String { rawValue }`. Extend `displayName` to cover every case via `String(localized: "muscleGroup.<case>", defaultValue: "<English>")` (English defaults from the mapping table; retained coarse cases keep region-name labels per D-04). NOTE: the matching `muscleGroup.<newcase>` localization keys are added in Plan 03 — using `String(localized:)` with a `defaultValue` compiles and falls back to English until then.

    Add a new `enum BodyRegion: String, Codable, CaseIterable, Identifiable` (cases legs, back, chest, shoulders, arms, core, fullBody) with `id { rawValue }`, `displayName` (String(localized: "bodyRegion.<case>", defaultValue:...)), and `systemImage`. Add to MuscleGroup: `var region: BodyRegion { switch self { ... } }` mapping EVERY case (old+new) per D-02/the table; `var systemImage: String { ... }` per case (plausible SF Symbols — figure.* / body symbols, planner discretion); and `static func suggestedSpecific(for coarse: MuscleGroup) -> MuscleGroup` returning the D-05 default for the 7 coarse values and returning the input unchanged for any already-specific value. Do NOT add any VersionedSchema or touch any @Model (D-06).

    Add `WorkloadAppTests/MuscleGroupTaxonomyTests.swift` covering every behavior bullet: rawValue stability for all 7 old values (decode via init(rawValue:)), region totality (loop allCases, assert each has a region — implicitly guaranteed by exhaustive switch, but assert a representative spread), the full suggestedSpecific mapping + idempotence, and non-empty displayName/systemImage for all cases.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/MuscleGroupTaxonomyTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Enums.swift `MuscleGroup` contains all 7 original cases with unchanged rawValues plus the new specific cases from the mapping table
    - `enum BodyRegion` exists with the 7 region cases, displayName, systemImage
    - `MuscleGroup.region` is an exhaustive switch (compiles without a default arm) returning a BodyRegion for every case
    - `MuscleGroup.systemImage` exists and is exhaustive; `suggestedSpecific(for:)` matches D-05 and is idempotent for specific inputs
    - MuscleGroupTaxonomyTests asserts old-value decode, the default mapping, idempotence, and non-empty labels — xcodebuild test exits 0
    - No `VersionedSchema`/`SchemaMigrationPlan` added; no @Model file modified
  </acceptance_criteria>
  <done>MuscleGroup taxonomy + BodyRegion + region/systemImage/suggestedSpecific exist as pure additive model code; all old rawValues still decode; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device → Supabase sync | This plan adds NO sync code. Serialization compatibility is handled in Plan 03. Enum rawValues are additive; existing blobs unaffected. |
| Stored SwiftData rows → enum decode | Old rawValues are retained, so every existing `muscleGroup` field decodes. No data rewrite, no migration plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering / Data Loss | Existing MuscleGroup rawValues | mitigate | All 7 original cases retained with exact rawValues (D-03); a test asserts `init(rawValue:)` still resolves each. No rename/removal. |
| T-22-02 | Denial of Service (crash) | Unknown rawValue decode | accept | Swift decode of a Codable enum / `init(rawValue:)` of an unknown string returns nil, never crashes; callers already use optional `MuscleGroup?`. No new failure mode introduced. |
| T-22-03 | Tampering | New test file in WorkloadAppTests target | accept | Tests added to the existing test target; no new SPM dependency, no .pbxproj source-file question beyond the one new test file (flag to add to target — see verification). |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/MuscleGroupTaxonomyTests` exits 0.
- `grep -n "case chest\|case back\|case legs\|case shoulders\|case arms\|case core\|case fullBody" WorkloadApp/Models/Enums.swift` confirms all 7 originals still present.
- `grep -n "VersionedSchema\|SchemaMigrationPlan" WorkloadApp/Models/Enums.swift` returns nothing.
- New test file `MuscleGroupTaxonomyTests.swift` must be added to the WorkloadAppTests target in the .pbxproj (per CLAUDE.md iOS note) — executor confirms the build picks it up (test run discovering the class proves membership).
</verification>

<success_criteria>
- MuscleGroup expanded to the full ROADMAP taxonomy with all originals retained (criteria 1 + graceful-migration foundation for criterion 3).
- BodyRegion + region mapping exist (criterion 2 foundation; UI consumes in Plan 02).
- Enum keeps all required conformances + gains systemImage (CLAUDE.md convention).
- suggestedSpecific default map ready for the re-specify UX (criterion 3).
- No destructive schema change; old rows decode.
</success_criteria>

<output>
Create `.planning/phases/22-granular-muscle-group-taxonomy/22-01-SUMMARY.md` when done.
</output>
