---
phase: 22-granular-muscle-group-taxonomy
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WorkloadApp/Resources/Localizable.xcstrings
  - Supabase/functions/parse-workout/index.ts
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "Every new MuscleGroup case has a `muscleGroup.<case>` key in Localizable.xcstrings with an English value AND a zh-Hans (Simplified Chinese) translation, matching the pattern of the existing 7 keys"
    - "Every BodyRegion case has a `bodyRegion.<case>` key in Localizable.xcstrings (English + zh-Hans)"
    - "The parse-workout Edge Function's muscle_group JSON-schema enum lists ALL valid MuscleGroup rawValues (old + new) plus null, so LLM import can emit specific muscles"
    - "The parse-workout prompt guidance points the model at specific muscles (not just the 7 coarse buckets)"
    - "Existing template/prescribed-workout groups_json blobs authored with old coarse values still decode (verified — no migration needed): MuscleGroup(rawValue:) is nil-safe and old cases retained"
    - "No new PostgreSQL column or SQL migration is added (muscle_group is not a DB column — it lives only in the groups_json blob and the Edge Function schema)"
  artifacts:
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "muscleGroup.* and bodyRegion.* keys for all new cases, English + zh-Hans"
      contains: "muscleGroup.quads"
    - path: "Supabase/functions/parse-workout/index.ts"
      provides: "Expanded muscle_group enum + prompt guidance"
      contains: "quads"
  key_links:
    - from: "MuscleGroup.displayName String(localized:) keys"
      to: "Localizable.xcstrings muscleGroup.* entries"
      via: "matching key strings"
      pattern: "muscleGroup\\."
---

<objective>
Close the serialization + localization loop for the expanded taxonomy. (1) Add a `muscleGroup.<case>` localization key (English + Simplified Chinese) for every new `MuscleGroup` case and a `bodyRegion.<case>` key for every `BodyRegion` case, so the localized build (zh-Hans shipped in Phase 23) does not regress to raw keys. (2) Update the `parse-workout` Supabase Edge Function's structured-output `muscle_group` enum and prompt to include all new rawValues, so LLM workout import can produce specific muscles (criterion 5 — sync/import handles new values). (3) Confirm — via inspection — that existing `groups_json` blobs with old coarse values still decode, requiring NO SQL migration.

This plan is **localization + Edge Function only**. It depends on Plan 01 (the final case list / rawValues). It is independent of Plan 02 (UI) and can run in the same wave.

Output: updated `Localizable.xcstrings` (new keys, EN+zh-Hans) and `parse-workout/index.ts` (expanded enum + prompt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-granular-muscle-group-taxonomy/22-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Current contracts. Extracted from codebase — do not re-explore. -->

From WorkloadApp/Resources/Localizable.xcstrings (existing 7 muscle keys, zh-Hans translated in Phase 23):
- `muscleGroup.chest`, `.back`, `.legs`, `.shoulders`, `.arms`, `.core`, `.fullBody` (lines ~1467-1586). Each entry has the xcstrings shape: a top-level key whose value object holds `"localizations": { "en": { "stringUnit": { "state":"translated", "value":"Chest" } }, "zh-Hans": { "stringUnit": { ... "value":"胸部" } } }`. REPLICATE this exact structure for new keys. (Read the existing entries to copy the precise JSON shape — xcstrings is a strict JSON format.)

From Supabase/functions/parse-workout/index.ts:
- Prompt (line ~24): `- muscle_group should be the primary muscle targeted. Use null if unclear or not applicable.`
- Schema (lines ~70-92): `muscle_group: { type: ["string","null"], enum: ["chest","back","legs","shoulders","arms","core","fullBody", null] }`
- `muscle_group` is in the `required` array (line ~110) — keep it required (null allowed).

From Plan 01 (WorkloadApp/Models/Enums.swift):
- The authoritative final list of MuscleGroup rawValues (old + new) and BodyRegion rawValues + their English defaultValues. The 22-CONTEXT mapping table lists English labels; zh-Hans translations are added per the table below.

Serialization facts (22-CONTEXT, verified):
- `muscle_group` is NOT a PostgreSQL column anywhere (zero matches in Supabase/migrations). It exists only inside the `groups_json` TEXT blob and the Edge Function schema. NO SQL migration is needed.
- Swift decode is nil-safe: `MuscleGroup(rawValue: $0)` (SyncService.swift:1338, WorkoutLLMImportService.swift:206) returns nil for unknown strings — old blobs with retained old values decode to the old case; any unknown future value degrades to nil, never crashes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add muscleGroup.* and bodyRegion.* localization keys (English + zh-Hans)</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings lines ~1467-1586 (the 7 existing muscleGroup.* entries — copy the EXACT JSON shape incl. zh-Hans stringUnit)
    - .planning/phases/22-granular-muscle-group-taxonomy/22-CONTEXT.md mapping table (English labels) + the zh-Hans table below
    - WorkloadApp/Models/Enums.swift (Plan 01: final case list, the exact key strings used in String(localized: "muscleGroup.<case>") / "bodyRegion.<case>")
  </read_first>
  <behavior>
    - For every new MuscleGroup case, a `muscleGroup.<case>` entry exists with en + zh-Hans translated stringUnits
    - For every BodyRegion case, a `bodyRegion.<case>` entry exists with en + zh-Hans
    - The 7 existing muscleGroup keys are left intact (no edits to their values)
    - The file remains valid JSON / valid xcstrings (parses, builds)
  </behavior>
  <action>
    Add new entries to Localizable.xcstrings for every new MuscleGroup case and every BodyRegion case, mirroring the exact structure of the existing `muscleGroup.chest` entry (string key -> localizations -> en/zh-Hans -> stringUnit{state:"translated", value:...}). Keys MUST exactly match the strings used by `String(localized:)` in Plan 01's enum. Use the English labels from the 22-CONTEXT mapping table and the Simplified Chinese translations below.

    zh-Hans translations (muscleGroup.*):
    quads 股四头肌 · hamstrings 腘绳肌 · glutes 臀肌 · calves 小腿 · hipFlexors 髋屈肌 · psoas 腰大肌 · adductors 内收肌 · hipRotators 髋旋转肌 · tibialisAnterior 胫骨前肌 · lats 背阔肌 · trapsUpper 上斜方肌 · trapsMid 中斜方肌 · trapsLower 下斜方肌 · rhomboids 菱形肌 · erectors 竖脊肌 · pecsUpper 上胸 · pecsLower 下胸 · anteriorDelts 前三角肌 · lateralDelts 中三角肌 · posteriorDelts 后三角肌 · biceps 二头肌 · triceps 三头肌 · forearms 前臂 · rectusAbdominis 腹直肌 · obliques 腹斜肌 · transverseAbdominis 腹横肌

    zh-Hans translations (bodyRegion.*):
    legs 腿部 · back 背部 · chest 胸部 · shoulders 肩部 · arms 手臂 · core 核心 · fullBody 全身

    Do NOT modify the 7 existing muscleGroup.* entries. If unsure of any zh-Hans term, prefer the common fitness-app rendering above; flag any low-confidence term in the SUMMARY for human review (zh-Hans is a shipped language).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15 && python3 -c "import json,sys; json.load(open('/Users/hanwen/Desktop/Tonus/WorkloadApp/Resources/Localizable.xcstrings')); print('xcstrings JSON OK')"</automated>
  </verify>
  <acceptance_criteria>
    - A `muscleGroup.<case>` key exists for every new case and a `bodyRegion.<case>` for every region, each with en + zh-Hans translated values
    - The 7 original muscleGroup entries are unchanged
    - xcstrings parses as valid JSON and the app builds
  </acceptance_criteria>
  <done>All new taxonomy + region keys localized in EN and zh-Hans; existing keys untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Expand parse-workout Edge Function muscle_group enum + prompt</name>
  <files>Supabase/functions/parse-workout/index.ts</files>
  <read_first>
    - Supabase/functions/parse-workout/index.ts lines ~18-115 (prompt guidance line 24; WORKOUT_SCHEMA muscle_group enum lines 70-92; required array line ~110)
    - Plan 01 WorkloadApp/Models/Enums.swift (authoritative final rawValue list — the enum strings must match exactly)
  </read_first>
  <behavior>
    - The muscle_group schema `enum` array lists every valid MuscleGroup rawValue (old 7 + all new) plus `null`
    - The strings exactly match the Swift enum rawValues (case-sensitive lowerCamelCase) so MuscleGroup(rawValue:) resolves them on import
    - The prompt guidance instructs the model to choose the most specific primary muscle (and may keep null when unclear)
    - muscle_group stays in `required` (null permitted by the union type)
  </behavior>
  <action>
    In `Supabase/functions/parse-workout/index.ts`, replace the hardcoded 7-value `muscle_group` enum (lines ~70-92) with the FULL list of MuscleGroup rawValues from Plan 01 (old + new) plus `null`, preserving the `type: ["string","null"]` union and the existing array/null formatting style. Update the prompt line (~24) to: instruct the model to return the most specific primary muscle when identifiable (e.g. "quads" not just "legs"), falling back to the coarse region value or null when unclear. Keep `muscle_group` in the `required` array. Do not change any other schema field. Strings MUST be byte-identical to the Swift rawValues.

    NOTE (deploy): this Edge Function change only takes effect after redeploy (`supabase functions deploy parse-workout`). Flag the deploy step in the SUMMARY for the user to run — do not assume CI deploys it.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && grep -c "quads\|hamstrings\|lats\|biceps" Supabase/functions/parse-workout/index.ts && node --check Supabase/functions/parse-workout/index.ts 2>/dev/null || npx --yes tsc --noEmit --allowJs Supabase/functions/parse-workout/index.ts 2>&1 | tail -5 || echo "syntax check skipped (deno file)"</automated>
  </verify>
  <acceptance_criteria>
    - muscle_group enum contains all new rawValues + the 7 old + null
    - Enum strings match Swift rawValues exactly (spot-check quads/lats/biceps/pecsUpper present)
    - Prompt nudges toward specific muscle; muscle_group still required
    - SUMMARY notes the manual `supabase functions deploy parse-workout` step
  </acceptance_criteria>
  <done>Edge Function structured-output enum + prompt cover the full taxonomy; deploy step flagged.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm groups_json backward-compat — no SQL migration needed</name>
  <files>Supabase/functions/parse-workout/index.ts</files>
  <read_first>
    - WorkloadApp/Services/SyncService.swift:1338 (decodeGroups: MuscleGroup(rawValue:) nil-safe), :1500-1522 (ExerciseDTO encodes rawValue)
    - Supabase/migrations/*.sql (confirm zero muscle_group columns)
    - 22-CONTEXT.md D-03/D-07 (retained aliases + sync round-trip)
  </read_first>
  <behavior>
    - Existing groups_json blobs containing "muscleGroup":"legs" (etc.) still decode to the retained .legs case (no migration)
    - New blobs with specific values like "muscleGroup":"quads" decode to .quads
    - An old client reading a new blob hits MuscleGroup(rawValue: "quads") -> nil (graceful: shows no muscle), never crashes
  </behavior>
  <action>
    This is a VERIFICATION-ONLY task (no code edit): confirm by inspection that (a) no Supabase migration defines a muscle_group column, (b) the Swift encode/decode is pure rawValue round-trip and nil-safe, and (c) old coarse blobs decode via the retained cases (Plan 01) — therefore NO SQL migration and NO data backfill are required. Document this conclusion + the old-client-reads-new-blob graceful-degradation note in the SUMMARY. (Per the MEMORY note about moving migration SQL to a proper migrations/ dir: confirm this phase introduces no new SQL, so that cleanup is out of scope here — note it.)
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && (grep -rn "muscle_group\|muscleGroup" Supabase/migrations/ || echo "CONFIRMED: no muscle_group column in any migration")</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY documents: no muscle_group DB column, no SQL migration added, old blobs decode via retained cases, old-client-reads-new-value degrades to nil (no crash)
    - grep confirms zero muscle_group references in Supabase/migrations/
  </acceptance_criteria>
  <done>Backward-compat of the groups_json serialization confirmed; no SQL migration required; documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM import → app | Edge Function structured output is constrained to the enum; the app re-validates via nil-safe MuscleGroup(rawValue:). A bad/unknown value degrades to nil, never crashes. |
| Old blob (old client) ↔ new enum | Retained old cases (D-03) mean old blobs always decode; new values from new clients degrade gracefully to nil on old clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering / Injection | LLM-emitted muscle_group string | mitigate | Structured-output enum constrains values; app-side nil-safe MuscleGroup(rawValue:) rejects anything off-list to nil. No raw string trusted as an enum. |
| T-22-07 | Data Loss | Existing groups_json blobs | mitigate | No migration; retained old cases decode every existing blob. Verified zero DB columns to alter. |
| T-22-08 | Availability | Edge Function deploy lag | accept | If the function isn't redeployed, import simply keeps emitting old coarse values (still valid). Deploy flagged for user; no crash. |
| T-22-09 | Localization regression | zh-Hans build (shipped) | mitigate | Every new String(localized:) key gets en + zh-Hans entries; xcstrings JSON validated; existing keys untouched. |
</threat_model>

<verification>
- App builds; Localizable.xcstrings parses as valid JSON (python json.load).
- `grep "muscleGroup.quads\|bodyRegion.legs" WorkloadApp/Resources/Localizable.xcstrings` finds new keys with zh-Hans values.
- `grep "quads\|lats\|pecsUpper" Supabase/functions/parse-workout/index.ts` finds new enum values.
- `grep -rn muscle Supabase/migrations/` returns nothing (no DB column; no SQL migration).
</verification>

<success_criteria>
- New taxonomy + regions fully localized (EN + zh-Hans); shipped zh-Hans build does not regress (criterion 6 localized).
- LLM import can emit specific muscles after redeploy (criterion 5).
- Existing data round-trips with no SQL migration and no crash on either client direction (criterion 5 "without breaking existing data").
</success_criteria>

<output>
Create `.planning/phases/22-granular-muscle-group-taxonomy/22-03-SUMMARY.md` when done.
</output>
