---
phase: 22-granular-muscle-group-taxonomy
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "The muscle-group selector in AddCustomExerciseSheet presents a region -> sub-group hierarchy (grouped by BodyRegion) instead of a flat list of all cases"
    - "A 'None' / no-muscle option remains selectable (muscleGroup stays optional)"
    - "When the picker opens on an exercise whose stored value is a coarse alias (e.g. .legs), the UI pre-selects MuscleGroup.suggestedSpecific(for:) so the user is nudged to specify, but the user can keep the coarse value or pick any specific one"
    - "Selecting a specific muscle passes that specific MuscleGroup back through the existing onAdd/onSelect closures unchanged (no closure signature change)"
    - "Read displays in ActiveWorkoutSheet, SessionDetailView, and both TemplateEditorSheets show the specific muscle displayName for new specific values and the region name for retained coarse values (verified, no code change needed there)"
    - "UI honors DESIGN.md: 0pt corners (Rectangle, no RoundedRectangle), no shadows, General Sans via Font.Tokens, ColorTokens only, 8pt-multiple spacing, region grouping communicated via text section headers (not color alone)"
    - "OPTIONAL (D-08): ExerciseDatabase seed exercises re-tagged from coarse to specific values where unambiguous"
  artifacts:
    - path: "WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift"
      provides: "Hierarchical region->muscle selector in AddCustomExerciseSheet; optional seed re-tagging"
      contains: "BodyRegion"
  key_links:
    - from: "AddCustomExerciseSheet muscle selector"
      to: "MuscleGroup.region"
      via: "group MuscleGroup.allCases by region into sections/navigation"
      pattern: "region"
---

<objective>
Rebuild the muscle-group selector in `AddCustomExerciseSheet` (inside `ExercisePickerView.swift`) from a flat `Picker` over `MuscleGroup.allCases` into a **region → sub-group hierarchy** grouped by the `BodyRegion`/`region` introduced in Plan 01, satisfying ROADMAP criterion 4. Keep the muscle optional ("None"), pre-select `suggestedSpecific(for:)` when editing a coarse-aliased value (criterion 3 "prompt to specify"), and pass the chosen specific value back through the existing closures unchanged. Verify (no change needed) that the read-side displays in workout/session/template views render the new specific names. Optionally (D-08, cuttable) re-tag the seed `ExerciseDatabase` exercises to specific muscles.

This plan is **UI only**. It depends on Plan 01's enum (`region`, `BodyRegion`, `suggestedSpecific`). It must obey DESIGN.md strictly (0pt corners, no shadows, General Sans, ColorTokens, 8pt grid, text-label grouping).

Output: modified `ExercisePickerView.swift` with a hierarchical muscle selector (and optional seed re-tagging).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-granular-muscle-group-taxonomy/22-CONTEXT.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Current contracts. Extracted from codebase — do not re-explore. -->

From WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift:
- `AddCustomExerciseSheet` — `@State private var muscleGroup: MuscleGroup?` (line 185). Current selector (lines 209-215):
  ```swift
  Picker("Muscle Group (optional)", selection: $muscleGroup) {
      Text("None").tag(MuscleGroup?.none)
      ForEach(MuscleGroup.allCases) { group in
          Text(group.displayName).tag(MuscleGroup?.some(group))
      }
  }
  .pickerStyle(.menu)
  ```
  This is the element to make hierarchical. `onAdd(name, category, muscleGroup)` closure passes the chosen value out — DO NOT change its signature.
- `ExercisePickerView.onSelect: (String, ExerciseCategory, MuscleGroup?) -> Void` (line 17) — closure shape unchanged.
- `ExerciseDatabase` (lines 282-413) — ~70 seed `ExerciseDefinition(name:category:muscleGroup:)` entries currently tagged with coarse values (.chest/.legs/.back/.fullBody/etc). Optional re-tag target (D-08).
- `FilterChip`, `SharpTextFieldStyle` — existing DESIGN-compliant components to reuse.

From Plan 01 (WorkloadApp/Models/Enums.swift):
- `MuscleGroup.region: BodyRegion`, `BodyRegion.allCases` (displayName + systemImage), `MuscleGroup.suggestedSpecific(for:)`.

Read-side displays to VERIFY (no edit expected — all call `.displayName`):
- ActiveWorkoutSheet.swift:712-713, SessionDetailView.swift:116-117, Views/TemplateEditorSheet.swift:373-374, Views/Coach/TemplateEditorSheet.swift:280-281.

DESIGN.md constraints (enforce): 0pt borders (Rectangle, never RoundedRectangle), no .shadow, General Sans via Font.Tokens.* (no .system/.headline), ColorTokens.* only (no hex), spacing in 8pt multiples, grouping via text section headers (zone/region communicated by label, optionally a hairline border — never color alone), accent color reserved for the readiness hero number only (do NOT use accent here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hierarchical region -> muscle selector in AddCustomExerciseSheet</name>
  <files>WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift</files>
  <read_first>
    - WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift (full file — AddCustomExerciseSheet body lines 196-248, the flat Picker 209-215, FilterChip + SharpTextFieldStyle for component reuse)
    - WorkloadApp/Models/Enums.swift (Plan 01 output: MuscleGroup.region, BodyRegion, suggestedSpecific)
    - DESIGN.md (typography/color/spacing/corners/no-shadow rules + zone-via-text rule)
  </read_first>
  <behavior>
    - The muscle selector shows muscles grouped by BodyRegion: each region is a labeled section/screen (region.displayName as header) listing its MuscleGroup members (member.displayName), plus a top-level "None" option that clears the selection
    - Picking a specific muscle sets `muscleGroup` to that specific case; the sheet's Add button passes it out via the existing onAdd closure unchanged
    - "None" remains selectable and yields muscleGroup == nil
    - If the sheet is opened with an existing coarse value (a retained alias like .legs), the selector visibly indicates/pre-selects suggestedSpecific(for:) (e.g. Quads) so the user is nudged to refine — but can keep the coarse value or pick another. (For AddCustomExerciseSheet which starts at nil this mainly applies if/when an edit path reuses it; implement the pre-select logic so any coarse initial value resolves to its suggestion while leaving nil as nil.)
    - No closure signature changes; ExercisePickerView.onSelect and AddCustomExerciseSheet.onAdd keep `(String, ExerciseCategory, MuscleGroup?)`
    - Visual: 0pt corners (Rectangle/hairline dividers like the existing list), General Sans via Font.Tokens, ColorTokens only, 8pt spacing, region headers are text labels (no color-only grouping), no shadows, no accent color
  </behavior>
  <action>
    Replace the flat `.menu` Picker in `AddCustomExerciseSheet` with a hierarchical region->muscle selector. Recommended implementation (planner/executor discretion within DESIGN.md): a sectioned selector — either (a) a `NavigationLink` row showing the current selection that pushes a grouped `List` (sections per `BodyRegion`, header = region.displayName, rows = that region's muscles via `MuscleGroup.allCases.filter { $0.region == region }`, plus a "None" section/row), or (b) an inline grouped `List`/`Form` with region section headers. Use the existing list/divider styling (Rectangle hairlines, ColorTokens.divider) and Font.Tokens — match the surrounding sheet's look. Add a small helper computing `groupedByRegion: [BodyRegion: [MuscleGroup]]` or iterate `BodyRegion.allCases` and filter. Implement coarse-value pre-selection: when the bound value is one of the 7 coarse aliases, resolve it through `MuscleGroup.suggestedSpecific(for:)` for the highlighted default while still allowing the coarse value to be kept (e.g. show the coarse value under its own region with the suggestion pre-highlighted). Keep nil -> "None". Do NOT alter the onAdd/onSelect signatures, the category Picker, or the save logic. Reuse `region.systemImage` for optional row icons (monochrome, ColorTokens.text2 — not accent).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -20</automated>
    <manual>Run app, open Add Custom Exercise: muscle selector shows regions (Legs/Back/Chest/Shoulders/Arms/Core/Full Body) each expandable to specific muscles; "None" works; selecting "Quads" then adding shows "Quads" on the logged exercise row. Confirm 0pt corners, no shadows, General Sans, no stray accent color.</manual>
  </verify>
  <acceptance_criteria>
    - The flat `ForEach(MuscleGroup.allCases)` menu Picker is gone; selection is grouped by BodyRegion with text headers
    - "None" selectable -> muscleGroup nil; selecting a specific muscle -> that case flows out via unchanged closures
    - Coarse initial values resolve their suggestedSpecific highlight; nil stays nil
    - Build succeeds; UI matches DESIGN.md (no RoundedRectangle, no .shadow, Font.Tokens, ColorTokens, 8pt spacing, no accent)
  </acceptance_criteria>
  <done>AddCustomExerciseSheet presents a DESIGN-compliant region->muscle hierarchy; closures unchanged; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify read displays + (OPTIONAL, cuttable) re-tag ExerciseDatabase seed to specific muscles</name>
  <files>WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift</files>
  <read_first>
    - WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift ExerciseDatabase (lines 282-413)
    - WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift:712, SessionDetailView.swift:116, Views/TemplateEditorSheet.swift:373, Views/Coach/TemplateEditorSheet.swift:280 (read-only verification that they call .displayName and need no change)
    - 22-CONTEXT.md D-08 + mapping table (which coarse seed tags map to which specific value)
  </read_first>
  <behavior>
    - Read-side displays render the specific displayName for specific values and the region name for retained coarse values WITHOUT any code change to those view files (they already call muscle.displayName)
    - OPTIONAL: seed exercises whose muscle is unambiguous are re-tagged to specific values (e.g. Barbell Back Squat -> .quads, Romanian Deadlift -> .hamstrings, Calf Raise -> .calves, Lat Pulldown -> .lats, Lateral Raise -> .lateralDelts, Bicep Curl -> .biceps, Tricep Pushdown -> .triceps, Plank -> .rectusAbdominis, Hanging Leg Raise -> .rectusAbdominis). Ambiguous/compound or cardio/full-body entries (e.g. Deadlift, Treadmill Run, Easy Run, Scrimmage) keep their existing coarse/fullBody/nil tag.
  </behavior>
  <action>
    First, VERIFY (do not edit) that ActiveWorkoutSheet, SessionDetailView, and both TemplateEditorSheet read displays only call `.displayName` and therefore correctly render new specific names with no change — note this in the SUMMARY. Then, OPTIONAL polish (D-08): re-tag unambiguous `ExerciseDatabase` seed `ExerciseDefinition` muscle values from coarse to specific per the mapping table. Touch ONLY clear single-muscle isolation/primary-mover entries; leave compound/cardio/full-body/nil entries unchanged. This task is cuttable — if time/scope pressure, skip the re-tag and ship Task 1 only (coarse seed tags still work via D-03 retention). Do NOT change ExerciseDatabase structure or the ExerciseDefinition type.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY documents that the 4 read displays need no change (confirmed call `.displayName`)
    - If re-tag done: only unambiguous single-muscle seed entries changed; compound/cardio/full-body entries untouched; build succeeds
    - If re-tag skipped: explicitly noted as deferred; build still succeeds
  </acceptance_criteria>
  <done>Read displays confirmed compatible; seed optionally re-tagged to specific muscles; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (muscle selection) → SwiftData | UI writes only valid MuscleGroup cases via the typed closures; no raw string input. |
| Seed data → stored exercises | Re-tagging changes only in-binary seed defaults; does not migrate existing user rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | Closure signature drift | mitigate | onAdd/onSelect signatures kept byte-identical; only the selector internals change. Callers (ActiveWorkoutSheet, both TemplateEditorSheets) compile unchanged. |
| T-22-05 | UI regression | DESIGN.md violations | mitigate | Reuse existing Rectangle/hairline/Font.Tokens/ColorTokens patterns; no RoundedRectangle, no shadow, no accent; region grouping via text headers (not color alone). Manual visual check in verify. |
</threat_model>

<verification>
- `xcodebuild build` exits 0.
- `grep -n "RoundedRectangle\|\.shadow(\|\.system(\|ColorTokens.accent" WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift` returns nothing new in the edited regions (DESIGN.md compliance).
- `grep -n "onSelect: (String, ExerciseCategory, MuscleGroup?)\|onAdd: @escaping (String, ExerciseCategory, MuscleGroup?)" WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift` confirms unchanged closure shapes.
- Manual: muscle selector is hierarchical by region; "None" works; chosen specific muscle displays on the exercise row.
</verification>

<success_criteria>
- ExercisePickerView muscle selector is region->sub-group hierarchical (criterion 4).
- Specific muscle names flow through unchanged closures and display in workout/session/template views (criterion 6).
- Coarse values nudge toward suggestedSpecific (criterion 3 "prompt to specify") without forcing a change.
- DESIGN.md fully honored.
- Optional seed re-tag improves out-of-box granularity.
</success_criteria>

<output>
Create `.planning/phases/22-granular-muscle-group-taxonomy/22-02-SUMMARY.md` when done.
</output>
