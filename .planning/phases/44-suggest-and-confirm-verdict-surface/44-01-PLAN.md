---
phase: 44-suggest-and-confirm-verdict-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Services/VerdictDecision.swift
  - WorkloadApp/ViewModels/TodayVerdictViewModel.swift
  - WorkloadAppTests/VerdictDecisionApplierTests.swift
  - WorkloadAppTests/TodayVerdictViewModelTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [MOD-10, MOD-11, MOD-12]

must_haves:
  truths:
    - "Accepting the suggestion marks verdictAppliedAt and never overwrites the authored targetWeightKg"
    - "Keeping the plan records athleteOverrode=true and leaves planned numbers unchanged"
    - "The athlete's feel is a first-class input that maps to accept/keep and is emitted as a VerdictDecision for Phase 45"
    - "The source authored template is never mutated"
    - "A cold-start verdict defers honestly (still-learning) rather than fabricating a trim"
  artifacts:
    - path: "WorkloadApp/Services/VerdictDecision.swift"
      provides: "Decision types (VerdictDecision/FeelOverride/TodayVerdictDisplay) + pure VerdictDecisionApplier (slot mutations only)"
      contains: "enum FeelOverride"
      min_lines: 80
    - path: "WorkloadApp/ViewModels/TodayVerdictViewModel.swift"
      provides: "@MainActor @Observable orchestration: assemble inputs, write slots via TodayVerdictService, build display, apply accept/keep/feel"
      contains: "TodayVerdictViewModel"
      min_lines: 90
    - path: "WorkloadAppTests/VerdictDecisionApplierTests.swift"
      provides: "accept/keep/feel slot-invariant tests"
    - path: "WorkloadAppTests/TodayVerdictViewModelTests.swift"
      provides: "refresh/display/decision-emit tests"
  key_links:
    - from: "WorkloadApp/ViewModels/TodayVerdictViewModel.swift"
      to: "WorkloadApp/Services/TodayVerdictService.swift"
      via: "evaluateAndWrite seam writes the Phase-42 slots"
      pattern: "evaluateAndWrite"
    - from: "WorkloadApp/Services/VerdictDecision.swift"
      to: "WorkloadApp/Models/WorkoutTemplate.swift"
      via: "VerdictDecisionApplier mutates only verdictAppliedAt/athleteOverrode"
      pattern: "verdictAppliedAt|athleteOverrode"
---

<objective>
Build the **state + decision layer** for the suggest-and-confirm verdict surface: the pure
`VerdictDecisionApplier` (accept / keep-plan / feel slot mutations that honor SC1/SC3 — never
overwrite the authored number, never touch the source template) and the `TodayVerdictViewModel`
that assembles readiness inputs, drives the Phase-43 `TodayVerdictService` to populate the
Phase-42 `TemplateSet` verdict slots, and exposes a display model + decision actions to the card
(Plan 44-02).

Purpose: SC1 (accept/decline recorded, planned never silently overwritten), SC3 (one-tap keep-plan),
and the feel-override + cold-start-defer machinery (SC2/SC4) live here as testable logic BEFORE any
pixels. The accept/keep/feel events are shaped so Phase 45 logs them by wiring one closure.
Output: `VerdictDecision.swift`, `TodayVerdictViewModel.swift`, two test files, pbxproj registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-suggest-and-confirm-verdict-surface/44-CONTEXT.md

# Read-first source (real types this plan consumes — do NOT invent signatures)
@WorkloadApp/Services/TodayVerdictService.swift
@WorkloadApp/Services/TodayVerdictEngine.swift
@WorkloadApp/Models/WorkoutTemplate.swift
@WorkloadApp/Models/PrescribedWorkout.swift
@WorkloadApp/Repositories/PlannedSessionRepository.swift

<interfaces>
<!-- Extracted from the codebase. Use these directly — no further exploration needed. -->

TemplateSet verdict slots (WorkloadApp/Models/WorkoutTemplate.swift) — Phase 44 OWNS the last two:
  var targetWeightKg: Double?            // the AUTHORED planned number — NEVER overwrite
  var targetReps: Int?
  var targetRPE: Double?
  var isWarmup: Bool
  var adjustedTargetWeightKg: Double?    // verdict suggestion (written by the Phase-43 service)
  var adjustedTargetRPE: Double?         // verdict RPE cap (written by the Phase-43 service)
  var verdictReason: String?             // one-line why (written by the Phase-43 service)
  var verdictAppliedAt: Date? = nil      // Phase 44 sets on ACCEPT
  var athleteOverrode: Bool = false      // Phase 44 sets on DECLINE / keep-plan

PrescribedWorkout (WorkloadApp/Models/PrescribedWorkout.swift):
  var allExercises: [TemplateExercise]   // flattened, ordered
TemplateExercise: var exerciseName: String; var muscleGroup: MuscleGroup?; var sortedSets: [TemplateSet]

TodayVerdictService (WorkloadApp/Services/TodayVerdictService.swift) — @MainActor final class:
  init(modelContext: ModelContext)
  // injectable SEAM — writes the slots, returns per-exercise results:
  @discardableResult func evaluateAndWrite(
      prescribedWorkout: PrescribedWorkout,
      decisionInput: ReasoningEngine.DecisionInput?,     // nil ⇒ cold-start defer (suggestion == plan)
      crossModalResult: CrossModalFatigueEngine.CrossModalResult?,
      plateStepKg: Double = TodayVerdictEngine.Constants.plateStepKg
  ) -> [TodayVerdictEngine.VerdictResult]
  func makeDecisionInput(built: PRSReadinessInputBuilder.BuiltReadiness,
                         recommendation: AutoregulationEngine.TrainingRecommendation)
      -> ReasoningEngine.DecisionInput

PRSReadinessInputBuilder (WorkloadApp/Services/PRSReadinessInputBuilder.swift):
  static func buildDetailed(recentSnapshots:[RecoverySnapshot], latestHRV:Double?, latestRHR:Double?,
      latestSleepMinutes:Double?, allSessions:[WorkoutSession],
      fatigueResult:FatigueIndexEngine.FatigueResult?, daysSinceRest:Int, wellnessScore:Double?,
      acwr:Double, acwrZone:ACWRZone, asOf:Date, calendar:Calendar)
      -> PRSReadinessInputBuilder.BuiltReadiness?    // nil ⇒ cold-start / honest-confidence defer
  // BuiltReadiness { input, readiness (ReadinessResult, has .confidence: Double), strain }
AutoregulationEngine.recommendReadiness(input: built.input) -> TrainingRecommendation

PlannedSessionRepository (@MainActor): init(modelContext:);
  func fetchTodaysPlannedSession(athleteId: UUID) -> PrescribedWorkout?

Repositories for input assembly (all @MainActor, init(modelContext:)):
  RecoveryRepository.fetchRecoveryHistory(days:Int, athlete:Athlete) throws -> [RecoverySnapshot]
  RecoveryRepository.fetchTodaySnapshot(athlete:) / fetchLatestSnapshot(athlete:)  // -> RecoverySnapshot?
    RecoverySnapshot: .hrvSDNN, .restingHR, .sleepDurationMinutes, .recoveryScore, .date
  WorkloadRepository.fetchLatestSnapshot(athlete:) throws -> WorkloadSnapshot?  // .acwr, .zone
  WorkoutRepository.fetchSessions(last:Int, athlete:) throws -> [WorkoutSession]

Fatigue assembly: mirror DashboardViewModel.swift lines 228–296 (the non-cold-start branch):
  recentSessionTSS / baselineTSS / sessionsIn14Days / baselineSessions14d /
  recentRecoveryScores / recentWellnessScores (14d WellnessCheckIn) /
  NiggleInjuryDeriver.softTissueInjuryCount + daysSinceLastInjury (SorenessLogRepository.fetchRecent)
  → FatigueIndexEngine.compute(input:cycleContext:nil,cyclesObserved:0) -> FatigueResult
daysSinceRest: mirror DashboardViewModel.computeDaysSinceRest (lines 441–458).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerdictDecision types + pure VerdictDecisionApplier</name>
  <files>WorkloadApp/Services/VerdictDecision.swift, WorkloadAppTests/VerdictDecisionApplierTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <behavior>
    - applyAccept(to: topSet, appliedAt: D): topSet.verdictAppliedAt == D AND topSet.athleteOverrode == false AND topSet.targetWeightKg UNCHANGED AND topSet.adjustedTargetWeightKg UNCHANGED.
    - applyKeepPlan(to: topSet): topSet.athleteOverrode == true AND topSet.verdictAppliedAt == nil AND topSet.targetWeightKg UNCHANGED.
    - effectiveTargetKg(topSet): verdictAppliedAt != nil ⇒ adjustedTargetWeightKg ?? targetWeightKg; else ⇒ targetWeightKg (the authored number wins until accepted).
    - applyAccept then applyKeepPlan on the same set flips state cleanly (verdictAppliedAt back to nil, athleteOverrode true) — keep-plan is the non-destructive reverse of accept.
    - VerdictDecision for FeelOverride.feelingStrong carries action == .feel(.feelingStrong); an accepted suggestion carries action == .accepted with hadAdjustment reflecting plannedKg vs adjustedKg.
  </behavior>
  <action>
Create `WorkloadApp/Services/VerdictDecision.swift` (Foundation only — NO SwiftUI import) with:

(1) `enum FeelOverride: String, Codable, CaseIterable { case feelingStrong, feelingRough }` — the athlete's first-class feel input (MOD-11).

(2) `enum VerdictAction: Equatable { case accepted; case keptPlan; case feel(FeelOverride) }` and
`struct VerdictDecision: Equatable { let action: VerdictAction; let plannedTopSetKg: Double; let adjustedTopSetKg: Double?; let hadAdjustment: Bool; let reasonLine: String; let decidedAt: Date }` — the event Phase 45 will log. Composite-only (kg + reason text; no raw HealthKit).

(3) `struct TodayVerdictDisplay: Equatable` with nested `enum Kind { case adjusted, asPlanned, deferred }` and `enum AppliedState { case pending, accepted, keptPlan }`, and fields `headlineExerciseName: String`, `plannedTopSetKg: Double`, `adjustedTopSetKg: Double`, `hasAdjustment: Bool`, `reasonLine: String`, `kind: Kind`, `confidenceNote: String?`, `appliedState: AppliedState`. Pure value type the card renders (no SwiftUI import).

(4) `enum VerdictDecisionApplier` with STATIC funcs that mutate ONLY the two Phase-44 slots — never `targetWeightKg`, never `adjustedTargetWeightKg`, never the source template (the applier only ever receives a frozen-prescription `TemplateSet`):
  - `static func applyAccept(to topSet: TemplateSet, appliedAt: Date = .now)` → `topSet.verdictAppliedAt = appliedAt; topSet.athleteOverrode = false`.
  - `static func applyKeepPlan(to topSet: TemplateSet)` → `topSet.athleteOverrode = true; topSet.verdictAppliedAt = nil`.
  - `static func effectiveTargetKg(_ topSet: TemplateSet) -> Double?` per the <behavior> rule.
  Doc-comment WHY: authored `targetWeightKg` is never written (SC1 "never silently overwrites the planned numbers"); the number the athlete trains is resolved at read time, so the plan stays intact and a keep-plan is non-destructive (SC3).

Register `VerdictDecision.swift` in project.pbxproj with the 4 explicit entries (PBXBuildFile + PBXFileReference + Sources build phase + group membership), mirroring the AC4303/EE4203 pattern from prior phases. Use an unused stable hex prefix (e.g. AA4401*). Do NOT create any " 2.swift" duplicate.

Write `WorkloadAppTests/VerdictDecisionApplierTests.swift` (auto-discovered — no pbxproj entry) covering every <behavior> bullet, PLUS an explicit "source template untouched" assertion: build a `WorkoutTemplate` with a `TemplateSet`, `deepCopyGroups()` into a `PrescribedWorkout`, run applyAccept/applyKeepPlan on the COPY's set, and assert the original template's `TemplateSet.targetWeightKg`, `verdictAppliedAt`, and `athleteOverrode` are unchanged.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/VerdictDecisionApplierTests 2>&1 | tail -20</automated>
  </verify>
  <done>VerdictDecision.swift exists with FeelOverride/VerdictDecision/TodayVerdictDisplay/VerdictDecisionApplier; 4 pbxproj entries present; VerdictDecisionApplierTests pass; applier provably never writes targetWeightKg and never mutates the source template.</done>
</task>

<task type="auto">
  <name>Task 2: TodayVerdictViewModel — assemble inputs, write slots, expose display + decision actions</name>
  <files>WorkloadApp/ViewModels/TodayVerdictViewModel.swift, WorkloadAppTests/TodayVerdictViewModelTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
Create `WorkloadApp/ViewModels/TodayVerdictViewModel.swift` as `@MainActor @Observable final class TodayVerdictViewModel` (mirrors DashboardViewModel conventions). Hold the `ModelContext` and a `TodayVerdictService` + `PlannedSessionRepository` as STORED properties created in `init(modelContext:)` (the stored-property pattern that avoids the iOS-26.1-sim `@MainActor` deinit SIGABRT documented in 42-02/43-03 — do NOT construct repositories as method locals in synchronously-tested code).

Published state: `var display: TodayVerdictDisplay?` (nil ⇒ render no card). Add `var onDecisionRecorded: ((VerdictDecision) -> Void)?` (Phase 45 wires the logger; nil in Phase 44) and `private(set) var lastDecision: VerdictDecision?` (for tests + debug).

`func refresh(athlete: Athlete)`:
  1. `guard let plan = plannedSessionRepository.fetchTodaysPlannedSession(athleteId: athlete.id) else { display = nil; return }`.
  2. Assemble readiness inputs from the repositories per <interfaces>: recentSnapshots (28d), today/latest snapshot → latestHRV/RHR/sleep, acwr/zone from WorkloadRepository.fetchLatestSnapshot (default `0` / `.noData` when absent), allSessions = WorkoutRepository.fetchSessions(last:90). Compute `daysSinceRest` mirroring `DashboardViewModel.computeDaysSinceRest`, and a real `FatigueIndexEngine.FatigueResult?` mirroring DashboardViewModel.swift lines 228–296 (extract a private helper `assembleFatigue(...)`). When there is no real workload/fatigue data leave `fatigueResult = nil` (cold-start → buildDetailed returns nil → honest defer; never fabricate).
  3. `let built = PRSReadinessInputBuilder.buildDetailed(... assembled inputs ..., wellnessScore: nil, asOf: .now, calendar: .current)`.
  4. Write the slots via the service SEAM (pass `crossModalResult: nil` — the shadow gate is OFF so cross-modal is exactly zero today; documented as the single line to revisit on a future gate flip):
       - if `let built`: `let rec = AutoregulationEngine.recommendReadiness(input: built.input); let di = verdictService.makeDecisionInput(built: built, recommendation: rec); verdictService.evaluateAndWrite(prescribedWorkout: plan, decisionInput: di, crossModalResult: nil)`
       - else (cold-start): `verdictService.evaluateAndWrite(prescribedWorkout: plan, decisionInput: nil, crossModalResult: nil)`
  5. Build `display` from the SESSION HEADLINE top set + the slots the service just wrote. Headline = across `plan.allExercises`, the non-warmup set with max `targetWeightKg > 0` (same rule the service uses). If none, `display = nil`. Read `plannedTopSetKg = headline.targetWeightKg`, `adjustedTopSetKg = headline.adjustedTargetWeightKg ?? plannedTopSetKg`, `reasonLine = headline.verdictReason ?? ""`. `hasAdjustment = adjusted < planned − 0.001`. `kind`: `built == nil ⇒ .deferred`, else `hasAdjustment ⇒ .adjusted`, else `.asPlanned`. `confidenceNote`: `.deferred ⇒ String(localized:"verdictCard.confidence.learning", defaultValue:"Still learning your baseline")`; otherwise `nil` (confidence is reported quietly and separately — never as a fear lever, SC4). `appliedState` from slots: `verdictAppliedAt != nil ⇒ .accepted`, `athleteOverrode ⇒ .keptPlan`, else `.pending`.

Decision methods — each iterates EVERY exercise's headline top set in `plan` (per-exercise non-warmup max-weight working set) and applies, then `try? modelContext.save()`, rebuilds `display`, builds a `VerdictDecision` for the headline, sets `lastDecision`, and calls `onDecisionRecorded?(decision)`:
  - `func accept()` → `VerdictDecisionApplier.applyAccept(to:)` on each top set; action `.accepted`.
  - `func keepPlan()` → `VerdictDecisionApplier.applyKeepPlan(to:)` on each top set; action `.keptPlan`. (SC3 one-tap keep — no confirmation step in the model layer.)
  - `func feelOverride(_ feel: FeelOverride)` → MAP feel to a slot outcome, then emit `.feel(feel)`:
      • `.feelingStrong` ⇒ dismiss the suggestion = `applyKeepPlan` on each top set (athlete feels good → keep their plan).
      • `.feelingRough` ⇒ take the conservative path = `applyAccept` on each top set that HAS an adjustment; sets with no adjustment are left as-is (NEVER fabricate a trim the engine did not produce — honest).
    Hold the `plan` reference so the decision methods operate on the same fetched session; re-fetch in `refresh` only.

Register `TodayVerdictViewModel.swift` with 4 explicit pbxproj entries (e.g. AA4402*). Test file auto-discovers.

Write `WorkloadAppTests/TodayVerdictViewModelTests.swift` using the stored-XCTestCase-prop pattern (own the `ModelContainer`/`ModelContext`/viewModel as test props set in `setUp`, cleared in `tearDown` — avoids the @MainActor deinit SIGABRT). Seed an in-memory `ModelContainer`. Cover:
  - No planned session ⇒ `display == nil` after refresh.
  - A planned session with a top set + enough seeded recovery/workout/fatigue data ⇒ `display != nil`, headline name + planned weight correct.
  - Cold-start (planned session but no fatigue/recovery data) ⇒ `display.kind == .deferred`, `confidenceNote` is the still-learning copy, `hasAdjustment == false` (defers — no fabricated trim).
  - `accept()` ⇒ every top set `verdictAppliedAt != nil`, `athleteOverrode == false`, `targetWeightKg` unchanged; `display.appliedState == .accepted`; `lastDecision?.action == .accepted` and `onDecisionRecorded` fired once.
  - `keepPlan()` ⇒ every top set `athleteOverrode == true`, `verdictAppliedAt == nil`, planned numbers unchanged; `lastDecision?.action == .keptPlan`.
  - `feelOverride(.feelingStrong)` ⇒ keep-plan slot state + `lastDecision?.action == .feel(.feelingStrong)`; `feelOverride(.feelingRough)` on a session WITH an adjustment ⇒ accept slot state + `.feel(.feelingRough)`.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/TodayVerdictViewModelTests 2>&1 | tail -25</automated>
  </verify>
  <done>TodayVerdictViewModel exists + registered (4 pbxproj entries); refresh writes slots via evaluateAndWrite and builds the headline display; accept/keepPlan/feelOverride mutate only the two Phase-44 slots, emit a VerdictDecision, and never overwrite targetWeightKg; cold-start defers; all TodayVerdictViewModelTests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| athlete tap → SwiftData write | accept/keep/feel mutate persisted frozen-prescription rows |
| frozen prescription ↔ source template | the authored program must remain immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | VerdictDecisionApplier writes to TemplateSet | mitigate | applier only ever writes verdictAppliedAt/athleteOverrode; targetWeightKg never written (unit-test enforced: accept/keep leave targetWeightKg unchanged) |
| T-44-02 | Tampering | accept/keep operating on the wrong graph | mitigate | applier acts on the frozen PrescribedWorkout copy only; "source template untouched" test proves the authored WorkoutTemplate is never mutated |
| T-44-03 | Information disclosure | VerdictDecision payload (Phase 45 consumer) | accept | composite-only (kg + reason string); no raw HealthKit fields; no sync path on PrescribedWorkout/TemplateSet (Phase 42 proved slot non-serialization) |
| T-44-04 | Repudiation | decline not recorded | mitigate | keep-plan persists athleteOverrode=true via modelContext.save(); SC1 "declines are recorded" is the persisted slot state, not transient UI |

No package-manager installs in this plan → no supply-chain (T-44-SC) checkpoint required.
</threat_model>

<verification>
- Both test suites green on sim `CAF84E71-BB64-491D-87C8-875A0143B26D`.
- `grep -n "targetWeightKg *=" WorkloadApp/Services/VerdictDecision.swift WorkloadApp/ViewModels/TodayVerdictViewModel.swift` returns ZERO assignments to `targetWeightKg` (the authored number is read-only here).
- No `" 2.swift"` duplicates created; `VerdictDecision.swift` and `TodayVerdictViewModel.swift` each have exactly 4 pbxproj entries.
</verification>

<success_criteria>
- Accept sets `verdictAppliedAt` and never writes `targetWeightKg` (SC1).
- Keep-plan sets `athleteOverrode=true`, leaves planned numbers unchanged, persists the decline (SC1/SC3).
- Feel-override maps to a logged `VerdictDecision` and nudges (rough→accept) / dismisses (strong→keep) the suggestion (SC2).
- Cold-start produces a deferred display with the still-learning confidence note, no fabricated trim (SC4).
- The source authored template is provably never mutated.
</success_criteria>

<output>
Create `.planning/phases/44-suggest-and-confirm-verdict-surface/44-01-SUMMARY.md` when done.
</output>
