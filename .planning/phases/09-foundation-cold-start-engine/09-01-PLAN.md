---
phase: 09-foundation-cold-start-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/TrainingProfile.swift
  - WorkloadApp/Models/WorkoutTemplate.swift
  - WorkloadApp/Models/Enums.swift
  - WorkloadApp/App/WorkloadApp.swift
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - "TrainingProfile model persists questionnaire answers, seeded ATL/CTL, and bias fields"
    - "WorkoutTemplate has isAthleteOwned, isFavorite, isArchived, lastUsedAt, usageCount, scheduledDays fields with safe defaults"
    - "BodyRegion enum and InjuryEntry struct exist for injury history encoding"
    - "TrainingProfile is registered in ModelContainer schema and app launches without crash"
  artifacts:
    - path: "WorkloadApp/Models/TrainingProfile.swift"
      provides: "TrainingProfile @Model with questionnaire + seed + bias + cold-start fields"
      contains: "@Model"
    - path: "WorkloadApp/Models/WorkoutTemplate.swift"
      provides: "Extended WorkoutTemplate with athlete ownership and template management fields"
      contains: "isAthleteOwned"
    - path: "WorkloadApp/Models/Enums.swift"
      provides: "BodyRegion enum and InjuryEntry Codable struct"
      contains: "enum BodyRegion"
    - path: "WorkloadApp/App/WorkloadApp.swift"
      provides: "TrainingProfile.self in schema array"
      contains: "TrainingProfile.self"
  key_links:
    - from: "WorkloadApp/Models/TrainingProfile.swift"
      to: "WorkloadApp/Models/Enums.swift"
      via: "BodyRegion type used in InjuryEntry stored as Data on TrainingProfile"
      pattern: "injuryHistory.*Data"
    - from: "WorkloadApp/App/WorkloadApp.swift"
      to: "WorkloadApp/Models/TrainingProfile.swift"
      via: "schema array registration"
      pattern: "TrainingProfile\\.self"
---

<objective>
Create the SwiftData data models for Phase 9: the new TrainingProfile model (standalone, one-to-one with athlete via athleteId UUID), extend WorkoutTemplate with additive athlete-ownership and template management fields, and add the BodyRegion enum + InjuryEntry Codable struct to Enums.swift. Register TrainingProfile in the ModelContainer schema.

Purpose: Establish the persistence layer that ColdStartEngine writes to and TemplateRepository queries against. All downstream plans and phases depend on these models existing.
Output: Four modified/created Swift files forming the complete model layer for cold-start and athlete-owned templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-foundation-cold-start-engine/09-CONTEXT.md
@.planning/phases/09-foundation-cold-start-engine/09-RESEARCH.md
@.planning/phases/09-foundation-cold-start-engine/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From WorkloadApp/Models/WorkoutTemplate.swift (current state):
```swift
@Model
final class WorkoutTemplate {
    @Attribute(.unique) var id: UUID
    var coachId: UUID
    var templateName: String
    var sportType: SportType
    var sessionType: SessionType
    var notes: String?
    var createdAt: Date
    var updatedAt: Date

    @Relationship(deleteRule: .cascade, inverse: \ExerciseGroup.template)
    var groups: [ExerciseGroup] = []
    // ... init and deepCopyGroups() ...
}
```

From WorkloadApp/App/WorkloadApp.swift (schema array, lines 22-39):
```swift
let schema = Schema([
    Athlete.self,
    WorkoutSession.self,
    ExerciseEntry.self,
    SetRecord.self,
    WorkloadSnapshot.self,
    RecoverySnapshot.self,
    WellnessCheckIn.self,
    PersonalRecord.self,
    CoachAthleteRelationship.self,
    WorkoutTemplate.self,
    ExerciseGroup.self,
    TemplateExercise.self,
    TemplateSet.self,
    PrescribedWorkout.self,
    CustomExercise.self,
    BehaviorTag.self,
])
```

From WorkloadApp/Models/Enums.swift (end of file, line 342):
```swift
// File ends after ExperienceLevel enum at line 342
// New BodyRegion enum goes after line 342
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrainingProfile model and add BodyRegion enum + InjuryEntry struct</name>
  <files>WorkloadApp/Models/TrainingProfile.swift, WorkloadApp/Models/Enums.swift</files>
  <read_first>
    - WorkloadApp/Models/Enums.swift (current state, ends at line 342)
    - WorkloadApp/Models/WellnessCheckIn.swift (analog for @Model pattern)
    - .planning/phases/09-foundation-cold-start-engine/09-PATTERNS.md (pattern assignments)
  </read_first>
  <action>
1. Create `WorkloadApp/Models/TrainingProfile.swift` as a new file with:
   - `import Foundation` and `import SwiftData`
   - `@Model final class TrainingProfile` with `@Attribute(.unique) var id: UUID`
   - `var athleteId: UUID` (plain UUID foreign key per D-07, NOT a @Relationship)
   - Required questionnaire fields per D-08: `var sessionsPerWeek: Int`, `var avgDurationMinutes: Int`, `var typicalSRPE: Double`, `var weeksAtLevel: Int`
   - Optional questionnaire fields per D-09: `var trainingAgeYears: Int?`, `var periodizationPreference: String?`, `var movementTypes: [String]?`, `var injuryHistory: Data?`
   - Seeded values per D-11: `var seededATL: Double`, `var seededCTL: Double`, `var seededAt: Date`
   - Bias fields per D-12: `var biasEstimatedATL: Double?`, `var biasEstimatedCTL: Double?`, `var biasActualATL: Double?`, `var biasActualCTL: Double?`, `var biasCapturedAt: Date?`
   - Cold-start window per D-13: `var coldStartCompletedAt: Date?`
   - Timestamps: `var createdAt: Date`, `var updatedAt: Date`
   - Init taking required fields + seeded values, with `id: UUID = UUID()`, timestamps default to `.now`

2. Append to `WorkloadApp/Models/Enums.swift` after line 342:
   - `// MARK: - Injury Enums` section header
   - `enum BodyRegion: String, Codable, CaseIterable, Identifiable` with cases: `shoulder, knee, back, hip, ankle, wrist, elbow, neck`
   - Include `var id: String { rawValue }` and `var displayName: String` computed property (capitalized case names)
   - `struct InjuryEntry: Codable` with properties: `let bodyRegion: BodyRegion`, `let notes: String?`, `let isActive: Bool`
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && xcodebuild -scheme "workload management" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5 && echo "--- pbxproj check ---" && grep -c "TrainingProfile.swift" "workload management.xcodeproj/project.pbxproj"</automated>
  </verify>
  <acceptance_criteria>
    - TrainingProfile.swift is included in .pbxproj (grep returns >= 1)
    - TrainingProfile.swift contains `@Model` and `final class TrainingProfile`
    - TrainingProfile.swift contains `@Attribute(.unique) var id: UUID`
    - TrainingProfile.swift contains `var athleteId: UUID` (NOT @Relationship)
    - TrainingProfile.swift contains `var sessionsPerWeek: Int`
    - TrainingProfile.swift contains `var avgDurationMinutes: Int`
    - TrainingProfile.swift contains `var typicalSRPE: Double`
    - TrainingProfile.swift contains `var weeksAtLevel: Int`
    - TrainingProfile.swift contains `var seededATL: Double`
    - TrainingProfile.swift contains `var seededCTL: Double`
    - TrainingProfile.swift contains `var seededAt: Date`
    - TrainingProfile.swift contains `var biasEstimatedATL: Double?`
    - TrainingProfile.swift contains `var coldStartCompletedAt: Date?`
    - TrainingProfile.swift contains `var injuryHistory: Data?`
    - Enums.swift contains `enum BodyRegion: String, Codable, CaseIterable, Identifiable`
    - Enums.swift contains `case shoulder, knee, back, hip, ankle, wrist, elbow, neck` (or individual case lines)
    - Enums.swift contains `struct InjuryEntry: Codable`
    - Enums.swift contains `let bodyRegion: BodyRegion`
  </acceptance_criteria>
  <done>TrainingProfile model file exists with all D-07 through D-13 fields. BodyRegion enum and InjuryEntry struct exist in Enums.swift. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extend WorkoutTemplate with additive fields and register TrainingProfile in schema</name>
  <files>WorkloadApp/Models/WorkoutTemplate.swift, WorkloadApp/App/WorkloadApp.swift</files>
  <read_first>
    - WorkloadApp/Models/WorkoutTemplate.swift (current state -- 180 lines, fields end at line 15)
    - WorkloadApp/App/WorkloadApp.swift (schema array at lines 22-39)
    - .planning/phases/09-foundation-cold-start-engine/09-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
1. Add the following fields to `WorkoutTemplate` class in `WorkloadApp/Models/WorkoutTemplate.swift`, after line 15 (after `var updatedAt: Date`) and before the `@Relationship` line 17. All fields MUST have defaults for lightweight migration safety:
   ```swift
   // MARK: - Athlete Ownership (D-01)
   var isAthleteOwned: Bool = false
   var athleteId: UUID? = nil
   // MARK: - Template Management (D-02)
   var isFavorite: Bool = false
   var isArchived: Bool = false
   var lastUsedAt: Date? = nil
   var usageCount: Int = 0
   var scheduledDays: [Int] = []  // ISO 8601: 1=Mon...7=Sun
   ```

2. In `WorkloadApp/App/WorkloadApp.swift`, add `TrainingProfile.self,` to the schema array after `BehaviorTag.self,` (line 38). The resulting array entry should be:
   ```swift
   BehaviorTag.self,
   TrainingProfile.self,
   ```
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && xcodebuild -scheme "workload management" -destination "platform=iOS Simulator,name=iPhone 16 Pro" build 2>&1 | tail -5 && echo "--- pbxproj check ---" && grep -c "TrainingProfile.swift" "workload management.xcodeproj/project.pbxproj"</automated>
  </verify>
  <acceptance_criteria>
    - TrainingProfile.swift is included in .pbxproj (grep returns >= 1)
    - WorkoutTemplate.swift contains `var isAthleteOwned: Bool = false`
    - WorkoutTemplate.swift contains `var athleteId: UUID? = nil`
    - WorkoutTemplate.swift contains `var isFavorite: Bool = false`
    - WorkoutTemplate.swift contains `var isArchived: Bool = false`
    - WorkoutTemplate.swift contains `var lastUsedAt: Date? = nil`
    - WorkoutTemplate.swift contains `var usageCount: Int = 0`
    - WorkoutTemplate.swift contains `var scheduledDays: [Int] = []`
    - WorkloadApp.swift contains `TrainingProfile.self`
    - All new fields have default values (no bare declarations without `=`)
    - Existing `coachId`, `templateName`, etc. fields are UNCHANGED
    - Build succeeds with zero errors
  </acceptance_criteria>
  <done>WorkoutTemplate has all D-01 and D-02 fields with defaults. TrainingProfile registered in ModelContainer. App builds and would not crash on launch for existing users (lightweight migration safe).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftData local store | Model fields are local-only until sync; no trust boundary crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | TrainingProfile.athleteId | accept | Local-only in this plan; RLS enforcement comes in Plan 03 migration. No remote write path yet. |
| T-09-02 | Denial of Service | WorkloadApp schema registration | mitigate | TrainingProfile.self MUST be added to schema array; missing registration causes fatalError crash on launch. Acceptance criteria verifies presence. |
</threat_model>

<verification>
- `xcodebuild build` succeeds with zero errors
- `grep -c "TrainingProfile.self" WorkloadApp/App/WorkloadApp.swift` returns 1
- `grep -c "isAthleteOwned" WorkloadApp/Models/WorkoutTemplate.swift` returns at least 1
- `grep -c "@Model" WorkloadApp/Models/TrainingProfile.swift` returns 1
</verification>

<success_criteria>
- TrainingProfile.swift exists as a complete @Model with all fields from D-07 through D-13
- WorkoutTemplate.swift has 7 new additive fields with defaults (D-01, D-02)
- BodyRegion enum has 8 cases, InjuryEntry struct has 3 properties
- TrainingProfile.self registered in WorkloadApp schema array
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation-cold-start-engine/09-01-SUMMARY.md`
</output>
