---
phase: 30-shadow-engine-quality-fixes
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
autonomous: true
requirements: [PRS-30-02]
files_modified:
  - WorkloadApp/Services/LoadDistributionEngine.swift
  - WorkloadAppTests/LoadDistributionEngineTests.swift
must_haves:
  truths:
    - "The unified daily-load series fed to Foster monotony/strain z-standardises the endurance-sRPE stream and the strength-strain stream SEPARATELY before summing, so strength is no longer drowned out when sessionRPE is logged."
    - "A stream with zero variance (or <2 nonzero days) contributes a constant 0 after standardisation — no NaN, no divide-by-zero — and the other stream drives monotony."
    - "The raw dailyLoadSeries (endurance+strength absolute load) keeps its existing meaning for non-monotony consumers; standardisation is a separate path used only for monotony/strain."
    - "The completeness gate (>= monotonyMinLoggedDays logged days + nonzero variance) is unchanged and still guards Foster."
    - "Pure monotony/strain known-value tests on raw arrays are unaffected; live behavior unchanged (engine is shadow-only)."
  artifacts:
    - path: "WorkloadApp/Services/LoadDistributionEngine.swift"
      provides: "Per-stream z-standardised unified series feeding Foster monotony/strain"
      contains: "standardiz"
    - path: "WorkloadAppTests/LoadDistributionEngineTests.swift"
      provides: "Re-derived dailyLoad oracle + standardisation contribution tests (strength no longer noise)"
      contains: "standardiz"
  key_links:
    - from: "LoadDistributionEngine.distribution"
      to: "z-standardised endurance + strength sub-series"
      via: "per-stream standardise then sum, then monotony/strain"
      pattern: "standardiz|zScore"
---

<objective>
Fix Finding 2 (GA-30-B): the unified daily-load series sums endurance sRPE load (tens-hundreds) with strength strain weights (single digits), so endurance dominates Foster monotony/strain and strength becomes noise whenever sessionRPE is logged. Z-standardise each stream separately over the window before combining. Shadow-only engine — no live path.

Purpose: monotony/strain reflect BOTH load sources comparably so the shadow Strain-Risk monotony component is honest for serious lifters who also log endurance.
Output: corrected `LoadDistributionEngine` with a separate standardised path for monotony/strain, re-derived `LoadDistributionEngineTests` oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shadow-engine-quality-fixes/30-RESEARCH.md
@WorkloadApp/Services/LoadDistributionEngine.swift
@WorkloadApp/Services/StrengthLoadEngine.swift
@.planning/phases/30-shadow-engine-quality-fixes/30-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- LoadDistributionEngine.swift today:
     dailyLoadSeries (L66-91) builds [DailyLoad{dayStart, load}] where load = sessionUnifiedLoad = srpeLoad (if sessionRPE) + sessionStrengthLoad. This single series is fed to monotony/strain via distribution (L159-187).
     monotony([Double]) L125-130 = mean/sampleSD; strain L133-136 = sum*monotony. KEEP these pure helpers unchanged.
     completenessGate L150-156, fallbackLoadSignal L194-224 — KEEP unchanged.
     sessionUnifiedLoad L96-103 = srpeLoad + sessionStrengthLoad; sessionStrengthLoad L107-119 reuses StrengthLoadEngine.classify/strainWeight (Wave-1-corrected Double RIR is already reflected). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-stream z-standardised unified series for monotony/strain</name>
  <files>WorkloadApp/Services/LoadDistributionEngine.swift, WorkloadAppTests/LoadDistributionEngineTests.swift</files>
  <behavior>
    - Build two per-day sub-series over the window's logged days: enduranceByDay (sum srpeLoad) and strengthByDay (sum sessionStrengthLoad), keyed by the SAME logged days as the raw series.
    - Standardise each sub-series: z = (value - mean)/sd over that stream's per-day values; if sd==0 or fewer than 2 nonzero days, that stream contributes 0 for every day.
    - The monotony/strain input series = per-day (zEndurance + zStrength).
    - With endurance on every day and strength on a few, strength measurably moves monotony vs an endurance-only series.
    - Zero-variance stream produces no NaN/inf; the other stream drives the result.
    - distribution() still gates on the EXISTING completenessGate over the RAW series; only the values fed to monotony/strain change.
  </behavior>
  <action>Add private standardize(values:[Double]) -> [Double] (mean/sampleSD; if count<2 or sd==0 return zeros of same length). Add monotonyInputSeries(sessions:asOf:calendar:windowDays:) that builds the logged-day set exactly as dailyLoadSeries does (same windowing + same byDay keys), computes per-day endurance (sum WorkloadCalculator.srpeLoad for sessions with sessionRPE) and per-day strength (sum sessionStrengthLoad reusing the Wave-1-corrected StrengthLoadEngine.classify/strainWeight), orders by day, standardises each stream, and returns the element-wise sum. In distribution, feed monotonyInputSeries(...) to monotony/strain when the gate passes (NOT the raw series.map load); keep completenessGate(series) over the RAW series and loggedDays = series.count unchanged; keep fallbackLoadSignal unchanged. Leave dailyLoadSeries and the pure monotony/strain/sampleSD helpers untouched. Document the standardisation + zero-variance rule in a doc comment. Do NOT change the raw dailyLoadSeries oracle (day1.load == 280.0 stays — raw series is unchanged). Add tests: (a) strength stream measurably changes monotony vs endurance-only over a fixed dense series; (b) all-endurance / zero-strength-variance series yields finite monotony (no NaN); (c) distribution determinism unchanged.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/LoadDistributionEngineTests 2>&1 | tail -25</automated>
  </verify>
  <done>monotony/strain consume the per-stream-standardised summed series; strength measurably contributes; zero-variance stream produces no NaN; raw dailyLoadSeries unchanged; LoadDistributionEngineTests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression + fence integrity</name>
  <files>WorkloadApp/Services/LoadDistributionEngine.swift</files>
  <action>Run the FULL suite to confirm the StrainRiskEngine monotony-fed path (consumes LoadDistributionResult.monotony) still compiles. StrainRisk's OWN expectations are re-derived in Wave 3, so a StrainRisk worked-example test MAY now drift — note any such drift in the SUMMARY for Wave 3 to absorb, do NOT edit StrainRiskEngineTests here. Confirm git diff --stat shows zero changes to the three fence test files. If any NON-StrainRisk, non-fence test regresses, fix or HALT.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -15; cd /Users/hanwen/Desktop/Tonus && git diff --stat WorkloadAppTests/BaselineTierFenceTests.swift WorkloadAppTests/AutoregulationFlagFenceTests.swift WorkloadAppTests/DualRunFlagFenceTests.swift</automated>
  </verify>
  <done>Full suite compiles + runs; the three fence files show zero diff; any StrainRisk worked-example drift is noted for Wave 3 (not edited here).</done>
</task>

</tasks>

<verification>
- LoadDistributionEngineTests green with the standardisation contribution + no-NaN tests.
- Raw dailyLoadSeries oracle unchanged.
- The three fence files unmodified (git diff --stat empty).
- No flag default changed.
</verification>

<success_criteria>
Finding 2 fixed via per-stream z-standardisation; strength contributes to monotony/strain; zero-variance safe; raw series preserved; fences untouched.
</success_criteria>

<output>
Create `.planning/phases/30-shadow-engine-quality-fixes/30-02-SUMMARY.md` when done (note the standardisation scheme + any StrainRisk worked-example drift for Wave 3 to absorb).
</output>
