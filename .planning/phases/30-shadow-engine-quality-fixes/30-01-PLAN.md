---
phase: 30-shadow-engine-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PRS-30-03, PRS-30-04, PRS-30-05]
files_modified:
  - WorkloadApp/Services/StrengthLoadEngine.swift
  - WorkloadAppTests/StrengthLoadEngineTests.swift
  - WorkloadAppTests/StrainRiskEngineTests.swift
must_haves:
  truths:
    - "A working set at RPE 7.5 (RIR 2.5) is classified .easy, not .hard, when not hard-by-intensity."
    - "Chronic per-day strength load is computed over the window that EXCLUDES the acute window, so a steady-state athlete gets elevation ~0 instead of an artifactual ~4x ratio."
    - "A muscle whose chronic-exclusive window has zero load (new exercise / warmups only) reports elevation 0 AND hasChronicBaseline=false (insufficient baseline), never 0-as-safe-and-never-4x-as-max."
    - "MuscleStrengthLoad exposes easyCount (scored sets below the hard bar), captured during aggregation, for Wave-3 coverage confidence."
    - "windowed partitions acute (0..<acuteWindowDays) and chronic-exclusive (acuteWindowDays..<chronicWindowDays) with no overlapping boundary day."
    - "MuscleStrengthLoad remains a pure value type, never synced, absent from SyncService."
    - "Full WorkloadAppTests suite green via real xcodebuild after the MuscleStrengthLoad field additions; three live fences untouched."
  artifacts:
    - path: "WorkloadApp/Services/StrengthLoadEngine.swift"
      provides: "Double-RIR classify, chronic-excludes-acute elevation, easyCount + hasChronicBaseline on MuscleStrengthLoad"
      contains: "hasChronicBaseline"
    - path: "WorkloadAppTests/StrengthLoadEngineTests.swift"
      provides: "Re-derived fractional-RPE classification, chronic-exclusion elevation oracle, zero-chronic baseline-flag, windowed boundary tests"
      contains: "hasChronicBaseline"
  key_links:
    - from: "StrengthLoadEngine.classify"
      to: "estRIRPrecise (Double)"
      via: "hardByRIR compared in Double against 2.0"
      pattern: "estRIRPrecise|<= 2.0|<= 2\\.0"
    - from: "StrengthLoadEngine.perMuscleStrengthLoad"
      to: "chronic-exclusive window aggregation"
      via: "windowed(acuteWindowDays..<chronicWindowDays)"
      pattern: "chronicWindowDays|hasChronicBaseline"
---

<objective>
Fix Findings 3 + 4 in `StrengthLoadEngine` and capture the data Finding 5 needs, in ONE edit so the `MuscleStrengthLoad` shared-type change is paid once. NO live behavior: this engine feeds only the shadow `StrainRiskEngine` / shadow logging — grep already confirms it never reaches `AutoregulationEngine`/`RecoveryScoreEngine`.

Purpose: an RPE 7.5 set stops being misclassified hard; chronic baseline stops being a superset of acute (kills the artifactual ~4x elevation); new-exercise/zero-chronic muscles are flagged as insufficient-baseline instead of silently-safe or maxed; and easy scored sets are counted so Wave 3 can include them in coverage.
Output: corrected `StrengthLoadEngine`, expanded `MuscleStrengthLoad`, re-derived `StrengthLoadEngineTests`, and the compile-touch updates to `StrainRiskEngineTests` builder for the new `MuscleStrengthLoad` fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shadow-engine-quality-fixes/30-RESEARCH.md
@WorkloadApp/Services/StrengthLoadEngine.swift
@CLAUDE.md

<interfaces>
<!-- Current MuscleStrengthLoad (StrengthLoadEngine.swift L51-61): hardSetCount:Int, strengthLoad:Double, unscoredCount:Int, elevation:Double.
     This plan ADDS: easyCount:Int, hasChronicBaseline:Bool.
     Memberwise-init call sites to update: aggregateMuscle/perMuscleStrengthLoad (engine),
     StrainRiskEngineTests `strengthResult(...)` builder (L29-44 — constructs MuscleStrengthLoad),
     and any MuscleStrengthLoad(...) in PRSShadowArmTests (grep to confirm).
     classify (L140-157) currently: hardByRIR = (rir ?? Int.max) <= Constants.hardSetRIRThreshold, rir = estRIR (Int, truncating).
     perMuscleElevation (L221-228): guard chronic>0 else return 0; ratio = acute/chronic.
     windowed (L315-327): diff >= 0 && diff <= days  (inclusive both ends = days+1 days). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Double-precision RIR classification (Finding 4, GA-30-D)</name>
  <files>WorkloadApp/Services/StrengthLoadEngine.swift, WorkloadAppTests/StrengthLoadEngineTests.swift</files>
  <behavior>
    - estRIR(Int?) keeps its current truncating contract and existing tests (RPE 8 → 2, RPE 10 → 0, RPE 6 → 4) stay green.
    - New estRIRPrecise(SetRecord) -> Double?: logged rir wins as Double(rir); else max(0, rpeToRIRMax - rpe) as Double; else nil.
    - classify computes hardByRIR using the precise Double value against threshold 2.0: RPE 7.5 → 2.5 RIR → hardByRIR false → .easy (when not hard-by-intensity). RPE 8.0 → 2.0 → hardByRIR true. RPE 7.6 → 2.4 → false.
    - A set hard-by-intensity (relIntensity >= 0.80) is still .hard regardless of RIR.
  </behavior>
  <action>Add `static func estRIRPrecise(_ set: SetRecord) -> Double?` mirroring `estRIR` but returning the un-truncated Double (rir as Double(rir); else `max(0.0, Constants.rpeToRIRMax - rpe)`; else nil). In `classify`, replace the Int `rir`/`hardByRIR` with the precise path: `let rirPrecise = estRIRPrecise(set)`, `let hardByRIR = (rirPrecise ?? .greatestFiniteMagnitude) <= Double(Constants.hardSetRIRThreshold)`. Keep the unscored guard checking that relIntensity OR rirPrecise is non-nil. Do NOT change `estRIR` (Int) — display/back-compat callers and L56-62 tests depend on it. Add new test cases: estRIRPrecise from RPE 7.5 → 2.5, classify RPE 7.5 low-intensity → .easy, classify RPE 8.0 → .hard(nil-or-bucket), classify RPE 7.6 → .easy. Leave existing estRIR(Int) tests unchanged.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/StrengthLoadEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>estRIRPrecise exists; classify uses Double comparison; RPE 7.5 low-intensity classifies .easy; all StrengthLoadEngineTests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chronic-excludes-acute elevation + zero-chronic baseline flag + easyCount (Findings 3 + 5-data, GA-30-C/E)</name>
  <files>WorkloadApp/Services/StrengthLoadEngine.swift, WorkloadAppTests/StrengthLoadEngineTests.swift, WorkloadAppTests/StrainRiskEngineTests.swift</files>
  <behavior>
    - windowed gets a half-open lower/upper form so acute = sessions with 0 <= diff < acuteWindowDays and chronic-exclusive = acuteWindowDays <= diff < chronicWindowDays (no shared boundary day).
    - chronicPerDay normaliser = (chronicWindowDays - acuteWindowDays); acutePerDay normaliser = acuteWindowDays.
    - Steady-state athlete (same load each window) → ratio ~1 → elevation 0.
    - New-exercise-only-in-acute / warmups-only history → chronic-exclusive load 0 → elevation 0 AND hasChronicBaseline=false.
    - Established chronic base + genuine acute spike → elevation > 0, hasChronicBaseline=true.
    - MuscleStrengthLoad.easyCount counts scored sets classified .easy in the ACUTE window (same window as hardSetCount/unscoredCount).
  </behavior>
  <action>Refactor `windowed` to a half-open range helper: add a `windowedRange(_:lowerDayInclusive:upperDayExclusive:asOf:calendar:)` (filter `diff >= lower && diff < upper`); express acute as `windowedRange(lower:0, upper:acuteWindowDays)` and chronic-exclusive as `windowedRange(lower:acuteWindowDays, upper:chronicWindowDays)`. Keep a thin `windowed(_:days:...)` wrapper if other callers exist, but the elevation path must use the partitioned ranges. In `perMuscleStrengthLoad`: aggregate `acute` over acute range and `chronicExcl` over chronic-exclusive range; `acutePerDay = acute.strengthLoad / Double(acuteWindowDays)`; `chronicPerDay = chronicExcl.strengthLoad / Double(max(1, chronicWindowDays - acuteWindowDays))`; `hasChronicBaseline = chronicExcl.strengthLoad > 0`; `elevation = hasChronicBaseline ? perMuscleElevation(acute: acutePerDay, chronic: chronicPerDay) : 0`. Extend `aggregateMuscle` to also return `easyCount` (increment on the `.easy` case instead of bare `continue`). Add `easyCount: Int` and `hasChronicBaseline: Bool` to `MuscleStrengthLoad` and populate them. The set of muscles considered must still include muscles appearing in EITHER window (so a new acute-only muscle is evaluated, flagged insufficient-baseline) — build the muscle set from acute ∪ chronic-exclusive sessions, not chronic only. Update the `StrainRiskEngineTests.strengthResult(...)` builder (and any PRSShadowArmTests MuscleStrengthLoad init) to pass `easyCount: 0, hasChronicBaseline: true` defaults so they compile. Re-derive StrengthLoadEngineTests elevation oracles: replace `perMuscleStrengthLoad` elevation expectations with the chronic-exclusive math; add a steady-state→elevation-0 test, a new-exercise→elevation-0+hasChronicBaseline-false test, an established+spike→elevation>0+hasChronicBaseline-true test, an easyCount test, and a windowed-boundary test (a session exactly at diff==acuteWindowDays lands in chronic-exclusive not acute). Keep the pure `perMuscleElevation(acute:chronic:)` unit tests (L182-200) as-is — that helper's contract is unchanged.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -30</automated>
  </verify>
  <done>MuscleStrengthLoad has easyCount + hasChronicBaseline; chronic excludes acute; steady-state→0, new-exercise→0+flag-false, spike→>0+flag-true; FULL suite green (shared-type change), three fences untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Local-only / no-sync guard for the expanded value type</name>
  <files>WorkloadApp/Services/StrengthLoadEngine.swift</files>
  <action>Confirm (grep) `MuscleStrengthLoad` / `StrengthLoadResult` are not referenced in `SyncService` and carry no Codable conformance enabling sync. They are pure structs (no `@Model`, no `: Codable` for sync). No code change expected; if any sync reference is found, HALT and report (invariant violation). Record the grep result in the SUMMARY.</action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && grep -rn "MuscleStrengthLoad\|StrengthLoadResult" WorkloadApp/Services/SyncService.swift 2>/dev/null | grep -vc '^$'; echo "expect: 0 sync references"</automated>
  </verify>
  <done>Zero SyncService references to the changed value types; documented in SUMMARY.</done>
</task>

</tasks>

<verification>
- StrengthLoadEngineTests green with re-derived elevation/classification oracles.
- FULL WorkloadAppTests green via real xcodebuild (MuscleStrengthLoad field additions are a shared-type change).
- `git diff --stat` shows NO changes to BaselineTierFenceTests / AutoregulationFlagFenceTests / DualRunFlagFenceTests.
- No flag default changed.
</verification>

<success_criteria>
Findings 3 + 4 fixed, Finding-5 data captured (easyCount), MuscleStrengthLoad expanded + all init sites updated, oracle tests re-derived to correct values, full suite green, fences untouched.
</success_criteria>

<output>
Create `.planning/phases/30-shadow-engine-quality-fixes/30-01-SUMMARY.md` when done (note the new MuscleStrengthLoad shape + every init site updated + the no-sync grep result).
</output>
