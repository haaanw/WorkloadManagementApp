---
phase: 30-shadow-engine-quality-fixes
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
autonomous: true
requirements: [PRS-30-01, PRS-30-05]
files_modified:
  - WorkloadApp/Services/StrainRiskEngine.swift
  - WorkloadAppTests/StrainRiskEngineTests.swift
must_haves:
  truths:
    - "Soft-tissue and rest-debt each contribute to the Strain-Risk score EXACTLY ONCE: component 3 (Accumulated fatigue) uses a fatigue value that EXCLUDES the FatigueIndex internal soft-tissue + rest-debt contributions, while standalone components 5 (soft-tissue + recurrence bonus) and 6 (rest debt) remain."
    - "The de-double-counted fatigue component is computed from the exposed FatigueResult component fields (load, density, recovery-trend, wellness-trend) re-normalised over their FatigueIndex weights — no FatigueIndexEngine change."
    - "Coverage confidence uses (hard + easy) / (hard + easy + unscored), so an all-easy fully-scored session reports full coverage rather than 0."
    - "Confidence is discounted for muscles lacking a chronic baseline (hasChronicBaseline == false) so a new-exercise heavy session reads as low-confidence, not high-strain and not silently safe."
    - "Sign-constraint preserved: raising any single risk input never lowers the score. Zone thresholds and the no-injury-prediction copy audit still pass."
    - "Engine stays shadow-only (grep: no AutoregulationEngine / RecoveryScoreEngine reference); live behavior unchanged."
  artifacts:
    - path: "WorkloadApp/Services/StrainRiskEngine.swift"
      provides: "Single-count soft-tissue/rest-debt fusion + easy-inclusive + baseline-discounted coverage confidence"
      contains: "hasChronicBaseline"
    - path: "WorkloadAppTests/StrainRiskEngineTests.swift"
      provides: "Re-derived worked-example score, single-count assertion, easy-inclusive coverage tests, baseline-discount confidence test"
      contains: "easyCount"
  key_links:
    - from: "StrainRiskEngine.fuse component 3"
      to: "fatigue-excluding-softtissue-restdebt"
      via: "re-normalise the 4 non-overlapping FatigueResult components"
      pattern: "loadElevation|sessionDensity|recoveryTrend|wellnessTrend"
    - from: "StrainRiskEngine.confidence"
      to: "easyCount + hasChronicBaseline"
      via: "(hard+easy)/(hard+easy+unscored) and baseline discount"
      pattern: "easyCount|hasChronicBaseline"
---

<objective>
Fix Finding 1 (soft-tissue + rest-debt double-count) and Finding 5 consumption (easy sets in coverage) in `StrainRiskEngine`, consuming the corrected Wave-1 `MuscleStrengthLoad` (easyCount, hasChronicBaseline) and Wave-2 monotony. Shadow-only — grep already confirms isolation from the live path.

Purpose: each underlying signal contributes to the Strain-Risk score exactly once; coverage confidence counts the easy scored sets it currently discards and honestly discounts when a muscle has no chronic baseline.
Output: corrected `StrainRiskEngine`, re-derived `StrainRiskEngineTests` (worked-example score, single-count proof, easy-inclusive coverage, baseline-discount confidence), absorbing any Wave-2 worked-example drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shadow-engine-quality-fixes/30-RESEARCH.md
@WorkloadApp/Services/StrainRiskEngine.swift
@WorkloadApp/Services/FatigueIndexEngine.swift
@.planning/phases/30-shadow-engine-quality-fixes/30-01-SUMMARY.md
@.planning/phases/30-shadow-engine-quality-fixes/30-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- StrainRiskEngine.fuse (L109-174): component 3 = clamp01(fatigue.index/100) at Weights.fatigueIndex (0.20). Components 5 (soft-tissue, 0.12, + recurrenceBonus) and 6 (rest debt, 0.08).
     FatigueResult exposes (FatigueIndexEngine.swift L45-54): index, loadElevation, sessionDensity, recoveryTrend, restDebt, wellnessTrend, softTissueRisk.
     FatigueIndex internal weights (L83-88): load 0.20, density 0.20, recovery 0.20, restDebt 0.15, wellness 0.15, softTissue 0.10. The composite index folds softTissue (0.10) + restDebt (0.15) → that is the double-count source.
     To get "fatigue excluding softtissue+restdebt": re-normalise the 4 remaining components (load 0.20 + density 0.20 + recovery 0.20 + wellness 0.15 = 0.75): excludedIndex = (load*0.20 + density*0.20 + recovery*0.20 + wellness*0.15) / 0.75 * 100. Feed clamp01(excludedIndex/100) to component 3.
     confidence (L191-209): coverage = hard/(hard+unscored). Change to (hard+easy)/(hard+easy+unscored) using the Wave-1 easyCount field; add a baseline-discount factor from hasChronicBaseline. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-count soft-tissue + rest-debt (Finding 1, GA-30-A)</name>
  <files>WorkloadApp/Services/StrainRiskEngine.swift, WorkloadAppTests/StrainRiskEngineTests.swift</files>
  <behavior>
    - Component 3 (Accumulated fatigue) consumes a fatigue value EXCLUDING the FatigueIndex internal soft-tissue + rest-debt contributions: from FatigueResult, recompute (loadElevation*0.20 + sessionDensity*0.20 + recoveryTrend*0.20 + wellnessTrend*0.15) / 0.75, clamped 0..1.
    - Components 5 (soft-tissue + recurrence bonus, weight 0.12) and 6 (rest debt, weight 0.08) stay standalone.
    - Each underlying signal (soft-tissue, rest-debt) now contributes once; raising softTissueRisk OR restDebt still raises the score (sign-constraint holds via comps 5/6).
    - all-zero input → score 0; worked-example all-maxed → score recomputed to its new correct value (will differ from the old > 0.9; assert the new exact/near value).
  </behavior>
  <action>Add a private helper fatigueExcludingSoftTissueRestDebt(_ f: FatigueResult) -> Double that re-normalises the 4 non-overlapping components ((loadElevation*0.20 + sessionDensity*0.20 + recoveryTrend*0.20 + wellnessTrend*0.15)/0.75), clamped 0..1. In fuse, change component 3 to append ("Accumulated fatigue", fatigueExcludingSoftTissueRestDebt(input.fatigue), Weights.fatigueIndex) instead of fatigue.index/100. Leave comps 5 and 6 unchanged (still read softTissueRisk + recurrence bonus, and restDebt). Use the documented FatigueIndex internal weights as named local constants (do NOT reach into FatigueIndexEngine's private weights — re-declare them locally with a comment citing FatigueIndexEngine.swift L83-88, since they are private there). Re-derive StrainRiskEngineTests: recompute test_fuse_allComponentsPresent expected score under the new component-3 formula (with the all-maxed builder, the four exposed component fields are 0 in the test builder fatigue() helper EXCEPT index — NOTE: the test builder sets index directly and leaves loadElevation/sessionDensity/recoveryTrend/wellnessTrend = 0, so component 3 will now be ~0; update the builder OR the assertion accordingly so the worked example exercises a realistic non-zero fatigue — set the four component fields in the all-maxed case to 1.0 so the de-double-counted fatigue is 1.0 and the worked example still reaches its intended high score). Add an explicit single-count test: two inputs identical except softTissueRisk raised → score rises by ONLY the comp-5 contribution (not comp-5 + a comp-3 echo); assert the delta equals the expected single-channel weighting within tolerance. Keep the sign-constraint, zone-threshold, redistribution, recurrence, determinism, and no-injury-prediction-copy tests; adjust their numeric expectations only where the new component-3 changes them.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/StrainRiskEngineTests 2>&1 | tail -25</automated>
  </verify>
  <done>Component 3 uses de-double-counted fatigue; soft-tissue/rest-debt counted once; single-count delta test passes; sign-constraint + copy audit still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Easy-inclusive + baseline-discounted coverage confidence (Finding 5 consumption, GA-30-E/C)</name>
  <files>WorkloadApp/Services/StrainRiskEngine.swift, WorkloadAppTests/StrainRiskEngineTests.swift</files>
  <behavior>
    - coverage = (hard + easy) / (hard + easy + unscored), summing easyCount + hardSetCount over perMuscle; an all-easy fully-scored session → coverage 1.0 (was 0).
    - When all contributing muscles lack a chronic baseline (hasChronicBaseline false), confidence is discounted (multiply by a baseline-coverage factor = fraction of muscles with hasChronicBaseline, blended like coverage so it never zeroes a present baseline but pulls down honestly).
    - confidence stays 0 when baselineConfidence is nil; rises with data; stays 0..1.
  </behavior>
  <action>In confidence(_:), sum easy = perMuscle.values.reduce hardSetCount? no — sum easyCount; hard = sum hardSetCount; unscored = sum unscoredCount. coverage = (hard+easy) > 0 || unscored > 0 ? Double(hard+easy)/Double(hard+easy+unscored) : 0. Add baselineCoverage = fraction of perMuscle entries with hasChronicBaseline == true (muscleCount>0 ? withBaseline/muscleCount : 0). Fold baselineCoverage into the blend the same shape as coverage: raw = baselineConf * gateFactor * (0.5 + 0.5*coverage) * (0.5 + 0.5*baselineCoverage). Keep clamp01. Update tests: add test_confidence_allEasyScored_isFullCoverage (an all-easy session reports coverage contribution as if fully scored, confidence > the all-unscored case); add test_confidence_discountsWhenNoChronicBaseline (two inputs identical except hasChronicBaseline flipped false on all muscles → confidence strictly lower); update the existing test_confidence_risesWithData expectations to the new formula (the low case uses hardSetCount 1/unscored 9 → with easyCount 0 still low coverage; the high case full coverage + baseline true). Update the test builder strengthResult(...) to accept and pass easyCount + hasChronicBaseline (defaulting hasChronicBaseline true) — this builder was already given the new params in Wave 1; confirm and use them.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/StrainRiskEngineTests 2>&1 | tail -25</automated>
  </verify>
  <done>Coverage includes easy sets; all-easy session reports full coverage; confidence discounts when no chronic baseline; confidence still 0 on nil baseline; coverage/confidence tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green + isolation grep</name>
  <files>WorkloadApp/Services/StrainRiskEngine.swift</files>
  <action>Run the FULL suite (absorbs any Wave-2 worked-example drift now resolved here). Confirm StrainRiskEngine still has zero reference from AutoregulationEngine / RecoveryScoreEngine (isolation invariant). Confirm git diff --stat shows zero changes to the three fence files. Confirm no "injury prediction" copy introduced.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -15; cd /Users/hanwen/Desktop/Tonus && echo "isolation (expect 0):"; grep -rc "StrainRisk" WorkloadApp/Services/AutoregulationEngine.swift WorkloadApp/Services/RecoveryScoreEngine.swift 2>/dev/null; echo "fence diff (expect empty):"; git diff --stat WorkloadAppTests/BaselineTierFenceTests.swift WorkloadAppTests/AutoregulationFlagFenceTests.swift WorkloadAppTests/DualRunFlagFenceTests.swift</automated>
  </verify>
  <done>Full suite green; StrainRisk isolated from live engines (0 refs); fences unmodified; no injury-prediction copy.</done>
</task>

</tasks>

<verification>
- StrainRiskEngineTests green with re-derived worked-example, single-count, easy-inclusive, baseline-discount tests.
- Full suite green.
- Isolation grep: AutoregulationEngine / RecoveryScoreEngine reference StrainRisk == 0.
- Three fence files unmodified.
</verification>

<success_criteria>
Findings 1 + 5 fixed; each signal counted once; easy sets in coverage; baseline-discounted confidence; sign-constraint + copy audit intact; full suite green; fences untouched.
</success_criteria>

<output>
Create `.planning/phases/30-shadow-engine-quality-fixes/30-03-SUMMARY.md` when done (record the new component-3 formula, the new confidence formula, and the new worked-example expected score).
</output>
