---
phase: 30-shadow-engine-quality-fixes
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
autonomous: true
requirements: [PRS-30-06]
files_modified:
  - WorkloadApp/Services/PRSDualRunSurface.swift
  - WorkloadAppTests/PRSDualRunSurfaceTests.swift
must_haves:
  truths:
    - "Flag-OFF: PRSDualRunSurface.adjust returns nil FIRST and mutates nothing — DualRunFlagFenceTests byte-identical no-op is preserved (fence file unmodified)."
    - "Flag-ON with targetVolume nil (the default for a normal first-time PrescribedWorkout): the volume modifier is APPLIED to a derived effective base (template-derived sum, else neutral 1.0) so a 0.5 modifier yields a real reduced target and a 0.0 (rest) yields 0.0 — never silently discarded as nil."
    - "Flag-ON with an existing targetVolume: behavior byte-identical to today (existing * volumeModifier) — the existing flag-on fence assertion stays green."
    - "Any mutation bumps updatedAt; the timestamp is injected (now: Date = .now) so the bump is deterministically testable."
    - "Copy/behavior unchanged otherwise; PRSActivation stays default FALSE; the surface is never live this milestone."
  artifacts:
    - path: "WorkloadApp/Services/PRSDualRunSurface.swift"
      provides: "nil-targetVolume effective-base derivation + updatedAt bump in adjust"
      contains: "updatedAt"
    - path: "WorkloadAppTests/PRSDualRunSurfaceTests.swift"
      provides: "nil-targetVolume first-time prescription coverage (NOT in the fence file)"
      contains: "targetVolume"
  key_links:
    - from: "PRSDualRunSurface.adjust"
      to: "effective volume base when targetVolume nil"
      via: "template-derived sum else neutral 1.0, then * volumeModifier; bump updatedAt"
      pattern: "targetVolume == nil|allExercises|updatedAt"
---

<objective>
Fix Finding 6 (GA-30-F): in the flag-ON `PRSDualRunSurface.adjust`, when a normal first-time `PrescribedWorkout` has `targetVolume == nil` (the default — never populated at creation/sync), the volume modifier is silently discarded in the else branch. Derive an effective base so the reduction is applied, and bump `updatedAt`. This path runs flag-ON only (never live this milestone) but must be correct for future activation.

Purpose: a 50%-volume or rest recommendation actually reduces a first-time prescription instead of being dropped, while the flag-OFF no-op and the existing-targetVolume flag-on behavior stay byte-identical (fence preserved).
Output: corrected `PRSDualRunSurface.adjust`, a new NON-fence test `PRSDualRunSurfaceTests.swift` covering the nil-targetVolume case + updatedAt bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shadow-engine-quality-fixes/30-RESEARCH.md
@WorkloadApp/Services/PRSDualRunSurface.swift
@WorkloadApp/Models/PrescribedWorkout.swift
@CLAUDE.md

<interfaces>
<!-- PRSDualRunSurface.adjust (L59-85): guard PRSActivation.isEnabled else return nil (KEEP FIRST — fence no-op). RPE capped downward. Volume: if let existing = workout.targetVolume { existing * recommendation.volumeModifier } else { nil } ← the discard bug. Mutates workout.targetRPE/targetVolume, returns WorkoutAdjustment.
     PrescribedWorkout: targetVolume:Double? (nil by default), updatedAt:Date, allExercises:[TemplateExercise] (flattened from sortedGroups). TemplateExercise → TemplateSet (check for a target-volume-ish field: targetReps/targetWeight/sets count) to derive a base; if no clean derivable volume, fall back to neutral 1.0.
     recommendation.volumeModifier: Double (1.0 full, 0.5 half, 0.0 rest).
     DualRunFlagFenceTests seeds targetVolume = 100.0 (L20) — DO NOT TOUCH that file; its flag-off no-op + flag-on existing-volume assertions must stay byte-identical. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply volume modifier to a derived base when targetVolume is nil + bump updatedAt</name>
  <files>WorkloadApp/Services/PRSDualRunSurface.swift, WorkloadAppTests/PRSDualRunSurfaceTests.swift</files>
  <behavior>
    - Flag OFF → adjust returns nil, no mutation, updatedAt unchanged (fence preserved).
    - Flag ON, existing targetVolume = 100, modifier 0.5 → newVolume 50 (byte-identical to today); updatedAt bumped.
    - Flag ON, targetVolume nil, modifier 0.5 → newVolume = effectiveBase * 0.5 (NOT nil). effectiveBase = template-derived sum if derivable, else neutral 1.0 → 0.5.
    - Flag ON, targetVolume nil, modifier 0.0 (rest) → newVolume 0.0 (a real rest signal, not nil).
    - Every mutation sets workout.updatedAt = now (injected), assertable deterministically.
  </behavior>
  <action>Add an injected timestamp param: adjust(prescribedWorkout:with:now: Date = .now). After the existing flag guard (KEEP guard PRSActivation.isEnabled else return nil FIRST), keep the RPE cap. For volume: compute effectiveBase = workout.targetVolume ?? derivedBaseVolume(workout); newVolume = effectiveBase * recommendation.volumeModifier (always non-nil now). Add private derivedBaseVolume(_ workout: PrescribedWorkout) -> Double that derives from the prescription's template — sum a defensible per-set target across workout.allExercises (e.g. count of target sets, or Σ targetReps if present); if nothing derivable, return 1.0 (neutral, so the modifier becomes the fraction-of-full). Document the derivation + the 1.0 fallback in a comment. After computing, set workout.targetRPE = cappedRPE, workout.targetVolume = newVolume, workout.updatedAt = now, return WorkoutAdjustment(newTargetRPE: cappedRPE, newTargetVolume: newVolume). Create NEW file PRSDualRunSurfaceTests.swift (NOT in the fence file): test_flagOff_nilVolume_noOp (returns nil, targetVolume stays nil, updatedAt unchanged), test_flagOn_nilVolume_appliesModifierToDerivedBase (modifier 0.5 → non-nil reduced volume), test_flagOn_nilVolume_restModifierZero (0.0 → 0.0), test_flagOn_existingVolume_byteIdentical (100 * 0.5 = 50, matches legacy), test_flagOn_anyMutation_bumpsUpdatedAt (inject a fixed now, assert equality). Toggle PRSActivation.isEnabled within tests the SAME way the existing PRSShadowArm / DualRunFlagFence tests do (read that pattern; restore the flag in teardown). DO NOT edit DualRunFlagFenceTests.swift.</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/PRSDualRunSurfaceTests -only-testing:WorkloadAppTests/DualRunFlagFenceTests 2>&1 | tail -25</automated>
  </verify>
  <done>nil-targetVolume modifier is applied to a derived base (never discarded); existing-volume path byte-identical; updatedAt bumped via injected now; flag-off no-op preserved; both PRSDualRunSurfaceTests and DualRunFlagFenceTests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green + fence + flag integrity</name>
  <files>WorkloadApp/Services/PRSDualRunSurface.swift</files>
  <action>Run the FULL suite. Confirm git diff --stat shows zero changes to DualRunFlagFenceTests.swift (and the other two fence files). Confirm PRSActivation.isEnabled + PRSMasterActivation.isEnabled defaults are still FALSE (grep). Confirm no "injury prediction" copy and "Tuwa"-only naming preserved in any touched string (none expected — adjust touches no copy).</action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -15; cd /Users/hanwen/Desktop/Tonus && echo "fence diff (expect empty):"; git diff --stat WorkloadAppTests/BaselineTierFenceTests.swift WorkloadAppTests/AutoregulationFlagFenceTests.swift WorkloadAppTests/DualRunFlagFenceTests.swift; echo "flag defaults (expect false):"; grep -n "isEnabled" WorkloadApp/Services/PRSActivation.swift WorkloadApp/Services/PRSMasterActivation.swift 2>/dev/null</automated>
  </verify>
  <done>Full suite green; three fence files unmodified; both activation flags default FALSE.</done>
</task>

</tasks>

<verification>
- PRSDualRunSurfaceTests + DualRunFlagFenceTests green.
- Full suite green.
- DualRunFlagFenceTests.swift (and the other two fences) unmodified.
- PRSActivation + PRSMasterActivation default FALSE.
</verification>

<success_criteria>
Finding 6 fixed: flag-on nil-targetVolume applies the modifier to a derived base + bumps updatedAt; flag-off no-op and existing-volume flag-on behavior byte-identical; fences untouched; flags FALSE; full suite green.
</success_criteria>

<output>
Create `.planning/phases/30-shadow-engine-quality-fixes/30-04-SUMMARY.md` when done (record the derivedBaseVolume scheme + the injected-now signature change).
</output>
