---
phase: 15
slug: template-sharing
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-13
revised: 2026-05-13
---

# Phase 15 — UI Design Contract

> Visual and interaction contract for Template Sharing. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (native iOS — no web component system) |
| Preset | not applicable |
| Component library | SwiftUI native |
| Icon library | SF Symbols |
| Font | General Sans Variable (Regular 400 + Medium 500) via `Font.Tokens.*` |

---

## Spacing Scale

Declared values (8pt base grid per DESIGN.md):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 8pt | Icon-label gaps, inline padding, weekday letter spacing |
| sm | 16pt | Card internal horizontal padding, text field horizontal inset |
| md | 24pt | Card internal vertical padding, standard vertical gaps |
| lg | 32pt | Section gaps between share code display and preview content |
| xl | 48pt | Major section breaks |
| 2xl | 64pt | Page-level top/bottom breathing room |

Exceptions: none

---

## Typography

All text uses `Font.Tokens.*` backed by General Sans Variable. No `.system()` or semantic styles.

| Role | Token | Size | Weight | Line Height | Usage in this phase |
|------|-------|------|--------|-------------|---------------------|
| Section head | `.Tokens.sectionHead` | 19pt | 500 (Medium) | 1.3 | Template name in import preview, "Share Template" sheet title |
| Body | `.Tokens.body` | 17pt | 400 (Regular) | 1.6 | Exercise names in preview, share code display |
| Label | `.Tokens.label` | 15pt | 400 (Regular) | 1.5 | Set/rep summaries, sport type subtitle, toolbar buttons |
| Small label | `.Tokens.smallLabel` | 13pt | 400 (Regular) | 1.5 | "Shared by" attribution, expiry note, banner text, group name headers (uppercase variant uses +1.2pt tracking to differentiate) |

Share code display: `.Tokens.body` at 17pt with `.monospacedDigit()` and `.tracking(2.0)` for letter-spaced readability. Rendered in `ColorTokens.text1`.

Group name headers: `.Tokens.smallLabel` at 13pt with `.textCase(.uppercase)` and `.tracking(1.2)` in `ColorTokens.text3`. The uppercase + tracking treatment visually differentiates group headers from attribution and expiry text without requiring a fifth size.

---

## Color

| Role | Dark Mode | Light Mode | Usage |
|------|-----------|------------|-------|
| Dominant (60%) | `#0B0B0A` | `#F4F1ED` | `ColorTokens.background` — sheet backgrounds, scroll area |
| Secondary (30%) | `#131312` | `#EDEAE6` | `ColorTokens.surface` — share code card, preview card areas |
| Divider | `#232321` | `#CFCBC5` | `ColorTokens.divider` — hairline separators between groups, 0.5pt |
| Text primary | `#C2BEB7` | `#1C1915` | `ColorTokens.text1` — template name, exercise names, share code, CTA labels |
| Text secondary | `#7C7972` | `#696560` | `ColorTokens.text2` — set summaries, sport/session type, dismiss button |
| Text tertiary | `#3A3835` | `#AFABA5` | `ColorTokens.text3` — group name labels, placeholder text |
| Destructive | `#7E5C5C` | `#6E3A3A` | `ColorTokens.zoneDanger` — not used in this phase (no destructive actions) |

Accent reserved for: hero readiness score only (per DESIGN.md). Not used anywhere in Phase 15 views.

---

## Component Inventory

### New Views

| Component | File | Purpose |
|-----------|------|---------|
| `ShareCodeSheet` | `Views/WorkoutLog/ShareCodeSheet.swift` | Displays generated 8-char share code with copy button and share link |
| `ShareImportSheet` | `Views/WorkoutLog/ShareImportSheet.swift` | Text field for entering share code, transitions to preview on lookup |
| `ShareImportPreviewSheet` | `Views/WorkoutLog/ShareImportPreviewSheet.swift` | Full template preview with "Import Template" CTA |

### Reused Patterns

| Pattern | Source | Reuse |
|---------|--------|-------|
| Exercise group list | `TemplatePreviewSheet.swift` | Same layout: smallLabel group header (uppercase + tracking), exercise rows with set summary |
| Weekday row | `TemplatePreviewSheet.weekdayRow()` | Extract to shared helper or duplicate inline |
| Set summary | `TemplatePreviewSheet.setSummary()` | Same format: "3 x 10 @ 80kg" |
| Code generation | `InviteService.makeLocalCode()` | Extend from 6 to 8 chars |
| Deep link handling | `AppRouter.onOpenURL` | Add `tuwa.app/t/{code}` route |

---

## View Specifications

### ShareCodeSheet

Presented as `.sheet` from template detail/preview context menu "Share" action.

**Focal point:** The share code string — the large tracked monospaced text is the single element the user's eye must land on immediately.

**Layout (top to bottom):**
1. Navigation bar: "Share Template" title (`.Tokens.sectionHead`), "Done" button (`.Tokens.label`, `text2`)
2. Template name (`.Tokens.body`, `text1`) — 16pt horizontal padding, 16pt top
3. Sport + session type (`.Tokens.label`, `text2`) — 16pt horizontal padding, 8pt below name
4. Hairline divider (0.5pt, `divider`) — 16pt top margin
5. Share code card: `surface` background, 0pt corners, 0.5pt `divider` border
   - "SHARE CODE" smallLabel (`.Tokens.smallLabel`, `text3`, uppercase, +1.2 tracking) — 16pt horizontal, 16pt top
   - Code string (`.Tokens.body`, `text1`, `.monospacedDigit()`, `.tracking(2.0)`) — 16pt horizontal, 8pt below label
   - "Expires in 30 days" (`.Tokens.smallLabel`, `text3`) — 16pt horizontal, 8pt below code, 16pt bottom
6. Two full-width buttons stacked vertically with 8pt gap, 16pt horizontal margin, 24pt top:
   - "Copy Code" — `surface` fill, 0pt corners, 0.5pt `divider` border, 48pt height, `.Tokens.body` `text1` centered
   - "Share Link" — `surface` fill, 0pt corners, 0.5pt `divider` border, 48pt height, `.Tokens.body` `text1` centered. Triggers `ShareLink` with URL `https://tuwa.app/t/{code}`.

**Presentation:** `.presentationDetents([.medium])`

**States:**
- Loading: code generation in progress — show `ProgressView()` centered
- Success: display code + buttons
- Error: "Could not generate share code. Check your connection and try again." (`.Tokens.smallLabel`, `text2`, centered)

### ShareImportSheet

Presented as `.sheet` from "Import" button in WorkoutLog toolbar.

**Focal point:** The text field for code entry — the single input the user must interact with to proceed.

**Layout (top to bottom):**
1. Navigation bar: "Import Template" title (`.Tokens.sectionHead`), "Close" button (`.Tokens.label`, `text2`)
2. Instruction text: "Enter the 8-character share code" (`.Tokens.label`, `text2`) — 16pt horizontal, 24pt top
3. Text field: custom `SharpTextFieldStyle` (0pt corners per Phase 13), placeholder "ABCD1234" in `text3`, input uppercased, max 8 characters, `.monospacedDigit()`, 16pt horizontal margin, 16pt top
4. "Look Up" button — full width, `surface` fill, 0pt corners, 0.5pt `divider` border, 48pt height, `.Tokens.body` `text1` centered. Disabled when input length < 8 (text1 changes to `text3` when disabled). 16pt horizontal margin, 16pt top.

**Presentation:** `.presentationDetents([.medium])`

**States:**
- Idle: text field empty, button disabled
- Valid input: 8 characters entered, button enabled
- Loading: button shows `ProgressView()` replacing text, field disabled
- Not found: "No template found for this code. Check the code and try again." (`.Tokens.smallLabel`, `zoneDanger`) below the button, 8pt top
- Success: dismiss this sheet, present `ShareImportPreviewSheet` with fetched template data

### ShareImportPreviewSheet

Presented after successful code lookup. Also presented when app opens via universal link `tuwa.app/t/{code}`.

**Focal point:** The "Import Template" sticky CTA button at the bottom — the single action the user came here to complete.

**Layout (top to bottom):**
1. Navigation bar: empty title, "Close" button (`.Tokens.label`, `text2`)
2. "SHARED TEMPLATE" banner (`.Tokens.smallLabel`, `text3`, uppercase, +1.2 tracking) — 16pt horizontal, 16pt top
3. Template name (`.Tokens.sectionHead`, `text1`) — 16pt horizontal, 8pt below banner
4. Sport + session type (`.Tokens.label`, `text2`) — 16pt horizontal, 8pt below name
5. Weekday row (same pattern as `TemplatePreviewSheet`) — 16pt horizontal, 16pt below subtitle
6. Hairline divider (0.5pt) — 16pt top
7. Exercise group list (identical layout to `TemplatePreviewSheet`):
   - Group name: `.Tokens.smallLabel`, `text3`, uppercase, +1.2 tracking, 16pt horizontal, 16pt top, 8pt bottom
   - Exercise row: `HStack` with exercise name (`.Tokens.body`, `text1`) + set summary (`.Tokens.label`, `text2`, `.monospacedDigit()`), 16pt horizontal, 8pt vertical
8. Notes section (if present): hairline divider, notes text (`.Tokens.label`, `text2`), 16pt horizontal, 8pt top/bottom
9. Sticky bottom area: "Import Template" button — full width, `text1` background color, 0pt corners, 48pt height, `.Tokens.body` weight medium, `background` text color (inverted for contrast). 16pt horizontal margin, 16pt bottom (safe area aware).

**Presentation:** `.presentationDetents([.medium, .large])`

**States:**
- Preview: full template content displayed, "Import Template" button active
- Importing: button shows `ProgressView()`, disabled
- Success: dismiss sheet, show inline confirmation (toast or brief overlay: "Template imported")
- Error: "Could not import template. Try again." (`.Tokens.smallLabel`, `zoneDanger`) above the button

---

## Interaction Contract

### Share Flow
1. User long-presses or taps context menu on owned template in template list
2. Menu shows "Share" option (SF Symbol: `square.and.arrow.up`)
3. Tap "Share" presents `ShareCodeSheet`
4. Sheet calls Supabase to create `shared_templates` row, returns 8-char code
5. User taps "Copy Code" — code copied to clipboard, button text briefly changes to "Copied" for 1.5s
6. User taps "Share Link" — iOS share sheet with `https://tuwa.app/t/{code}`

### Import Flow (Manual Code Entry)
1. User taps "Import" button (SF Symbol: `square.and.arrow.down`) in WorkoutLog toolbar
2. `ShareImportSheet` presented
3. User types 8-char code, taps "Look Up"
4. App queries `shared_templates` by `share_code`
5. On success: dismiss `ShareImportSheet`, present `ShareImportPreviewSheet`
6. User reviews template, taps "Import Template"
7. App calls `deepCopyGroups()` pattern: new UUIDs, current user as owner, `targetWeightKg` stripped
8. Template saved locally, synced to Supabase
9. Sheet dismissed, confirmation shown

### Import Flow (Universal Link)
1. User taps `https://tuwa.app/t/ABCD1234` in any app
2. iOS opens Tuwa app, `AppRouter.onOpenURL` parses code
3. App queries `shared_templates` by share code
4. On success: present `ShareImportPreviewSheet` directly (skip code entry)
5. On failure: present `ShareImportSheet` with error message pre-populated

### Animations
- Sheet presentation/dismissal: system default (no custom spring)
- "Copied" text swap: 150ms linear crossfade
- Button disable/enable: 150ms linear opacity transition
- Loading spinner: system `ProgressView()` default

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (share) | "Copy Code" / "Share Link" |
| Primary CTA (import) | "Import Template" |
| Code entry instruction | "Enter the 8-character share code" |
| Code entry placeholder | "ABCD1234" |
| Look up button | "Look Up" |
| Shared banner label | "SHARED TEMPLATE" |
| Expiry note | "Expires in 30 days" |
| Copied confirmation | "Copied" (replaces "Copy Code" for 1.5s) |
| Import success | "Template imported" |
| Sheet dismiss (ShareCodeSheet) | "Done" |
| Sheet dismiss (ShareImportSheet) | "Close" |
| Sheet dismiss (ShareImportPreviewSheet) | "Close" |
| Empty state heading | n/a (no empty state — this phase adds share/import actions to existing template list) |
| Empty state body | n/a |
| Error: code generation | "Could not generate share code. Check your connection and try again." |
| Error: code not found | "No template found for this code. Check the code and try again." |
| Error: import failed | "Could not import template. Try again." |
| Error: link expired | "This share link has expired. Ask the sender for a new code." |

No destructive actions in this phase. Importing is additive (creates a copy). Sharing creates a row but does not modify the original template.

---

## Accessibility Contract

- All buttons have accessibility labels matching their visible text
- Share code text field: `.accessibilityLabel("Share code input")`
- Share code display: `.accessibilityLabel("Share code")` + `.accessibilityValue("{code}")` read character-by-character
- Exercise list in preview: each exercise row readable as "{name}, {set summary}"
- "Import Template" CTA: `.accessibilityHint("Saves a copy of this template to your library")`
- All text respects Dynamic Type (General Sans scales via `Font.custom` with relative sizing)
- Minimum touch target: 44pt for all interactive elements (buttons are 48pt height)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | n/a | Native iOS — no web component registry |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
