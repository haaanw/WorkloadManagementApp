---
phase: 25-soreness-tweak-self-log
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - WorkloadApp/Services/NiggleInjuryDeriver.swift
  - WorkloadApp/ViewModels/DashboardViewModel.swift
  - WorkloadAppTests/NiggleInjuryDeriverTests.swift
autonomous: true
requirements: [P25-DERIVE, P25-FATIGUE-WIRE, P25-WELLNESS-WIRE]
must_haves:
  truths:
    - "D-10: a pure static helper derives softTissueInjuryCount = count of niggles where type in {pain, tweak} AND (limitedTraining OR severity >= 7) within a ~28-day window; soreness-type logs are EXCLUDED (DOMS guard)"
    - "D-11: a pure static helper derives daysSinceLastInjury = days since the most recent qualifying niggle, nil if none"
    - "Thresholds (severity cut 7, window 28d) are named constants, tunable (D-13), NOT magic numbers"
    - "The derivation lives in a pure struct (Foundation-only, static, takes [SorenessLog], returns primitives) — NOT stuffed in the ViewModel (established pattern)"
    - "D-12: DashboardViewModel fetches the last 14 days of WellnessCheckIn.wellnessScore and passes them as recentWellnessScores (no longer empty [])"
    - "DashboardViewModel.swift:252 softTissueInjuryCount and :253 daysSinceLastInjury are wired to the deriver over fetched SorenessLogs (no longer 0/nil)"
    - "All three new fetches/derivations stay inside the non-cold-start else branch so cold-start does no extra work (unchanged cold-start behavior)"
    - "DOMS-exclusion is proven: a soreness-type log with severity 10 + limitedTraining true is NOT counted"
    - "Empty data is safe: no logs -> count 0, daysSince nil; no wellness check-ins -> recentWellnessScores empty -> fatigue wellness component neutral"
  artifacts:
    - path: "WorkloadApp/Services/NiggleInjuryDeriver.swift"
      provides: "pure struct: qualifyingSeverityCut, injuryWindowDays constants; softTissueInjuryCount(logs:asOf:); daysSinceLastInjury(logs:asOf:)"
      contains: "softTissueInjuryCount"
    - path: "WorkloadApp/ViewModels/DashboardViewModel.swift"
      provides: "14d wellness fetch + deriver-backed injury count/days-since wired into FatigueInput"
      contains: "NiggleInjuryDeriver"
    - path: "WorkloadAppTests/NiggleInjuryDeriverTests.swift"
      provides: "qualifying-rule, DOMS-exclusion, severity-only, impact-only, window-edge, days-since, empty-data tests"
      contains: "NiggleInjuryDeriver"
  key_links:
    - from: "DashboardViewModel.load FatigueInput"
      to: "NiggleInjuryDeriver + WellnessCheckIn 14d"
      via: "softTissueInjuryCount/daysSinceLastInjury from deriver; recentWellnessScores from 14d fetch"
      pattern: "NiggleInjuryDeriver|recentWellnessScores"
    - from: "NiggleInjuryDeriver.softTissueInjuryCount"
      to: "qualifying SorenessLogs"
      via: "type in {pain,tweak} AND (limitedTraining OR severity>=cut) AND within window"
      pattern: "qualifyingSeverityCut"
---

<objective>
Replace the three hardcoded fatigue inputs in `DashboardViewModel.load()` with real data: wire the last-14-day wellness history (D-12) and derive `softTissueInjuryCount` / `daysSinceLastInjury` from logged niggles (D-10/D-11). The derivation math lives in a new **pure struct** `NiggleInjuryDeriver` (Foundation-only, static, takes `[SorenessLog]`) — per the established `FatigueIndexEngine` convention, NOT in the ViewModel.

Purpose: today `recentWellnessScores: []`, `softTissueInjuryCount: 0`, `daysSinceLastInjury: nil` make the wellness-trend (0.15 weight) and soft-tissue (0.10 weight) fatigue components inert. This plan makes them real, with the DOMS-vs-niggle discriminator that D-10 hinges on: a routine `soreness`-type log NEVER inflates the injury count — only functional `pain`/`tweak` niggles do.

Output: `NiggleInjuryDeriver.swift` with named tunable constants, the three wired inputs (inside the existing non-cold-start branch), and pure tests covering the DOMS-exclusion, threshold, and window edges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md
@.planning/phases/25-soreness-tweak-self-log/25-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase — do not re-explore. SorenessLog/NiggleType/SorenessLogRepository from Plan 01. -->

From WorkloadApp/Services/FatigueIndexEngine.swift:
- `struct FatigueInput` (lines 22–43). Relevant fields: `recentWellnessScores: [Double]` (doc says "last 7 days oldest first" but the engine only uses it for a slope gated on `count >= 3` — 14 points are valid, RESEARCH §4 A2); `softTissueInjuryCount: Int` (doc "last 12 months" but D-10 deliberately narrows to 28d — the `computeSoftTissueRisk` math at 327–343 is window-agnostic: `countScore = 1 - exp(-0.5n)`, recency `exp(-days/120)`, nil-days → 0.5 multiplier); `daysSinceLastInjury: Int?`.
- `static func baselineSessionsPer14Days(sessions:asOf:maxHistoryDays:) -> Double?` (242–256) — the pure-static-helper-over-models template to mirror (takes [WorkoutSession], returns primitive, Foundation-only).

From WorkloadApp/ViewModels/DashboardViewModel.swift (load(), lines 64–300):
- Line 236: `let recentWellnessScores: [Double] = []  // TODO: fetch from WellnessCheckIn history` → replace (D-12).
- Lines 238–263: `if isColdStartActive { fatigueIndex = nil; fatigueZone = nil } else { let fatigueInput = FatigueIndexEngine.FatigueInput( … softTissueInjuryCount: 0, daysSinceLastInjury: nil ) … }` — line 252 `softTissueInjuryCount: 0`, line 253 `daysSinceLastInjury: nil`. Replace both. KEEP new fetches inside the `else` branch (cold-start must do no extra work).
- In scope at the wiring point: `recoveryRepo` (107), `recentSnapshots` (115, 28d), `allSessions` (182), `athlete`, `modelContext`. No wellness or niggle repo yet — add them. Precedent for inline FetchDescriptor in the VM: PersonalRecord at lines ~308–312.

From WorkloadApp/Models/WellnessCheckIn.swift:
- `var wellnessScore: Double` and `var date: Date` — fetch last 14d, map `\.wellnessScore`. `var athlete: Athlete?` (filter in Swift to avoid the relationship-predicate trap if a repo method is added).

From Plan 01:
- `SorenessLog` (date/typeRaw/severity/limitedTraining), `NiggleType {soreness,pain,tweak}`, `SorenessLogRepository.fetchRecent(days:athlete:) -> [SorenessLog]`.

Locked decisions: D-10 (qualifying rule + DOMS exclusion), D-11 (days-since), D-12 (14d wellness), D-13 (named tunable constants ≥7/10, 28d). RESEARCH §4 A3: "distinct active" = qualifying logs in window (no region-dedup, no resolution tracking — deferred); v1 counts qualifying logs in window.
KNOWN TRAP: keep the deriver PURE over `[SorenessLog]` arrays so its tests need no SwiftData predicate (sidesteps the iOS 26.1 in-memory trap entirely).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NiggleInjuryDeriver pure struct — qualifying-count + days-since with DOMS exclusion</name>
  <files>WorkloadApp/Services/NiggleInjuryDeriver.swift, WorkloadAppTests/NiggleInjuryDeriverTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - WorkloadApp/Services/FatigueIndexEngine.swift (lines 242–256 baselineSessionsPer14Days — the pure-static-helper pattern; 327–343 computeSoftTissueRisk — what consumes the outputs)
    - WorkloadApp/Models/SorenessLog.swift + WorkloadApp/Models/Enums.swift NiggleType (from Plan 01)
    - WorkloadAppTests/FatigueIndexEngineCycleTests.swift (lines 1–45 — pure-engine test style, builds structs directly, no container)
    - .planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md (D-10, D-11, D-13)
  </read_first>
  <behavior>
    - qualifies iff type in {pain, tweak} AND (limitedTraining == true OR severity >= qualifyingSeverityCut) AND date within injuryWindowDays of asOf.
    - DOMS-exclusion (highest-value test): a `soreness`-type log, severity 10, limitedTraining true, today → NOT counted.
    - impact-only: `pain`, severity 3, limitedTraining true, today → counted.
    - severity-only: `tweak`, severity 8, limitedTraining false, today → counted.
    - neither: `pain`, severity 3, limitedTraining false, today → NOT counted.
    - window edge: a qualifying `pain` log at day -29 (outside 28d) → NOT counted; at the boundary day -28 → counted (document inclusive boundary).
    - count = number of qualifying logs in window (no region-dedup, v1 — RESEARCH §4 A3); two qualifying logs same region → count 2.
    - daysSinceLastInjury: nil when no qualifying log; otherwise startOfDay day-diff to the most recent qualifying log's date.
    - empty: `softTissueInjuryCount([]) == 0`, `daysSinceLastInjury([]) == nil`.
  </behavior>
  <action>
    Create `WorkloadApp/Services/NiggleInjuryDeriver.swift` as a pure `struct NiggleInjuryDeriver` (Foundation-only, static methods, no SwiftData/HealthKit import), mirroring `FatigueIndexEngine.baselineSessionsPer14Days`. Define named tunable constants (D-13): `static let qualifyingSeverityCut: Int = 7` and `static let injuryWindowDays: Int = 28`. Add a private `static func isQualifying(_ log: SorenessLog, asOf: Date) -> Bool` encoding the D-10 rule: `NiggleType(rawValue: log.typeRaw)` ∈ {.pain, .tweak} AND (`log.limitedTraining` OR `log.severity >= qualifyingSeverityCut`) AND `log.date` within `injuryWindowDays` of `asOf` (compare on `Calendar.startOfDay`; document boundary as inclusive of day -28). Add `static func softTissueInjuryCount(logs: [SorenessLog], asOf: Date = .now) -> Int` = count of qualifying logs (no dedup, v1). Add `static func daysSinceLastInjury(logs: [SorenessLog], asOf: Date = .now) -> Int?` = `Calendar.dateComponents([.day], from: startOfDay(mostRecentQualifying.date), to: startOfDay(asOf)).day`, nil if none (mirror the day-diff math in resolveOutcomes/daysSinceRest). Create `WorkloadAppTests/NiggleInjuryDeriverTests.swift` (`final class … XCTestCase`, builds plain `[SorenessLog]` arrays via the model init — no ModelContainer needed since the helper is pure) covering every behavior case above, especially the DOMS-exclusion and the window boundary. **CLAUDE.md iOS rule:** add `NiggleInjuryDeriver.swift` to the app target and `NiggleInjuryDeriverTests.swift` to the `WorkloadAppTests` target in `project.pbxproj`. (SorenessLog instances can be built directly in tests without a container; if SwiftData requires a context to instantiate @Model, build a throwaway in-memory container in test setup — but prefer direct init.)
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' -only-testing:WorkloadAppTests/NiggleInjuryDeriverTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `NiggleInjuryDeriver` is a pure struct (Foundation-only; `grep -n "import SwiftData\|import HealthKit" WorkloadApp/Services/NiggleInjuryDeriver.swift` returns nothing) with `qualifyingSeverityCut`/`injuryWindowDays` named constants.
    - `softTissueInjuryCount` applies type∈{pain,tweak} AND (limitedTraining OR severity≥7) AND within-28d; `soreness`-type excluded (DOMS test passes).
    - impact-only, severity-only, neither, and window-edge cases pass exactly as specified.
    - `daysSinceLastInjury` returns nil for none and the correct day-diff for the most recent qualifying log.
    - Both new .swift files are in the correct targets in project.pbxproj.
    - xcodebuild test exits 0.
  </acceptance_criteria>
  <done>A pure, deterministic injury-derivation helper encodes the functional (not severity-only) qualification rule with a proven DOMS exclusion and tunable named constants.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire 14d wellness + deriver-backed injury inputs into DashboardViewModel.load() (non-cold-start branch)</name>
  <files>WorkloadApp/ViewModels/DashboardViewModel.swift</files>
  <read_first>
    - WorkloadApp/ViewModels/DashboardViewModel.swift (lines 105–115 repo scope; 222–263 the wiring point incl. the cold-start else branch and the FatigueInput construction; ~308–312 inline FetchDescriptor precedent)
    - WorkloadApp/Services/NiggleInjuryDeriver.swift (Task 1)
    - WorkloadApp/Repositories/SorenessLogRepository.swift (Plan 01 — fetchRecent)
    - WorkloadApp/Models/WellnessCheckIn.swift (wellnessScore/date/athlete)
    - .planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <behavior>
    - recentWellnessScores is the last 14 days of WellnessCheckIn.wellnessScore for the athlete, oldest-first (D-12) — pass all 14 (NOT .suffix(7); D-12 says 14, the slope is valid for 14, RESEARCH §4 A2/Open-Q4).
    - softTissueInjuryCount = NiggleInjuryDeriver.softTissueInjuryCount(logs: <fetched 28d niggles>); daysSinceLastInjury = NiggleInjuryDeriver.daysSinceLastInjury(logs: <same>).
    - All three fetches/derivations happen ONLY in the non-cold-start else branch (cold-start path does no extra fetch and still sets fatigueIndex/Zone nil — unchanged).
    - Empty wellness history → recentWellnessScores empty → fatigue wellness component stays neutral (no crash); no niggles → count 0, daysSince nil.
  </behavior>
  <action>
    In `DashboardViewModel.load()`: (1) Replace line 236 `let recentWellnessScores: [Double] = []` with a 14-day fetch — instantiate a wellness fetch (a small `WellnessRepository`-style method is preferred for testability, but an inline `FetchDescriptor<WellnessCheckIn>` is acceptable per the PersonalRecord precedent), filter to the athlete IN SWIFT (avoid the relationship-predicate trap), window to the last 14 days, sort oldest-first, and `map(\.wellnessScore)`. Pass all 14 points (do NOT `.suffix(7)` — D-12 locks 14). (2) Inside the non-cold-start `else` branch, fetch the recent niggles once: `let niggleLogs = SorenessLogRepository(modelContext: modelContext).fetchRecent(days: NiggleInjuryDeriver.injuryWindowDays, athlete: athlete)`. (3) Replace line 252 `softTissueInjuryCount: 0` with `softTissueInjuryCount: NiggleInjuryDeriver.softTissueInjuryCount(logs: niggleLogs)` and line 253 `daysSinceLastInjury: nil` with `daysSinceLastInjury: NiggleInjuryDeriver.daysSinceLastInjury(logs: niggleLogs)`. Keep the niggle fetch + derivation INSIDE the `else` branch so cold-start does no extra work (the wellness fetch may sit just above the if/else since it is also used only by the else branch — keep it cheap or guard it). Do NOT change the cold-start branch, the cycle-context arg, or any non-fatigue wiring. Confirm by diff that the only changes are the three input replacements + their supporting fetches.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' 2>&1 | tail -15</automated>
    <manual>Read the DashboardViewModel diff: only lines 236/252/253 (+ their fetches) changed; cold-start branch and cycle-context arg untouched; recentWellnessScores passes 14 points (no .suffix(7)).</manual>
  </verify>
  <acceptance_criteria>
    - `recentWellnessScores` is the last 14d `WellnessCheckIn.wellnessScore` (oldest-first, no `.suffix(7)`); the empty `[]` is gone.
    - `softTissueInjuryCount` and `daysSinceLastInjury` come from `NiggleInjuryDeriver` over a 28d niggle fetch (no longer 0/nil).
    - The niggle fetch + derivation are inside the non-cold-start else branch (cold-start does no extra work; its branch is unchanged).
    - No relationship-predicate fetch crashes (athlete filtered in Swift); empty data degrades gracefully.
    - xcodebuild build exits 0.
  </acceptance_criteria>
  <done>The dashboard fatigue path is fed real wellness history and real niggle-derived injury inputs, with cold-start behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SorenessLog rows → fatigue engine | Only derived integer count + day-diff feed the engine; no note text or PII crosses into the fatigue computation. |
| Device → Supabase sync | No new sync path: the deriver is pure, the VM reads local SorenessLog/WellnessCheckIn only. FatigueIndex already stays on-device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-25-07 | Information Disclosure | NiggleInjuryDeriver / VM wiring | accept | Pure read over local SorenessLog/WellnessCheckIn; produces only an Int + Int? consumed locally by FatigueIndexEngine. No new persistence or sync. |
| T-25-08 | Tampering | fatigue-input correctness (DOMS inflation) | mitigate | The qualification predicate excludes `soreness`-type logs (D-10); the DOMS-exclusion unit test guards against spurious fatigue inflation. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Apple frameworks only). |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/NiggleInjuryDeriverTests` exits 0; `xcodebuild build ...` exits 0 (iPhone 17 Pro Max sim id=8E872500-703D-4292-9758-38ADFCCFB126).
- DOMS-exclusion: a soreness-type severity-10 limitedTraining-true log is NOT counted (test asserts 0).
- D-12: `recentWellnessScores` is no longer `[]`; passes 14 points (grep the diff — no `.suffix(7)` on the wellness series).
- Purity: `grep -n "import SwiftData\|import HealthKit" WorkloadApp/Services/NiggleInjuryDeriver.swift` returns nothing.
- Cold-start: the niggle fetch + derivation sit inside the non-cold-start else branch (diff review).
</verification>

<success_criteria>
- A pure `NiggleInjuryDeriver` encodes the functional qualification rule (type + impact, DOMS-excluded) with tunable named constants (D-10/D-11/D-13).
- DashboardViewModel feeds real 14d wellness history (D-12) and deriver-backed injury inputs into FatigueInput; the three hardcoded values are gone.
- Cold-start behavior is unchanged; empty data degrades gracefully; build + pure tests are green via real xcodebuild.
</success_criteria>

<output>
Create `.planning/phases/25-soreness-tweak-self-log/25-03-SUMMARY.md` when done (record the window-boundary inclusivity choice, the wellness-fetch shape (repo vs inline), and the 14-vs-7 decision as 14 per D-12).
</output>
