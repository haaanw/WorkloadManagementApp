---
phase: 25-soreness-tweak-self-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/SorenessLog.swift
  - WorkloadApp/Models/Enums.swift
  - WorkloadApp/Repositories/SorenessLogRepository.swift
  - WorkloadApp/App/WorkloadApp.swift
  - WorkloadAppTests/SorenessLogModelTests.swift
autonomous: true
requirements: [P25-MODEL, P25-ENUM, P25-REPO]
must_haves:
  truths:
    - "D-01: a new dedicated local-only SorenessLog @Model exists, DISTINCT from WellnessCheckIn (which is untouched)"
    - "D-02: one SorenessLog captures region (MuscleGroup rawValue), type (NiggleType), severity 0-10, limitedTraining Bool, optional note, plus id/date/athlete"
    - "D-03: limitedTraining is a required Bool defaulting to false (the functional-impact discriminator)"
    - "Local-only contract: SorenessLog has NO Codable, NO *Row DTO, NO push*/pull* method, and the type name appears NOWHERE in SyncService.swift"
    - "NiggleType enum {soreness, pain, tweak} lives in Enums.swift with a stable String rawValue and displayName"
    - "Region stored as MuscleGroup.rawValue (taxonomy-aligned for Phase 27), NOT MuscleRegion/BodyRegion"
    - "SorenessLog.self is registered in BOTH the app Schema (WorkloadApp.swift) AND every test ModelContainer schema that needs it"
    - "A SorenessLogRepository (@MainActor final class) provides insert + fetchRecent(days:athlete:) returning [SorenessLog]"
    - "Build is green via real xcodebuild; new .swift files are members of BOTH the app target and WorkloadAppTests in the .pbxproj"
  artifacts:
    - path: "WorkloadApp/Models/SorenessLog.swift"
      provides: "local-only @Model: id, date, regionRaw, typeRaw, severity, limitedTraining, note, updatedAt, athlete?"
      contains: "regionRaw"
    - path: "WorkloadApp/Models/Enums.swift"
      provides: "NiggleType enum {soreness, pain, tweak} with rawValue + displayName"
      contains: "enum NiggleType"
    - path: "WorkloadApp/Repositories/SorenessLogRepository.swift"
      provides: "@MainActor repo: insert(...) and fetchRecent(days:athlete:) -> [SorenessLog]"
      contains: "fetchRecent"
    - path: "WorkloadApp/App/WorkloadApp.swift"
      provides: "SorenessLog.self added to the Schema([...]) array"
      contains: "SorenessLog.self"
    - path: "WorkloadAppTests/SorenessLogModelTests.swift"
      provides: "persistence round-trip + field-fidelity + NiggleType rawValue-stability tests"
      contains: "SorenessLog"
  key_links:
    - from: "WorkloadApp/App/WorkloadApp.swift Schema"
      to: "SorenessLog @Model"
      via: "SorenessLog.self in the Schema array"
      pattern: "SorenessLog.self"
    - from: "SorenessLogRepository.fetchRecent"
      to: "SorenessLog rows"
      via: "FetchDescriptor windowed by date"
      pattern: "fetchRecent"
---

<objective>
Lay the data foundation for the niggle self-log: a new dedicated, **local-only** `SorenessLog` SwiftData `@Model` (region + type + 0-10 severity + limited-training flag + optional note), a new `NiggleType` domain enum, and a thin `@MainActor` repository. This is the single model both downstream halves (the `.niggleSeverity` shadow outcome and the fatigue-input derivation) consume, so it ships first in its own wave.

Purpose: give Phase 27's localized Strain-Risk channel a real, on-device, never-synced source of localized breakdown signal, while keeping the existing whole-body `WellnessCheckIn.soreness` path completely untouched (D-01, D-05).

Output: `SorenessLog.swift`, `NiggleType` in `Enums.swift`, `SorenessLogRepository.swift`, Schema registration, and persistence tests — all build-green with correct `.pbxproj` target membership.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md
@.planning/phases/25-soreness-tweak-self-log/25-RESEARCH.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Extracted from codebase — do not re-explore. -->

Local-only @Model template to COPY (no Codable, no sync field, "Local-only — never syncs" doc-comment):
- WorkloadApp/Models/ShadowArmPrediction.swift (lines 16–63) — `@Attribute(.unique) var id: UUID`, raw-string columns, parent relationship, NO Codable.
- WorkloadApp/Models/CyclePredictionLog.swift (lines 28–160) — local-only @Model with raw-string storage (`outcomeRaw`/`phaseBucketRaw` String pattern) and `var athlete: Athlete?` bare inverse.

Schema registration site:
- WorkloadApp/App/WorkloadApp.swift:70 `let schema = Schema([` … contains `CyclePredictionLog.self` (78), `ShadowArmPrediction.self` (79). Config is `ModelConfiguration(... isStoredInMemoryOnly: false)` → lightweight migration; additive model needs NO MigrationPlan.

Region taxonomy (Enums.swift):
- `enum MuscleRegion` (line 177) — 7 cases: legs, back, chest, shoulders, arms, core, fullBody; each has `displayName` + `systemImage`.
- `enum MuscleGroup` (line 221) — 33 cases; the 7 coarse cases (`legs/back/chest/shoulders/arms/core/fullBody`) share rawValues with MuscleRegion; `MuscleGroup.region` rolls up to MuscleRegion. STORE this enum's rawValue.
- `enum BodyRegion` (line 538) — joint enum; do NOT use.
- Enum style to mirror: `enum PRType: String, Codable, CaseIterable` (line 365).

Sync exclusion convention (SyncService.swift): a model is local-only by OMISSION — no `*Row: Codable` DTO, no `push*`/`pull*` helper, name absent from the file entirely. `CyclePredictionLog`/`ShadowArmPrediction` appear nowhere in SyncService.swift. `SorenessLog` must likewise appear nowhere.

Existing inverse pattern: `WellnessCheckIn` uses a bare `var athlete: Athlete?` with NO `[SorenessLog]` array on `Athlete`. Match this — do NOT add an explicit inverse array to `Athlete`.

Test container schema sites:
- WorkloadAppTests/ShadowAnalyticsServiceTests.swift:12 `let schema = Schema([` … :20 `ModelContainer(`.
- WorkloadAppTests/ShadowDataContractTests.swift:18 `Schema([` … :26 `ModelContainer(`.
- KNOWN TRAP: in-memory SwiftData store traps on optional to-one relationship `#Predicate` on the iOS 26.1 sim (ShadowAnalyticsServiceTests.swift:130,179 use `XCTSkipIf`). Model round-trip tests that do NOT filter by `athlete?.id` via #Predicate are safe; if a fetch filters by relationship predicate, use the same XCTSkip pattern OR fetch-all-then-filter-in-Swift.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NiggleType enum + local-only SorenessLog @Model, register in app + test schemas</name>
  <files>WorkloadApp/Models/Enums.swift, WorkloadApp/Models/SorenessLog.swift, WorkloadApp/App/WorkloadApp.swift, WorkloadAppTests/SorenessLogModelTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - WorkloadApp/Models/ShadowArmPrediction.swift (full — the local-only @Model template: no Codable, raw-string columns, doc-comment)
    - WorkloadApp/Models/CyclePredictionLog.swift (lines 28–80 — local-only style, raw-string storage, bare athlete inverse)
    - WorkloadApp/Models/WellnessCheckIn.swift (lines 4–43 — bare `var athlete: Athlete?` inverse; do NOT duplicate its fields)
    - WorkloadApp/Models/Enums.swift (lines 177–267, 365–383 — MuscleRegion/MuscleGroup + the PRType enum style to mirror)
    - WorkloadApp/App/WorkloadApp.swift (lines 70–93 — the Schema array)
    - WorkloadAppTests/ShadowAnalyticsServiceTests.swift (lines 1–30, 120–135 — test container schema + the XCTSkip trap note)
  </read_first>
  <behavior>
    - Test 1: a SorenessLog inserted with regionRaw="legs", typeRaw="tweak", severity=8, limitedTraining=true, note="left hamstring" persists and fetches back with all fields equal (field fidelity).
    - Test 2: NiggleType has exactly cases soreness, pain, tweak; rawValues equal "soreness"/"pain"/"tweak" (permanent serialization contract — stability assertion).
    - Test 3: a SorenessLog with note=nil round-trips with note nil (optional fidelity); limitedTraining defaults to false when not supplied.
    - Test 4: a stored MuscleGroup coarse rawValue (e.g. "legs") round-trips and `MuscleGroup(rawValue:)` reconstructs it (taxonomy-alignment sanity).
  </behavior>
  <action>
    Add `enum NiggleType: String, Codable, CaseIterable, Identifiable { case soreness; case pain; case tweak }` to `Enums.swift`, mirroring the `PRType` style — include `var id: String { rawValue }` and a localized `displayName` (e.g. "Soreness"/"Pain"/"Tweak"). The enum being `Codable` is fine (only the @Model must avoid Codable). Create `WorkloadApp/Models/SorenessLog.swift` as a **local-only** `@Model final class SorenessLog` copied in shape from `ShadowArmPrediction`/`CyclePredictionLog`: a leading doc-comment "Local-only — never syncs to Supabase (P25 D-01)"; fields `@Attribute(.unique) var id: UUID`, `var date: Date` (real timestamp — do NOT force start-of-day in init; window math uses Calendar.startOfDay at read time), `var regionRaw: String` (stores a `MuscleGroup.rawValue` per RESEARCH §2 / D-02 — NOT MuscleRegion, NOT BodyRegion), `var typeRaw: String` (a `NiggleType.rawValue`), `var severity: Int` (0–10, Int not Double — RESEARCH §1; the outcome converts to Double at resolution time), `var limitedTraining: Bool` (D-03, default false, REQUIRED), `var note: String?`, `var updatedAt: Date`, and a bare `var athlete: Athlete?` inverse (match WellnessCheckIn — do NOT add a `[SorenessLog]` array on `Athlete`). Provide a memberwise `init` with `limitedTraining: Bool = false` and `note: String? = nil` defaults. Add NO `Codable`, NO encoder, NO `*Row` DTO; the type must NOT be referenced in SyncService.swift. Register `SorenessLog.self` in the `Schema([...])` array in `WorkloadApp.swift` (additive → SwiftData lightweight migration, no MigrationPlan, matching the `ShadowArmPrediction` precedent). Create `WorkloadAppTests/SorenessLogModelTests.swift` (`final class SorenessLogModelTests: XCTestCase`, `@testable import workload_management`) building an in-memory `ModelContainer` whose schema INCLUDES `SorenessLog.self`, with the behavior tests above; keep tests free of optional-relationship `#Predicate` fetches (fetch-all + filter in Swift) to dodge the iOS 26.1 in-memory trap. **CRITICAL (CLAUDE.md iOS rule):** add `SorenessLog.swift` to the app target and `SorenessLogModelTests.swift` to the `WorkloadAppTests` target in `project.pbxproj` (PBXBuildFile + PBXFileReference + the correct PBXSourcesBuildPhase for each target). Note: SourceKit "cannot find type" diagnostics here are routinely STALE — only the xcodebuild in <verify> proves green.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' -only-testing:WorkloadAppTests/SorenessLogModelTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `NiggleType` enum exists in Enums.swift with cases soreness/pain/tweak, stable rawValues, id, displayName.
    - `SorenessLog.swift` exists as a local-only @Model with id/date/regionRaw/typeRaw/severity(Int)/limitedTraining(Bool, default false)/note(String?)/updatedAt/athlete? — and NO Codable.
    - `grep -n "Codable\|import Supabase\|encoder" WorkloadApp/Models/SorenessLog.swift` returns nothing; `grep -n "SorenessLog" WorkloadApp/Services/SyncService.swift` returns nothing.
    - `SorenessLog.self` is in the app Schema (WorkloadApp.swift) and in the test ModelContainer schema (SorenessLogModelTests).
    - Both new .swift files are members of the correct targets in project.pbxproj (SorenessLog → app; SorenessLogModelTests → WorkloadAppTests).
    - SorenessLogModelTests round-trip + field-fidelity + NiggleType-stability tests pass; xcodebuild test exits 0.
  </acceptance_criteria>
  <done>A local-only SorenessLog @Model + NiggleType enum exist, registered in both the app and test schemas, with passing persistence tests and correct .pbxproj membership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SorenessLogRepository (@MainActor) — insert + windowed fetchRecent</name>
  <files>WorkloadApp/Repositories/SorenessLogRepository.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - WorkloadApp/Repositories/CyclePredictionLogRepository.swift (full — @MainActor final class shape, ModelContext init, windowed FetchDescriptor + startOfDay math)
    - WorkloadApp/Models/SorenessLog.swift (the model from Task 1)
    - WorkloadApp/Services/ShadowAnalyticsService.swift (lines 130–203 — startOfDay windowing + fetchWellnessByDay style for date-windowed fetches)
  </read_first>
  <action>
    Create `WorkloadApp/Repositories/SorenessLogRepository.swift` as a `@MainActor final class SorenessLogRepository` taking `private let modelContext: ModelContext` in init (mirror `CyclePredictionLogRepository`). Provide: (1) `func insert(region: MuscleGroup, type: NiggleType, severity: Int, limitedTraining: Bool, note: String?, athlete: Athlete?) -> SorenessLog` that constructs a `SorenessLog(date: .now, regionRaw: region.rawValue, typeRaw: type.rawValue, …)`, inserts it into the context, and returns it (caller saves, matching existing repo convention — or save inside if that matches the dominant pattern; document the choice). (2) `func fetchRecent(days: Int, athlete: Athlete?) -> [SorenessLog]` returning all logs whose `date >= Calendar.startOfDay(for: now - days)`, time-ordered (newest or oldest first — document which; the derivation helper in Plan 03 is order-agnostic but consistency helps). To avoid the iOS 26.1 optional-relationship `#Predicate` trap, filter by `athlete` in Swift after a date-windowed `FetchDescriptor` (or by `athlete?.id` only if the existing repos already do so safely on-device — but tests should not depend on the relationship predicate). Add NO sync code. **CLAUDE.md iOS rule:** add `SorenessLogRepository.swift` to the app target in `project.pbxproj`.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `SorenessLogRepository` is a `@MainActor final class` with a `ModelContext` init, an `insert(...)` that constructs+inserts a SorenessLog, and `fetchRecent(days:athlete:) -> [SorenessLog]` windowed by date.
    - `fetchRecent` does not crash on the in-memory store (no optional-relationship #Predicate; filter relationship in Swift).
    - `SorenessLogRepository.swift` is a member of the app target in project.pbxproj.
    - `grep -n "SorenessLog" WorkloadApp/Services/SyncService.swift` still returns nothing (repo introduces no sync path).
    - xcodebuild build exits 0.
  </acceptance_criteria>
  <done>A @MainActor repository can insert niggle logs and fetch a recent date-windowed slice, with the new file in the app target and zero sync surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device → Supabase sync | SorenessLog is local-only: no Codable, no `*Row` DTO, no push/pull helper, name absent from SyncService.swift. Sensitive health-adjacent localized pain data never leaves the device. |
| User free-text note → SwiftData | The optional `note` is a local-only String; no network/SQL injection surface (on-device SwiftData store). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-25-01 | Information Disclosure | SorenessLog new @Model | mitigate | No Codable, no encoder, no `*Row` DTO, no push/pull method; type name absent from SyncService.swift (grep-verified). Stays on-device by omission. |
| T-25-02 | Tampering | SwiftData schema migration | mitigate | Additive new model with bare optional `athlete?` inverse → SwiftData lightweight migration (matches ShadowArmPrediction precedent); registered in app + test schemas; no MigrationPlan, no destructive change. |
| T-25-03 | Information Disclosure | free-text `note` field | accept | Optional local-only String; never synced; constrained to the on-device store. No transmission surface. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (Apple frameworks only); no third-party supply-chain surface. |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/SorenessLogModelTests` exits 0 on the iPhone 17 Pro Max sim (id=8E872500-703D-4292-9758-38ADFCCFB126).
- `xcodebuild build ...` exits 0 with `SorenessLog.self` in the app Schema.
- Privacy: `grep -n "SorenessLog" WorkloadApp/Services/SyncService.swift` returns nothing; `grep -n "Codable" WorkloadApp/Models/SorenessLog.swift` returns nothing.
- `.pbxproj`: SorenessLog.swift + SorenessLogRepository.swift are app-target members; SorenessLogModelTests.swift is a WorkloadAppTests member (build/test would fail otherwise).
</verification>

<success_criteria>
- A dedicated, local-only `SorenessLog` @Model (D-01/D-02/D-03) and `NiggleType` enum exist, with region stored as a `MuscleGroup.rawValue` (Phase-27-aligned).
- The model is never-synced (by omission from SyncService) and registered in both the app and test schemas (additive lightweight migration).
- A `@MainActor` repository provides insert + windowed fetch for the downstream consumers.
- Build + persistence tests are green via real xcodebuild; all new files have correct `.pbxproj` target membership.
</success_criteria>

<output>
Create `.planning/phases/25-soreness-tweak-self-log/25-01-SUMMARY.md` when done (record the final model/field names, the insert-saves-vs-caller-saves choice, and the fetchRecent ordering).
</output>
</content>
</invoke>
