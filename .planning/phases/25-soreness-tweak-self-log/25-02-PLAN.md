---
phase: 25-soreness-tweak-self-log
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - WorkloadApp/Services/ShadowPredictor.swift
  - WorkloadApp/Models/ShadowArmPrediction.swift
  - WorkloadApp/Models/CyclePredictionLog.swift
  - WorkloadApp/Services/ShadowAnalyticsService.swift
  - WorkloadAppTests/ShadowDataContractTests.swift
autonomous: true
requirements: [P25-OUTCOME, P25-RESOLVE]
must_haves:
  truths:
    - "D-04: ShadowPredictor.Outcome gains a 5th case .niggleSeverity (graded 0-10, max niggle severity in the outcome window, 0 if none)"
    - "D-05: the existing .pain (whole-body 1-5) outcome is NOT touched; no binary breakdown-event outcome is added"
    - "Every switch over Outcome compiles with a .niggleSeverity branch: phaseOffset, series(for:), outcomeRaw(for:), and all three aggregate/metrics actual(_:) switches"
    - "CyclePredictionLog gains var niggleSeverityActual: Double? (additive, local-only, lightweight migration)"
    - "D-06: niggleSeverityActual is resolved strictly on Calendar.startOfDay(for: row.targetDate) — no same-day leak — mirroring painActual"
    - "The actual is max SorenessLog.severity (as Double) logged on the target day, else 0.0 (dense label, always resolvable on targetDate elapse, like completionActual)"
    - "A niggle logged on prediction day D (targetDate D+1) does NOT resolve that D->D+1 row; only a niggle on D+1 does (no-leak regression)"
    - "Both existing arms (baseline, cycleAware) return nil for .niggleSeverity — no misleading 50.0 prediction is stored; the harness tolerates n=0 gracefully (no crash)"
    - "phaseOffset(for:outcome:) returns 0 for .niggleSeverity (no cycle offset in v1)"
    - "Local-only contract preserved: SorenessLog still appears nowhere in SyncService.swift; no Codable added anywhere"
  artifacts:
    - path: "WorkloadApp/Services/ShadowPredictor.swift"
      provides: ".niggleSeverity case; phaseOffset branch returns 0; both arms guard nil for it"
      contains: "niggleSeverity"
    - path: "WorkloadApp/Models/CyclePredictionLog.swift"
      provides: "niggleSeverityActual: Double? column"
      contains: "niggleSeverityActual"
    - path: "WorkloadApp/Models/ShadowArmPrediction.swift"
      provides: "outcomeRaw(for:) returns \"niggleSeverity\""
      contains: "niggleSeverity"
    - path: "WorkloadApp/Services/ShadowAnalyticsService.swift"
      provides: "max-niggle-severity-by-target-day join in resolveOutcomes; series + all three actual switches handle .niggleSeverity"
      contains: "niggleSeverityActual"
    - path: "WorkloadAppTests/ShadowDataContractTests.swift"
      provides: "max-in-window, 0-if-none, no-same-day-leak, and nil-prediction-tolerance tests for .niggleSeverity"
      contains: "niggleSeverity"
  key_links:
    - from: "ShadowAnalyticsService.resolveOutcomes"
      to: "SorenessLog rows on targetDate"
      via: "maxNiggleSeverityByDay[startOfDay(targetDate)] ?? 0.0"
      pattern: "niggleSeverityActual"
    - from: "ShadowPredictor.registeredArms predict closures"
      to: "nil for .niggleSeverity"
      via: "guard outcome != .niggleSeverity else { return nil }"
      pattern: "niggleSeverity"
---

<objective>
Add ONE new **graded** validation outcome `.niggleSeverity` to the Phase-24 shadow harness, end-to-end and **date-contract-safe**, so Phase 27's localized Strain-Risk channel has a resolvable target the moment it ships (filling the Phase-24 D-07 contract slot). The actual label is the **max `SorenessLog` severity (0-10) logged on the prediction's `targetDate`, 0 if none** — a dense, gracefully-degrading label resolved strictly on `targetDate` (no same-day leak), mirroring exactly how `painActual` is resolved.

Purpose: validation plumbing only. No arm predicts `.niggleSeverity` in v1 (both existing arms return `nil`), so the harness accumulates the actual substrate without storing a misleading prediction; the master activation flag stays OFF and users see nothing. The existing `.pain` (whole-body 1-5) outcome is left completely untouched (D-05).

Output: the 5th `Outcome` case, a new `niggleSeverityActual` column, the target-day resolution join, all switch-ripple sites updated, and no-leak/0-if-none/nil-tolerance tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md
@.planning/phases/25-soreness-tweak-self-log/25-RESEARCH.md
@.planning/phases/24-validation-data-contract/24-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase — do not re-explore. SorenessLog/NiggleType from Plan 01. -->

From WorkloadApp/Services/ShadowPredictor.swift:
- `enum Outcome: CaseIterable { case recovery; case wellness; case completion; case pain }` (lines 25–30). NOTE: switches over Outcome are NOT defaulted — adding a case forces every switch to be updated (the compiler is the gate).
- `static func phaseOffset(for phase:outcome:) -> Double` (line 113) — luteal switch at 120–127 over {recovery,wellness,completion,pain}.
- `static func registeredArms() -> [ExperimentalArm]` (line ~165) — two arms; `predict` closures at the `baseline` (returns `baselinePrediction(series:)`) and `cycleAware` (returns `cycleAwarePrediction(series:context:outcome:)`) — closure signature is `(Outcome, [Double], CycleContext) -> Double?` (already optional-returning).

From WorkloadApp/Models/CyclePredictionLog.swift:
- `var painActual: Double?` (line 79); init param (line 116); assignment (line 146). Add `niggleSeverityActual: Double?` alongside, additive (default nil), local-only.

From WorkloadApp/Models/ShadowArmPrediction.swift:
- `static func outcomeRaw(for outcome:) -> String` (line 55) switch — `.pain` → "pain" (line 60). Add `.niggleSeverity` → "niggleSeverity" (stable permanent key).

From WorkloadApp/Services/ShadowAnalyticsService.swift (@MainActor struct):
- `func series(for outcome:) -> [Double]` (line 65) switch — `.pain` → painHistory (line 70). Add `.niggleSeverity` → `[]` (no input series; no arm consumes it in v1).
- `resolveOutcomes(...)`: the target-day join loop (lines 158–172) — `let day = calendar.startOfDay(for: row.targetDate)` (160); `row.painActual = Double(w.soreness)` (165); `row.completionActual = sessionDays.contains(day) ? 1.0 : 0.0` (168, the always-resolvable precedent). `wellnessByDay`/`recoveryByDay`/`sessionDays` are prebuilt earlier (≈152–155); `fetchWellnessByDay` (178–203) is the by-day fetch+group template.
- Three actual-extraction switches over Outcome that each have a `.pain` branch returning `row.painActual`: `aggregate` `actual(_:_:)` (line 214, case .pain at 219), `metricsReport` `actual(_:)` (line 264, case .pain at 269), `pairedMAEDifferenceCI` `actual(_:)` (line 315, case .pain at 320). Each needs `.niggleSeverity` → `row.niggleSeverityActual`.
- `recordPrediction` arm loop: `guard let predicted = arm.predict(outcome, series(for: outcome), context) else { continue }` (line 91) — a nil prediction simply writes no ShadowArmPrediction row.
- `aggregate` requires BOTH arms' predictions AND the actual `else { continue }` (≈226–228) then `guard n > 0 else { continue }` (≈233) — an outcome with no predictions yields n=0 and is omitted; no crash.

From WorkloadApp/Models/SorenessLog.swift (Plan 01):
- `var date: Date` (real timestamp), `var severity: Int` (0–10), `var typeRaw: String`, `var athlete: Athlete?`. Window by `Calendar.startOfDay(for: log.date)`.

Locked decisions: D-04 (graded max-in-window, 0 if none), D-05 (don't touch .pain, no binary outcome), D-06 (resolve on targetDate, no same-day leak). 24-CONTEXT D-07 (this is the promised resolvable contract slot).
KNOWN TRAP: iOS 26.1 in-memory SwiftData traps on optional to-one relationship `#Predicate` (ShadowDataContractTests/ShadowAnalyticsServiceTests use XCTSkipIf). Prefer testing the max-in-window grouping as a pure helper over plain `[SorenessLog]` arrays, OR use the existing XCTSkip pattern for any relationship-predicate fetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .niggleSeverity case + niggleSeverityActual column + update ALL Outcome switch-ripple sites (no arm predicts it)</name>
  <files>WorkloadApp/Services/ShadowPredictor.swift, WorkloadApp/Models/CyclePredictionLog.swift, WorkloadApp/Models/ShadowArmPrediction.swift, WorkloadApp/Services/ShadowAnalyticsService.swift, WorkloadAppTests/ShadowDataContractTests.swift</files>
  <read_first>
    - WorkloadApp/Services/ShadowPredictor.swift (lines 24–32 Outcome enum; 113–128 phaseOffset; 160–182 registeredArms predict closures)
    - WorkloadApp/Models/CyclePredictionLog.swift (lines 79, 116, 146 — painActual field/param/assignment)
    - WorkloadApp/Models/ShadowArmPrediction.swift (lines 55–62 — outcomeRaw switch)
    - WorkloadApp/Services/ShadowAnalyticsService.swift (lines 65–72 series; 214–221, 264–271, 315–322 the three actual switches)
    - .planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    - Test 1 (compile/exhaustiveness): the project compiles after adding .niggleSeverity — every Outcome switch has a branch (the compiler enforces this; the build is the test).
    - Test 2: `ShadowArmPrediction.outcomeRaw(for: .niggleSeverity) == "niggleSeverity"` (stable raw key).
    - Test 3: `ShadowPredictor.phaseOffset(for: <any luteal phase>, outcome: .niggleSeverity) == 0` (no cycle offset in v1).
    - Test 4 (nil-prediction tolerance): for `.niggleSeverity`, BOTH registered arms' `predict` closures return `nil`; running `recordPrediction` over a synthetic athlete writes ZERO `ShadowArmPrediction` rows for `.niggleSeverity` and `aggregate` omits it (n=0) without crashing.
  </behavior>
  <action>
    Add `case niggleSeverity   // max SorenessLog severity (0-10) in the outcome window, 0 if none (P25 D-04)` to `Outcome` (ShadowPredictor.swift:30). Then fix EVERY switch the compiler now flags — exactly these, per RESEARCH §3: (1) `phaseOffset` luteal switch (ShadowPredictor.swift:120–127): add `case .niggleSeverity: return 0`. (2) `series(for:)` (ShadowAnalyticsService.swift:65–72): add `case .niggleSeverity: return []` (no input series — no arm consumes it in v1). (3) `ShadowArmPrediction.outcomeRaw(for:)` (line 55–62): add `case .niggleSeverity: return "niggleSeverity"`. (4) all three actual switches (ShadowAnalyticsService.swift:214–221, 264–271, 315–322): add `case .niggleSeverity: return row.niggleSeverityActual`. Add `var niggleSeverityActual: Double?` to `CyclePredictionLog` alongside `painActual` (field at 79, init param at 116, assignment at 146; default nil) — additive, local-only, NO Codable. In `registeredArms()` (ShadowPredictor.swift:160–182), make BOTH arms' `predict` closures return `nil` for `.niggleSeverity` (a one-line `guard outcome != .niggleSeverity else { return nil }` at the top of each closure) — per RESEARCH §3, returning the 50.0 neutral score on a 0–10 scale would pollute metrics; nil is correct and the harness tolerates it. Do NOT touch the `.pain` branches anywhere (D-05). Add tests in `ShadowDataContractTests.swift`: the outcomeRaw key, the phaseOffset-0 assertion, and the nil-prediction/zero-rows tolerance over a synthetic in-memory container (use the existing XCTSkip pattern for any optional-relationship #predicate fetch). Note: stale SourceKit "non-exhaustive switch" warnings do not count — only xcodebuild proves green.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' -only-testing:WorkloadAppTests/ShadowDataContractTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `Outcome` has a 5th case `.niggleSeverity`; the project builds (every switch updated — phaseOffset, series, outcomeRaw, all three actual switches).
    - `CyclePredictionLog` has `niggleSeverityActual: Double?` (additive, default nil, no Codable).
    - `outcomeRaw(for: .niggleSeverity) == "niggleSeverity"`; `phaseOffset(..., outcome: .niggleSeverity) == 0`.
    - Both arms return nil for `.niggleSeverity` → zero ShadowArmPrediction rows for it; aggregate omits it (n=0) without crashing.
    - `.pain` branches are byte-unchanged (grep the diff — no edits inside any `.pain:` case).
    - xcodebuild test exits 0.
  </acceptance_criteria>
  <done>The 5th graded outcome exists with its actual column and a "no arm predicts it yet" posture; every Outcome switch compiles and the harness tolerates the unpredicted outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Date-contract-safe resolution — max niggle severity on targetDate (0 if none), no same-day leak</name>
  <files>WorkloadApp/Services/ShadowAnalyticsService.swift, WorkloadAppTests/ShadowDataContractTests.swift</files>
  <read_first>
    - WorkloadApp/Services/ShadowAnalyticsService.swift (lines 130–203 — the resolveOutcomes loop, the by-day prebuilt maps ≈152–155, the painActual join at 165, completionActual always-resolvable at 168, fetchWellnessByDay template 178–203)
    - WorkloadApp/Models/SorenessLog.swift (date/severity/athlete from Plan 01)
    - WorkloadAppTests/ShadowDataContractTests.swift (same-day-leak regression intent + XCTSkip trap pattern)
    - .planning/phases/25-soreness-tweak-self-log/25-CONTEXT.md (D-04, D-06)
  </read_first>
  <behavior>
    - Test 1 (max-in-window): two SorenessLogs on the target day (severity 4 and 8) → niggleSeverityActual == 8.0 (max, as Double).
    - Test 2 (0-if-none): a resolvable row whose targetDate has NO niggle → niggleSeverityActual == 0.0 (dense label; row still marked resolved on targetDate elapse, exactly like completionActual).
    - Test 3 (NO same-day leak): a niggle logged on prediction day D (a row with predictionDate D, targetDate D+1) does NOT set that row's niggleSeverityActual; only a niggle dated D+1 does. Mirrors the Phase-24 same-day-leak regression intent.
    - Test 4 (start-of-day bucketing): a niggle with `date` late on D+1 still buckets to startOfDay(D+1) and resolves the D->D+1 row.
  </behavior>
  <action>
    In `resolveOutcomes`, build a `maxNiggleSeverityByDay: [Date: Double]` once (like `wellnessByDay` / `fetchWellnessByDay`): fetch `SorenessLog` over the windowed `[earliestTarget … asOf]` range for the athlete, group by `Calendar.startOfDay(for: log.date)`, and take the **max** `Double(log.severity)` per day (RESEARCH §3). In the per-row join loop, using the existing `day = calendar.startOfDay(for: row.targetDate)` (line 160 — the TARGET day, never predictionDate), add: `row.niggleSeverityActual = maxNiggleSeverityByDay[day] ?? 0.0`. Because of the `?? 0.0`, `.niggleSeverity` is ALWAYS resolvable once targetDate has elapsed (the dense-label intent, D-04) — the row's existing `resolvedAt`/resolved-count logic is unchanged (it already marks resolution on targetDate elapse, like completionActual). Do NOT add any join on `row.predictionDate`/`row.date` (that is the exact Phase-24 same-day-leak bug). To dodge the iOS 26.1 optional-relationship #predicate trap, prefer building the by-day map from a date-windowed fetch filtered by athlete IN SWIFT (not via relationship #Predicate), matching the safe pattern. Add tests in `ShadowDataContractTests.swift` for max-in-window, 0-if-none, no-same-day-leak, and late-day start-of-day bucketing — reuse the existing XCTSkip pattern if any test must exercise the relationship predicate on the in-memory store, or assert the grouping via a pure helper over `[SorenessLog]` arrays (preferred — sidesteps the trap entirely).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=8E872500-703D-4292-9758-38ADFCCFB126' -only-testing:WorkloadAppTests/ShadowDataContractTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `resolveOutcomes` sets `niggleSeverityActual = maxNiggleSeverityByDay[startOfDay(targetDate)] ?? 0.0` — max severity on the target day, 0 if none.
    - The join uses `row.targetDate` ONLY; grep confirms no niggle join on `row.date`/`row.predictionDate` (no same-day leak).
    - max-in-window, 0-if-none, no-same-day-leak, and late-day bucketing tests pass.
    - The by-day fetch does not crash the in-memory store (athlete filtered in Swift, not via relationship #Predicate).
    - `.pain`/`wellness`/`completion`/`recovery` resolution is byte-unchanged.
    - xcodebuild test exits 0.
  </acceptance_criteria>
  <done>The .niggleSeverity actual resolves as max-severity-on-targetDay (0 if none), strictly date-contract-safe with a passing same-day-leak regression — the Phase-27 validation substrate now accumulates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SorenessLog rows → shadow harness | The harness reads only the derived max-severity Double per target day; no raw note text or PII enters the analytics path beyond the integer severity. |
| Device → Supabase sync | The new `niggleSeverityActual` column lives on the already-local-only CyclePredictionLog; SorenessLog stays absent from SyncService. Nothing new is synced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-25-04 | Information Disclosure | niggleSeverityActual column | mitigate | Added to the already-local-only CyclePredictionLog (no Codable, no encoder, absent from SyncService). Stores only a derived Double. |
| T-25-05 | Tampering | SwiftData schema migration | mitigate | Additive optional `Double?` column → lightweight migration (matches painActual posture). No destructive change. |
| T-25-06 | Repudiation/Leakage | same-day actual resolution | mitigate | Join keyed strictly on `startOfDay(row.targetDate)`; regression test asserts a D->D+1 row is never resolved by a niggle on D. Reproduces the Phase-24 no-leak guard. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Apple frameworks only). |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:WorkloadAppTests/ShadowDataContractTests` exits 0 on the iPhone 17 Pro Max sim (id=8E872500-703D-4292-9758-38ADFCCFB126).
- Outcome exhaustiveness: the project builds (every switch handles .niggleSeverity).
- No-leak: `grep -n "niggleSeverity" WorkloadApp/Services/ShadowAnalyticsService.swift` shows the join only inside the `targetDate` loop; no `row.predictionDate`/`row.date` niggle join.
- nil-prediction tolerance: zero ShadowArmPrediction rows are written for .niggleSeverity; aggregate omits it (n=0) without crashing.
- `.pain` untouched (D-05): the `.pain:` cases are byte-identical in the diff.
- Privacy: `grep -n "SorenessLog" WorkloadApp/Services/SyncService.swift` still returns nothing.
</verification>

<success_criteria>
- `.niggleSeverity` exists as a 5th graded outcome (max severity 0-10 in window, 0 if none), resolved strictly on targetDate (D-04/D-06), with a passing no-same-day-leak regression.
- Every Outcome switch compiles with the new case; `.pain` is untouched (D-05); both existing arms return nil for it and the harness tolerates n=0.
- Everything stays local-only/never-synced; master activation flag OFF; no user-facing change.
</success_criteria>

<output>
Create `.planning/phases/25-soreness-tweak-self-log/25-02-SUMMARY.md` when done (record the by-day max-severity grouping approach and the nil-prediction-arm decision).
</output>
