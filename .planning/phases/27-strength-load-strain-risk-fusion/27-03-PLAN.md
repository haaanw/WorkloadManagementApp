---
phase: 27-strength-load-strain-risk-fusion
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
autonomous: true
requirements: [P27-STRAIN-RISK-FUSION]
files_modified:
  - WorkloadApp/Services/StrainRiskEngine.swift
  - WorkloadApp/Models/Enums.swift
  - WorkloadApp/WorkloadAppTests/StrainRiskEngineTests.swift
  - workload management.xcodeproj/project.pbxproj
  - .planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md
must_haves:
  truths:
    - "StrainRiskEngine fuses strength-load elevation + endurance-load elevation + FatigueIndex + gated monotony/strain + soft-tissue memory + rest-debt into a single 0-1 heuristic score"
    - "Fusion weights are FIXED, sign-constrained, named constants — NOT fitted, NOT logistic"
    - "Output includes a categorical StrainRiskZone + ranked named human-readable factors + a 0-1 confidence"
    - "Missing inputs cause weight redistribution over present components (no mean-imputation)"
    - "When monotony gate fell back, the fallback is surfaced as an explicit factor"
    - "No user-facing copy says 'injury prediction'; framing is 'load-tolerance / overreaching caution' context"
    - "StrainRiskEngine output does NOT reach the live AutoregulationEngine recommendation or recovery score"
  artifacts:
    - path: WorkloadApp/Services/StrainRiskEngine.swift
      provides: "Pure struct: fixed glass-box Strain-Risk fusion + zone + ranked factors + confidence"
    - path: WorkloadApp/WorkloadAppTests/StrainRiskEngineTests.swift
      provides: "Fusion + sign-constraint + redistribution + determinism + no-prediction-copy + isolation tests"
  key_links:
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/FatigueIndexEngine.swift
      via: "consumes FatigueIndexEngine.compute as one component"
      pattern: "FatigueIndexEngine"
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/NiggleInjuryDeriver.swift
      via: "soft-tissue memory + recurrence"
      pattern: "NiggleInjuryDeriver|NiggleDerivation"
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/BaselineEngine.swift
      via: "endurance-load elevation vs personal baseline + confidence"
      pattern: "BaselineEngine"
---

<objective>
Build the pure `StrainRiskEngine` (static methods): a FIXED sign-constrained glass-box
heuristic fusion of strength-load elevation (Wave 1), gated monotony/strain (Wave 2),
endurance-load elevation vs personal baseline (`BaselineEngine`), `FatigueIndexEngine`
composite, soft-tissue memory + same-region recurrence (`NiggleInjuryDeriver`), and
rest-debt — producing a 0–1 Strain-Risk score, a categorical `StrainRiskZone`, ranked
named factors, and a 0–1 confidence. This is the honest heuristic **flag** per D-27-01 /
codex §301 — NOT logistic, NOT fitted, NOT injury prediction (Readiness is Phase 28).

Purpose: this is the Strain-Risk channel deliverable for Phase 27. It is display/shadow
context ONLY — it MUST NOT alter the live recommendation or recovery score this phase.

Output: `StrainRiskEngine.swift` + `StrainRiskZone` enum + comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-strength-load-strain-risk-fusion/27-RESEARCH.md

From Waves 1+2:
@WorkloadApp/Services/StrengthLoadEngine.swift
@WorkloadApp/Services/LoadDistributionEngine.swift

Components to FUSE (reuse as-is — do NOT duplicate their math, D-27-08):
@WorkloadApp/Services/FatigueIndexEngine.swift
@WorkloadApp/Services/NiggleInjuryDeriver.swift
@WorkloadApp/Services/BaselineEngine.swift

Style references — ranked explainability factors + named constants:
@WorkloadApp/Services/ReasoningEngine.swift
@WorkloadApp/Models/Enums.swift
</context>

<interfaces>
<!-- Verified 2026-05-30. Reuse these; do not reimplement. -->
FatigueIndexEngine.compute(FatigueInput) -> Double (0..1); FatigueInput has currentLoad, baselineLoad, sessionsLast14Days, recoveryScoresLast7Days, daysSinceRest, wellnessScoresLast7Days, injuryCount, daysSinceInjury.
NiggleInjuryDeriver.derive(logs:asOf:) -> NiggleDerivation { injuryCount, daysSinceInjury, qualifyingLogs } (DOMS-excluded).
BaselineEngine.zScore(state:observation:signal:) -> Double?; .confidence(state:signal:asOf:) -> Double (0..1); enum Signal { hrv, rhr, sleep }.
StrengthLoadEngine (Wave 1): StrengthLoadResult { perMuscle, perRegion, recurrenceFlags: Set<MuscleRegion>, ... elevation per muscle 0..1 }.
LoadDistributionEngine (Wave 2): LoadDistributionResult { monotony: Double?, strain: Double?, gateState: {computed,fellBack}, fallbackLoadSignal: Double }.
MuscleRegion: enum { legs, back, chest, shoulders, arms, core }.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: StrainRiskZone enum + StrainRiskFactor / StrainRiskResult types (contracts first)</name>
  <files>WorkloadApp/Models/Enums.swift, WorkloadApp/Services/StrainRiskEngine.swift</files>
  <action>
Add `enum StrainRiskZone: String, Codable, CaseIterable, Identifiable` to
`WorkloadApp/Models/Enums.swift` with cases `.low, .moderate, .elevated, .high`, a
`displayName` ("Low" / "Moderate" / "Elevated load-tolerance caution" / "High
overreaching caution") and `systemImage`. **Copy audit: NO case displayName, no doc
comment, and no factor string anywhere may contain "injury prediction", "predicts
injury", "will get injured", or equivalent.** Framing is strictly "load-tolerance
context / overreaching caution" (D-27-01). This is a shared-type change to `Enums.swift`,
so the FULL suite must run later (Task 4).

Create `WorkloadApp/Services/StrainRiskEngine.swift` shell with the result contracts only:
`struct StrainRiskFactor { let label: String; let contribution: Double }` and
`struct StrainRiskResult { let score: Double; let zone: StrainRiskZone; let factors:
[StrainRiskFactor]; let confidence: Double }`, plus the `StrainRiskEngine` struct and an
`enum Weights` / `enum Constants` block with the fixed sign-constrained weights (define
them, leave `fuse` to Task 2). Document each weight's sign and evidence anchor in a doc
comment.
  </action>
  <verify>
    <automated>xcodebuild build -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <done>StrainRiskZone + result/factor contracts compile; no "injury prediction" copy present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixed glass-box fusion + zone mapping + ranked factors + confidence</name>
  <files>WorkloadApp/Services/StrainRiskEngine.swift, WorkloadApp/WorkloadAppTests/StrainRiskEngineTests.swift</files>
  <behavior>
    - fuse(input) -> StrainRiskResult: each component clamped 0..1, multiplied by its fixed named weight, summed, renormalized over PRESENT components when some are nil (D-27-05 redistribution, no mean-impute).
    - Components (each its own named factor): strengthLoadElevation (highest weight per competitive §3.1), enduranceLoadElevation (BaselineEngine z mapped to 0..1), fatigueIndex (FatigueIndexEngine.compute), monotonyStrain (only if gateState==.computed; else fallbackLoadSignal as a lower-weight substitute factor labeled "load distribution (limited data)"), softTissue (from NiggleDerivation 1-exp(-0.5n) recency-decayed) + same-region recurrence bonus, restDebt (taken from FatigueIndex ONLY — do not double count).
    - zone mapping: score thresholds (named constants) -> StrainRiskZone.
    - confidence: composite of BaselineEngine.confidence + monotony gateState + hard-set data coverage (unscored ratio) — 0..1.
    - factors ranked by absolute contribution, top-N, human-readable.
    - Deterministic; sign-constrained (no component can DECREASE risk below its absence baseline incorrectly — all contributions non-negative toward risk).
  </behavior>
  <action>
Implement `StrainRiskEngine.fuse(...)`. Take a single input struct carrying the upstream
results: `StrengthLoadResult`, `LoadDistributionResult`, the `FatigueInput` (or a
precomputed FatigueIndex 0..1), `NiggleDerivation`, and the BaselineEngine-derived
endurance-load elevation + confidence. Compute the weighted sum with FIXED named weights;
when a component is unavailable (nil / no data), redistribute its weight proportionally
over present components (reuse the redistribution philosophy already in
`FatigueIndexEngine` / `RecoveryScoreEngine` — do NOT mean-impute). When
`LoadDistributionResult.gateState == .fellBack`, substitute the `fallbackLoadSignal` at a
reduced weight and emit an explicit factor "load distribution: limited data — using
fallback" (D-27 honesty). Map the final 0..1 score to `StrainRiskZone` via named
thresholds. Build the ranked `factors` array (top-N by contribution). Compute `confidence`
from BaselineEngine confidence + monotony gate state + hard-set unscored-coverage ratio.

Reuse `FatigueIndexEngine.compute`, `NiggleInjuryDeriver` output, and `BaselineEngine`
directly — do NOT reimplement soft-tissue or baseline math (D-27-08).

Tests (`StrainRiskEngineTests`): worked-example fusion with all components present →
known score + zone; component-missing → weights redistribute + score still 0..1 + factor
list omits the missing one; monotony fellBack → fallback factor present + reduced weight;
sign constraint (raising any single risk component never lowers the score); zone-threshold
boundaries; confidence rises with more data; determinism (same input twice → identical
result); a string-audit test asserting NO factor label / zone displayName contains
"injury prediction" or equivalents.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/StrainRiskEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>Fusion + redistribution + fallback + sign-constraint + determinism + no-prediction-copy tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Isolation guard — Strain-Risk does NOT touch the live recommendation/recovery path</name>
  <files>WorkloadApp/WorkloadAppTests/StrainRiskEngineTests.swift, .planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md</files>
  <action>
Prove the phase-specific invariant: `StrainRiskEngine` output is NOT consumed by the live
recommendation or recovery path. Grep-confirm (and record in notes) that
`AutoregulationEngine` and `RecoveryScoreEngine` contain NO reference to `StrainRiskEngine`
/ `StrainRiskResult` / `StrainRiskZone`, and that no master/activation flag was added or
flipped. The engine exists as standalone substrate this phase (display/shadow context
only, D-27-06).

Add a guard assertion in the test target documenting intent: a test that imports
`StrainRiskEngine` and asserts the live engines' public outputs are independent of it
(call `AutoregulationEngine` / `RecoveryScoreEngine` as today and confirm their results
are produced without any Strain-Risk input). Record the grep results + grep commands used
in `artifacts/27-03-notes.md`.
  </action>
  <verify>
    <automated>grep -n "StrainRisk" WorkloadApp/Services/AutoregulationEngine.swift WorkloadApp/Services/RecoveryScoreEngine.swift | grep -v '^#' | wc -l | grep -qx '       0' && echo "ISOLATION OK" || (echo "ISOLATION VIOLATION"; exit 1)</automated>
  </verify>
  <done>No live engine references StrainRisk*; isolation grep returns 0; notes recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression + tier-fence guard (shared-enum change)</name>
  <files>.planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md</files>
  <action>
Because `Enums.swift` (shared type) changed, run the FULL `WorkloadAppTests` suite per the
shared-type regression gotcha. Confirm `BaselineTierFenceTests` GREEN + unchanged (live
recovery score byte-unchanged) and the whole suite is green (catch any switch-exhaustiveness
ripple from adding `StrainRiskZone`). Discard `.xcstrings` build-churn. Record final fixed
weights + zone thresholds + full-suite pass count in `artifacts/27-03-notes.md`, and
confirm no activation/master flag exists or was flipped.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests 2>&1 | tail -25</automated>
  </verify>
  <done>Full suite green incl. BaselineTierFenceTests; no live default flipped; weights/thresholds recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strain-Risk engine ↔ live decision path | Output must NOT cross into AutoregulationEngine / RecoveryScoreEngine this phase |
| heuristic flag ↔ user copy | Framing must never claim injury prediction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering | Strain-Risk altering live recommendation/recovery score | mitigate | Task 3 isolation grep == 0; Task 4 tier-fence + full suite green; no master flag |
| T-27-08 | Repudiation | "injury prediction" liability copy | mitigate | String-audit test asserts no prediction language in any factor/zone copy |
| T-27-09 | Tampering | Fitted/per-user weights sneaking in (codex-forbidden) | mitigate | Weights are named compile-time constants; no fit/optimize code; reviewed in notes |
| T-27-10 | Spoofing | Mean-imputed missing components inflating score | mitigate | Redistribution-not-imputation tested; missingness reduces confidence |
| T-27-SC | Tampering | package installs | accept | None (Foundation-only Swift) |
</threat_model>

<verification>
- `StrainRiskEngineTests` green (fusion + redistribution + fallback + sign-constraint + determinism + no-prediction-copy + isolation).
- Isolation grep: `AutoregulationEngine`/`RecoveryScoreEngine` reference StrainRisk* == 0.
- Full `WorkloadAppTests` suite green; `BaselineTierFenceTests` unchanged + green.
- Weights are fixed named constants; no fitting/logistic code; no master/activation flag.
</verification>

<success_criteria>
A pure deterministic `StrainRiskEngine` produces an honest heuristic Strain-Risk flag
(score + zone + ranked factors + confidence) from a fixed sign-constrained glass-box
fusion of the strength-load, endurance-load, FatigueIndex, gated monotony/strain, and
soft-tissue components — provably isolated from the live recommendation/recovery path,
with no injury-prediction copy and the live recovery score byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-strength-load-strain-risk-fusion/27-03-SUMMARY.md` when done.
</output>
