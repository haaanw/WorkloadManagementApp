---
phase: 27-strength-load-strain-risk-fusion
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
autonomous: true
requirements: [P27-STRAIN-RISK-FUSION]
files_modified:
  - WorkloadApp/Services/StrainRiskEngine.swift
  - WorkloadApp/Models/Enums.swift
  - WorkloadAppTests/StrainRiskEngineTests.swift
  - workload management/workload management.xcodeproj/project.pbxproj
  - .planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md
must_haves:
  truths:
    - "StrainRiskEngine fuses strength-load elevation + endurance-load elevation + FatigueIndex + gated monotony/strain + soft-tissue memory + rest-debt into a single 0-1 heuristic score"
    - "Fusion weights are FIXED, sign-constrained, named constants — NOT fitted, NOT logistic"
    - "Output includes a categorical StrainRiskZone + ranked named human-readable factors + a 0-1 confidence"
    - "Missing inputs cause weight redistribution over present components (no mean-imputation), mirroring FatigueIndexEngine"
    - "When the monotony gate fell back, the fallback is surfaced as an explicit factor at reduced weight"
    - "No user-facing copy says 'injury prediction'; framing is 'load-tolerance / overreaching caution' context"
    - "StrainRiskEngine output does NOT reach the live AutoregulationEngine recommendation or recovery score (isolation grep == 0)"
  artifacts:
    - path: WorkloadApp/Services/StrainRiskEngine.swift
      provides: "Pure struct: fixed glass-box Strain-Risk fusion + zone + ranked factors + confidence"
    - path: WorkloadAppTests/StrainRiskEngineTests.swift
      provides: "Fusion + sign-constraint + redistribution + determinism + no-prediction-copy + isolation tests"
  key_links:
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/FatigueIndexEngine.swift
      via: "consumes FatigueIndexEngine.compute(input:) as one component"
      pattern: "FatigueIndexEngine"
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/NiggleInjuryDeriver.swift
      via: "soft-tissue count + recency"
      pattern: "NiggleInjuryDeriver|softTissueInjuryCount"
    - from: WorkloadApp/Services/StrainRiskEngine.swift
      to: WorkloadApp/Services/BaselineEngine.swift
      via: "endurance-load elevation vs personal baseline + confidence"
      pattern: "BaselineEngine"
---

<objective>
Build the pure `StrainRiskEngine` (static methods): a FIXED sign-constrained glass-box
heuristic fusion of strength-load elevation (Wave 1), gated monotony/strain (Wave 2),
endurance-load elevation vs personal baseline (`BaselineEngine`), the `FatigueIndexEngine`
composite, soft-tissue memory + same-region recurrence (`NiggleInjuryDeriver` +
StrengthLoadEngine recurrence flags), and rest-debt — producing a 0–1 Strain-Risk score, a
categorical `StrainRiskZone`, ranked named factors, and a 0–1 confidence. This is the honest
heuristic **flag** per D-27-01 / codex §301 — NOT logistic, NOT fitted, NOT injury
prediction (Readiness is Phase 28).

Purpose: the Strain-Risk channel deliverable for Phase 27. Display/shadow context ONLY — it
MUST NOT alter the live recommendation or recovery score this phase.

Output: `StrainRiskEngine.swift` + `StrainRiskZone` enum + comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-strength-load-strain-risk-fusion/27-RESEARCH.md
@CLAUDE.md

From Waves 1+2:
@WorkloadApp/Services/StrengthLoadEngine.swift
@WorkloadApp/Services/LoadDistributionEngine.swift

Components to FUSE (reuse as-is — do NOT duplicate their math, D-27-08):
@WorkloadApp/Services/FatigueIndexEngine.swift
@WorkloadApp/Services/NiggleInjuryDeriver.swift
@WorkloadApp/Services/BaselineEngine.swift

Style references — redistribution pattern + named constants + clamp:
@WorkloadApp/Services/ReasoningEngine.swift
@WorkloadApp/Models/Enums.swift
</context>

<interfaces>
<!-- Verified from source 2026-05-30. Reuse these EXACT signatures; do not reimplement. -->
FatigueIndexEngine.compute(input: FatigueInput) -> FatigueResult
  FatigueInput: { recentSessionTSS: [Double]; baselineSessionTSS: Double?; sessionsIn14Days: Int;
    baselineSessionsIn14Days: Double?; trainingStreakDays: Int; daysSinceRestPeriod: Int?;
    recentRecoveryScores: [Double]; recentWellnessScores: [Double]; softTissueInjuryCount: Int;
    daysSinceLastInjury: Int? }
  FatigueResult: { index: Double (0-100); zone; loadElevation; sessionDensity; recoveryTrend;
    restDebt; wellnessTrend; softTissueRisk (each 0-1) }
NiggleInjuryDeriver.softTissueInjuryCount(logs: [SorenessLog], asOf: Date) -> Int
NiggleInjuryDeriver.daysSinceLastInjury(logs: [SorenessLog], asOf: Date) -> Int?
BaselineEngine.score(state: SignalState, observation: Double, higherIsBetter: Bool, floor: Double) -> (z: Double?, innovation: Double)
BaselineEngine.confidence(...) (see BaselineEngine.swift) -> Double   // treat any nil/absent as 0 in fusion confidence
StrengthLoadEngine (Wave 1): StrengthLoadResult { perMuscle, perRegion, recurrenceFlags: Set<MuscleRegion>; per-muscle elevation 0..1 }
LoadDistributionEngine (Wave 2): LoadDistributionResult { monotony: Double?; strain: Double?; gateState: {computed,fellBack}; fallbackLoadSignal: Double }
MuscleRegion: 7-case enum (legs, back, chest, shoulders, arms, core, fullBody).
NOTE: take upstream results as INPUTS to fuse(...) — the engine itself stays free of SwiftData fetches.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: StrainRiskZone enum + StrainRiskFactor / StrainRiskResult types (contracts first)</name>
  <files>WorkloadApp/Models/Enums.swift, WorkloadApp/Services/StrainRiskEngine.swift</files>
  <read_first>
    - WorkloadApp/Models/Enums.swift (FatigueZone / ACWRZone enum style: String, Codable, CaseIterable, Identifiable + displayName + systemImage)
  </read_first>
  <action>
Add `enum StrainRiskZone: String, Codable, CaseIterable, Identifiable` to
`WorkloadApp/Models/Enums.swift` with cases `.low, .moderate, .elevated, .high`, an `id`,
a `displayName` ("Low" / "Moderate" / "Elevated load-tolerance caution" / "High
overreaching caution"), and a `systemImage`. Copy audit: NO displayName, doc comment, or
factor string may contain the phrases "injury prediction", "predicts injury", "injury
risk", or "will get injured". Framing is strictly "load-tolerance context / overreaching
caution" (D-27-01). This is a shared-type change to `Enums.swift` so the FULL suite runs in
Task 4 (switch-exhaustiveness ripple guard).

Create `WorkloadApp/Services/StrainRiskEngine.swift` shell with the result contracts only:
`struct StrainRiskFactor { let label: String; let contribution: Double }`,
`struct StrainRiskResult { let score: Double; let zone: StrainRiskZone; let factors:
[StrainRiskFactor]; let confidence: Double }`, the `StrainRiskEngine` struct, and an
`enum Weights` / `enum Constants` block defining the fixed sign-constrained weights (define
them; leave `fuse` to Task 2). Document each weight's sign + evidence anchor in a triple-
slash comment. Add `StrainRiskEngine.swift` to the APP target in `project.pbxproj` (sole
pbxproj writer in Wave 3).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <done>StrainRiskZone + result/factor contracts compile; no prohibited prediction copy present; file in app target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixed glass-box fusion + zone mapping + ranked factors + confidence</name>
  <files>WorkloadApp/Services/StrainRiskEngine.swift, WorkloadAppTests/StrainRiskEngineTests.swift</files>
  <read_first>
    - WorkloadApp/Services/FatigueIndexEngine.swift (lines 169-238 — the present-component weighted-sum + redistribution pattern to mirror)
  </read_first>
  <behavior>
    - fuse(input) -> StrainRiskResult: each component clamped 0..1, multiplied by its fixed named weight, summed, renormalized over PRESENT components when some are nil (redistribution like FatigueIndexEngine, no mean-impute).
    - Components (each its own named factor): strengthLoadElevation (highest weight, competitive §3.1); enduranceLoadElevation (passed-in 0..1 from BaselineEngine z, or nil); fatigueIndex (FatigueIndexEngine.compute(input:).index / 100); monotonyStrain (only if gateState==.computed → normalized monotony; else fallbackLoadSignal at reduced weight, factor labeled "load distribution: limited data"); softTissue (a SINGLE source — either FatigueResult.softTissueRisk OR a NiggleInjuryDeriver-derived 1-exp(-0.5n) recency term, not both) + same-region recurrence bonus from StrengthLoadEngine.recurrenceFlags; restDebt taken from FatigueIndexEngine ONLY (D-27-05).
    - zone mapping: score thresholds (named constants) -> StrainRiskZone.
    - confidence: composite of BaselineEngine confidence (nil -> 0) + monotony gateState + hard-set unscored-coverage ratio, 0..1.
    - factors ranked by contribution desc, top-N, human-readable.
    - Deterministic; sign-constrained: every component contributes non-negatively toward risk (raising any single risk input never lowers the score).
  </behavior>
  <action>
Implement `StrainRiskEngine.fuse(...)`. Take ONE input struct carrying the upstream
results: `StrengthLoadResult`, `LoadDistributionResult`, a precomputed `FatigueResult` (or
`FatigueInput`), the soft-tissue count/recency, and the BaselineEngine-derived
endurance-load elevation (0..1, optional) + its confidence. Compute the weighted sum with
FIXED named weights; when a component is nil, redistribute its weight proportionally over
present components — mirror the `FatigueIndexEngine.compute(input:)` present-component
pattern (lines 169-238); do NOT mean-impute. When `LoadDistributionResult.gateState ==
.fellBack`, substitute `fallbackLoadSignal` at a reduced weight and emit factor "load
distribution: limited data — using fallback". Map the final 0..1 score to `StrainRiskZone`
via named thresholds. Build the ranked `factors` array (top-N by contribution). Compute
`confidence` from BaselineEngine confidence (nil -> 0) + monotony gate state + hard-set
unscored-coverage ratio.

REUSE `FatigueIndexEngine.compute`, `NiggleInjuryDeriver`, and `BaselineEngine` directly —
do NOT reimplement soft-tissue or baseline math (D-27-08). Choose a SINGLE soft-tissue
source to avoid double counting (note the choice in the SUMMARY).

Tests (`final class StrainRiskEngineTests: XCTestCase`, `@testable import
workload_management`, dates from a fixed anchor): worked-example fusion with all components
present → known score + zone; component-missing → weights redistribute + score still 0..1 +
missing factor omitted; monotony .fellBack → fallback factor present + reduced weight; sign
constraint (raising any single risk component never lowers the score); zone-threshold
boundaries; confidence rises with more data and is 0 when baseline confidence is nil;
determinism (same input twice → identical); a string-audit test asserting NO factor label /
zone displayName contains "injury prediction"/"injury risk"/"predicts injury"/"will get
injured".
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/StrainRiskEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>Fusion + redistribution + fallback + sign-constraint + determinism + no-prediction-copy tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Isolation guard — Strain-Risk does NOT touch the live recommendation/recovery path</name>
  <files>WorkloadAppTests/StrainRiskEngineTests.swift, .planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md</files>
  <action>
Prove the phase-specific invariant: `StrainRiskEngine` output is NOT consumed by the live
recommendation or recovery path. Grep-confirm (and record commands + output in notes) that
`AutoregulationEngine.swift` and `RecoveryScoreEngine.swift` contain NO reference to
`StrainRiskEngine` / `StrainRiskResult` / `StrainRiskZone`, and that no master/activation
flag was added or flipped. The engine is standalone substrate this phase (display/shadow
context only, D-27-06).

Add a guard test documenting intent: call `AutoregulationEngine` / `RecoveryScoreEngine`
exactly as today and confirm their outputs are produced with no Strain-Risk input
(independence). Record the grep results in `artifacts/27-03-notes.md`.
  </action>
  <verify>
    <automated>cd /Users/hanwen/Desktop/Tonus && grep -E "StrainRisk" "WorkloadApp/Services/AutoregulationEngine.swift" "WorkloadApp/Services/RecoveryScoreEngine.swift" && echo "ISOLATION VIOLATION" && exit 1 || echo "ISOLATION OK"</automated>
  </verify>
  <done>No live engine references StrainRisk*; isolation grep finds nothing; notes recorded.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite regression + tier-fence guard (shared-enum change)</name>
  <files>.planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-03-notes.md</files>
  <action>
Because `Enums.swift` (shared type) changed, run the FULL `WorkloadAppTests` suite per the
shared-type regression gotcha. Confirm `BaselineTierFenceTests` GREEN + unchanged (live
recovery score byte-unchanged) and the whole suite is green (catch any
switch-exhaustiveness ripple from adding `StrainRiskZone`). Discard `.xcstrings`
build-churn. Record final fixed weights + zone thresholds + full-suite pass count in
`artifacts/27-03-notes.md`, and confirm no activation/master flag exists or was flipped.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests 2>&1 | tail -25</automated>
  </verify>
  <done>Full suite green incl. BaselineTierFenceTests; no live default flipped; weights/thresholds recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strain-Risk engine ↔ live decision path | Output must NOT cross into AutoregulationEngine / RecoveryScoreEngine this phase |
| heuristic flag ↔ user copy | Framing must never claim injury prediction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering | Strain-Risk altering live recommendation/recovery score | mitigate | Task 3 isolation grep finds nothing; Task 4 tier-fence + full suite green; no master flag |
| T-27-08 | Repudiation | "injury prediction" liability copy | mitigate | String-audit test asserts no prediction language in any factor/zone copy |
| T-27-09 | Tampering | Fitted/per-user weights sneaking in (codex-forbidden) | mitigate | Weights are named compile-time constants; no fit/optimize code; reviewed in notes |
| T-27-10 | Spoofing | Mean-imputed missing components inflating score | mitigate | Redistribution-not-imputation tested; missingness reduces confidence |
| T-27-SC | Tampering | package installs | accept | None (Foundation-only Swift) |
</threat_model>

<verification>
- `StrainRiskEngineTests` green (fusion + redistribution + fallback + sign-constraint + determinism + no-prediction-copy + isolation).
- Isolation grep: `AutoregulationEngine`/`RecoveryScoreEngine` reference StrainRisk* finds nothing.
- Full `WorkloadAppTests` suite green; `BaselineTierFenceTests` unchanged + green.
- Weights are fixed named constants; no fitting/logistic code; no master/activation flag.
</verification>

<success_criteria>
A pure deterministic `StrainRiskEngine` produces an honest heuristic Strain-Risk flag
(score + zone + ranked factors + confidence) from a fixed sign-constrained glass-box
fusion of the strength-load, endurance-load, FatigueIndex, gated monotony/strain, and
soft-tissue components — provably isolated from the live recommendation/recovery path,
with no injury-prediction copy and the live recovery score byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-strength-load-strain-risk-fusion/27-03-SUMMARY.md` when done.
</output>
