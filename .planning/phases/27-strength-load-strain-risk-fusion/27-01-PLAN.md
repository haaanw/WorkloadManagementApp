---
phase: 27-strength-load-strain-risk-fusion
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [P27-STRENGTH-LOAD]
files_modified:
  - WorkloadApp/Services/StrengthLoadEngine.swift
  - WorkloadApp/Models/StrengthLoadState.swift
  - WorkloadApp/App/WorkloadApp.swift
  - WorkloadAppTests/StrengthLoadEngineTests.swift
  - workload management/workload management.xcodeproj/project.pbxproj
  - .planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-01-notes.md
must_haves:
  truths:
    - "Per-muscle hard sets are counted from non-warmup SetRecords by relative-intensity OR RIR, never raw tonnage"
    - "est-1RM reuses the existing SetRecord.estimated1RM (Epley) computed property — Epley is NOT reimplemented"
    - "Relative intensity = weightKg / per-(muscleGroup,exerciseName) rolling-best est-1RM reference, bucketed light/moderate/heavy/maximal"
    - "Per-muscle acute-vs-chronic strength-load elevation is clamped 0-1 with a deadband (FatigueIndex loadElevation philosophy, NOT ACWR)"
    - "Same-region recurrence flag is produced over the 7-case MuscleRegion (generalized cascade, no tendon-specific claim)"
    - "Unscored sets (no weightKg AND no rpe AND no rir) land in a separate unscored bucket, never counted as hard or easy"
    - "StrengthLoadEngine is a pure deterministic struct with static methods (Foundation-only, no Date.now/Calendar.current — dates passed in)"
    - "If StrengthLoadState model is added it is local-only and never synced (no Codable, absent from SyncService)"
  artifacts:
    - path: WorkloadApp/Services/StrengthLoadEngine.swift
      provides: "Pure struct: rel-intensity buckets (via SetRecord.estimated1RM), RIR bridge, per-muscle hard sets + elevation + recurrence"
    - path: WorkloadAppTests/StrengthLoadEngineTests.swift
      provides: "Numerics + determinism + edge-case unit tests"
  key_links:
    - from: WorkloadApp/Services/StrengthLoadEngine.swift
      to: WorkloadApp/Models/SetRecord.swift
      via: "Reads SetRecord.estimated1RM / weightKg / reps / rpe / rir / isWarmup"
      pattern: "estimated1RM|weightKg|isWarmup"
    - from: WorkloadApp/Services/StrengthLoadEngine.swift
      to: WorkloadApp/Models/Enums.swift
      via: "MuscleGroup.region rollup to MuscleRegion"
      pattern: "MuscleGroup|MuscleRegion|\\.region"
---

<objective>
Build the pure `StrengthLoadEngine` (static methods): relative-intensity buckets (using the
EXISTING `SetRecord.estimated1RM` Epley property — do NOT reimplement Epley), an RPE→RIR
bridge that also honors the logged `SetRecord.rir`, per-muscle HARD-SET counting, per-muscle
acute-vs-chronic strength-load elevation, and a same-region recurrence flag over
`MuscleRegion` — all derived from relative intensity, NEVER raw tonnage. Optionally persist
the per-(muscleGroup, exerciseName) rolling-best est-1RM reference in a new local-only
`StrengthLoadState` model (D-27-02). No fusion in this wave.

Purpose: this is the strength-load substrate — the structural moat input (competitive
§3.1, CHANGE 1). Wave 3's `StrainRiskEngine` consumes it.

Output: `StrengthLoadEngine.swift` + tests (+ optional `StrengthLoadState.swift`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-strength-load-strain-risk-fusion/27-RESEARCH.md
@CLAUDE.md

Models this engine reads (field names verified — use exactly these):
@WorkloadApp/Models/SetRecord.swift
@WorkloadApp/Models/ExerciseEntry.swift
@WorkloadApp/Models/WorkoutSession.swift
@WorkloadApp/Models/Enums.swift

Reference — existing pure engine + named-constant style (clamp helper, MARK sections, redistribution):
@WorkloadApp/Services/FatigueIndexEngine.swift

Reference — local-only never-synced model pattern (mirror this EXACTLY if adding StrengthLoadState):
@WorkloadApp/Models/SorenessLog.swift
@WorkloadApp/Models/BaselineState.swift
</context>

<interfaces>
<!-- Verified from source 2026-05-30. Use these EXACT names; do not re-explore. -->

SetRecord (@Model): { id: UUID; setNumber: Int; reps: Int?; weightKg: Double?; distanceMeters: Double?;
  durationSeconds: Int?; rpe: Double?; rir: Int?; isWarmup: Bool; completed: Bool; entry: ExerciseEntry?;
  var estimated1RM: Double? (Epley: weightKg*(1+reps/30), returns weightKg when reps==1, nil when no weight/reps) }
  --> REUSE SetRecord.estimated1RM. Do NOT write your own Epley.

ExerciseEntry (@Model): { id; exerciseName: String; orderIndex: Int; muscleGroup: MuscleGroup?;
  entryType: ExerciseLogType; sets: [SetRecord]; session: WorkoutSession? }

WorkoutSession (@Model): { id; sessionDate: Date; sportType: SportType; sessionType: SessionType;
  durationSeconds: Int; sessionRPE: Double?; exerciseEntries: [ExerciseEntry]; sortedEntries: [ExerciseEntry];
  athlete: Athlete? }

MuscleGroup: String enum, ~33 cases (Phase 22), `var region: MuscleRegion`.
MuscleRegion: String enum, 7 cases: legs, back, chest, shoulders, arms, core, fullBody. (NOTE: 7, includes fullBody + the engine must handle all 7.)

Local-only @Model contract (SorenessLog/BaselineState): @Attribute(.unique) var id; bare `var athlete: Athlete?`
inverse; var updatedAt: Date; NO Codable; NO *Row DTO; name ABSENT from SyncService.swift; registered in
Schema([...]) in WorkloadApp/App/WorkloadApp.swift.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relative-intensity buckets, RIR bridge, hard-set classification (pure primitives, reuse SetRecord.estimated1RM)</name>
  <files>WorkloadApp/Services/StrengthLoadEngine.swift, WorkloadAppTests/StrengthLoadEngineTests.swift</files>
  <read_first>
    - WorkloadApp/Models/SetRecord.swift (full — confirm estimated1RM, rir, rpe, weightKg, reps, isWarmup)
    - WorkloadApp/Services/FatigueIndexEngine.swift (lines 345-350 clamp helper + named-constant/MARK style to mirror)
  </read_first>
  <behavior>
    - estRIR(set): use set.rir directly when present; else `max(0, 10 - rpe)` when set.rpe present; else nil.
    - relativeIntensity(set, e1RMReference): `set.weightKg / ref` when set.weightKg != nil && ref > 0; else nil.
    - intensityBucket(relIntensity): light(<0.65), moderate(0.65..<0.80), heavy(0.80..<0.90), maximal(>=0.90); named constants.
    - classify(set, e1RMReference) -> SetClassification: .warmup if isWarmup; else if relIntensity != nil OR estRIR != nil -> evaluate hard/easy (hard when relIntensity >= hardSetIntensityThreshold OR estRIR <= hardSetRIRThreshold, carrying the bucket); else .unscored (no weight AND no rpe/rir).
    - All thresholds named constants in a nested `enum Constants`.
  </behavior>
  <action>
Create `WorkloadApp/Services/StrengthLoadEngine.swift` as a pure `struct` of `static`
methods, Foundation-only, deterministic, NO `Date.now`/`Calendar.current` (any date math
takes a passed-in `asOf: Date` + a passed-in `Calendar`, matching the pure-engine
convention). Mirror `FatigueIndexEngine`'s named-constant + `clamp` + `// MARK:` style.
Define nested types: `enum IntensityBucket { case light, moderate, heavy, maximal }` and
`enum SetClassification { case warmup, unscored, easy, hard(IntensityBucket) }`. Implement
`estRIR(_:)`, `relativeIntensity(set:e1RMReference:)`, `intensityBucket(_:)`, and
`classify(set:e1RMReference:)` per the behavior block (D-27-03). For est-1RM, REUSE
`SetRecord.estimated1RM` — do not write a second Epley. Named constants:
`hardSetIntensityThreshold`, `hardSetRIRThreshold`, bucket cut points, per-bucket strain
weights. NEVER compute or expose raw tonnage (no `weight*reps` sum anywhere).

Write `StrengthLoadEngineTests.swift` (`final class StrengthLoadEngineTests: XCTestCase`,
`@testable import workload_management`). Cover: RIR bridge (rir present wins; rpe 8 → RIR
2; rpe 10 → 0; none → nil); relativeIntensity nil when no weight / ref<=0; bucket
boundaries (0.64/0.65/0.79/0.80/0.89/0.90); hard via intensity, hard via RIR, warmup →
.warmup, unscored (no weight & no rpe/rir) → .unscored; determinism (same set twice →
equal classification). WorkloadAppTests is a synchronized group → no pbxproj edit for the
test file.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/StrengthLoadEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>Primitive tests pass; SetRecord.estimated1RM reused (no second Epley); no raw-tonnage sum; engine pure + deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-muscle hard-set aggregation, elevation, same-region recurrence + optional StrengthLoadState</name>
  <files>WorkloadApp/Services/StrengthLoadEngine.swift, WorkloadApp/Models/StrengthLoadState.swift, WorkloadApp/App/WorkloadApp.swift, WorkloadAppTests/StrengthLoadEngineTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - WorkloadApp/Services/FatigueIndexEngine.swift (lines 260-283 — computeLoadElevation deadband/clamp ratio philosophy to mirror, NOT ACWR)
    - WorkloadApp/Models/SorenessLog.swift + WorkloadApp/Models/BaselineState.swift (local-only @Model template, ONLY if adding the model)
    - WorkloadApp/App/WorkloadApp.swift (the Schema([...]) array — insert site, ONLY if adding the model)
  </read_first>
  <behavior>
    - e1RMReference per (muscleGroup, exerciseName): rolling best of SetRecord.estimated1RM across passed-in session history.
    - perMuscleStrengthLoad(sessions, asOf, calendar, acuteWindowDays): for each MuscleGroup, classify every set (Task 1 rule), count hard sets, sum per-bucket strain weights → strengthLoad; tally unscoredCount.
    - perMuscleElevation(acute, chronic): ratio-with-deadband, returns 0 inside deadband / when chronic<=0, clamps 0..1 (named deadband + scale constants).
    - sameRegionRecurrence(sorenessRegions: [MuscleRegion], elevatedRegions: Set<MuscleRegion>) -> Set<MuscleRegion>: regions present in BOTH (generalized cascade, D-27-05/§5.2). No tendon-specific output. Handles all 7 MuscleRegion cases.
  </behavior>
  <action>
Extend `StrengthLoadEngine` with per-muscle aggregation over a window of `WorkoutSession`
(named `acuteWindowDays`/`chronicWindowDays` constants; day math via the passed-in
`Calendar` + `asOf`, NOT `Calendar.current`). Produce `StrengthLoadResult`:
`perMuscle: [MuscleGroup: MuscleStrengthLoad]` (hardSetCount, strengthLoad, unscoredCount,
elevation), `perRegion: [MuscleRegion: Double]` rollup via `MuscleGroup.region`, and
`recurrenceFlags: Set<MuscleRegion>`. `perMuscleElevation` mirrors
`FatigueIndexEngine.computeLoadElevation` (acute/chronic ratio with deadband + clamp — NOT
ACWR). Keep the engine free of SwiftData fetches: take `sorenessRegions: [MuscleRegion]` as
a plain parameter (Wave 3 supplies it from Phase 25). The e1RM reference is a pure
recompute (rolling best `SetRecord.estimated1RM`) from the passed-in history FIRST.

Per D-27-02: add `StrengthLoadState` ONLY if the recompute proves it needs an unacceptable
full-history scan each run. If added, it MUST mirror `BaselineState`: local-only `@Model`,
NO Codable, bare `var athlete: Athlete?`, `var updatedAt: Date`, ABSENT from
`SyncService.swift`, and registered in the `Schema([...])` in
`WorkloadApp/App/WorkloadApp.swift` (additive → lightweight migration). Add the file to the
APP target in `project.pbxproj` (explicit PBXBuildFile + PBXFileReference + app
PBXSourcesBuildPhase — this plan is the sole pbxproj writer in Wave 1) and add a
persistence + sync-omission test (grep SyncService for "StrengthLoadState" → absent),
mirroring `BaselineStateModelTests`. If you SKIP the model, remove
`StrengthLoadState.swift` and the pbxproj edit from the SUMMARY's files and record the
skip + recompute rationale in `artifacts/27-01-notes.md`.

Extend tests: per-muscle hard-set counting across multiple exercises/sessions mapping to
the same MuscleGroup; e1RM reference = rolling best; elevation deadband + clamp +
chronic<=0 guard; same-region recurrence = intersection only (true when soreness AND
elevation coincide in a region; false otherwise; exercise all 7 regions incl fullBody);
unscored tally; determinism on a fixed fixture (dates from a fixed anchor, no `.now`).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/StrengthLoadEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>Per-muscle aggregation + elevation + recurrence tests pass; if StrengthLoadState added it is local-only/never-synced + schema-registered; if skipped, rationale recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression + tier-fence guard</name>
  <files>.planning/phases/27-strength-load-strain-risk-fusion/artifacts/27-01-notes.md</files>
  <action>
Run the FULL `WorkloadAppTests` suite (not just the new tests) per the shared-type
regression gotcha. Confirm `BaselineTierFenceTests` (live recovery-score machine lock) is
GREEN + unchanged. Discard any `.xcstrings` build-churn diffs before considering the wave
done. Record in `artifacts/27-01-notes.md`: whether `StrengthLoadState` was added or
skipped (+ rationale), the final hard-set/intensity/deadband constant values chosen, and
the full-suite pass count. Confirm no live engine (`RecoveryScoreEngine`,
`AutoregulationEngine`) was modified and no master/activation flag was touched.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests 2>&1 | tail -25</automated>
  </verify>
  <done>Full suite green including BaselineTierFenceTests; no live engine touched; notes recorded; .xcstrings churn discarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftData ↔ engine | Engine reads logged set/exercise data; must tolerate nil muscleGroup / nil rpe / nil rir / nil weightKg without crashing |
| new model ↔ SyncService | StrengthLoadState (if added) must never cross to Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information disclosure | StrengthLoadState (if added) leaking to Supabase | mitigate | Local-only @Model, no Codable, absent from SyncService; sync-omission grep test asserts it |
| T-27-02 | Tampering | Live recovery score / recommendation altered | mitigate | No live engine modified; BaselineTierFenceTests + full suite must stay green |
| T-27-03 | Denial of service | Malformed set data (nil/zero) crashing engine | mitigate | Guard nil rpe/rir / nil weightKg / ref<=0 / nil muscleGroup; unit tests cover edge cases |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs (Foundation-only Swift) |
</threat_model>

<verification>
- `StrengthLoadEngineTests` green (primitives + per-muscle + recurrence + determinism).
- Full `WorkloadAppTests` suite green; `BaselineTierFenceTests` unchanged + green.
- `SetRecord.estimated1RM` reused; no raw-tonnage sum anywhere in `StrengthLoadEngine`.
- If `StrengthLoadState` added: schema-registered, local-only, never-synced (test proves it).
- No live engine modified; no activation/master flag touched.
</verification>

<success_criteria>
Pure deterministic `StrengthLoadEngine` produces per-muscle hard-set counts (relative
intensity / RIR via existing `SetRecord.estimated1RM`, never tonnage), intensity buckets,
per-muscle elevation, and a same-region recurrence flag — fully unit-tested, with the live
recovery score provably byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-strength-load-strain-risk-fusion/27-01-SUMMARY.md` when done.
</output>
