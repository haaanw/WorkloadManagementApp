---
phase: 19-cycle-context-ui-guidance
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - WorkloadApp/Components/CycleFuelingCard.swift
  - WorkloadApp/Components/REDSAttentionBanner.swift
  - WorkloadApp/Views/Recovery/RecoveryView.swift
  - WorkloadApp/Views/Profile/ProfileView.swift
autonomous: true
requirements: [CYCLE-07, CYCLE-08]
must_haves:
  truths:
    - "SC3/D-08: a fueling/recovery card on the Recovery tab suggests avoiding fasted hard workouts and post-training protein within 45 min, shown only when the D-03 phase gate passes"
    - "D-08: luteal-only fueling adds hydration/cooling (heat sensitivity) and extra protein/carbs guidance"
    - "SC4/D-10: a RED-S attention banner appears when REDSRiskEngine.classify returns .monitor (3+ missed periods OR cycle length >35 consistently)"
    - "SC4/D-11: the RED-S banner is suppressed for pregnancy, lactation, OC, PCOS, and perimenopause"
    - "D-12: RED-S copy is non-diagnostic, clinician-referral only — never says RED-S / amenorrhea / diagnosis"
    - "D-11a: ProfileView Cycle & Hormones section gains PCOS and Perimenopausal toggles bound to the new Athlete fields"
    - "SC6/D-01: fueling card and RED-S banner are invisible when no MenstrualCycleSnapshot rows exist; SC5: no training-prescription wording"
    - "D-13: RED-S banner uses caution (not danger) styling with a text label and is dismissible without nagging daily"
  artifacts:
    - path: "WorkloadApp/Components/CycleFuelingCard.swift"
      provides: "Phase-bucket fueling/recovery suggestions card (Dr. Sims), flat InsightCard-style"
      contains: "cycle.fueling"
    - path: "WorkloadApp/Components/REDSAttentionBanner.swift"
      provides: "Non-diagnostic clinician-referral banner, caution-bordered, dismissible"
      contains: "cycle.reds"
    - path: "WorkloadApp/Views/Recovery/RecoveryView.swift"
      provides: "Gated rendering of fueling card + RED-S banner; REDSRiskEngine input derivation"
      contains: "REDSRiskEngine"
    - path: "WorkloadApp/Views/Profile/ProfileView.swift"
      provides: "PCOS + Perimenopausal toggles in Cycle & Hormones section"
      contains: "profile.row.pcos"
  key_links:
    - from: "RecoveryView"
      to: "REDSRiskEngine.classify"
      via: "derive CycleHistoryInput from @Query'd MenstrualCycleSnapshot history + Athlete exclusion flags"
      pattern: "REDSRiskEngine|CycleHistoryInput"
    - from: "ProfileView toggles"
      to: "Athlete.hasPCOS / Athlete.isPerimenopausal"
      via: "Binding get/set + saveAthlete (mirrors existing reproductive toggles)"
      pattern: "hasPCOS|isPerimenopausal"
---

<objective>
Deliver the two guidance surfaces: a cycle-aware fueling/recovery card (Dr. Sims) and a non-diagnostic RED-S attention banner, both on the Recovery tab, plus the PCOS / perimenopause exclusion toggles in Profile that the RED-S exclusion logic (Plan 01) requires. The fueling card is phase-bucket-specific and gated like the recovery-card context; the RED-S banner renders only when the pure `REDSRiskEngine` (Plan 01) returns `.monitor` and never for excluded users.

Purpose: Deliver CYCLE-07 (SC3 — fueling/recovery prompts) and CYCLE-08 (SC4 — RED-S monitoring with clinician-referral language and full exclusion handling). Framing stays readiness-first and non-alarmist: fueling is "Aim for…/Consider…", RED-S is "it may be worth speaking with a healthcare provider" — never a diagnosis, never a training override.

Output: New `CycleFuelingCard` + `REDSAttentionBanner` components; RecoveryView renders both (gated); ProfileView gains the two exclusion toggles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md
@CLAUDE.md
@.planning/phases/19-cycle-context-ui-guidance/19-01-PLAN.md

<interfaces>
<!-- Contracts from Plan 01 + the codebase — do not re-explore. -->

From Plan 01:
- `struct REDSRiskEngine` (pure) with `enum RiskState { case none, monitor }` and `static func classify(input: CycleHistoryInput) -> RiskState`.
- `struct CycleHistoryInput { recentCycleLengths: [Int]; medianCycleLength: Int?; daysSinceLastCycleStart: Int?; hasSnapshotData: Bool; isPregnant; isLactating; isOnHormonalContraceptive; hasPCOS; isPerimenopausal: Bool }`.
- `Athlete.hasPCOS: Bool?`, `Athlete.isPerimenopausal: Bool?` (additive, local-only).
- Localized keys: `cycle.fueling.title/fasted/protein/lutealHeat/lutealNutrition`, `cycle.reds.title/body/dismiss`, `profile.row.pcos`, `profile.row.perimenopausal`.

From WorkloadApp/Components/FatigueAttentionBanner.swift (EXACT template for REDSAttentionBanner):
- HStack { Rectangle().fill(borderColor).frame(width: 2) ; VStack { micro-caps label + secondary message } } .background(ColorTokens.surface) .overlay(Rectangle().stroke(ColorTokens.divider, lineWidth: 0.5)). No shadow, no rounded corner.

From WorkloadApp/Views/Recovery/InsightCard.swift — flat informational card template for the fueling card.

From WorkloadApp/Views/Recovery/RecoveryView.swift:
- body top (line 38) — insertion point for RED-S banner (above MorningCheckInPrompt/RecoveryScoreCard) and fueling card (after the score card region).
- `@Query cycleSnapshots` added in Plan 02 Task 3 — reuse it; `athlete = athletes.first`.

From WorkloadApp/Views/Profile/ProfileView.swift:
- "Cycle & Hormones" section lines 93-153 — three existing Toggle rows (hormonalContraceptive/pregnant/lactating) bound via `Binding(get:set:)` + `saveAthlete(athlete)`; `divider()` between rows; `showCycleSection` gate (lines 15-19). Mirror exactly for the two new toggles.

From WorkloadApp/Services/CycleTrackingService.swift (math to MIRROR in the viewmodel/view, NOT import into the engine):
- cycle lengths = day-diffs between consecutive `isCycleStart` snapshot dates; median of those lengths; daysSinceLastCycleStart = days from the most recent `isCycleStart` snapshot to now (Calendar in the view layer, not the engine).

Locked decisions: D-08/D-09 (fueling), D-10/D-11/D-12/D-13 (RED-S), D-11a (toggles), D-01 (visibility), D-03 (phase gate for fueling), SC5.
DESIGN.md: Rectangle only, no .shadow, Font.Tokens.*, ColorTokens only (caution = ColorTokens.zoneCaution), 8pt spacing, zone via text label + optional border, accent on hero score only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PCOS + Perimenopausal toggles in ProfileView</name>
  <files>WorkloadApp/Views/Profile/ProfileView.swift</files>
  <read_first>
    - WorkloadApp/Views/Profile/ProfileView.swift lines 15-19 (showCycleSection gate) and 93-153 (the three existing reproductive Toggle rows + divider() + saveAthlete pattern)
    - WorkloadApp/Models/Athlete.swift (hasPCOS / isPerimenopausal from Plan 01)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-11a)
  </read_first>
  <behavior>
    - The Cycle & Hormones section shows two new toggle rows: "PCOS" and "Perimenopausal", bound to Athlete.hasPCOS / Athlete.isPerimenopausal
    - Toggling persists via saveAthlete (same as existing flags)
    - showCycleSection still gates the section (extend it so the section also appears if either new flag is set, matching the existing reproductive-flag logic)
  </behavior>
  <action>
    Extend `showCycleSection` (lines 15-19) to also include `athlete?.hasPCOS != nil || athlete?.isPerimenopausal != nil`. After the existing "lactating" toggle row (line ~150) and before the section's closing `sectionDivider()`, add two Toggle rows mirroring the existing pattern exactly: label `Text("profile.row.pcos")` / `Text("profile.row.perimenopausal")` (Font.Tokens.body, ColorTokens.text1), `Toggle("", isOn: Binding(get: { athlete.hasPCOS ?? false }, set: { athlete.hasPCOS = $0; saveAthlete(athlete) }))` (and the perimenopausal analog), `.labelsHidden()`, `.padding(.horizontal,16).padding(.vertical,16)`, with a `divider()` between them. Keep styling identical to the existing rows.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ProfileView Cycle & Hormones section has PCOS + Perimenopausal toggle rows bound to Athlete.hasPCOS / isPerimenopausal with saveAthlete persistence
    - showCycleSection accounts for the two new flags
    - Labels use localized keys (profile.row.pcos / profile.row.perimenopausal); styling matches existing rows
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>Users can declare PCOS / perimenopause in Profile; these feed the RED-S exclusion gate.</done>
</task>

<task type="auto">
  <name>Task 2: CycleFuelingCard component (phase-bucket fueling/recovery)</name>
  <files>WorkloadApp/Components/CycleFuelingCard.swift</files>
  <read_first>
    - WorkloadApp/Views/Recovery/InsightCard.swift (flat card template)
    - WorkloadApp/Models/Enums.swift (CyclePhase + the luteal cases) and 19-01-PLAN keys
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-08, D-09, SC5)
  </read_first>
  <behavior>
    - Shows a titled card ("Fueling & recovery") with two always-on suggestions: avoid fasted hard sessions; protein within ~45 min
    - When the phase is luteal (earlyLuteal/lateLuteal): adds hydration/cooling + extra protein/carbs lines
    - Suggestions only, no prescription wording (no volume/intensity directives)
    - Flat ColorTokens.surface card, 0.5pt divider, Font.Tokens.*, 8pt spacing, no accent, no rounded corner, no shadow
  </behavior>
  <action>
    Create `CycleFuelingCard.swift`: `struct CycleFuelingCard: View` taking `let phase: CyclePhase`. Compute `isLuteal = (phase == .earlyLuteal || phase == .lateLuteal)`. Render a VStack(alignment:.leading, spacing: 8 or 16): a micro-caps or section-style title `Text("cycle.fueling.title")` (Font.Tokens.micro tracking 1.2 ColorTokens.text3, matching the recovery section-header style), then bullet-style rows for `cycle.fueling.fasted` and `cycle.fueling.protein` (Font.Tokens.label, ColorTokens.text2, `.fixedSize(horizontal:false, vertical:true)`), and when `isLuteal` two more rows `cycle.fueling.lutealHeat` + `cycle.fueling.lutealNutrition`. Use a small leading marker that respects the design (e.g. a 2x2pt `Rectangle().fill(ColorTokens.text3)` dot or a hanging indent — no SF Symbol bullet that implies status color). Container: `.padding(.horizontal,16).padding(.vertical,16).background(ColorTokens.surface).overlay(Rectangle().stroke(ColorTokens.divider, lineWidth: 0.5))`. No accent, no RoundedRectangle, no shadow.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CycleFuelingCard.swift exists, added to app target in project.pbxproj (`grep -c CycleFuelingCard` non-zero)
    - Renders fasted + protein lines always; luteal heat + nutrition lines only when phase is luteal
    - All strings are localized keys (cycle.fueling.*); no prescription wording
    - Only ColorTokens + Font.Tokens.*; no RoundedRectangle, no .shadow, no accent
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>A calm, suggestion-only fueling card exists, phase-bucket aware, fully design-compliant.</done>
</task>

<task type="auto">
  <name>Task 3: REDSAttentionBanner component (non-diagnostic, caution, dismissible)</name>
  <files>WorkloadApp/Components/REDSAttentionBanner.swift</files>
  <read_first>
    - WorkloadApp/Components/FatigueAttentionBanner.swift (EXACT structural template: colored 2pt left border + micro-caps label + secondary message + flat surface + 0.5pt overlay border)
    - 19-01-PLAN keys (cycle.reds.title/body/dismiss)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-12 copy, D-13 caution + dismissible)
  </read_first>
  <behavior>
    - Shows a caution-bordered banner: micro-caps title ("Cycle pattern change") + body (clinician-referral) + a dismiss control
    - Body copy is the non-diagnostic clinician-referral text; never says RED-S / amenorrhea / diagnosis
    - Dismiss invokes a closure (the caller persists dismissal); zone communicated via the text label, border is supplementary
    - Caution color = ColorTokens.zoneCaution; flat surface, no shadow, no rounded corner
  </behavior>
  <action>
    Create `REDSAttentionBanner.swift` by adapting FatigueAttentionBanner: `struct REDSAttentionBanner: View { let onDismiss: () -> Void }`. Layout: leading `Rectangle().fill(ColorTokens.zoneCaution).frame(width: 2)` (D-13 caution border), then a VStack(alignment:.leading, spacing: 8): an HStack with `Text("cycle.reds.title")` (Font.Tokens.micro, tracking 0.88, ColorTokens.zoneCaution — the text label carries the state, DESIGN rule 5) + Spacer() + a dismiss Button (`Image(systemName:"xmark")` Font.system(size:13) ColorTokens.text2, `.accessibilityLabel(Text("cycle.reds.dismiss.a11y"))`); below it `Text("cycle.reds.body")` (Font.Tokens.smallLabel, ColorTokens.text2, `.fixedSize(horizontal:false, vertical:true)`). Container `.background(ColorTokens.surface).overlay(Rectangle().stroke(ColorTokens.divider, lineWidth: 0.5))`. No accent, no RoundedRectangle, no shadow. The body string must be exactly the localized `cycle.reds.body` (verified non-diagnostic in Plan 01).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - REDSAttentionBanner.swift exists, added to app target in project.pbxproj (`grep -c REDSAttentionBanner` non-zero)
    - Uses ColorTokens.zoneCaution border + text label (state not color-only); flat surface; no rounded corner / shadow / accent
    - Strings are localized keys; no diagnostic terms in the component
    - Has a working dismiss closure + accessibility label
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>A non-alarmist, non-diagnostic, dismissible clinician-referral banner exists, design-compliant.</done>
</task>

<task type="auto">
  <name>Task 4: Render fueling card + RED-S banner in RecoveryView (gated, REDSRiskEngine wired)</name>
  <files>WorkloadApp/Views/Recovery/RecoveryView.swift</files>
  <read_first>
    - WorkloadApp/Views/Recovery/RecoveryView.swift (full file — body top line 38; cycleSnapshots @Query + latestCycleSnapshot from Plan 02 Task 3; athletes.first)
    - WorkloadApp/Components/CycleFuelingCard.swift + REDSAttentionBanner.swift (Tasks 2-3)
    - WorkloadApp/Services/REDSRiskEngine.swift + CycleHistoryInput (Plan 01)
    - WorkloadApp/Services/CycleTrackingService.swift lines 131-177 + 337-344 (cycle-start detection / length / median shape to MIRROR in the view, using snapshot.isCycleStart dates)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-01, D-03, D-08, D-10..D-13)
  </read_first>
  <behavior>
    - Fueling card renders below the RecoveryScoreCard only when cycle data exists AND the D-03 phase gate passes; luteal vs follicular content driven by the latest snapshot phase
    - RED-S banner renders at the top of the Recovery tab only when REDSRiskEngine.classify(...) == .monitor, using exclusion flags from the athlete + snapshots; dismissal persists via @AppStorage keyed to a coarse period (month) so it does not nag daily but can re-surface
    - When cycleSnapshots empty: neither fueling card nor RED-S banner renders (SC6)
    - Existing RecoveryView content (check-in prompt, charts, insights) unchanged
  </behavior>
  <action>
    Derive a `redsRiskState` computed property in RecoveryView: from athlete-scoped `cycleSnapshots`, build the `CycleHistoryInput` — collect dates where `isCycleStart == true` (sorted), compute consecutive-day-diff cycle lengths and their median (mirror CycleTrackingService.computeCycleLengths/median in the view layer, NOT in the engine), compute `daysSinceLastCycleStart` via `Calendar.current.dateComponents([.day], from: lastStart, to: .now)`, set `hasSnapshotData = !cycleSnapshots.isEmpty`, and pull `isPregnant/isLactating/isOnHormonalContraceptive/hasPCOS/isPerimenopausal` from the athlete (`?? false`). Call `REDSRiskEngine.classify(input:)`. Add `@AppStorage("redsBannerDismissedPeriod") private var redsDismissedPeriod: String = ""` and a computed `currentPeriodKey` (e.g. "yyyy-MM"); show the banner only when `redsRiskState == .monitor && redsDismissedPeriod != currentPeriodKey`; the banner's onDismiss sets `redsDismissedPeriod = currentPeriodKey`. Place `if showREDSBanner { REDSAttentionBanner(onDismiss: ...) ; divider }` at the very top of the VStack (line 38, above MorningCheckInPrompt). Place the fueling card after the RecoveryScoreCard region: `if let snap = latestCycleSnapshot, fuelingGatePasses(snap) { CycleFuelingCard(phase: snap.estimatedPhase ?? .unknown) ; divider }` where `fuelingGatePasses` reuses the D-03 gate (confidence >= 0.7 && phase != .unknown && !exclusion). Do NOT modify charts, insights, or the score card internals beyond what Plan 02 added.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - RecoveryView derives CycleHistoryInput from snapshots + athlete flags and calls REDSRiskEngine.classify
    - RED-S banner shows only when .monitor and not dismissed for the current month; dismissal persists and can re-surface next period
    - Fueling card shows only when cycle data exists and the D-03 gate passes; luteal content only in luteal phase
    - With zero snapshots, neither renders (SC6); existing RecoveryView content unchanged
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>The Recovery tab surfaces fueling guidance and a non-diagnostic RED-S notice exactly when warranted, fully gated and dismissible, with the pure engine doing the classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenstrualCycleSnapshot + Athlete flags -> REDSRiskEngine | View derives plain Ints/Bools and passes them to the pure engine; no raw records or HealthKit access in the engine. |
| Device -> Supabase sync | New PCOS/perimenopause flags are local-only (Phase 18 CR-01); no sync code touched; no cycle data uploaded. |
| Engine/UI -> user | RED-S surfaces only a non-diagnostic clinician-referral message (D-12); no diagnosis, no risk score shown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Information Disclosure | New PCOS/perimenopause flags | mitigate | Local-only Athlete fields (Plan 01); ProfileView only reads/writes locally via saveAthlete; not in any sync payload. Verified by Plan 01 grep gate. |
| T-19-09 | Safety / Misinformation | RED-S banner copy + gating | mitigate | Copy is non-diagnostic clinician-referral (D-12); engine excludes pregnancy/lactation/OC/PCOS/perimenopause (D-11) to avoid false alarms; conservative thresholds (D-14). |
| T-19-10 | Tampering | New component files registration | mitigate | CycleFuelingCard.swift + REDSAttentionBanner.swift added to app target in project.pbxproj; build gate confirms. No package installs. |
| T-19-11 | Annoyance / Alert fatigue | RED-S banner frequency | mitigate | Dismissal persisted per-month via @AppStorage; re-surfaces only if the pattern persists into a new period — non-nagging (D-13). |
</threat_model>

<verification>
- `xcodebuild build` exits 0.
- DESIGN.md compliance: `grep -n "RoundedRectangle\|\.shadow(\|ColorTokens.accent" WorkloadApp/Components/CycleFuelingCard.swift WorkloadApp/Components/REDSAttentionBanner.swift` returns nothing.
- RED-S copy is non-diagnostic: the component references only `cycle.reds.*` keys; Plan 01 acceptance already grepped the catalog for forbidden terms.
- With zero MenstrualCycleSnapshot rows, neither the fueling card nor the RED-S banner renders (source inspection: guarded by `latestCycleSnapshot`/`hasSnapshotData`).
- ProfileView toggles bind to the local-only Athlete flags; `grep -n "hasPCOS\|isPerimenopausal" WorkloadApp/Services/SyncService.swift` still returns nothing.
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- SC3: fueling/recovery prompts (fasted-avoidance, 45-min protein, luteal hydration/cooling) on the Recovery tab (D-08).
- SC4: RED-S monitoring fires on 3+ missed periods OR consistent >35-day cycles, with clinician-referral language and full exclusion handling (D-10, D-11, D-12).
- SC5: no training-prescription wording in any prompt.
- SC6: fueling card + RED-S banner invisible without HealthKit menstrual permission (D-01).
- DESIGN.md respected: 0pt corners, no shadow, caution-color-with-text-label, Font.Tokens.*, ColorTokens only, accent on hero score only.
</success_criteria>

<output>
Create `.planning/phases/19-cycle-context-ui-guidance/19-03-SUMMARY.md` when done.
</output>
