---
phase: 19-cycle-context-ui-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/Athlete.swift
  - WorkloadApp/Models/Enums.swift
  - WorkloadApp/Services/REDSRiskEngine.swift
  - WorkloadAppTests/REDSRiskEngineTests.swift
  - WorkloadApp/Resources/Localizable.xcstrings
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CYCLE-06, CYCLE-07, CYCLE-08]
must_haves:
  truths:
    - "A new pure struct REDSRiskEngine classifies cycle history into .none or .monitor using static methods only (no HealthKit / SwiftData imports)"
    - "D-10 long-cycle rule: the 3 most recent cycle lengths all > 35 days returns .monitor"
    - "D-10 missed-period rule: days since the last cycle start >= 3x median cycle length (with a >=90 day floor) returns .monitor"
    - "D-11 exclusions: isPregnant OR isLactating OR isOnHormonalContraceptive OR hasPCOS OR isPerimenopausal returns .none regardless of cycle pattern"
    - "Regular ~28-day cycles return .none"
    - "D-11a: Athlete gains additive local-only optional Bool fields hasPCOS and isPerimenopausal (default nil), NOT added to any Supabase sync payload"
    - "D-05: CyclePhase exposes phase-context copy keys for the follicular and luteal buckets (readiness-first, never prescriptive)"
    - "D-15: all new user-facing strings exist as keys in Localizable.xcstrings with English + zh-Hans values"
  artifacts:
    - path: "WorkloadApp/Services/REDSRiskEngine.swift"
      provides: "Pure RED-S risk classifier (struct + static classify) with RiskState enum and CycleHistoryInput"
      contains: "REDSRiskEngine"
    - path: "WorkloadAppTests/REDSRiskEngineTests.swift"
      provides: "Unit tests for long-cycle rule, missed-period rule, all exclusions, and the regular-cycle negative case"
      contains: "monitor"
    - path: "WorkloadApp/Models/Athlete.swift"
      provides: "Additive local-only hasPCOS / isPerimenopausal fields"
      contains: "isPerimenopausal"
    - path: "WorkloadApp/Models/Enums.swift"
      provides: "CyclePhase context-copy helper (bucket -> localized readiness-first string key)"
      contains: "contextCopyKey"
  key_links:
    - from: "REDSRiskEngine.classify"
      to: "CycleHistoryInput (exclusion flags + cycle lengths + last start)"
      via: "exclusion gate evaluated first, then long-cycle / missed-period rules"
      pattern: "hasExclusion|RiskState"
---

<objective>
Build the non-UI foundation for Phase 19: the pure `REDSRiskEngine` safety classifier, the additive `Athlete` exclusion fields (PCOS / perimenopause) that ROADMAP SC4 requires, the `CyclePhase` readiness-first context-copy helper, and ALL new localized strings (EN + zh-Hans). No views are touched in this plan — Plans 02 and 03 consume these contracts.

Purpose: Isolate the only new logic (RED-S classification) and shared vocabulary (copy keys, model fields) so the UI plans are pure rendering against stable, unit-tested contracts. RED-S is safety-adjacent, so its rules and exclusions must be deterministic and tested in isolation, with no HealthKit/SwiftData dependency (the pure-engine convention). This plan also closes the requirements-traceability gap for the v1.4 CYCLE-* IDs.

Output: New `REDSRiskEngine.swift` + tests; additive `Athlete` fields; a `CyclePhase` context-copy helper; new keys in `Localizable.xcstrings`; CYCLE-01..08 added to `.planning/REQUIREMENTS.md` traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from the codebase — do not re-explore. -->

From WorkloadApp/Models/MenstrualCycleSnapshot.swift:
- `@Model final class MenstrualCycleSnapshot` — date, cycleDay: Int?, estimatedPhase: CyclePhase?, confidence: Double, cycleLength: Int?, isCycleStart: Bool, isOnHormonalContraceptive/isPregnant/isLactating: Bool. Local-only (D-12).
- `struct CycleContext { phase, confidence, cycleDay, cycleLength, isOnHormonalContraceptive, isPregnant, isLactating; var hasExclusion; static let none }`. `hasExclusion = OC || pregnant || lactating`.

From WorkloadApp/Models/Enums.swift (line 380):
- `enum CyclePhase: String, Codable, CaseIterable, Identifiable` — earlyFollicular, lateFollicular, ovulatory, earlyLuteal, lateLuteal, unknown. Has `var displayName` using `String(localized:defaultValue:)`.

From WorkloadApp/Models/Athlete.swift (lines 21-23):
- `var isOnHormonalContraceptive: Bool?`, `var isPregnant: Bool?`, `var isLactating: Bool?` — additive optional flags, local-only (Phase 18 CR-01 removed reproductive flags from sync).

From WorkloadApp/Services/CycleTrackingService.swift (math to MIRROR, not import — engine stays pure):
- cycle-length computation = day-diffs between consecutive cycle-start dates; `median([Int]) -> Int?`. RED-S reuses the same shape but receives plain arrays.

Locked decisions (19-CONTEXT.md): D-05 (copy), D-10 (RED-S rules), D-11/D-11a (exclusions + new fields), D-12 (non-diagnostic copy), D-14 (pure engine, display-state only), D-15 (localization).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive Athlete exclusion fields + CyclePhase context-copy helper</name>
  <files>WorkloadApp/Models/Athlete.swift, WorkloadApp/Models/Enums.swift</files>
  <read_first>
    - WorkloadApp/Models/Athlete.swift (lines 1-60 — the existing isOnHormonalContraceptive/isPregnant/isLactating fields + the memberwise/SwiftData init; mirror their additive-optional shape)
    - WorkloadApp/Models/Enums.swift lines 380-401 (CyclePhase + its localized displayName pattern)
    - WorkloadApp/Services/SyncService.swift (CONFIRM AthleteRow / pushAthlete / pullAthlete do NOT reference reproductive flags after Phase 18 CR-01 — the new fields must follow the same local-only treatment: NOT added here)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-05, D-11a)
  </read_first>
  <behavior>
    - Athlete has `var hasPCOS: Bool?` and `var isPerimenopausal: Bool?`, both default nil, never required
    - Neither new field appears in AthleteRow, pushAthlete, or pullAthlete (local-only, per Phase 18 CR-01)
    - CyclePhase gains a helper mapping each case to a 2-bucket readiness-first context-copy localized string: follicular cases -> follicular copy key; luteal cases -> luteal copy key; .unknown -> nil
    - The follicular/luteal copy is explanatory (cycle explains the score) and contains no training prescription words (no "deload"/"rest"/"go hard")
  </behavior>
  <action>
    In `Athlete`, add two additive optional stored properties `var hasPCOS: Bool? = nil` and `var isPerimenopausal: Bool? = nil` adjacent to the existing reproductive flags (lines 21-23), and wire them into the init with default nil (mirror the existing three flags exactly). Do NOT add them to SyncService / AthleteRow / push / pull — they are local-only like the existing trio (verify via the read_first grep). In `Enums.swift`, extend `CyclePhase` with a computed helper `var contextCopyKey: String.LocalizationValue?` (or `String?` returning a key) that returns the follicular context key for earlyFollicular/lateFollicular/ovulatory, the luteal context key for earlyLuteal/lateLuteal, and nil for .unknown — mirroring the `displayName` localized pattern. Use the key strings defined in Task 3 (e.g. `cyclePhase.context.follicular` / `cyclePhase.context.luteal`). Keep the helper string-key-returning so the views localize at render time.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Athlete declares `hasPCOS: Bool?` and `isPerimenopausal: Bool?` defaulting nil; init updated
    - `grep -n "hasPCOS\|isPerimenopausal" WorkloadApp/Services/SyncService.swift` returns nothing (not synced)
    - CyclePhase has a context-copy helper returning follicular key, luteal key, or nil(.unknown)
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>Athlete carries two additive local-only exclusion flags; CyclePhase exposes a readiness-first 2-bucket context-copy key. Build passes; nothing new synced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REDSRiskEngine pure classifier + unit tests</name>
  <files>WorkloadApp/Services/REDSRiskEngine.swift, WorkloadAppTests/REDSRiskEngineTests.swift</files>
  <read_first>
    - WorkloadApp/Services/RecoveryScoreEngine.swift (pure-struct / static-method / `// MARK:` convention to replicate; Foundation-only import)
    - WorkloadApp/Services/CycleTrackingService.swift lines 164-201 + 337-344 (computeCycleLengths + median shape to mirror as plain-array helpers; do NOT import the service)
    - WorkloadAppTests/RecoveryScoreEngineTests.swift (XCTest style: `@testable import workload_management`, `func test_...`, XCTAssert patterns)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-10 rules, D-11 exclusions, D-12 non-diagnostic, D-14 display-state-only, validation cases in <specifics>)
  </read_first>
  <behavior>
    - classify returns `.monitor` when the 3 most recent cycle lengths are all > 35 (e.g. [38,41,37])
    - classify returns `.monitor` when daysSinceLastCycleStart >= max(3 * medianCycleLength, 90) AND snapshot data is otherwise present
    - classify returns `.none` for regular cycles (e.g. [27,28,29], recent start within range)
    - classify returns `.none` whenever any exclusion is true: isPregnant, isLactating, isOnHormonalContraceptive, hasPCOS, isPerimenopausal — exclusions evaluated FIRST, short-circuiting
    - classify returns `.none` when there is insufficient cycle history (fewer than 3 computed lengths AND no usable last-start signal) — never a false positive on sparse data
    - The engine exposes NO user-facing string and NO numeric risk score (display state only, per D-14); copy lives in the views via localized keys
  </behavior>
  <action>
    Create `REDSRiskEngine.swift` as a pure `struct REDSRiskEngine` (import Foundation only) following the RecoveryScoreEngine convention. Define `enum RiskState { case none, monitor }` and an input value type `struct CycleHistoryInput { let recentCycleLengths: [Int]; let medianCycleLength: Int?; let daysSinceLastCycleStart: Int?; let hasSnapshotData: Bool; let isPregnant: Bool; let isLactating: Bool; let isOnHormonalContraceptive: Bool; let hasPCOS: Bool; let isPerimenopausal: Bool }`. Implement `static func classify(input: CycleHistoryInput) -> RiskState`:
    1. Exclusion gate FIRST (D-11): if `isPregnant || isLactating || isOnHormonalContraceptive || hasPCOS || isPerimenopausal` -> `.none`.
    2. Guard `input.hasSnapshotData` else `.none` (no data -> no alert; D-14 conservative).
    3. Long-cycle rule (D-10): take the most recent 3 of `recentCycleLengths`; if there are exactly >=3 and ALL > 35 -> `.monitor`.
    4. Missed-period rule (D-10): if `daysSinceLastCycleStart`, `medianCycleLength` both present and `daysSinceLastCycleStart >= max(3 * medianCycleLength, 90)` -> `.monitor`.
    5. Otherwise `.none`.
    Keep it deterministic, no I/O, no Calendar/Date inside the engine (callers pass precomputed Ints). Add `REDSRiskEngineTests.swift` covering every behavior bullet plus the <specifics> validation cases, including each exclusion individually short-circuiting a would-be-monitor pattern.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:WorkloadAppTests/REDSRiskEngineTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - REDSRiskEngine.swift exists as a pure struct (no `import HealthKit`, no `import SwiftData`)
    - The new file is added to the app + test targets in project.pbxproj (`grep -c REDSRiskEngine "workload management/workload management.xcodeproj/project.pbxproj"` non-zero)
    - `classify` returns .monitor for [38,41,37] and for the >=3x-median missed-period case; .none for regular cycles
    - Each of the 5 exclusion flags individually forces .none even with a monitor-pattern history
    - Sparse-history inputs return .none (no false positive)
    - xcodebuild test exits 0
  </acceptance_criteria>
  <done>A pure, fully unit-tested RED-S classifier exists, exclusions short-circuit first, no false positives on sparse data, no user-facing copy or score in the engine.</done>
</task>

<task type="auto">
  <name>Task 3: New localized strings (EN + zh-Hans) + requirements traceability</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings, .planning/REQUIREMENTS.md</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings (existing structure: per-key `localizations` with `en` + `zh-Hans` `stringUnit` value; mirror existing cycle keys like `dashboard.cycleAware.title` / `profile.section.cycleHormones`)
    - .planning/REQUIREMENTS.md (Traceability table format; v1.4 CYCLE rows are absent and must be appended)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (<specifics> verbatim copy seeds; D-05, D-08, D-12 copy rules)
  </read_first>
  <behavior>
    - Every new user-facing string used by Plans 02/03 has a key in Localizable.xcstrings with BOTH `en` and `zh-Hans` values
    - Copy obeys D-05 (readiness-first, no prescription) and D-12 (RED-S never says "RED-S"/"amenorrhea"/"diagnosis")
    - REQUIREMENTS.md traceability includes CYCLE-01..CYCLE-08 mapped to phases 17/18/19
  </behavior>
  <action>
    Add the following keys to Localizable.xcstrings (EN value shown; provide an accurate zh-Hans translation for each, marked `translated`, density-conscious per Phase 23 conventions):
    - `cycle.indicator.label` = "CYCLE"
    - `cycle.indicator.day` = "Day %lld" (cycle day)
    - `cyclePhase.context.luteal` = "You're in your luteal phase — lower HRV and a higher resting heart rate are expected here. Your readiness score already accounts for this."
    - `cyclePhase.context.follicular` = "You're in your follicular phase — HRV and recovery markers tend to run higher. Your readiness score reflects your own same-phase normal."
    - `cycle.fueling.title` = "Fueling & recovery"
    - `cycle.fueling.fasted` = "Avoid hard sessions fasted — fuel before high-intensity work."
    - `cycle.fueling.protein` = "Aim for protein within ~45 minutes after training."
    - `cycle.fueling.lutealHeat` = "You may run warmer this phase — prioritize hydration and cooling during hard efforts."
    - `cycle.fueling.lutealNutrition` = "A little extra protein and complex carbs can help this phase."
    - `cycle.reds.title` = "Cycle pattern change"
    - `cycle.reds.body` = "Your cycle patterns have changed recently. This can sometimes mean your body needs more fuel or recovery. It may be worth speaking with a healthcare provider."
    - `cycle.reds.dismiss` = "Dismiss"
    - `profile.row.pcos` = "PCOS"
    - `profile.row.perimenopausal` = "Perimenopausal"
    - Accessibility labels as needed (e.g. `cycle.reds.dismiss.a11y` = "Dismiss cycle pattern notice")
    Then append CYCLE-01..CYCLE-08 rows to the REQUIREMENTS.md Traceability table (CYCLE-01/02/03 -> Phase 17; CYCLE-04/05 -> Phase 18; CYCLE-06/07/08 -> Phase 19) and a short "v1.4 Female Athlete Optimization" requirements subsection documenting the three Phase-19 IDs per 19-CONTEXT.md <requirements>. Do NOT alter unrelated existing strings.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus" && plutil -lint "WorkloadApp/Resources/Localizable.xcstrings" && grep -c "cyclePhase.context.luteal\|cycle.reds.body\|cycle.fueling.protein" "WorkloadApp/Resources/Localizable.xcstrings"</automated>
  </verify>
  <acceptance_criteria>
    - All listed keys exist in Localizable.xcstrings with both `en` and `zh-Hans` stringUnit values
    - `plutil -lint` reports the catalog OK (valid plist/JSON)
    - No RED-S copy contains "RED-S", "amenorrhea", "diagnosis", or "disorder" (D-12)
    - REQUIREMENTS.md Traceability lists CYCLE-01..CYCLE-08 mapped to phases 17/18/19
  </acceptance_criteria>
  <done>All Phase-19 strings are localized EN + zh-Hans, RED-S copy is non-diagnostic, and the v1.4 CYCLE requirements are traced in REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthKit menstrual data -> REDSRiskEngine | The engine receives only precomputed Ints (cycle lengths, days-since-start) and Bool exclusion flags — never raw menstrual records or dates. Pure struct, Foundation-only. |
| Device -> Supabase sync | New `hasPCOS` / `isPerimenopausal` Athlete fields are local-only; this plan adds NO sync code. MenstrualCycleSnapshot remains local-only. |
| Engine -> user | RED-S engine emits a coarse display state (.none/.monitor) only — no numeric risk score, no diagnostic label (D-12, D-14). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | New Athlete reproductive flags | mitigate | `hasPCOS`/`isPerimenopausal` follow Phase 18 CR-01: not added to AthleteRow/pushAthlete/pullAthlete. Verified by grep that SyncService references neither. Local-only @Model field. |
| T-19-02 | Information Disclosure | REDSRiskEngine input | mitigate | Engine takes aggregate Ints + Bools, never raw HealthKit records or cycle-start dates; no HealthKit/SwiftData import in the pure struct. |
| T-19-03 | Tampering | New source file registration | mitigate | REDSRiskEngine.swift added to app + test targets in project.pbxproj; build/test gate confirms inclusion. No package installs. |
| T-19-04 | Safety / Misinformation | RED-S user-facing copy | mitigate | Copy is non-diagnostic, clinician-referral only (D-12); acceptance criterion greps for forbidden diagnostic terms. Engine never diagnoses. |
</threat_model>

<verification>
- `xcodebuild test -only-testing:WorkloadAppTests/REDSRiskEngineTests` exits 0 with all behavior + validation cases passing.
- `grep -n "import HealthKit\|import SwiftData" WorkloadApp/Services/REDSRiskEngine.swift` returns nothing (pure struct, Foundation only).
- `grep -n "hasPCOS\|isPerimenopausal" WorkloadApp/Services/SyncService.swift` returns nothing (local-only).
- `plutil -lint WorkloadApp/Resources/Localizable.xcstrings` reports OK; all new keys present with en + zh-Hans.
- REQUIREMENTS.md traceability includes CYCLE-01..08.
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- Pure, unit-tested REDSRiskEngine with exclusion-first gating (D-10, D-11) and no false positives on sparse data (D-14).
- Additive local-only Athlete `hasPCOS`/`isPerimenopausal` (D-11a), not synced.
- CyclePhase readiness-first 2-bucket context-copy helper (D-05).
- All Phase-19 strings localized EN + zh-Hans (D-15); RED-S copy non-diagnostic (D-12).
- CYCLE-01..08 traced in REQUIREMENTS.md.
- Build + targeted test suite green.
</success_criteria>

<output>
Create `.planning/phases/19-cycle-context-ui-guidance/19-01-SUMMARY.md` when done.
</output>