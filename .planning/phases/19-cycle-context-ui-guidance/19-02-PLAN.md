---
phase: 19-cycle-context-ui-guidance
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - WorkloadApp/Components/CycleStatusStrip.swift
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/Views/Recovery/RecoveryView.swift
autonomous: true
requirements: [CYCLE-06]
must_haves:
  truths:
    - "SC6/D-01: when no MenstrualCycleSnapshot rows exist (HealthKit menstrual permission not granted), NO cycle UI renders anywhere"
    - "SC1/D-07: the Dashboard shows an unobtrusive cycle day/phase indicator (CycleStatusStrip) above the MetricsStrip when cycle data is available"
    - "SC2/D-03: the recovery card shows a readiness-first phase-context line only when confidence >= 0.7 AND phase != .unknown AND no exclusion"
    - "D-03 below-gate behavior: when confidence < 0.7 or phase is .unknown, the indicator shows cycle DAY only (no phase interpretation)"
    - "D-04: OC users (isOnHormonalContraceptive == true) never see phase interpretation (phase is .unknown by design); cycle day may still show"
    - "SC5: no cycle copy contains training-prescription language (no deload/rest/go-hard wording)"
    - "D-06: the hero readiness card is unchanged except for the separate indicator strip; accent color stays only on the hero score number"
  artifacts:
    - path: "WorkloadApp/Components/CycleStatusStrip.swift"
      provides: "Reusable flat cycle day/phase indicator cell styled like MetricStripCell"
      contains: "CycleStatusStrip"
    - path: "WorkloadApp/Views/Dashboard/DashboardView.swift"
      provides: "Gated CycleStatusStrip insertion above MetricsStrip"
      contains: "CycleStatusStrip"
    - path: "WorkloadApp/Views/Recovery/RecoveryView.swift"
      provides: "Readiness-first phase-context row inside RecoveryScoreCard"
      contains: "contextCopyKey"
  key_links:
    - from: "CycleStatusStrip / RecoveryScoreCard"
      to: "MenstrualCycleSnapshot (latest) + CyclePhase.contextCopyKey"
      via: "@Query latest snapshot, D-03 gate, localized copy"
      pattern: "cycleSnapshots|contextCopyKey"
---

<objective>
Surface cycle context on the two primary read surfaces — an unobtrusive cycle day/phase indicator on the Dashboard and a readiness-first phase-context line in the Recovery score card — using the copy keys and `CyclePhase.contextCopyKey` helper from Plan 01. All rendering is gated so it is invisible without HealthKit menstrual permission (no snapshots) and shows phase *interpretation* only when the cycle is high-confidence and actually influenced the recovery baseline.

Purpose: Deliver CYCLE-06 (SC1, SC2, SC5, SC6). The framing is strictly readiness-first: the indicator is one more calm instrument metric; the recovery-card line *explains* the number ("…your readiness score already accounts for this") and never prescribes training. This plan adds no new logic beyond display gating.

Output: New reusable `CycleStatusStrip` component; Dashboard renders it above the MetricsStrip; `RecoveryScoreCard` gains an optional phase-context row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md
@CLAUDE.md
@.planning/phases/19-cycle-context-ui-guidance/19-01-PLAN.md

<interfaces>
<!-- Contracts extracted from the codebase + Plan 01 — do not re-explore. -->

From Plan 01:
- `CyclePhase.contextCopyKey` -> follicular key / luteal key / nil(.unknown).
- Localized keys: `cycle.indicator.label` ("CYCLE"), `cycle.indicator.day` ("Day %lld"), `cyclePhase.context.follicular`, `cyclePhase.context.luteal`.

From WorkloadApp/Models/MenstrualCycleSnapshot.swift:
- `@Model MenstrualCycleSnapshot` — date, cycleDay: Int?, estimatedPhase: CyclePhase?, confidence: Double, isOnHormonalContraceptive/isPregnant/isLactating: Bool.

From WorkloadApp/Views/Dashboard/DashboardView.swift:
- `@Query private var cycleSnapshots: [MenstrualCycleSnapshot]` (line 24); `showCyclePrompt = !cyclePromptDismissed && cycleSnapshots.isEmpty` (line 38) — the opt-in pattern.
- `MetricsStrip(viewModel:)` rendered at line 109; `MetricStripCell` (line 451) = micro-caps label + value, flat `ColorTokens.surface`, 0.5pt divider — the STYLE template for CycleStatusStrip.
- `HeroReadinessCard` (line 254) — accent only on the score number (DESIGN.md). Do NOT add cycle content inside it.

From WorkloadApp/Views/Recovery/RecoveryView.swift:
- `RecoveryScoreCard(recovery: RecoverySnapshot?)` (line 231) — flat surface card; insertion point for the context row is after the component rows (before the closing of the data branch, ~line 289).
- RecoveryView has `@Query athletes` + recovery snapshots but NOT yet a cycle-snapshot query — add `@Query` for MenstrualCycleSnapshot mirroring DashboardView line 24.

Locked decisions (19-CONTEXT.md): D-01 (visibility gate), D-02 (latest snapshot), D-03 (interpretation gate), D-04 (OC), D-05 (copy), D-06 (hero untouched), D-07 (indicator form).
DESIGN.md: Rectangle only (no RoundedRectangle), no .shadow, Font.Tokens.*, ColorTokens only, 8pt spacing, accent only on hero score, zone via text label (+ optional border).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CycleStatusStrip component (flat day/phase indicator)</name>
  <files>WorkloadApp/Components/CycleStatusStrip.swift</files>
  <read_first>
    - WorkloadApp/Views/Dashboard/DashboardView.swift lines 451-482 (MetricStripCell — exact label/value styling, micro-caps + tracking, flat surface, 0.5pt divider to replicate)
    - WorkloadApp/Components/MetricTile.swift (project component conventions)
    - WorkloadApp/Models/MenstrualCycleSnapshot.swift + WorkloadApp/Models/Enums.swift (snapshot fields + CyclePhase.contextCopyKey/displayName)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-03, D-04, D-07)
  </read_first>
  <behavior>
    - Given a snapshot with cycleDay and a high-confidence non-unknown phase + no exclusion: shows "CYCLE" micro-caps label, "Day N", and the phase displayName
    - Given a snapshot below the interpretation gate (confidence < 0.7, .unknown phase, or exclusion): shows "CYCLE" + "Day N" only — NO phase name
    - Given a snapshot with no cycleDay: shows the phase name only if gate passes, otherwise renders nothing meaningful (caller decides whether to show — see Task 2 gate)
    - Uses no accent color, no icon emphasis, flat ColorTokens.surface, 0.5pt divider, 8pt-grid spacing, Font.Tokens.* only
  </behavior>
  <action>
    Create `CycleStatusStrip.swift`: a `struct CycleStatusStrip: View` taking `let snapshot: MenstrualCycleSnapshot`. Compute a private `showsPhase: Bool` = `snapshot.confidence >= 0.7 && (snapshot.estimatedPhase ?? .unknown) != .unknown && !(snapshot.isOnHormonalContraceptive || snapshot.isPregnant || snapshot.isLactating)` (D-03/D-04). Lay out a horizontal row mirroring MetricStripCell vocabulary: a micro-caps "CYCLE" label (`cycle.indicator.label`, Font.Tokens.micro, tracking 1.2, ColorTokens.text3), then the value area: if `snapshot.cycleDay` present show `String(format: cycle.indicator.day, cycleDay)` (Font.Tokens.sectionHead or label, monospacedDigit, ColorTokens.text1); if `showsPhase`, append the phase `displayName` (Font.Tokens.label, ColorTokens.text2). Container: `.padding(.horizontal,16).padding(.vertical,16)`, `.background(ColorTokens.surface)`, `.overlay(alignment:.bottom){ Rectangle().fill(ColorTokens.divider).frame(height:0.5) }`. No accent, no RoundedRectangle, no shadow. Add an `.accessibilityElement(children:.combine)` with a combined label.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CycleStatusStrip.swift exists and is added to the app target in project.pbxproj (`grep -c CycleStatusStrip` non-zero)
    - showsPhase gate matches D-03 (confidence >= 0.7 && phase != .unknown && !exclusion)
    - Uses only ColorTokens + Font.Tokens.*; no RoundedRectangle, no .shadow, no ColorTokens.accent
    - All visible strings are localized keys (cycle.indicator.label / cycle.indicator.day) + CyclePhase.displayName
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>A flat, calm, opt-in cycle indicator component exists, showing day always and phase only when high-confidence/non-excluded, with no accent or decoration.</done>
</task>

<task type="auto">
  <name>Task 2: Render CycleStatusStrip on the Dashboard (gated)</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift</files>
  <read_first>
    - WorkloadApp/Views/Dashboard/DashboardView.swift (full file — `@Query cycleSnapshots` line 24, soft-prompt block lines 65-105, MetricsStrip insertion line 109)
    - WorkloadApp/Components/CycleStatusStrip.swift (Task 1 output)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-01 visibility, D-07 placement)
  </read_first>
  <behavior>
    - When cycleSnapshots is empty: no CycleStatusStrip renders (SC6 — invisible without permission)
    - When cycleSnapshots is non-empty: the latest snapshot's CycleStatusStrip renders directly above the MetricsStrip
    - The existing soft-prompt (showCyclePrompt) and all other Dashboard content are unchanged
  </behavior>
  <action>
    Add a computed `private var latestCycleSnapshot: MenstrualCycleSnapshot?` = `cycleSnapshots.sorted { $0.date > $1.date }.first` (the `@Query cycleSnapshots` already exists; if it is unsorted, sort here — D-02). In `body`, just before `MetricsStrip(viewModel: viewModel)` (line 109), insert: `if let snap = latestCycleSnapshot { CycleStatusStrip(snapshot: snap) ; Spacer().frame(height: 8) }`. Do NOT modify HeroReadinessCard, the soft-prompt block, or MetricsStrip (D-06). Keep the existing `Spacer().frame(height: 8)` rhythm consistent (8pt grid).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - DashboardView renders CycleStatusStrip only when `latestCycleSnapshot != nil` (i.e. cycleSnapshots non-empty)
    - The strip is placed above MetricsStrip; HeroReadinessCard and soft-prompt untouched
    - No accent color introduced outside the hero score
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>The Dashboard shows the unobtrusive cycle indicator when data exists and nothing when it does not.</done>
</task>

<task type="auto">
  <name>Task 3: Readiness-first phase-context line in RecoveryScoreCard (gated)</name>
  <files>WorkloadApp/Views/Recovery/RecoveryView.swift</files>
  <read_first>
    - WorkloadApp/Views/Recovery/RecoveryView.swift (full file — RecoveryView @Query block lines 4-34; RecoveryScoreCard lines 231-300; component-rows region ~262-289 is the insertion point)
    - WorkloadApp/Models/Enums.swift (CyclePhase.contextCopyKey from Plan 01)
    - .planning/phases/19-cycle-context-ui-guidance/19-CONTEXT.md (D-03 gate, D-05 copy, SC5 no-prescription)
  </read_first>
  <behavior>
    - When the latest cycle snapshot passes the D-03 gate, RecoveryScoreCard shows a phase-context line below the HRV/RHR/sleep rows, using CyclePhase.contextCopyKey -> localized follicular/luteal copy
    - When no snapshot / below gate / exclusion: no context line renders
    - The line is secondary text (ColorTokens.text2, Font.Tokens.label/smallLabel), wraps fully, no accent, no border emphasis
    - Recovery score/zone/badge rendering is unchanged
  </behavior>
  <action>
    Add `@Query private var cycleSnapshots: [MenstrualCycleSnapshot]` to RecoveryView (mirror DashboardView line 24) and a computed `latestCycleSnapshot` (latest by date, athlete-scoped if athlete id available). Thread the latest snapshot into `RecoveryScoreCard` by adding `var cycleSnapshot: MenstrualCycleSnapshot? = nil` to the struct and passing it at the call site (line 48). Inside RecoveryScoreCard's data branch, after the component VStack (~line 289), add a gated context row: compute `showsPhase` exactly as in CycleStatusStrip (confidence >= 0.7 && phase != .unknown && !exclusion); if it passes and `phase.contextCopyKey` is non-nil, render `Text(LocalizedStringKey(key))` (or `Text(String(localized:))`) styled Font.Tokens.label + ColorTokens.text2 + `.fixedSize(horizontal:false, vertical:true)` so it wraps. Precede it with a 0.5pt `Rectangle().fill(ColorTokens.divider)` to separate from the metric rows. Ensure NO training-prescription wording (the copy from Plan 01 already complies, SC5). Do NOT touch the score number, zone badge, or component rows.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - RecoveryView passes the latest cycle snapshot into RecoveryScoreCard; card renders the context line only when the D-03 gate passes and contextCopyKey != nil
    - The context line uses the localized follicular/luteal copy (Plan 01 keys), secondary text, wraps, no accent/border emphasis
    - Score/zone/component rows unchanged
    - No prescription wording present (copy is readiness-first)
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>The recovery card explains the score in cycle terms when high-confidence, stays silent otherwise, and never prescribes training.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenstrualCycleSnapshot -> views | Views read local-only snapshots via `@Query`; no new persistence, no sync. Display only. |
| Device -> Supabase sync | No sync code touched; no new fields persisted or uploaded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | Cycle UI visibility | mitigate | All cycle UI gated on snapshot presence (D-01); invisible without HealthKit menstrual permission (SC6). No cycle data leaves the device. |
| T-19-06 | Tampering | New component file registration | mitigate | CycleStatusStrip.swift added to app target in project.pbxproj; build gate confirms. No package installs. |
| T-19-07 | Misinformation | Phase-context copy | accept | Copy is readiness-first explanation (Plan 01, D-05); interpretation only shown when the engine actually applied the same-phase baseline (gate matches Phase 18 D-04). Low residual risk. |
</threat_model>

<verification>
- `xcodebuild build` exits 0.
- DESIGN.md compliance: `grep -n "RoundedRectangle\|\.shadow(\|ColorTokens.accent" WorkloadApp/Components/CycleStatusStrip.swift` returns nothing (no rounded corners / shadows / accent in the new component).
- No literal user-facing strings in the new/edited cycle UI (all via localized keys + CyclePhase helpers).
- With zero MenstrualCycleSnapshot rows, neither the Dashboard strip nor the recovery-card line renders (source inspection: both guarded by `latestCycleSnapshot != nil` / the D-03 gate).
- HeroReadinessCard is unchanged (no cycle content added inside it; accent still only on the score number).
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- SC1: Dashboard shows the unobtrusive CycleStatusStrip when cycle data is available (D-07).
- SC2: Recovery card shows the readiness-first phase-context line when high-confidence (D-03, D-05).
- SC5: no prescription wording anywhere in cycle copy.
- SC6: all cycle UI invisible without HealthKit menstrual permission (D-01).
- DESIGN.md respected: 0pt corners, no shadow, Font.Tokens.*, ColorTokens only, accent on hero score only.
</success_criteria>

<output>
Create `.planning/phases/19-cycle-context-ui-guidance/19-02-SUMMARY.md` when done.
</output>
