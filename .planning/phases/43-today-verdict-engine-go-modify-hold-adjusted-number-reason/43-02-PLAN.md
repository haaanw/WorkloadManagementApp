---
phase: 43-today-verdict-engine-go-modify-hold-adjusted-number-reason
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - WorkloadApp/Services/VerdictReasonBuilder.swift
  - WorkloadAppTests/VerdictReasonBuilderTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [VERDICT-03]

must_haves:
  truths:
    - "Each verdict carries a one-line plain-language reason naming the driving signal(s), assembled from ReasoningEngine.explainDecision"
    - "The reason names the cross-modal cause ONLY when CrossModalShadowGate.crossModalDrivesVerdict is true AND cross-modal is the dominant driver — otherwise it is never mentioned"
    - "Confidence is reported SEPARATELY from the verdict and reason (a distinct field, not folded into the one-liner)"
    - "On low confidence / cold-start the reason DEFERS to the plan ('going with your plan, still learning your baseline') rather than fabricating a trim rationale"
    - "The reason copy never says 'injury prediction' or 'injury risk'"
  artifacts:
    - path: "WorkloadApp/Services/VerdictReasonBuilder.swift"
      provides: "Pure reason+confidence assembler over explainDecision, with the cross-modal-gated cause line and cold-start defer copy"
      min_lines: 70
      contains: "struct VerdictReasonBuilder"
    - path: "WorkloadAppTests/VerdictReasonBuilderTests.swift"
      provides: "Tests for one-liner assembly, gate-gated cross-modal naming, separate confidence, cold-start defer, no-injury grep"
      contains: "func test_"
  key_links:
    - from: "WorkloadApp/Services/VerdictReasonBuilder.swift"
      to: "ReasoningEngine.explainDecision"
      via: "prefix(1-2) of the ranked DecisionReasons becomes the headline reason"
      pattern: "ReasoningEngine\\.explainDecision"
    - from: "WorkloadApp/Services/VerdictReasonBuilder.swift"
      to: "CrossModalShadowGate.crossModalDrivesVerdict"
      via: "cross-modal cause named only when the gate is on"
      pattern: "crossModalDrivesVerdict"
---

<objective>
Build `VerdictReasonBuilder`: the one-line plain-language reason for a verdict (VERDICT-03), assembled from the EXISTING `ReasoningEngine.explainDecision` ranked reasons, with two locked guardrails — (1) the cross-modal cause is named ONLY when `CrossModalShadowGate.crossModalDrivesVerdict` is true and cross-modal is the dominant driver, and (2) confidence is reported as a SEPARATE field, with a low-confidence / cold-start path that DEFERS to the plan instead of fabricating a trim rationale.

Purpose: VERDICT-03 plus the honest-confidence and cross-modal-naming guardrails from the research nocebo/autonomy analysis. The output is the reason string + confidence the Phase 43-03 slot-writer persists and the Phase 44 card renders.
Output: `VerdictReasonBuilder.swift` (pure struct) + its test suite, building green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-CONTEXT.md
@.planning/research/v2-verdict-engine.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-01-SUMMARY.md

<read_first>
- @WorkloadApp/Services/ReasoningEngine.swift — `explainDecision(input: DecisionInput) -> [DecisionReason]`. `DecisionReason { text: String; source: String; contribution: Double }` already RANKED by |contribution| (most influential first). `DecisionInput { readiness: ReadinessFusionEngine.ReadinessResult; strainRisk: StrainRiskEngine.StrainRiskResult; recommendation: AutoregulationEngine.TrainingRecommendation; personalSleepBaselineMinutes: Double? }`. It already interleaves readiness factors ("Holding back — HRV 1.2σ below your baseline") and strain factors ("Caution — per-muscle strength-load elevation") and is grep-guarded against injury language. The one-liner is a `prefix(1)` (optionally join `prefix(2)`).
- @WorkloadApp/Services/CrossModalShadowGate.swift — `crossModalDrivesVerdict` (default false) + test-only `withEnabled`. Name the cross-modal cause ONLY when this is true.
- @WorkloadApp/Services/CrossModalFatigueEngine.swift — `CrossModalResult.dominantReason: String?` ("legs still loaded from recent cross-modal work") — the cross-modal cause line, surfaced only under the gate. `perRegionElevation` lets you decide whether cross-modal is the DOMINANT driver for the planned region.
- @WorkloadApp/Services/PRSReadinessInputBuilder.swift — `build(...) -> AutoregulationEngine.ReadinessInput?` returns `nil` on cold-start / no usable personal baseline. This nil is the LOCKED honest-confidence-defer signal (re-stated in its doc comment §"Honest-confidence gate"). The reason builder takes a flag/optional reflecting that nil and emits the defer copy. ALSO note `ReadinessFusionEngine.ReadinessResult.confidence: Double (0…1)` is the separate confidence to surface.
- @WorkloadApp/Services/TodayVerdictEngine.swift (from 43-01) — `Verdict` and `VerdictResult` you pair the reason with.
- @WorkloadAppTests/ReasoningDecisionExplanationTests.swift — the existing test style + the no-injury source-grep fence to mirror.
</read_first>

<interfaces>
From ReasoningEngine.swift:
```swift
struct DecisionReason: Equatable { let text: String; let source: String; let contribution: Double }
struct DecisionInput {
    let readiness: ReadinessFusionEngine.ReadinessResult
    let strainRisk: StrainRiskEngine.StrainRiskResult
    let recommendation: AutoregulationEngine.TrainingRecommendation
    let personalSleepBaselineMinutes: Double?
}
static func explainDecision(input: DecisionInput) -> [DecisionReason]   // ranked desc by |contribution|
```

From ReadinessFusionEngine.swift:
```swift
struct ReadinessResult: Equatable { let readiness: Double; let zone: ReadinessZone; let factors: [ReadinessFactor]; let confidence: Double /* 0…1, reported separately */ }
```

From CrossModalFatigueEngine.swift:
```swift
struct CrossModalResult: Equatable { let perRegionElevation: [MuscleRegion: Double]; let dominantReason: String? /* surface only under the gate */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing VerdictReasonBuilder test suite (RED)</name>
  <files>WorkloadAppTests/VerdictReasonBuilderTests.swift</files>
  <behavior>
    Reason assembly (VERDICT-03):
    - Test: given a `DecisionInput` whose top-ranked `explainDecision` reason is the HRV depression, the assembled one-liner contains that signal's text (the headline is the prefix(1), or a prefix(2) join when a strong second factor exists). The reason is a single line (no newlines).
    - Test: the assembled result exposes `confidence` as a SEPARATE field equal to `readiness.confidence` — it is NOT interpolated into the reason string (assert the reason text does not contain the numeric confidence).

    Cross-modal naming, gated:
    - Test: with the gate OFF (default) and a `CrossModalResult` whose `dominantReason` would name legs, the assembled reason does NOT contain the cross-modal cause text (gate off then never mentioned).
    - Test: inside `CrossModalShadowGate.withEnabled(true)`, when cross-modal is the DOMINANT driver for the planned region (its elevation is the strongest down-pressure), the reason DOES contain the cross-modal cause line ("…loaded from…"). Assert on substring presence.
    - Test: inside `withEnabled(true)` but when cross-modal is NOT dominant (readiness is the bigger driver), the cross-modal cause is NOT forced into the line (readiness reason leads).

    Cold-start defer (locked honest-confidence rule):
    - Test: when the builder is told readiness input was unavailable (the `PRSReadinessInputBuilder.build` returned nil case — modeled as a `deferToPlan` flag / nil `decisionInput`), the result's reason is the defer copy ("going with your plan, still learning your baseline" or equivalent), `deferredToPlan == true`, and confidence is reported as low/unknown — NOT a fabricated trim rationale.

    Honesty fence:
    - Test: `test_reasonBuilder_neverSaysInjuryPrediction_sourceGrep` reads the source via `#filePath` and asserts neither "injury prediction" nor "injury risk" appears.
  </behavior>
  <action>Author `VerdictReasonBuilderTests` mirroring `ReasoningDecisionExplanationTests.swift`. Construct `ReadinessResult` / `StrainRiskResult` / `TrainingRecommendation` fixtures via their inits to drive `explainDecision`. For the cross-modal tests, build a `CrossModalResult` (its memberwise init: perRegionCarry, perRegionElevation, systemicFactor, dominantReason) and use `CrossModalShadowGate.withEnabled`. Reference the public API designed in Task 2: `VerdictReasonBuilder.AssembledReason { reasonLine: String; confidence: Double; deferredToPlan: Bool }` and `VerdictReasonBuilder.build(decisionInput:crossModalResult:plannedRegion:deferToPlan:)`. This task ends RED (the type does not yet exist).</action>
  <verify>
    <automated>test -f WorkloadAppTests/VerdictReasonBuilderTests.swift &amp;&amp; grep -c "func test_" WorkloadAppTests/VerdictReasonBuilderTests.swift</automated>
  </verify>
  <done>Test file exists with at least 7 `test_` methods covering one-liner assembly, separate confidence, gate-gated cross-modal naming (off / on-dominant / on-not-dominant), cold-start defer, and the no-injury grep. RED until Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VerdictReasonBuilder — one-liner + separate confidence + gated cross-modal + defer (GREEN)</name>
  <files>WorkloadApp/Services/VerdictReasonBuilder.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <behavior>
    Pure struct, static methods, Foundation-only (mirrors ReasoningEngine). Public surface:
    - `struct AssembledReason: Equatable { let reasonLine: String; let confidence: Double; let deferredToPlan: Bool }`
    - `static func build(decisionInput: ReasoningEngine.DecisionInput?, crossModalResult: CrossModalFatigueEngine.CrossModalResult?, plannedRegion: MuscleRegion, deferToPlan: Bool) -> AssembledReason`
  </behavior>
  <action>
Implement per the locked decisions and research §3.2/§4:

1. Cold-start defer first: if `deferToPlan == true` OR `decisionInput == nil` (the `PRSReadinessInputBuilder.build` returned nil signal), return `AssembledReason(reasonLine: <defer copy>, confidence: decisionInput?.readiness.confidence ?? 0, deferredToPlan: true)`. Defer copy via `String(localized:)` — e.g. "Going with your plan — still learning your baseline." Tuwa voice, no "should". This is the LOCKED honest-confidence guardrail: never trim on a guess.
2. Otherwise call `ReasoningEngine.explainDecision(input: decisionInput!)`. Take `prefix(1)` as the headline driver; optionally join a `prefix(2)` second factor only if its |contribution| is within a documented band of the first (so the line stays single and meaningful). Compose a single-line string (collapse any newlines).
3. Cross-modal cause, GATED: only when `CrossModalShadowGate.crossModalDrivesVerdict == true` AND `crossModalResult != nil` AND cross-modal is the DOMINANT driver for `plannedRegion` (its `perRegionElevation[plannedRegion]`, or the max for `.fullBody`, exceeds the leading readiness/strain down-pressure), append/lead with `crossModalResult.dominantReason`. When the gate is off, NEVER reference cross-modal — the locked rule "names the cross-modal cause only when the gate is on and cross-modal is the driver". Document this branch.
4. Confidence is the readiness confidence (`decisionInput.readiness.confidence`), carried in its OWN `confidence` field — never interpolated into `reasonLine` (research §3.3 "confidence is shown, not folded"). `deferredToPlan = false` on this path.
5. All user-facing copy via `String(localized:)`, Tuwa voice, NEVER "injury prediction"/"injury risk" (grep-guarded). No fenced code in copy.

Register the new app-target file with the 4 explicit pbxproj entries (mirror CrossModalShadowGate.swift quad). Use a fresh unique object-id pair (do NOT reuse 43-01's ids). No " 2.swift" duplicate.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&amp;1 | tail -3</automated>
  </verify>
  <done>`** BUILD SUCCEEDED **`. `VerdictReasonBuilder` exists with `build(...)` returning `AssembledReason`; cross-modal naming is gate-guarded; confidence is a separate field; defer copy is emitted on cold-start. pbxproj has 4 entries; no " 2.swift" duplicate.</done>
</task>

<task type="auto">
  <name>Task 3: Run the suite GREEN + lock the gate / defer / no-injury fences</name>
  <files>WorkloadAppTests/VerdictReasonBuilderTests.swift</files>
  <action>Run `VerdictReasonBuilderTests` on the known-alive sim. Confirm all pass (RED to GREEN). If a test fails, fix the BUILDER (Task 2) to satisfy the documented contract — do not weaken the test. Confirm specifically: the gate-off path never contains the cross-modal cause substring; the defer path returns the defer copy with `deferredToPlan == true`; confidence is a separate numeric field absent from `reasonLine`; the no-injury source-grep passes.</action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/VerdictReasonBuilderTests 2>&amp;1 | tail -5</automated>
  </verify>
  <done>`** TEST SUCCEEDED **` with all VerdictReasonBuilder cases green (one-liner, separate confidence, gated cross-modal naming, cold-start defer, no-injury grep). XCTest host did not crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explainDecision → reason line | The reason trusts already-ranked, injury-guarded engine output; the builder must not re-introduce forbidden framing |
| cross-modal gate → reason copy | The gate fences whether the cross-modal cause is ever named to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Information disclosure | reason copy | mitigate | No "injury prediction"/"injury risk"; strain framed as load-tolerance caution; source-grep fence test |
| T-43-05 | Elevation of privilege | cross-modal cause named prematurely | mitigate | `dominantReason` appended ONLY under `crossModalDrivesVerdict == true` AND dominance check; gate-off test asserts absence |
| T-43-06 | Spoofing | fabricated confidence on cold-start | mitigate | Cold-start returns the defer copy with `deferredToPlan == true`, never a synthesized trim rationale; confidence reported separately |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Swift) — no new dependency surface |
</threat_model>

<verification>
- App builds green on sim `CAF84E71-BB64-491D-87C8-875A0143B26D`.
- `VerdictReasonBuilderTests` all pass; XCTest host does not crash.
- `grep -n "VerdictReasonBuilder.swift" "workload management/workload management.xcodeproj/project.pbxproj"` shows exactly 4 entries.
- No injury-prediction language: `grep -iv '^//' WorkloadApp/Services/VerdictReasonBuilder.swift | grep -ci "injury prediction\|injury risk"` returns 0.
- Cross-modal gate read present: `grep -c crossModalDrivesVerdict WorkloadApp/Services/VerdictReasonBuilder.swift` ≥ 1.
</verification>

<success_criteria>
- VERDICT-03: each verdict carries a one-line plain-language reason from `explainDecision` naming the driving signals.
- Cross-modal cause is named ONLY when the gate is on and cross-modal is the dominant driver.
- Confidence is reported separately; cold-start defers to the plan rather than fabricating.
- Reason copy is grep-clean of injury language. Commit on `main`, never a new branch.
</success_criteria>

<output>
Create `.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-02-SUMMARY.md` when done.
</output>
