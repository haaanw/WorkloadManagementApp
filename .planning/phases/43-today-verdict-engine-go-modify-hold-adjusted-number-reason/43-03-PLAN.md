---
phase: 43-today-verdict-engine-go-modify-hold-adjusted-number-reason
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - WorkloadApp/Services/PRSReadinessInputBuilder.swift
  - WorkloadApp/Services/TodayVerdictService.swift
  - WorkloadAppTests/TodayVerdictServiceTests.swift
  - WorkloadAppTests/PRSReadinessInputBuilderTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [VERDICT-01, VERDICT-02, VERDICT-03]

must_haves:
  truths:
    - "Given today's planned PrescribedWorkout, the service selects the top set per exercise, picks the per-lift muscle region, computes the verdict + adjusted number + reason, and writes them into the TemplateSet verdict slots"
    - "It writes adjustedTargetWeightKg / adjustedTargetRPE / verdictReason (a SUGGESTION) and never sets verdictAppliedAt or athleteOverrode (those are the Phase-44 accept/decline)"
    - "It never mutates the source authored template — only the frozen PrescribedWorkout's working sets"
    - "On cold-start (PRSReadinessInputBuilder.buildDetailed returns nil) it writes no trim — the suggestion equals the plan (defer) and the reason is the defer copy"
    - "Cross-modal contributes zero to every written number while the shadow gate is off (the shipped default)"
    - "The PRODUCTION reason path is sourced: the real DecisionInput (ReadinessFusionEngine.ReadinessResult + StrainRiskEngine.StrainRiskResult with factors + the recommendation) is surfaced by PRSReadinessInputBuilder.buildDetailed and consumed by the production wrapper — the live VERDICT-03 reason is not test-injected-only"
    - "When the planned set carries no RPE target (plannedRPE == nil), adjustedTargetRPE stays nil — the service never emits a bare intensity cap the athlete never planned"
  artifacts:
    - path: "WorkloadApp/Services/PRSReadinessInputBuilder.swift"
      provides: "buildDetailed(...) -> BuiltReadiness? surfacing { input, readiness, strain }; build(...) -> ReadinessInput? preserved by delegating + unwrapping"
      contains: "buildDetailed"
    - path: "WorkloadApp/Services/TodayVerdictService.swift"
      provides: "@MainActor orchestration: reads a planned session, runs the two engines, writes the Phase-42 slots; production wrapper assembles the real DecisionInput from buildDetailed"
      min_lines: 90
      contains: "TodayVerdictService"
    - path: "WorkloadAppTests/TodayVerdictServiceTests.swift"
      provides: "Tests: slot-write, no verdictAppliedAt/athleteOverrode, source-template untouched, cold-start defer, gate-off zero cross-modal, nil-RPE leaves adjustedTargetRPE nil, PRODUCTION-wrapper reason path over a real session"
      contains: "func test_"
    - path: "WorkloadAppTests/PRSReadinessInputBuilderTests.swift"
      provides: "Regression: existing cold-start-nil behavior unchanged for both build() and buildDetailed(); buildDetailed surfaces non-nil readiness+strain on a populated athlete and build() returns the SAME ReadinessInput"
      contains: "buildDetailed"
  key_links:
    - from: "WorkloadApp/Services/TodayVerdictService.swift"
      to: "TodayVerdictEngine.evaluate"
      via: "computes the verdict + adjusted number for each exercise's top set"
      pattern: "TodayVerdictEngine\\.evaluate"
    - from: "WorkloadApp/Services/TodayVerdictService.swift"
      to: "TemplateSet.adjustedTargetWeightKg"
      via: "writes the suggestion into the frozen working set's verdict slots"
      pattern: "adjustedTargetWeightKg|verdictReason"
    - from: "WorkloadApp/Services/TodayVerdictService.swift"
      to: "PRSReadinessInputBuilder.buildDetailed"
      via: "production wrapper sources the real DecisionInput (readiness+strain results); nil ⇒ cold-start defer"
      pattern: "PRSReadinessInputBuilder\\.buildDetailed"
    - from: "WorkloadApp/Services/PRSReadinessInputBuilder.swift"
      to: "ReasoningEngine.DecisionInput"
      via: "buildDetailed surfaces the ReadinessResult + StrainRiskResult the DecisionInput needs (no longer discarded)"
      pattern: "BuiltReadiness|buildDetailed"
---

<objective>
Build `TodayVerdictService`: the @MainActor surface that ties the two pure engines (43-01 `TodayVerdictEngine`, 43-02 `VerdictReasonBuilder`) to a real "today's planned session" `PrescribedWorkout` and WRITES the verdict suggestion into the Phase-42 nullable `TemplateSet` slots. It selects each exercise's top set, picks the per-lift muscle region (cross-modal anchor, gate-off this ship), reuses the existing `PRSReadinessInputBuilder` for the readiness/strain inputs (and its nil = cold-start defer signal), and persists a SUGGESTION only — never `verdictAppliedAt`/`athleteOverrode`, never the source template.

**Critical fix (this plan now sources the live reason path):** the production reason path (VERDICT-03 / ROADMAP SC3) needs the FULLY-FUSED `ReadinessFusionEngine.ReadinessResult` + `StrainRiskEngine.StrainRiskResult` (with factors + confidence) to assemble `ReasoningEngine.DecisionInput`. Today `PRSReadinessInputBuilder.build` COMPUTES both internally (`readinessResult` ~L95, `strainResult` ~L115) but DISCARDS them at the collapsed `ReadinessInput` return (~L126) — so only hand-injected test inputs could feed the reason builder and the live path was unsourced. This plan extends `PRSReadinessInputBuilder` to ALSO surface those two results (a new `buildDetailed(...) -> BuiltReadiness?`) so the production wrapper assembles a REAL `DecisionInput`. The existing `build(...) -> ReadinessInput?` signature is preserved (Phase-41 `DashboardViewModel.buildDualRunMessage` depends on it) by having `build` delegate to `buildDetailed` and unwrap.

Purpose: completes VERDICT-01/02/03 end-to-end as a writable suggestion the Phase-44 card and accept/decline action consume — with a real, non-injected reason source. This is the engine-to-slots wiring — no UI card (Phase 44), no measurement (Phase 45).
Output: extended `PRSReadinessInputBuilder.swift` + `TodayVerdictService.swift` (@MainActor) + both test suites, building green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-CONTEXT.md
@.planning/research/v2-verdict-engine.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-01-SUMMARY.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-02-SUMMARY.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-01-SUMMARY.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-02-SUMMARY.md

<read_first>
- @WorkloadApp/Models/PrescribedWorkout.swift — `allExercises: [TemplateExercise]`, `sortedGroups`, frozen denormalized `templateName`/`sportType`/`sessionType`. This is today's planned session (from `PlannedSessionRepository`). The working sets are `TemplateSet`s.
- @WorkloadApp/Models/WorkoutTemplate.swift — `TemplateExercise { exerciseName; muscleGroup: MuscleGroup?; sortedSets }` (`muscleGroup` is OPTIONAL, WorkoutTemplate.swift:127); `TemplateSet { setIndex; targetReps; targetWeightKg: Double?; targetRPE: Double?; isWarmup; adjustedTargetWeightKg: Double?; adjustedTargetRPE: Double?; verdictReason: String?; verdictAppliedAt: Date?; athleteOverrode: Bool }` (WorkoutTemplate.swift:127,162,165,167,179-183). The Phase-42 verdict slots default nil/false. WRITE adjustedTargetWeightKg/adjustedTargetRPE/verdictReason; DO NOT set verdictAppliedAt or athleteOverrode (Phase 44 accept). `MuscleGroup.region: MuscleRegion` (Enums.swift:318) is the per-lift region selector; default `.fullBody` when `muscleGroup == nil`.
- @WorkloadApp/Repositories/PlannedSessionRepository.swift — `fetchTodaysPlannedSession(athleteId:) -> PrescribedWorkout?`. The service reads today's plan via this (or accepts an injected PrescribedWorkout for testability).
- @WorkloadApp/Services/PRSReadinessInputBuilder.swift — THE FILE THIS PLAN EXTENDS. Currently `build(recentSnapshots:latestHRV:latestRHR:latestSleepMinutes:allSessions:fatigueResult:daysSinceRest:wellnessScore:acwr:acwrZone:asOf:calendar:) -> AutoregulationEngine.ReadinessInput?`. Returns nil on cold-start (no FatigueResult OR all three personal z's nil) = the LOCKED defer signal. It already computes `let readinessResult = ReadinessFusionEngine.compute(...)` (~L95) and `let strainResult = StrainRiskEngine.fuse(...)` (~L115) but returns ONLY the collapsed `AutoregulationEngine.ReadinessInput` (~L126), discarding both. Task 1 extends it to ALSO surface them (see Task 1 action). REUSE it for readiness/strain — do not recompute the fold yourself in the service.
- @WorkloadApp/ViewModels/DashboardViewModel.swift (lines ~356-382, `buildDualRunMessage`) — the EXACT live call site of `PRSReadinessInputBuilder.build`: it builds `ReadinessInput`, calls `AutoregulationEngine.recommendReadiness`, and feeds `PRSDualRunSurface.dualRunMessage`. Note it NEVER assembles a `DecisionInput` today — that is precisely why the verdict reason path was unsourced in production. Mirror its input sourcing (recentSnapshots, latestHRV/RHR/sleep, allSessions, fatigueResult, daysSinceRest, acwr/acwrZone, wellnessScore: nil) for the production wrapper. KEEP this call site green: `build(...)` must still return the same `ReadinessInput?`.
- @WorkloadApp/Services/ReasoningEngine.swift — `struct DecisionInput { let readiness: ReadinessFusionEngine.ReadinessResult; let strainRisk: StrainRiskEngine.StrainRiskResult; let recommendation: AutoregulationEngine.TrainingRecommendation; let personalSleepBaselineMinutes: Double? }` (ReasoningEngine.swift:116-133). THIS is what the production reason path requires the two fused results for. `static func explainDecision(input: DecisionInput) -> [DecisionReason]` reads `input.readiness.factors` / `input.strainRisk.factors`.
- @WorkloadApp/Services/ReadinessFusionEngine.swift — `struct ReadinessResult: Equatable { let readiness: Double; let zone: ReadinessZone; let factors: [ReadinessFactor]; let confidence: Double }` (ReadinessFusionEngine.swift:85-89). This is what `buildDetailed` surfaces from `readinessResult`.
- @WorkloadApp/Services/StrainRiskEngine.swift — `struct StrainRiskResult: Equatable { let score: Double; let zone: StrainRiskZone; let factors: [StrainRiskFactor] }` (StrainRiskEngine.swift:39-42). This is what `buildDetailed` surfaces from `strainResult`.
- @WorkloadApp/Services/AutoregulationEngine.swift — `struct TrainingRecommendation { let intensityCap: Double /* 1-10 */; let volumeModifier: Double; ... }` (AutoregulationEngine.swift:41-43); `static func recommendReadiness(input: ReadinessInput) -> TrainingRecommendation` (AutoregulationEngine.swift:237). `intensityCap` is NON-optional Double; `TemplateSet.targetRPE` is `Double?` (see nil-RPE handling in Task 2).
- @WorkloadApp/Services/TodayVerdictEngine.swift (43-01) — `evaluate(recommendation:plannedTopSet:crossModalResult:plateStepKg:) -> VerdictResult` with `PlannedTopSet { exerciseName; region; plannedTopSetKg; plannedReps; plannedRPE }` and `VerdictResult { verdict; adjustedTopSetKg; volumeCutSets; loadFactor }`. Confirm against 43-01-SUMMARY before coding.
- @WorkloadApp/Services/VerdictReasonBuilder.swift (43-02) — `build(decisionInput:crossModalResult:plannedRegion:deferToPlan:) -> AssembledReason { reasonLine; confidence; deferredToPlan }`. Confirm against 43-02-SUMMARY before coding.
- @WorkloadApp/Services/CrossModalFatigueEngine.swift — `compute(sessions:systemicReadiness:asOf:calendar:) -> CrossModalResult`. Build the `CrossModalResult` so the gate-on path is wired, but it has ZERO effect while the gate is off (the engines already enforce that — the service just passes it through).
- @WorkloadApp/Services/CrossModalShadowGate.swift — `crossModalDrivesVerdict` (default false); `withEnabled` (test-only).
- @WorkloadAppTests/PlannedSessionRepositoryTests.swift — the `@MainActor`-repo XCTest pattern (own ModelContainer/ModelContext as stored props set in setUp, cleared in tearDown) to avoid the iOS 26.1-sim deinit crash. FOLLOW IT for this service's tests.
- @WorkloadAppTests/PRSReadinessInputBuilderTests.swift — the EXISTING builder test suite (cold-start-nil cases). Task 1 ADDS to it without breaking it; these regression cases MUST stay green.
</read_first>

<interfaces>
From TodayVerdictEngine.swift (43-01) — confirm against 43-01-SUMMARY:
```swift
enum Verdict { case go, modify, hold }
struct PlannedTopSet { let exerciseName: String; let region: MuscleRegion; let plannedTopSetKg: Double; let plannedReps: Int?; let plannedRPE: Double? }
struct VerdictResult: Equatable { let verdict: Verdict; let adjustedTopSetKg: Double; let volumeCutSets: Int?; let loadFactor: Double }
static func evaluate(recommendation:plannedTopSet:crossModalResult:plateStepKg:) -> VerdictResult
```

From VerdictReasonBuilder.swift (43-02) — confirm against 43-02-SUMMARY:
```swift
struct AssembledReason: Equatable { let reasonLine: String; let confidence: Double; let deferredToPlan: Bool }
static func build(decisionInput:crossModalResult:plannedRegion:deferToPlan:) -> AssembledReason
```

From ReasoningEngine.swift (the reason path's required input — VERIFIED in code):
```swift
struct DecisionInput {
    let readiness: ReadinessFusionEngine.ReadinessResult   // needs .factors + .confidence
    let strainRisk: StrainRiskEngine.StrainRiskResult      // needs .factors
    let recommendation: AutoregulationEngine.TrainingRecommendation
    let personalSleepBaselineMinutes: Double?
}
```

NEW from PRSReadinessInputBuilder.swift (this plan adds — Task 1):
```swift
struct BuiltReadiness {
    let input: AutoregulationEngine.ReadinessInput
    let readiness: ReadinessFusionEngine.ReadinessResult
    let strain: StrainRiskEngine.StrainRiskResult
}
static func buildDetailed(/* SAME params as build */) -> BuiltReadiness?
// build(...) is preserved: delegates to buildDetailed and returns BuiltReadiness?.input
```

TemplateSet verdict slots (Phase 42, write these — never verdictAppliedAt/athleteOverrode):
```swift
var targetWeightKg: Double? = nil      // the authored/frozen plan number — DO NOT mutate
var targetRPE: Double? = nil           // the authored/frozen plan RPE — DO NOT mutate; may be nil
var adjustedTargetWeightKg: Double? = nil
var adjustedTargetRPE: Double? = nil   // leave nil when targetRPE was nil (Task 2)
var verdictReason: String? = nil
var verdictAppliedAt: Date? = nil      // DO NOT SET — Phase 44 accept/decline
var athleteOverrode: Bool = false      // DO NOT SET — Phase 44 accept/decline
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface the fused readiness+strain from PRSReadinessInputBuilder (sources the live reason path)</name>
  <files>WorkloadApp/Services/PRSReadinessInputBuilder.swift, WorkloadAppTests/PRSReadinessInputBuilderTests.swift</files>
  <action>
Extend the EXISTING `PRSReadinessInputBuilder` (a Phase-41-owned file — edit surgically; do not rewrite the fold) so the two fused results it already computes are no longer discarded.

1. Add a nested struct `BuiltReadiness { let input: AutoregulationEngine.ReadinessInput; let readiness: ReadinessFusionEngine.ReadinessResult; let strain: StrainRiskEngine.StrainRiskResult }`.
2. Rename the current `build(...)` body to `buildDetailed(...)` with the IDENTICAL parameter list and IDENTICAL cold-start/defer logic (both nil guards: the `guard let fatigue = fatigueResult else { return nil }` strain-channel guard, and the `guard hrvZ != nil || rhrZ != nil || sleepZ != nil else { return nil }` honest-confidence gate). At its existing return (~L126) it ALREADY has `readinessResult` (~L95) and `strainResult` (~L115) in scope — return `BuiltReadiness(input: <the ReadinessInput it already builds>, readiness: readinessResult, strain: strainResult)` instead of the bare `ReadinessInput`. NO logic change beyond the return shape.
3. Re-add `build(...)` as a thin delegating wrapper with the UNCHANGED signature `-> AutoregulationEngine.ReadinessInput?`: `buildDetailed(...)?.input`. This keeps Phase-41 `DashboardViewModel.buildDualRunMessage` (the only production caller) and the existing builder tests byte-compatible — same nil-on-cold-start, same ReadinessInput on success.

Conventions: Foundation-only, deterministic, no `.now` baked in (asOf/calendar stay injected). Keep the existing doc comment's "no fabrication / honest-confidence defer" contract intact; add one line noting `buildDetailed` surfaces the fused results for the verdict reason path. NEVER "injury prediction"/"injury risk" (grep-guarded).

REGRESSION (in `PRSReadinessInputBuilderTests.swift`, ADD — do not weaken existing cases):
- Keep every existing cold-start-nil case green for `build(...)`.
- Add: `buildDetailed(...)` returns nil in the SAME cold-start cases (no FatigueResult; all-z-nil) — mirror the existing nil cases.
- Add: on a populated athlete (real-ish recentSnapshots + a FatigueResult so ≥1 personal z is non-nil), `buildDetailed(...)` is non-nil AND `build(...)?.input` equals `buildDetailed(...)?.input` (the delegation is faithful), AND `buildDetailed(...)?.readiness` / `.strain` are the SAME values the internal fold produced (assert `.factors` non-empty / zone matches the recomputed result so the surfaced results are real, not defaulted).
  </action>
  <verify>
    <automated>grep -c "buildDetailed\|struct BuiltReadiness" WorkloadApp/Services/PRSReadinessInputBuilder.swift &amp;&amp; grep -c "buildDetailed" WorkloadAppTests/PRSReadinessInputBuilderTests.swift</automated>
  </verify>
  <done>`PRSReadinessInputBuilder` exposes `buildDetailed(...) -> BuiltReadiness?` surfacing `{ input, readiness, strain }`; `build(...) -> AutoregulationEngine.ReadinessInput?` preserved by delegating to `buildDetailed(...)?.input`. Existing cold-start-nil builder tests stay green; new tests assert delegation faithfulness + that the surfaced readiness/strain are the real fold outputs. `DashboardViewModel.buildDualRunMessage` compiles unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the failing TodayVerdictService test suite (RED)</name>
  <files>WorkloadAppTests/TodayVerdictServiceTests.swift</files>
  <behavior>
    Use the @MainActor XCTest pattern from `PlannedSessionRepositoryTests` (own ModelContainer/ModelContext/PlannedSessionRepository as stored props; set in setUp, clear in tearDown). Build a `PrescribedWorkout` with one strength exercise (muscleGroup .legs) carrying a top set at a known `targetWeightKg` and a `targetRPE`, plus a back-off set.

    Slot-write happy path (VERDICT-01/02/03): drive the service via the injected seam with a `DecisionInput` (or a recommendation) that warrants a MODIFY trim.
    - Test: after `evaluateAndWrite`, the exercise's top `TemplateSet.adjustedTargetWeightKg` is set, plate-rounded, < its `targetWeightKg`, and >= `targetWeightKg` * 0.90 (bound).
    - Test: `verdictReason` is non-nil and a single line.
    - Test: `verdictAppliedAt` is STILL nil and `athleteOverrode` is STILL false (suggestion only — not accepted).

    nil-RPE handling (WARNING-3): build a SECOND exercise whose planned top set has `targetRPE == nil`.
    - Test: after a non-defer write, that set's `adjustedTargetRPE` is STILL nil (the service does NOT emit a bare intensity cap the athlete never planned). The set whose `targetRPE` was non-nil DOES get `adjustedTargetRPE = min(plannedRPE, intensityCap)` (downward-cap only).

    Plan-never-overwritten: the source authored `WorkoutTemplate` (if any) and the original `targetWeightKg`/`targetRPE` are untouched.
    - Test: the frozen `targetWeightKg`/`targetRPE` are unchanged (only the adjusted* slots are written); a separate source template object passed in is not mutated.

    Per-lift region selection:
    - Test: an exercise with `muscleGroup == nil` is treated as `.fullBody` (no crash, still produces a verdict).

    Cold-start defer (locked): drive with the nil-readiness case (service told `decisionInput == nil` / `deferToPlan == true`).
    - Test: adjustedTargetWeightKg == top set `targetWeightKg` (no trim; suggestion equals plan), verdict is go/plan-as-written, `adjustedTargetRPE` is nil (no cap on a defer), and verdictReason is the defer copy.

    Gate-off cross-modal = zero:
    - Test: with the shadow gate at its default (off), the written adjustedTargetWeightKg is IDENTICAL whether a leg-loaded CrossModalResult is supplied or nil (cross-modal contributes zero this ship).

    PRODUCTION reason path (covers the live VERDICT-03 seam, not just injection):
    - Test: drive the PRODUCTION wrapper `evaluateTodaysPlannedSession(athleteId:...)` over a seeded today's `PrescribedWorkout` + a populated athlete history (real-ish snapshots + a FatigueResult so `PRSReadinessInputBuilder.buildDetailed` returns non-nil). Assert it writes a non-nil `verdictReason` AND the reason is derived from the real `DecisionInput` (e.g. the reason is NOT the defer copy; or assert via a small testable assembly helper `makeDecisionInput(built:recommendation:) -> ReasoningEngine.DecisionInput` that the service uses, exercised over the real `BuiltReadiness`). The point: prove the live reason source is wired, not only the hand-injected seam.
    - Test: same wrapper with an EMPTY-history athlete (`buildDetailed` returns nil) writes the defer copy + plan-equal suggestion (cold-start defers end-to-end through the wrapper).
  </behavior>
  <action>Author `TodayVerdictServiceTests` following the @MainActor-repo XCTest pattern. Seed an in-memory ModelContainer including PrescribedWorkout/ExerciseGroup/TemplateExercise/TemplateSet (+ any related @Models the schema needs) and an Athlete + RecoverySnapshot/WorkoutSession history for the production-wrapper tests. Reference the Task-3 API: `TodayVerdictService(modelContext:)` with the injectable seam `func evaluateAndWrite(prescribedWorkout: PrescribedWorkout, decisionInput: ReasoningEngine.DecisionInput?, crossModalResult: CrossModalFatigueEngine.CrossModalResult?, plateStepKg: Double) -> [VerdictResult]` and the production wrapper `func evaluateTodaysPlannedSession(athleteId:recentSnapshots:latestHRV:latestRHR:latestSleepMinutes:allSessions:fatigueResult:daysSinceRest:acwr:acwrZone:asOf:calendar:) -> [VerdictResult]?`. Also reference the assembly helper `makeDecisionInput(built:recommendation:)` so the production reason path is testable at a fine seam. RED until Task 3.</action>
  <verify>
    <automated>test -f WorkloadAppTests/TodayVerdictServiceTests.swift &amp;&amp; grep -c "func test_" WorkloadAppTests/TodayVerdictServiceTests.swift</automated>
  </verify>
  <done>Test file exists with at least 9 `test_` methods covering slot-write, no verdictAppliedAt/athleteOverrode, nil-RPE leaves adjustedTargetRPE nil, source-untouched, nil-muscleGroup region, cold-start defer, gate-off zero cross-modal, AND the production-wrapper reason path (real DecisionInput) + production-wrapper cold-start defer. Uses the @MainActor-repo XCTest lifetime pattern. RED until Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Implement TodayVerdictService — read plan, source the real DecisionInput, write the slots (GREEN)</name>
  <files>WorkloadApp/Services/TodayVerdictService.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
Implement the @MainActor orchestration (mirrors PlannedSessionRepository / pipeline conventions). Public surface:
- `@MainActor final class TodayVerdictService { init(modelContext: ModelContext) }`
- The injectable seam: `func evaluateAndWrite(prescribedWorkout:decisionInput:crossModalResult:plateStepKg:) -> [VerdictResult]`
- The production wrapper: `func evaluateTodaysPlannedSession(athleteId:recentSnapshots:latestHRV:latestRHR:latestSleepMinutes:allSessions:fatigueResult:daysSinceRest:acwr:acwrZone:asOf:calendar:) -> [VerdictResult]?`
- The reason-input assembly helper: `func makeDecisionInput(built: PRSReadinessInputBuilder.BuiltReadiness, recommendation: AutoregulationEngine.TrainingRecommendation) -> ReasoningEngine.DecisionInput` — assembles `DecisionInput(readiness: built.readiness, strainRisk: built.strain, recommendation: recommendation, personalSleepBaselineMinutes: nil)`. This is the SEAM that makes the live reason path real and unit-testable.

Production wrapper flow (sources the live VERDICT-03 reason path):
1. `guard let plan = PlannedSessionRepository(modelContext: modelContext).fetchTodaysPlannedSession(athleteId: athleteId) else { return nil }`.
2. `let built = PRSReadinessInputBuilder.buildDetailed(recentSnapshots:..., fatigueResult:..., wellnessScore: nil, acwr:..., acwrZone:..., asOf:..., calendar:...)` — mirror `DashboardViewModel.buildDualRunMessage` input sourcing exactly.
3. Defer branch: if `built == nil` (cold-start), call `evaluateAndWrite(prescribedWorkout: plan, decisionInput: nil, crossModalResult: <compute or nil>, plateStepKg: ...)` (the nil decisionInput drives deferToPlan inside the seam).
4. Otherwise: `let recommendation = AutoregulationEngine.recommendReadiness(input: built.input)`; `let decisionInput = makeDecisionInput(built: built, recommendation: recommendation)`; `let crossModal = CrossModalFatigueEngine.compute(sessions: allSessions, systemicReadiness: built.readiness.readiness, asOf:..., calendar:...)`; then `evaluateAndWrite(prescribedWorkout: plan, decisionInput: decisionInput, crossModalResult: crossModal, plateStepKg: ...)`.

In `evaluateAndWrite`, for each exercise in `prescribedWorkout.allExercises`:
1. Select the TOP set = the non-warmup `TemplateSet` with the max `targetWeightKg` (skip warmups; skip exercises with no working weight — leave their slots untouched). Pick `region = exercise.muscleGroup?.region ?? .fullBody`.
2. Build `PlannedTopSet` (exerciseName, region, plannedTopSetKg = top set `targetWeightKg`, plannedReps = `targetReps`, plannedRPE = `targetRPE` — may be nil).
3. Defer branch: if `decisionInput == nil` (cold-start), set the suggestion EQUAL to the plan: `adjustedTargetWeightKg = plannedTopSetKg`; `adjustedTargetRPE = nil` (a defer caps nothing); `verdictReason = VerdictReasonBuilder.build(decisionInput: nil, crossModalResult: …, plannedRegion: region, deferToPlan: true).reasonLine`. No trim on a guess (locked).
4. Otherwise: `let recommendation = decisionInput.recommendation`; `let result = TodayVerdictEngine.evaluate(recommendation: recommendation, plannedTopSet: planned, crossModalResult: crossModalResult, plateStepKg: plateStepKg)`. Build the reason via `VerdictReasonBuilder.build(decisionInput: decisionInput, crossModalResult:..., plannedRegion: region, deferToPlan: false)`.
5. WRITE into the top set's slots:
   - `adjustedTargetWeightKg = result.adjustedTopSetKg`.
   - `adjustedTargetRPE` — NIL-RPE RULE (WARNING-3): if `topSet.targetRPE == nil`, leave `adjustedTargetRPE = nil` (do NOT emit a bare `intensityCap` the athlete never planned). Only when `topSet.targetRPE` is non-nil: `adjustedTargetRPE = min(plannedRPE, recommendation.intensityCap)` (downward-cap only — reuse the cap posture from `PRSDualRunSurface.adjust`; never raise above the planned RPE).
   - `verdictReason = reason.reasonLine`.
   - DO NOT set `verdictAppliedAt` or `athleteOverrode` (Phase 44). If `result.volumeCutSets != nil`, encode the back-off-set guidance in the reason text or a trailing clause (do NOT delete sets — suggestion only). Accumulate the `VerdictResult` into the return array.
6. `try? modelContext.save()` once after the loop. The frozen PrescribedWorkout is the only thing mutated — the source authored template is never touched (it was deep-copied at plan time). Cross-modal passes straight through to the engines, which zero it out while the gate is off (no special-casing here).

PERIODIZATION-POSITION DEFERRAL (WARNING-2 — conscious, not silent): ROADMAP SC1's "driven by … periodization position where a plan position is known" sub-clause is NOT modeled this phase — there is no Phase-42 plan-position field to read. Add a `// DEFERRED:` doc comment at the top of the wrapper: "periodization-position input is not yet modeled; the verdict reads it when present — deferred until plan-position data lands." Build no scaffolding for it; just record the conscious deferral so it is not a silent drop.

Conventions: Tuwa voice, NEVER "injury prediction"/"injury risk" (grep-guarded). Register the new app-target file with the 4 explicit pbxproj entries (mirror PlannedSessionRepository.swift's quad — note it is a Repository-style @MainActor class registered as a Service). Use a fresh unique object-id pair. No " 2.swift" duplicate. (PRSReadinessInputBuilder.swift is ALREADY registered — do not add pbxproj entries for it.)
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&amp;1 | tail -3</automated>
  </verify>
  <done>`** BUILD SUCCEEDED **`. `TodayVerdictService` exists with the injectable `evaluateAndWrite` seam, the production wrapper, and the `makeDecisionInput` assembly helper that sources the real `DecisionInput` from `PRSReadinessInputBuilder.buildDetailed`. Writes adjustedTargetWeightKg/adjustedTargetRPE/verdictReason only; adjustedTargetRPE stays nil when the planned RPE was nil; never sets verdictAppliedAt/athleteOverrode; reuses buildDetailed for the defer signal. Periodization-position deferral recorded in a `// DEFERRED:` comment. pbxproj has 4 NEW entries (for TodayVerdictService.swift only); no " 2.swift" duplicate.</done>
</task>

<task type="auto">
  <name>Task 4: Run the suites GREEN + full-phase build + slot-write/reason-source fences</name>
  <files>WorkloadAppTests/TodayVerdictServiceTests.swift</files>
  <action>Run `TodayVerdictServiceTests` AND `PRSReadinessInputBuilderTests` on the known-alive sim using the @MainActor-repo lifetime pattern. Confirm all pass (RED to GREEN); fix the SERVICE/BUILDER if a test fails, not the test. Then run the FULL Phase-43 engine+service+builder suites together to confirm no cross-plan regression: `-only-testing:WorkloadAppTests/TodayVerdictEngineTests -only-testing:WorkloadAppTests/VerdictReasonBuilderTests -only-testing:WorkloadAppTests/TodayVerdictServiceTests -only-testing:WorkloadAppTests/PRSReadinessInputBuilderTests`. Confirm the fences hold: `verdictAppliedAt` nil + `athleteOverrode` false after a write; adjustedTargetWeightKg within the −10% bound; adjustedTargetRPE nil when planned RPE was nil; cold-start writes plan-equal suggestion + defer copy; gate-off cross-modal identical; the PRODUCTION wrapper writes a reason derived from the real DecisionInput (buildDetailed-sourced), not the defer copy, on a populated athlete. Add the `test_service_neverSaysInjuryPrediction_sourceGrep` fence (over both TodayVerdictService.swift and the new PRSReadinessInputBuilder additions) if not already present.</action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/TodayVerdictServiceTests -only-testing:WorkloadAppTests/PRSReadinessInputBuilderTests 2>&amp;1 | tail -5</automated>
  </verify>
  <done>`** TEST SUCCEEDED **` for TodayVerdictServiceTests + PRSReadinessInputBuilderTests (and the combined Phase-43 suites). XCTest host did not crash. Slot-write writes the suggestion only; verdictAppliedAt/athleteOverrode untouched; nil-RPE leaves adjustedTargetRPE nil; cold-start defers; gate-off cross-modal contributes zero; the production reason path is sourced from the real DecisionInput (not injected-only). Existing PRSReadinessInputBuilder cold-start-nil + DashboardViewModel fences stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readiness builder → recommendation | Cold-start nil from PRSReadinessInputBuilder.buildDetailed must force the defer path, not a fabricated trim |
| builder → reason path | The fused ReadinessResult/StrainRiskResult surfaced by buildDetailed must be the REAL fold outputs (not defaulted) so the production reason is honest |
| service → frozen plan slots | The service writes ONLY the frozen PrescribedWorkout's verdict slots — the authored template is out of bounds |
| suggestion vs acceptance | Writing the suggestion must not impersonate acceptance (verdictAppliedAt/athleteOverrode reserved for Phase 44) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | Tampering | source authored template | mitigate | Service only mutates the frozen deep-copy's TemplateSets; test asserts source template + frozen `targetWeightKg`/`targetRPE` unchanged |
| T-43-08 | Repudiation | suggestion mistaken for acceptance | mitigate | `verdictAppliedAt`/`athleteOverrode` are NEVER set here; test asserts they stay nil/false post-write |
| T-43-09 | Spoofing | fabricated trim on cold-start | mitigate | nil readiness ⇒ adjustedTargetWeightKg == plannedTopSetKg + defer copy + adjustedTargetRPE nil; test asserts plan-equal suggestion through the production wrapper too |
| T-43-10 | Elevation of privilege | cross-modal influencing written number | mitigate | Cross-modal passes through to gate-guarded engines; gate-off-identical test asserts zero influence |
| T-43-11 | Information disclosure | unsourced/fabricated reason | mitigate | Production reason is assembled from the REAL DecisionInput (buildDetailed-sourced readiness+strain factors), not injected-only; production-wrapper test asserts the live reason is not the defer copy on a populated athlete |
| T-43-12 | Tampering | breaking the Phase-41 build() contract | mitigate | `build(...)` keeps its `-> ReadinessInput?` signature by delegating to `buildDetailed(...)?.input`; regression test asserts byte-equal ReadinessInput + DashboardViewModel compiles unchanged |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Swift + existing frameworks) — no new dependency surface |
</threat_model>

<verification>
- App builds green on sim `CAF84E71-BB64-491D-87C8-875A0143B26D`.
- `TodayVerdictServiceTests` + `PRSReadinessInputBuilderTests` (and combined Phase-43 suites) pass; XCTest host does not crash.
- `grep -n "TodayVerdictService.swift" "workload management/workload management.xcodeproj/project.pbxproj"` shows exactly 4 entries (PRSReadinessInputBuilder.swift already registered — entry count unchanged for it).
- `PRSReadinessInputBuilder.buildDetailed` exists and `build(...)` delegates to it: `grep -c "buildDetailed(.*)?\.input\|buildDetailed" WorkloadApp/Services/PRSReadinessInputBuilder.swift` > 0.
- Slots written are exactly adjustedTargetWeightKg/adjustedTargetRPE/verdictReason: `grep -c "verdictAppliedAt\|athleteOverrode" WorkloadApp/Services/TodayVerdictService.swift` returns 0 (the service never references them).
- nil-RPE handling present: the service guards `targetRPE` before writing `adjustedTargetRPE` (covered by `test_` assertion, not just grep).
- No injury-prediction language: `grep -iv '^//' WorkloadApp/Services/TodayVerdictService.swift | grep -ci "injury prediction\|injury risk"` returns 0; same over the PRSReadinessInputBuilder additions.
- Periodization-position deferral is consciously recorded: `grep -c "DEFERRED" WorkloadApp/Services/TodayVerdictService.swift` > 0.
</verification>

<success_criteria>
- VERDICT-01/02/03 land as a writable suggestion: verdict + adjusted plate-rounded number + one-line reason written into the Phase-42 slots on today's planned session.
- The production reason path (VERDICT-03 / SC3) is SOURCED from the real DecisionInput via `PRSReadinessInputBuilder.buildDetailed` — not test-injected-only.
- The suggestion is written without setting verdictAppliedAt/athleteOverrode; the source template is never overwritten; adjustedTargetRPE stays nil when the planned RPE was nil.
- Cold-start defers (plan-equal suggestion + defer copy) end-to-end through the production wrapper; cross-modal contributes zero while the gate is off.
- Periodization-position input is consciously deferred (not silently dropped) until plan-position data lands.
- The Phase-41 `build(...)` contract and `DashboardViewModel.buildDualRunMessage` stay green.
- Commit on `main`, never a new branch.
</success_criteria>

<output>
Create `.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-03-SUMMARY.md` when done.
</output>
