---
phase: 43-today-verdict-engine-go-modify-hold-adjusted-number-reason
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Services/TodayVerdictEngine.swift
  - WorkloadAppTests/TodayVerdictEngineTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [VERDICT-01, VERDICT-02]

must_haves:
  truths:
    - "A planned strength session is collapsed into exactly one of go / modify / hold from the existing recommendation's intensityCap + volumeModifier + sessionType — no 4th decision model"
    - "When the day warrants a trim, the engine produces a concrete adjusted top-set weight bounded to −5% default / −10% ceiling, volume-cut preferred over load-cut, rounded to loadable plates"
    - "An adjusted weight is never rounded UP past the planned weight, and a sub-increment delta collapses to GO (no false change)"
    - "Cross-modal fatigue contributes ZERO to the number while CrossModalShadowGate.crossModalDrivesVerdict is false (the shipped default)"
  artifacts:
    - path: "WorkloadApp/Services/TodayVerdictEngine.swift"
      provides: "Pure-struct verdict engine: trichotomy collapse + bounded plate-rounded adjusted top-set number"
      min_lines: 90
      contains: "struct TodayVerdictEngine"
    - path: "WorkloadAppTests/TodayVerdictEngineTests.swift"
      provides: "Tests for mapping, bounds, plate-rounding, volume-cut preference, gate-off cross-modal=0"
      contains: "func test_"
  key_links:
    - from: "WorkloadApp/Services/TodayVerdictEngine.swift"
      to: "AutoregulationEngine.TrainingRecommendation"
      via: "reads intensityCap / volumeModifier / sessionType"
      pattern: "intensityCap|volumeModifier|sessionType"
    - from: "WorkloadApp/Services/TodayVerdictEngine.swift"
      to: "WeightFormatter.snapToIncrement"
      via: "plate rounding of the adjusted weight"
      pattern: "WeightFormatter\\.snapToIncrement"
    - from: "WorkloadApp/Services/TodayVerdictEngine.swift"
      to: "CrossModalShadowGate.crossModalDrivesVerdict"
      via: "gate read — off ⇒ cross-modal factor forced to 1.0"
      pattern: "crossModalDrivesVerdict"
---

<objective>
Build the pure `TodayVerdictEngine` core: collapse the EXISTING `AutoregulationEngine.recommendReadiness` output into a go / modify / hold trichotomy (VERDICT-01) and compute a concrete, evidence-bounded, plate-rounded adjusted top-set number / back-off volume cut (VERDICT-02). This is a DERIVED engine — a pure function of the recommendation already produced by Phase 41's activated pipeline, never a new decision model.

Purpose: the verdict is the v2.0 wedge's decision core. It must be glass-box, deterministic, and conservative — the math the Phase 44 UI card and the Phase 43-03 slot-writer both consume.
Output: `TodayVerdictEngine.swift` (pure struct, static methods) + its test suite, both building green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-CONTEXT.md
@.planning/research/v2-verdict-engine.md

<read_first>
Read these BEFORE writing any code — the engine is a thin derivation over real signatures:
- @WorkloadApp/Services/AutoregulationEngine.swift — the `TrainingRecommendation { intensityCap: Double /*max RPE 1-10*/, volumeModifier: Double /*1.0=full…0.0*/, sessionType, warnings, headline, detail }` and its `RecommendedSessionType` cases (power/strength/hypertrophy/conditioning/activeRecovery/rest). This struct is the SOLE decision input — the verdict does not call recommendReadiness itself; it RECEIVES a recommendation.
- @WorkloadApp/Services/PRSDualRunSurface.swift — `adjust(...)` (lines ~60-90) ALREADY caps RPE downward at `intensityCap` and scales volume by `volumeModifier`, plus `derivedBaseVolume(_:)`. REUSE this shape/posture; do not reinvent autoregulation. The verdict computes the *planned-top-set number*, which `adjust` does not (it scales an aggregate volume target). They are complementary.
- @WorkloadApp/Utilities/WeightFormatter.swift — `snapToIncrement(_ value: Double, to step: Double) -> Double` is the PUBLIC plate-rounding primitive (ProgressionEngine.roundToNearest is private — do not try to call it). `displayValue`/`toKg` handle kg/lb conversion. Plate step is in the SAME unit you pass.
- @WorkloadApp/Services/CrossModalShadowGate.swift — `static var crossModalDrivesVerdict: Bool { _override ?? false }` (default FALSE) + the test-only `withEnabled(_:_:)` helper. The engine READS this gate; gate off ⇒ cross-modal must contribute exactly 1.0 (zero effect).
- @WorkloadApp/Services/CrossModalFatigueEngine.swift — `CrossModalResult.exerciseAdjustment(forRegion: MuscleRegion) -> Double` in (0,1]; `MuscleRegion` cases legs/back/chest/shoulders/arms/core/fullBody. The engine accepts an OPTIONAL `CrossModalResult` + a target `MuscleRegion` so the gate-on path is wired now without re-architecture, but multiplies by 1.0 when the gate is off OR the result is nil.
- @WorkloadApp/Models/Enums.swift (line 41 WeightUnit; 177-211 MuscleRegion; 457-475 StrainRiskZone; 502+ ReadinessZone) — enum cases you will switch on.
</read_first>

<interfaces>
<!-- Real signatures the executor consumes directly — no codebase exploration needed. -->

From AutoregulationEngine.swift:
```swift
struct TrainingRecommendation {
    let intensityCap: Double         // Max RPE (1-10)
    let volumeModifier: Double       // 1.0 = full, 0.5 = half
    let sessionType: RecommendedSessionType
    let warnings: [Warning]
    let headline: String
    let detail: String
    enum RecommendedSessionType: String { case power, strength, hypertrophy, conditioning, activeRecovery, rest }
}
```

From WeightFormatter.swift:
```swift
static func snapToIncrement(_ value: Double, to step: Double) -> Double   // (value/step).rounded()*step
static func displayValue(_ kg: Double, unit: WeightUnit) -> Double
static func toKg(_ value: Double, from unit: WeightUnit) -> Double
```

From CrossModalShadowGate.swift:
```swift
static var crossModalDrivesVerdict: Bool { _override ?? false }   // default false
static func withEnabled<T>(_ value: Bool, _ body: () throws -> T) rethrows -> T   // TEST-ONLY
```

From CrossModalFatigueEngine.swift:
```swift
struct CrossModalResult: Equatable {
    func exerciseAdjustment(forRegion region: MuscleRegion) -> Double   // (0,1]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing TodayVerdictEngine test suite (RED)</name>
  <files>WorkloadAppTests/TodayVerdictEngineTests.swift</files>
  <behavior>
    Tests assert against the engine's public API designed in this task. Define the suite FIRST so the contract is explicit before implementation (RED then GREEN).

    Verdict mapping (VERDICT-01) — feed a synthetic `TrainingRecommendation` (construct via the AutoregulationEngine output shape) + a planned top set and assert the trichotomy:
    - Test: full recommendation (intensityCap at least the planned RPE, volumeModifier at least 0.95, sessionType strength/power/hypertrophy) AND adjusted weight at the planned weight then verdict == .go.
    - Test: volumeModifier in [0.75, 0.95) (back-off-set cut) with intensityCap near planned then verdict == .modify, loadFactor near 1.0, a non-nil volumeCutSets (at least 1 back-off set), adjusted weight == planned.
    - Test: a load trim that moves the adjusted weight at least one plate step below planned then verdict == .modify with adjustedTopSetKg < plannedTopSetKg.
    - Test: sessionType == .rest OR .activeRecovery then verdict == .hold, and the HOLD carries a number (top set held at planned weight, no progression) — NOT a nil/"don't train".

    Bounds + plate rounding (VERDICT-02):
    - Test: a recommendation implying a deep cut still floors the LOAD trim at −10% (ceiling) of plannedTopSetKg — adjustedTopSetKg at least plannedTopSetKg * 0.90.
    - Test: default trim magnitude is −5% when the recommendation says "modify but mild" (document the trigger in the action).
    - Test: adjustedTopSetKg is always a multiple of the loadable plate step (e.g. 2.5 kg) via snapToIncrement, and is NEVER rounded UP past plannedTopSetKg.
    - Test: when (plannedTopSetKg − rawAdjusted) < plate step, the engine returns .go with adjustedTopSetKg == plannedTopSetKg (no false-precision micro-change).
    - Test: volume-cut-preferred — when readiness is only mildly down (volumeModifier in [0.85,0.95), intensityCap near planned), the engine prefers cutting a back-off set (volumeCutSets non-nil) and keeps the top-set load unchanged.

    Cross-modal gate (forward-compatible; this ship = zero effect):
    - Test: with `CrossModalShadowGate` at its default (off) and a NON-nil `CrossModalResult` that would trim legs, the adjustedTopSetKg is IDENTICAL to the same call with `crossModalResult: nil`. (Proves gate-off then cross-modal contributes exactly 0.)
    - Test: inside `CrossModalShadowGate.withEnabled(true) { ... }` with a leg-loaded `CrossModalResult` for a `.legs` target, the adjusted weight is at most the gate-off adjusted weight (the future wiring activates with no re-architecture). Keep this test tolerant (use a `<=` comparison, not exact) since magnitudes are heuristic.
  </behavior>
  <action>Author `TodayVerdictEngineTests` as an XCTestCase mirroring the engine-test style in `AutoregulationEngineTests.swift` / `CrossModalFatigueEngineTests.swift` (pure value tests, no ModelContainer needed — this engine is Foundation-only). Build `TrainingRecommendation` fixtures directly via its memberwise init. Reference the public types `TodayVerdictEngine.Verdict` (enum: go/modify/hold), `TodayVerdictEngine.PlannedTopSet` (input: exerciseName, region, plannedTopSetKg, plannedReps, plannedRPE), and `TodayVerdictEngine.VerdictResult` (output: verdict, adjustedTopSetKg, volumeCutSets, loadFactor) — these are DEFINED in Task 2; the suite failing to compile is the expected RED state. Use the `CrossModalShadowGate.withEnabled` helper for the gate-on test. Plate step in tests: 2.5 kg. This task ends in RED (compile failure is the acceptable RED marker since the engine type does not exist yet).</action>
  <verify>
    <automated>test -f WorkloadAppTests/TodayVerdictEngineTests.swift &amp;&amp; grep -c "func test_" WorkloadAppTests/TodayVerdictEngineTests.swift</automated>
  </verify>
  <done>Test file exists with at least 10 `test_` methods covering mapping, bounds, plate-rounding, volume-cut preference, and gate-off-identical / gate-on at-most cross-modal behavior. Compiles only after Task 2 (RED until then).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TodayVerdictEngine — trichotomy + bounded plate-rounded number (GREEN)</name>
  <files>WorkloadApp/Services/TodayVerdictEngine.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <behavior>
    Pure struct, static methods, Foundation-only (mirrors AutoregulationEngine convention — NO @MainActor, no SwiftData, no Date.now baked in; inject anything date-dependent). Public surface:
    - `enum Verdict { case go, modify, hold }`
    - `struct PlannedTopSet { let exerciseName: String; let region: MuscleRegion; let plannedTopSetKg: Double; let plannedReps: Int?; let plannedRPE: Double? }`
    - `struct VerdictResult: Equatable { let verdict: Verdict; let adjustedTopSetKg: Double; let volumeCutSets: Int?; let loadFactor: Double }` (volumeCutSets nil = no back-off cut; loadFactor is the applied multiplier in [0.90,1.00])
    - `enum Constants` (the SINGLE home for tunables, mirroring CrossModalFatigueEngine.Constants): `defaultLoadTrim = 0.05`, `maxLoadTrim = 0.10`, `plateStepKg = 2.5`, and the modify/hold thresholds.
    - `static func evaluate(recommendation:plannedTopSet:crossModalResult:plateStepKg:) -> VerdictResult`
  </behavior>
  <action>
Implement the derivation exactly per the locked decisions and research §2.2/§3.1:

1. Derive a `loadFactor` from the recommendation. Map `volumeModifier`/`intensityCap`/`sessionType` to a discrete, documented trim:
   - sessionType in {rest, activeRecovery} then HOLD path: loadFactor 1.0, hold the planned weight (no progression), volumeCutSets nil, verdict .hold. Document that HOLD is "the number" (planned top set held), per the nocebo guard — never a nil/"don't train".
   - Else if volumeModifier at least 0.95 AND intensityCap implies no load reduction then candidate GO: loadFactor 1.0.
   - Else if the recommendation is mildly down (volumeModifier in [0.85, 0.95)) and intensity is near-planned then VOLUME-CUT-PREFERRED: loadFactor 1.0, volumeCutSets = derived back-off cut (e.g. round((1 − volumeModifier) * plannedBackoffSets); at minimum 1 when volumeModifier < 0.95). Keep top-set load unchanged. This is the "keep the top set, cut a back-off set" branch (research §2.1 bottom-line).
   - Else (clearly down: volumeModifier < 0.85 OR a real intensity cut) then LOAD trim: loadFactor = clamp between (1 − maxLoadTrim) and (1 − defaultLoadTrim) based on how far volumeModifier is below 0.85. NEVER below 1 − maxLoadTrim (−10% ceiling). Document the interpolation in a comment with the bound.
2. Compute `rawAdjusted = plannedTopSetKg * loadFactor`. Round to the plate step: snap via `WeightFormatter.snapToIncrement(rawAdjusted, to: plateStepKg)`, then if the snapped value > plannedTopSetKg when trimming, floor it to plannedTopSetKg (NEVER round up past the plan when trimming — guardrail from research §2.3). adjustedTopSetKg is in kg (storage unit).
3. Apply cross-modal as a multiplicative factor on loadFactor ONLY when `CrossModalShadowGate.crossModalDrivesVerdict == true` AND `crossModalResult != nil`: `effectiveFactor = loadFactor * crossModalResult.exerciseAdjustment(forRegion: plannedTopSet.region)`, re-clamped to at least (1 − maxLoadTrim). When the gate is off OR result is nil, multiply by exactly 1.0 (cross-modal contributes zero — the locked "gate OFF ⇒ cross-modal contributes ZERO this ship"). Recompute the snapped weight from effectiveFactor.
4. Collapse to the verdict (research §3.1): if (plannedTopSetKg − adjustedTopSetKg) < plateStepKg AND volumeCutSets == nil AND sessionType not rest/activeRecovery then .go (adjustedTopSetKg = plannedTopSetKg, no false change). Else if sessionType in {rest, activeRecovery} then .hold. Else then .modify.
5. Use `plateStepKg` parameter (default Constants.plateStepKg) so lb-unit callers can pass a converted step later; the engine works in kg internally per storage convention. Add `///` doc comments naming every constant as a documented threshold and stating the −5%/−10% bound and the volume-cut-preference rule. NEVER emit fractional-percent — the only number out is a plate-snapped kg weight.

Register the new app-target file with the 4 explicit pbxproj entries (the app target is NOT a synchronized group). Mirror the CrossModalShadowGate.swift quad EXACTLY:
- PBXBuildFile (in the `Begin PBXBuildFile section`, around line 159): `… /* TodayVerdictEngine.swift in Sources */ = {isa = PBXBuildFile; fileRef = … /* TodayVerdictEngine.swift */; };`
- PBXFileReference (in `Begin PBXFileReference section`, around line 341): `… /* TodayVerdictEngine.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = TodayVerdictEngine.swift; sourceTree = "<group>"; };`
- Services group child (around line 560, the `Services` group children list)
- Sources build phase entry (around line 1053)
Use a fresh unique object-id pair (do NOT reuse the AC4103 ids). Do NOT leave any " 2.swift" duplicate file.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&amp;1 | tail -3</automated>
  </verify>
  <done>`** BUILD SUCCEEDED **`. `TodayVerdictEngine` exists as a pure struct with `evaluate(...)`, the four required types, and a `Constants` enum documenting −5%/−10% bounds. pbxproj has exactly one fileRef + 4 entries; no " 2.swift" duplicate. Cross-modal read is gate-guarded.</done>
</task>

<task type="auto">
  <name>Task 3: Run the suite GREEN + lock gate-off / no-injury fences</name>
  <files>WorkloadAppTests/TodayVerdictEngineTests.swift</files>
  <action>Run the TodayVerdictEngine tests on the known-alive sim. Confirm all pass (RED to GREEN). If any bound/mapping test fails, fix the ENGINE (Task 2) to satisfy the documented contract — do not weaken the test to pass. Add (if not already present from Task 1) a source-grep fence test `test_verdictEngine_neverSaysInjuryPrediction_sourceGrep` that reads `TodayVerdictEngine.swift` via `#filePath` and asserts it contains neither "injury prediction" nor "injury risk" (mirrors the existing grep-guard tests in `ReasoningDecisionExplanationTests` / `CrossModalShadowArmTests`). Confirm the gate-off-identical test passes byte-for-byte (adjustedTopSetKg equal with crossModalResult nil vs non-nil while the gate is off).</action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" &amp;&amp; xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/TodayVerdictEngineTests 2>&amp;1 | tail -5</automated>
  </verify>
  <done>`** TEST SUCCEEDED **` with all TodayVerdictEngine cases green (mapping, bounds, plate-rounding, volume-cut preference, gate-off-identical, gate-on at-most, no-injury-prediction grep). The XCTest host did not crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recommendation → verdict | The verdict trusts the recommendation's `intensityCap`/`volumeModifier`/`sessionType` as already-validated engine output; no external/untrusted input crosses here |
| cross-modal gate → number | The gate is the trust fence: cross-modal influence on the user-facing number is forbidden until `crossModalDrivesVerdict` flips (a future human pass) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | adjusted-number bounds | mitigate | Hard-clamp loadFactor to ≥ (1 − maxLoadTrim); test asserts adjustedTopSetKg ≥ plannedTopSetKg × 0.90 so no path can over-trim |
| T-43-02 | Information disclosure | verdict copy | mitigate | No "injury prediction"/"injury risk" language (source-grep fence test); strain framed as load-tolerance only |
| T-43-03 | Elevation of privilege | cross-modal driving the number prematurely | mitigate | Cross-modal factor applied ONLY when `crossModalDrivesVerdict == true`; gate-off-identical test proves zero influence at the shipped default |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure Swift on existing frameworks) — no new dependency surface |
</threat_model>

<verification>
- App builds green on sim `CAF84E71-BB64-491D-87C8-875A0143B26D`.
- `TodayVerdictEngineTests` all pass; XCTest host does not crash.
- `grep -c "struct TodayVerdictEngine" WorkloadApp/Services/TodayVerdictEngine.swift` ≥ 1.
- `grep -n "TodayVerdictEngine.swift" "workload management/workload management.xcodeproj/project.pbxproj"` shows exactly 4 entries.
- No injury-prediction language: `grep -iv '^//' WorkloadApp/Services/TodayVerdictEngine.swift | grep -ci "injury prediction\|injury risk"` returns 0.
</verification>

<success_criteria>
- VERDICT-01: a deterministic, documented go/modify/hold mapping derived purely from `intensityCap`/`volumeModifier`/`sessionType` — verified by mapping tests.
- VERDICT-02: a concrete adjusted top-set number bounded −5% default / −10% ceiling, volume-cut preferred over load-cut, plate-rounded, never rounded up past plan — verified by bounds + plate-rounding + volume-cut tests.
- Cross-modal is wired through `CrossModalShadowGate` but contributes exactly zero at the shipped default (gate-off-identical test).
- Commit on `main`, never a new branch.
</success_criteria>

<output>
Create `.planning/phases/43-today-verdict-engine-go-modify-hold-adjusted-number-reason/43-01-SUMMARY.md` when done.
</output>
