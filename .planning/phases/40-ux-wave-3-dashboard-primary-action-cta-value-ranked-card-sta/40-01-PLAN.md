---
phase: 40-ux-wave-3-dashboard-primary-action-cta-value-ranked-card-sta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - "V152-UX-SPEC.md §C"

must_haves:
  truths:
    - "A recommendation-aware primary-action CTA is visible directly below the hero readiness score (not only in the toolbar)."
    - "Tapping the CTA presents ActiveWorkoutSheet (sets showActiveWorkout = true) — logging is never blocked, regardless of label."
    - "The CTA label adapts to viewModel.recommendation (train vs lighter/rest variant); nil recommendation falls back to neutral 'Log Workout'."
    - "The CTA uses a dominant non-accent treatment (text1 fill + background-color label); no accent color anywhere except the hero score."
    - "For established users (hasRealData == true) the stack orders: hero → CTA → recommendation → load context (TrainingLoadSection) + FatigueAttentionBanner near top → MetricsStrip → WeeklySummary → RecentSessions, with setup/prompt cards demoted below the fold."
    - "For cold-start users (hasRealData == false) the relevant setup/connect prompts stay prominent (HealthKit empty-states, TrainingProfileCard, WelcomeActionCard)."
    - "Every conditional card present before the reorder is still present after, with its render guard preserved (nothing dropped)."
    - "PRSDualRunCard remains flag-gated / EmptyView exactly as before (flag untouched)."
    - "Niggle log button stays at the bottom."
    - "New strings localized en + zh-Hans; build green on iPhone 17 Pro sim; regression gate clean on edited files."
  artifacts:
    - path: "WorkloadApp/Views/Dashboard/DashboardView.swift"
      provides: "Reordered body VStack + recommendation-aware PrimaryActionCTA subview"
      contains: "showActiveWorkout = true"
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "CTA label strings (en + zh-Hans)"
  key_links:
    - from: "PrimaryActionCTA (DashboardView)"
      to: "showActiveWorkout state → .sheet ActiveWorkoutSheet()"
      via: "button action sets showActiveWorkout = true"
      pattern: "showActiveWorkout = true"
    - from: "PrimaryActionCTA label"
      to: "viewModel.recommendation"
      via: "label mapping from recommendation.sessionType / nil-fallback"
      pattern: "viewModel.recommendation"
---

<objective>
Implement V152-UX-SPEC §C on the Dashboard (DashboardView.swift): (C.1) add a recommendation-aware primary-action CTA directly below the hero readiness score, and (C.2) value-rank the Dashboard card stack so the established no-coach user gets "am I ready + what do I do + act" immediately, while cold-start users keep their setup/connect prompts prominent.

Purpose: This is the last phase of v1.5.2 UX flow rework — it makes the daily readiness answer and the primary action immediate, and demotes setup noise once the user is established.

Output: A new `PrimaryActionCTA` subview + a reordered `body` VStack in DashboardView.swift, plus CTA label strings in Localizable.xcstrings (en + zh-Hans). NO algorithm/flag/engine/ViewModel-logic change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-ux-wave-3-dashboard-primary-action-cta-value-ranked-card-sta/40-CONTEXT.md
@.planning/v1.5-audit/V152-UX-SPEC.md
@DESIGN.md
@./CLAUDE.md
@WorkloadApp/Views/Dashboard/DashboardView.swift
@WorkloadApp/Views/Dashboard/WelcomeActionCard.swift
@WorkloadApp/Components/CardStyle.swift

<interfaces>
<!-- Contracts the executor needs. Use directly — no extra codebase exploration required. -->

AutoregulationEngine.TrainingRecommendation (READ ONLY — do NOT modify AutoregulationEngine.swift):
```swift
struct TrainingRecommendation {
    let intensityCap: Double
    let volumeModifier: Double
    let sessionType: RecommendedSessionType   // .power/.strength/.hypertrophy/.conditioning/.activeRecovery/.rest
    let warnings: [Warning]
    let headline: String                      // already shown in HeroReadinessCard footer (line ~356)
    let detail: String
}
enum RecommendedSessionType: String {
    case power, strength, hypertrophy, conditioning, activeRecovery, rest
}
```

DashboardViewModel (READ ONLY for this plan — no new computed properties):
```swift
var recommendation: AutoregulationEngine.TrainingRecommendation?   // line ~30
var hasRealData: Bool                                               // line ~48
```

Dominant non-accent CTA style (canonical reuse — from OnboardingView.swift lines 243-253):
```swift
Text(<label>)
    .font(.Tokens.bodyMedium)
    .foregroundStyle(ColorTokens.background)   // label reads on the fill
    .frame(maxWidth: .infinity)
    .padding(.vertical, Spacing.sm)            // 16pt
    .background(ColorTokens.text1)             // dominant fill — NOT accent
    .overlay(Rectangle().stroke(ColorTokens.text1, lineWidth: 0.5))
```

CardStyle primitives (CardStyle.swift): Spacing.xs=8, Spacing.sm=16, Spacing.md=24, Spacing.lg=32; .cardStyle(); SectionContainer; SectionHeader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add recommendation-aware PrimaryActionCTA subview</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - WorkloadApp/Views/Dashboard/DashboardView.swift (current body ~48-291; HeroReadinessCard ~295-406; toolbar ~238-246; .sheet showActiveWorkout ~247-249)
    - WorkloadApp/Services/AutoregulationEngine.swift (TrainingRecommendation.RecommendedSessionType cases — derive label mapping; DO NOT EDIT this file)
    - WorkloadApp/Views/Dashboard/WelcomeActionCard.swift (existing dashboard.action.logWorkout usage; this card's own Log button must stay)
    - DESIGN.md (accent = hero score ONLY; CTA must NOT use accent; 0pt corners, no shadow, Font.Tokens, 8pt grid)
    - WorkloadApp/Components/CardStyle.swift (Spacing tokens)
    - <interfaces> block above (dominant text1-fill CTA style)
  </read_first>
  <action>
    Add a private `PrimaryActionCTA` View struct in DashboardView.swift (near HeroReadinessCard). It takes `let recommendation: AutoregulationEngine.TrainingRecommendation?` and `let onTap: () -> Void`.

    Label mapping (trivial view-side mapping ONLY — no new ViewModel property, no engine call): derive a localized label key from `recommendation?.sessionType`:
      - .rest → "dashboard.cta.logRestDay" (e.g. "Log a rest day")
      - .activeRecovery → "dashboard.cta.logLightSession" (e.g. "Log a light session")
      - .power/.strength/.hypertrophy/.conditioning → "dashboard.cta.startSession" (e.g. "Start session")
      - recommendation == nil → "dashboard.cta.logWorkout" neutral fallback (reuse text identical to existing "dashboard.action.logWorkout" value but a distinct key is fine; planner discretion — you MAY reuse the existing dashboard.action.logWorkout key for the nil case to avoid a redundant string).

    Style: use the dominant non-accent treatment from <interfaces> (text1 fill, ColorTokens.background label, .font(.Tokens.bodyMedium), .frame(maxWidth: .infinity), .padding(.vertical, Spacing.sm), Rectangle overlay stroke, 0pt corners). The whole thing is a Button { onTap() } with .buttonStyle(.plain). Wrap with .padding(.horizontal, Spacing.sm) and a small top gap (Spacing.xs) so it sits cleanly under the hero. NO ColorTokens.accent anywhere. The action ALWAYS calls onTap (logging never blocked) regardless of label.

    Add an .accessibilityLabel reflecting the chosen label.

    Add the 3 new string keys (dashboard.cta.startSession, dashboard.cta.logLightSession, dashboard.cta.logRestDay; plus dashboard.cta.logWorkout if you chose a distinct nil key) to Localizable.xcstrings with en + zh-Hans translations. Match the existing dashboard.* key style and the catalog's source-language/translations JSON shape (see existing dashboard.action.logWorkout entry as the template). Do NOT add the CTA into the body yet — that wiring happens in Task 2.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus" && xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `PrimaryActionCTA` struct exists in DashboardView.swift, takes `recommendation` + `onTap`.
    - grep confirms NO `ColorTokens.accent` inside the new subview; CTA fill is `ColorTokens.text1`.
    - grep confirms 0 `RoundedRectangle` / `.cornerRadius` / `.shadow` / `.system(` introduced in this file.
    - All 3 (or 4) new string keys present in Localizable.xcstrings with both "en" and "zh-Hans" translations.
    - Build green on iPhone 17 Pro sim (id CAF84E71-BB64-491D-87C8-875A0143B26D).
  </acceptance_criteria>
  <done>PrimaryActionCTA subview compiles with recommendation-aware label mapping + dominant non-accent style; new CTA strings localized en+zh-Hans; build green. CTA not yet placed in body.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Value-rank the body VStack + wire the CTA (context-aware)</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift</files>
  <read_first>
    - WorkloadApp/Views/Dashboard/DashboardView.swift (FULL current body 48-291 — every conditional block + its render guard must be inventoried before reordering)
    - .planning/phases/40-ux-wave-3-dashboard-primary-action-cta-value-ranked-card-sta/40-CONTEXT.md (C.2 LOCKED ordering + context-aware demotion rules)
    - .planning/v1.5-audit/V152-UX-SPEC.md (§C item 2)
    - WorkloadApp/ViewModels/DashboardViewModel.swift (hasRealData ~48; recommendation ~30 — READ ONLY)
  </read_first>
  <action>
    Reorder the `body` ScrollView VStack(spacing: 0) per C.2. First, inventory EVERY existing block + its guard so none is dropped: HeroReadinessCard; PRSDualRunCard(flag/EmptyView); WelcomeActionCard(showWelcomeCard); TrainingProfileCard(showTrainingProfileCard); HealthKit empty-state switch(!hasRealData + connectionState .notRequested/.requestedNoData/.connected); cycle prompt(showCyclePrompt); CycleStatusStrip(latestCycleSnapshot != nil); MetricsStrip; WeeklySummaryCard + NotificationPrePermissionCard(!prePermissionShown) + firstWeekPrompt else-branch; cold-start "building baseline" / FatigueAttentionBanner block; TrainingLoadSection; RecentSessionsSection; niggle Button.

    Target order:
    1. HeroReadinessCard (recommendation.headline stays in its footer — satisfies "recommendation up top"; do NOT extract it into a separate card).
    2. PRSDualRunCard — leave exactly where it is relative to hero (immediately after), flag-gated/EmptyView, UNTOUCHED.
    3. PrimaryActionCTA(recommendation: viewModel.recommendation, onTap: { showActiveWorkout = true }) — placed directly under the hero/PRSDualRun area, ABOVE the rest of the stack.
    4. Established-user priority cluster, gated on `viewModel.hasRealData`: the fatigue/cold-start banner block (FatigueAttentionBanner) + TrainingLoadSection (today's load context) + MetricsStrip + WeeklySummary block + RecentSessionsSection — moved NEAR THE TOP in this order.
    5. Demote BELOW the established cluster (still present, guards preserved): WelcomeActionCard, TrainingProfileCard, HealthKit empty-state switch, cycle prompt, CycleStatusStrip, NotificationPrePermissionCard.

    Context-aware rule (the key invariant): the existing render guards already make setup/prompt cards appear ONLY when the user lacks data (showWelcomeCard = recentSessions.isEmpty && allCheckIns.isEmpty; showTrainingProfileCard = trainingProfiles.isEmpty; HealthKit switch gated on !viewModel.hasRealData; showCyclePrompt; latestCycleSnapshot). KEEP those guards verbatim. Because of them, a cold-start user (hasRealData == false) still sees the connect/setup prompts. To ensure cold-start prompts remain PROMINENT (not stranded below an empty established cluster), guard the established priority cluster so it only renders when `viewModel.hasRealData` (or its inner guards already no-op when there's no data — verify each: TrainingLoadSection shows "---" when noData, MetricsStrip shows "—"). Concretely: when hasRealData == false, the setup/connect prompts (HealthKit EmptyStateCard / WelcomeActionCard / TrainingProfileCard) must appear at/near the top of the visible content rather than below the fold. Implement this by either (a) wrapping the established cluster in `if viewModel.hasRealData { ... }` and placing the setup-prompt group first when !hasRealData, or (b) ordering setup prompts immediately after the CTA but letting their existing guards hide them once established — planner-executor discretion, but the OBSERVABLE result must be: established → load/metrics/summary first, prompts demoted; cold-start → connect/setup prompts prominent. Do not invent new state; rely on hasRealData + existing guards.

    Niggle Button stays LAST. PRSDualRunCard flag UNTOUCHED. Remove NOTHING. Keep all Spacer().frame(height: Spacing.*) rhythm consistent (8pt grid). You MAY keep the toolbar "Log Workout" button OR remove it now that the body CTA exists — planner discretion; if removed, ensure no other entry point breaks (WelcomeActionCard's own Log button stays). Do NOT touch .sheet/.task/.onChange/loadData.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus" && xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Card-block inventory: every conditional view present before the edit is still present after (HeroReadinessCard, PRSDualRunCard, WelcomeActionCard, TrainingProfileCard, HealthKit EmptyStateCard + HealthKitNoDataCard switch, cycle prompt, CycleStatusStrip, MetricsStrip, WeeklySummaryCard, NotificationPrePermissionCard, firstWeekPrompt, cold-start/FatigueAttentionBanner, TrainingLoadSection, RecentSessionsSection, niggle Button). Document the count in the SUMMARY.
    - Each render guard is byte-equivalent (showWelcomeCard, showTrainingProfileCard, !viewModel.hasRealData + connectionState switch, showCyclePrompt, latestCycleSnapshot, !prePermissionShown, isColdStartActive/fatigueZone). grep each guard token to confirm preserved.
    - PrimaryActionCTA is wired with onTap: { showActiveWorkout = true } and sits above the bulk of the stack.
    - PRSDualRunCard line + viewModel.dualRunMessage usage unchanged (grep: still `PRSDualRunCard(message: viewModel.dualRunMessage)`); PRSActivation flag NOT referenced/changed.
    - Niggle Button is the last child of the VStack.
    - Build green on iPhone 17 Pro sim.
  </acceptance_criteria>
  <done>Body VStack reordered per C.2 with the CTA wired; established cluster prioritized, setup prompts context-aware demoted; every card + guard preserved; PRS flag untouched; niggle last; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Regression gate + design-rule sweep on edited files</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - WorkloadApp/Views/Dashboard/DashboardView.swift (final edited state)
    - DESIGN.md (regression rules: accent = hero score ONLY; 0pt corners; no .shadow; 8pt grid Spacing.*; Font.Tokens not .system)
    - .planning/phases/40-ux-wave-3-dashboard-primary-action-cta-value-ranked-card-sta/40-CONTEXT.md (§ Specifics — regression gate rules 1-7, CRITICAL: CTA must not use accent)
  </read_first>
  <action>
    Run the regression gate on the edited files (per-file grep — rtk mangles multi-file rg). Verify on DashboardView.swift:
      1. accent: exactly ONE `ColorTokens.accent` usage remains in the file, and it is the hero score line (HeroReadinessCard, `.foregroundStyle(ColorTokens.accent)` on the recoveryScore Text ~line 320). The PrimaryActionCTA must contain ZERO accent.
      2. corners: 0 `RoundedRectangle` and 0 `.cornerRadius` (Rectangle only).
      3. shadows: 0 `.shadow(`.
      4. fonts: 0 `.system(` — only `.Tokens.*`.
      5. spacing: no off-grid magic numbers introduced (use Spacing.* / documented baselinePair). grep for newly introduced numeric padding/spacing literals and confirm each maps to a Spacing token (existing 16-literals in Onboarding-style CTA should use Spacing.sm).
    On Localizable.xcstrings: confirm every new dashboard.cta.* key has both "en" and "zh-Hans" entries with non-empty values.
    Fix any violation found, then re-run the build gate. Use `grep -v '^#' | grep -c` style counts where comments could contaminate.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus" && test "$(grep -c 'ColorTokens.accent' 'WorkloadApp/Views/Dashboard/DashboardView.swift')" = "1" && ! grep -q 'RoundedRectangle\|\.cornerRadius\|\.shadow(\|\.system(' 'WorkloadApp/Views/Dashboard/DashboardView.swift' && xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 `ColorTokens.accent` in DashboardView.swift (the hero score) — CTA has zero accent.
    - 0 RoundedRectangle / .cornerRadius / .shadow( / .system( in DashboardView.swift.
    - No off-grid spacing literals introduced (all map to Spacing.*).
    - All new dashboard.cta.* keys have en + zh-Hans translations.
    - Build green on iPhone 17 Pro sim.
  </acceptance_criteria>
  <done>Regression gate rules 1-7 clean on both edited files; CTA confirmed non-accent; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UX-only reorder + a presentation CTA. No new input, network, persistence, or trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | DashboardView body reorder dropping a card/guard | mitigate | Task 2 inventory + per-guard grep in Task 2/3 acceptance criteria; no card removed. |
| T-40-02 | Elevation | Accidental activation of dormant v1.6 PRS path | mitigate | PRSDualRunCard line + PRSActivation flag explicitly UNTOUCHED (Task 2 acceptance); grep-asserted. |
| T-40-03 | Information disclosure | CTA mislabels rest-day as train-day, nudging overtraining | accept | Label is advisory only; logging always allowed by design (C.1). No false safety claim; recommendation source unchanged. |

No npm/pip/cargo package installs in this phase — package legitimacy gate N/A.
</threat_model>

<verification>
- Build green on iPhone 17 Pro sim (id CAF84E71-BB64-491D-87C8-875A0143B26D) after each task (incremental, serial).
- Visual (human, post-build): established user → hero → CTA → load/metrics/summary up top, setup prompts demoted; cold-start user → connect/setup prompts prominent. CTA label reflects recommendation (train vs lighter/rest).
- Regression gate rules 1-7 clean on DashboardView.swift + Localizable.xcstrings.
</verification>

<success_criteria>
- Recommendation-aware primary-action CTA visible under the hero, presents ActiveWorkoutSheet, never blocks logging, no accent.
- Value-ranked stack for established users; context-aware prominence for cold-start; no card/guard dropped; PRS flag untouched; niggle last.
- New strings localized en + zh-Hans; build green; no algorithm/flag/engine/ViewModel-logic change.
</success_criteria>

<output>
Create `.planning/phases/40-ux-wave-3-dashboard-primary-action-cta-value-ranked-card-sta/40-01-SUMMARY.md` when done. Include the card-block inventory (count before/after) proving nothing was dropped.
</output>
