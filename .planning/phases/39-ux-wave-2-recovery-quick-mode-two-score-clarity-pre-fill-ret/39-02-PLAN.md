---
phase: 39-ux-wave-2-recovery-quick-mode-two-score-clarity-pre-fill-ret
plan: 02
type: execute
wave: 2
depends_on:
  - "39-01"
files_modified:
  - WorkloadApp/Views/Recovery/RecoveryView.swift
  - WorkloadApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - "V152-UX-SPEC.md §B"
user_setup: []

must_haves:
  truths:
    - "The Recovery Score card carries an honest subtitle stating it blends wearable signals (HRV, resting HR, sleep) with how you feel — NOT claiming the score is wearable-only"
    - "Today's subjective wellness check-in score is surfaced on the Recovery tab as a distinct, clearly-labeled 'how you feel' element"
    - "The 'how you feel' element is visually subordinate — NOT a second 0-100 zone-badge hero competing with the composite Recovery Score"
    - "A short low-emphasis 'why these differ' note explains: recovery = wearable + how-you-feel combined; wellness check-in = the how-you-feel part alone"
    - "No engine change, no code fusion, no new score math in the view — the wellness score read is WellnessCheckIn.wellnessScore from today's existing check-in"
    - "Build is green on the iPhone 17 Pro simulator; RecoveryScoreEngine unchanged"
  artifacts:
    - path: "WorkloadApp/Views/Recovery/RecoveryView.swift"
      provides: "Honest blend subtitle on RecoveryScoreCard, a distinct labeled 'how you feel' element from today's check-in, and a 'why these differ' note"
      contains: "recovery.blend.subtitle"
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "Localized en + zh-Hans blend subtitle, how-you-feel label, why-differ note"
      contains: "recovery.feel.label"
  key_links:
    - from: "WorkloadApp/Views/Recovery/RecoveryView.swift (how-you-feel element)"
      to: "WellnessCheckIn.wellnessScore (today's check-in)"
      via: "todayCheckIn read, no new computation"
      pattern: "todayCheckIn.*wellnessScore|wellnessScore"
---

<objective>
Implement B.2 — Two-score clarity with HONEST BLEND framing. The top Recovery Score
(RecoverySnapshot/RecoveryScoreEngine) is ALREADY a composite that blends HRV/RHR/sleep (75%) with
the subjective wellness check-in (25%); the standalone wellness check-in score is the how-you-feel
part alone. Make this honest and unmistakable: (1) add a blend subtitle to the Recovery Score card;
(2) surface today's subjective wellness score on the Recovery tab as a distinct, clearly-labeled,
visually-subordinate "how you feel" element (not a second zone-badge hero); (3) add a short
low-emphasis "why these differ" note.

Purpose: Two near-identical 0-100 scores currently look like rivals; the no-coach user needs to
know what each means and that they are honestly related, not independent.
Output: Labeling/UX-only changes to RecoveryView + 3 localized strings. NO engine change, NO code
fusion, NO new score computation in the view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ux-wave-2-recovery-quick-mode-two-score-clarity-pre-fill-ret/39-CONTEXT.md
@.planning/v1.5-audit/V152-UX-SPEC.md
@DESIGN.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

RecoveryView (Views/Recovery/RecoveryView.swift) — existing computed accessors to REUSE:
  private var todayCheckIn: WellnessCheckIn?    // today's subjective check-in (may be nil)
  private var todayRecovery: RecoverySnapshot?  // today's composite snapshot (may be nil)
  // body lays out (in order): REDS banner, MorningCheckInPrompt (when todayCheckIn==nil),
  //   RecoveryScoreCard(recovery: todayRecovery, ...), cycle fueling, HRV trend, sleep trend,
  //   WellnessHistorySection, insights.

RecoveryScoreCard (same file, lines 294-385) — composite card to ANNOTATE:
  let recovery: RecoverySnapshot?
  // header Text "recovery.section.recoveryScore" (line 312)
  // hero: Text("\(Int(recovery.recoveryScore))") pageTitle + "/ 100" + ZoneBadge (lines 317-330)
  // then a 0.5pt divider + component rows (HRV/RHR/sleep)
  // ZoneBadge here is the ONE hero zone badge — do not add a second.

WellnessCheckIn (Models/WellnessCheckIn.swift):
  var wellnessScore: Double   // computed = sum(1..5 fields)/20*100, 0-100. READ ONLY — do not recompute.

RecoveryScoreEngine (Services/RecoveryScoreEngine.swift) — DO NOT MODIFY:
  // weights: HRV/RHR ~ 50%, sleep 25%, subjective wellness 25% (confirmed). Wellness already
  // feeds the composite — the view must NOT duplicate or re-fuse this.

Design tokens:
  Spacing.sm = 16, Spacing.xs = 8, Spacing.baselinePair = 4 (label-value only)
  Font.Tokens.label / .micro / .body / .sectionHead; ColorTokens.text1/text2/text3
  CardStyle.swift: .cardStyle(), SectionContainer(header:), RowSeparator. 0pt corners, no shadow,
  accent (ColorTokens.accent) reserved for Dashboard hero — NOT used here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add honest blend subtitle + "how you feel" element + "why differ" note to RecoveryView</name>
  <files>WorkloadApp/Views/Recovery/RecoveryView.swift</files>
  <read_first>
    - WorkloadApp/Views/Recovery/RecoveryView.swift (full file — todayCheckIn line 31-33, todayRecovery 35-37, body layout 116-243, RecoveryScoreCard 294-385, WellnessHistorySection 407-442)
    - WorkloadApp/Models/WellnessCheckIn.swift (wellnessScore computed)
    - WorkloadApp/Services/RecoveryScoreEngine.swift (confirm 75/25 blend; DO NOT modify)
    - .planning/phases/39-ux-wave-2-recovery-quick-mode-two-score-clarity-pre-fill-ret/39-CONTEXT.md (B.2 locked: honest blend, distinct labeled how-you-feel element NOT a second zone-badge hero, short why-differ note, NO fusion/engine/new math)
    - DESIGN.md (Font.Tokens, ColorTokens tiers, Spacing.*; 0pt corners, no shadow, accent Dashboard-hero-only)
  </read_first>
  <action>
    Three labeling-only edits in RecoveryView.swift:

    (1) Blend subtitle on RecoveryScoreCard — directly under the existing
    "recovery.section.recoveryScore" header (line ~312), add a Text using key
    `recovery.blend.subtitle`, styled `.font(.Tokens.label)` + `.foregroundStyle(ColorTokens.text2)`,
    with `.fixedSize(horizontal: false, vertical: true)` and leading frame so it wraps. This states
    the score blends wearable signals + how you feel. Shown whenever the card renders (both the
    populated `recovery` branch and ideally the empty branch — at minimum the populated branch).

    (2) "How you feel" element — surface today's subjective wellness score as a DISTINCT, subordinate
    element. Add it in the RecoveryView body, placed AFTER the RecoveryScoreCard (after line ~141,
    before the cycle/HRV sections), ONLY when `todayCheckIn != nil`. Render it as a single labeled
    row INSIDE the existing Recovery Score card region OR as a low-emphasis standalone row — NOT a
    new .cardStyle hero and NOT a ZoneBadge. Concretely: an HStack with a leading Text
    `recovery.feel.label` (.font(.Tokens.label), ColorTokens.text2) and a trailing
    `Text("\(Int(checkIn.wellnessScore))/100")` (.font(.Tokens.label), .monospacedDigit(),
    ColorTokens.text1). Read the score from `todayCheckIn?.wellnessScore` — do NOT compute any new
    score. Keep it visually smaller than the pageTitle composite hero (label tier, no badge, no
    accent). Preferred placement: append this row inside RecoveryScoreCard below the component rows,
    separated by the existing 0.5pt divider pattern, by passing today's check-in (or its score) into
    RecoveryScoreCard via a new optional parameter `wellnessScore: Double? = nil` — wire it from the
    call site `RecoveryScoreCard(recovery: todayRecovery, cycleSnapshot: latestCycleSnapshot)`
    (line ~138) to also pass `wellnessScore: todayCheckIn?.wellnessScore`. If the executor prefers
    not to thread a parameter, render the labeled row in the body immediately under the card instead
    — either way it must be subordinate (label tier, no second zone hero).

    (3) "Why these differ" note — a short low-emphasis Text using key `recovery.feel.note`,
    `.font(.Tokens.micro)` (or .label) + `.foregroundStyle(ColorTokens.text3)`, placed adjacent to
    the how-you-feel element (same card section or directly under it), wrapping. Explains:
    recovery = wearable + how-you-feel combined; this score = the how-you-feel part alone.

    Hard rules: NO ColorTokens.accent; no second ZoneBadge; no RoundedRectangle/.cornerRadius; no
    .shadow; spacing via Spacing.*; fonts via Font.Tokens (no .system). DO NOT modify
    RecoveryScoreEngine, the recoveryScore value, or any score math — read existing values only.
  </action>
  <verify>
    <automated>grep -n "recovery.blend.subtitle\|recovery.feel.label\|recovery.feel.note\|wellnessScore" "WorkloadApp/Views/Recovery/RecoveryView.swift"</automated>
  </verify>
  <acceptance_criteria>
    - `recovery.blend.subtitle` Text rendered in RecoveryScoreCard (text2/label tier, wrapping)
    - A labeled "how you feel" element using `recovery.feel.label` + a `\(Int(...wellnessScore))/100` value, rendered only when today's check-in exists, sourced from todayCheckIn.wellnessScore (no new computation)
    - The how-you-feel element is subordinate: label tier, no ZoneBadge, no .cardStyle hero, no accent
    - `recovery.feel.note` ("why these differ") rendered low-emphasis (text3) and wrapping
    - Exactly ONE ZoneBadge remains in RecoveryScoreCard (the composite hero) — grep shows no added ZoneBadge for the wellness element
    - No ColorTokens.accent, no RoundedRectangle/.cornerRadius, no .shadow, no .system in the edited regions
    - No change to RecoveryScoreEngine; recovery.recoveryScore read unchanged
  </acceptance_criteria>
  <done>Composite card honestly framed as a blend; today's subjective score surfaced as a distinct subordinate labeled element with a why-differ note; no fusion/engine/new-math.</done>
</task>

<task type="auto">
  <name>Task 2: Add localized blend/feel strings (en + zh-Hans) and build-verify</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings (entry shape — recovery.section.recoveryScore ~line 9696: extractionState "manual", localizations.en/zh-Hans stringUnit, state "translated"; recovery.* region)
    - .planning/phases/39-ux-wave-2-recovery-quick-mode-two-score-clarity-pre-fill-ret/39-CONTEXT.md (B.2 wording: honest blend, how-you-feel, why differ)
  </read_first>
  <action>
    Add three new string entries to Localizable.xcstrings, each following the exact existing JSON
    entry shape (extractionState "manual"; localizations.en + localizations.zh-Hans each with a
    stringUnit { state "translated", value ... }). Place them in the `recovery.*` key region. Use:

      recovery.blend.subtitle
        en: "Blends your wearable signals (HRV, resting HR, sleep) with how you feel"
        zh-Hans: "综合你的可穿戴设备数据（心率变异性、静息心率、睡眠）与你的主观感受"

      recovery.feel.label
        en: "How you feel"
        zh-Hans: "你的主观感受"

      recovery.feel.note
        en: "Recovery combines your wearable signals with how you feel. This is the how-you-feel part on its own."
        zh-Hans: "恢复评分结合了可穿戴设备数据与你的主观感受。这一项仅代表主观感受本身。"

    Keep valid JSON (commas, ordering). Then run the build gate (last file in plan):
      xcodebuild -project "workload management/workload management.xcodeproj" \
        -scheme "workload management" \
        -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" \
        build
    Trust xcodebuild over SourceKit phantom diagnostics. Fix real compile errors before done.
  </action>
  <verify>
    <automated>grep -n "recovery.blend.subtitle\|recovery.feel.label\|recovery.feel.note" "WorkloadApp/Resources/Localizable.xcstrings" && python3 -c "import json; json.load(open('WorkloadApp/Resources/Localizable.xcstrings')); print('json-valid')"</automated>
  </verify>
  <acceptance_criteria>
    - All three keys (recovery.blend.subtitle, recovery.feel.label, recovery.feel.note) exist with both en and zh-Hans stringUnit values, state "translated"
    - Localizable.xcstrings is valid JSON (python json.load succeeds)
    - xcodebuild build succeeds on the named simulator (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>Three blend/feel strings localized en + zh-Hans, catalog valid JSON, app builds green.</done>
</task>

</tasks>

<verification>
- Recovery tab: composite Recovery Score card shows the honest blend subtitle.
- With today's check-in present: a distinct, subordinate "How you feel" labeled value (N/100) appears, plus a low-emphasis "why these differ" note; no second zone-badge hero.
- Without today's check-in: no how-you-feel element (and MorningCheckInPrompt already prompts to add one).
- Regression gate on RecoveryView.swift: 0 ColorTokens.accent, no RoundedRectangle/.cornerRadius, no .shadow, Spacing.*, Font.Tokens (no .system), exactly one ZoneBadge in the card.
- RecoveryScoreEngine.swift unchanged (git diff empty).
- xcodebuild BUILD SUCCEEDED.
</verification>

<success_criteria>
- Honest blend subtitle on the composite Recovery Score card.
- Today's subjective wellness score surfaced as a distinct, subordinate, clearly-labeled "how you feel" element (no second hero/badge), sourced from existing wellnessScore (no new math).
- Short "why these differ" note present and low-emphasis.
- 3 strings localized en + zh-Hans; catalog valid JSON.
- Build green; no engine change, no code fusion.
</success_criteria>

<output>
Create `.planning/phases/39-ux-wave-2-recovery-quick-mode-two-score-clarity-pre-fill-ret/39-02-SUMMARY.md` when done
</output>
