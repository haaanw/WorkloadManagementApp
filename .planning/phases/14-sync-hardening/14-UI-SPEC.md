---
phase: 14
slug: sync-hardening
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-10
---

# Phase 14 -- UI Design Contract

> Visual and interaction contract for sync hardening UI elements. Generated by gsd-ui-researcher.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (native iOS, no web component library) |
| Preset | not applicable |
| Component library | SwiftUI native |
| Icon library | SF Symbols |
| Font | Alpino Regular (400) + Alpino Medium (500) |

---

## Spacing Scale

Declared values per DESIGN.md (multiples of 8pt):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 8pt | Icon-label gaps, inline spacing |
| sm | 16pt | Card internal padding (horizontal) |
| md | 24pt | Card internal padding (vertical) |
| lg | 32pt | Section gaps |
| xl | 48pt | Major section breaks |
| 2xl | 64pt | Page-level breathing room |

Exceptions: 6pt diameter for sync health dot badge (see below -- intentionally sub-grid for a dot indicator that must not dominate the tab bar icon).

---

## Typography

Per DESIGN.md type scale. Only these roles are used in Phase 14 UI:

| Role | Size | Weight | Line Height | Color Token |
|------|------|--------|-------------|-------------|
| Section head | 19pt | 500 (Medium) | 1.4 | --text-1 |
| Body | 17pt | 400 (Regular) | 1.6 | --text-1 |
| Label | 15pt | 400 (Regular) | 1.4 | --text-2 |
| Small label | 13pt | 400 (Regular) | 1.4 | --text-2 |
| Micro / caps | 12pt | 400 (Regular) | 1.2 | --text-3 |

All text rendered in Alpino via `Font.Tokens` extension. No system fonts.

---

## Color

| Role | Dark Hex | Light Hex | Usage |
|------|----------|-----------|-------|
| Dominant (60%) | #0B0B0A | #F4F1ED | Background surfaces |
| Secondary (30%) | #131312 | #EDEAE6 | Sync status detail cards |
| Accent (10%) | #A8A090 | #7A6E5C | Hero readiness score ONLY -- not used in this phase |
| Zone Caution | #7E7252 | #6B5828 | Sync warning dot badge, failed entity row indicator |
| Zone Optimal | #607869 | #3E5C49 | Successful entity row indicator |
| Text 2 | #7C7972 | #696560 | Timestamp labels, secondary info |
| Text 3 | #3A3835 | #AFABA5 | Micro labels |
| Divider | #232321 | #CFCBC5 | Hairline separators between entity rows |

Accent reserved for: hero readiness score number only (unchanged -- Phase 14 does not introduce any new accent usage).

Sync warning indicator uses `zoneCaution` (barely-amber) -- not accent. This follows the existing zone color pattern for cautionary states.

---

## Phase 14 UI Components

This phase has minimal UI surface. Two new elements:

### 1. Sync Health Dot Badge (Profile Tab Icon)

**Location:** Profile tab bar item, top-trailing corner of the SF Symbol icon.

**Behavior:**
- Visible when any `SyncEntity` has a failed last sync attempt (error stored in memory or UserDefaults)
- Hidden when all entity types have succeeded on their last sync
- No animation on appearance -- simply present or absent (150ms linear fade per DESIGN.md motion rules)

**Visual spec:**
- Shape: `Circle()` -- 6pt diameter (exception to 8pt grid, documented above)
- Fill: `ColorTokens.zoneCaution` (barely-amber, same as ACWR caution zone)
- Position: offset(x: 8, y: -8) from tab icon center
- No border, no shadow, no pulse animation

### 2. Sync Status Detail View

**Location:** New section within ProfileView, below existing profile settings. Accessible by tapping a "Sync Status" row.

**Layout:** Single scrollable list of entity rows within a card container.

**Card container:**
- `Rectangle().fill(ColorTokens.surface)` with `ColorTokens.divider` 0.5pt border overlay
- 0pt corner radius
- 16pt horizontal padding, 24pt vertical padding

**Section header:**
- Text: "SYNC STATUS" -- Alpino Regular, 12pt, `ColorTokens.text3`, letter-spacing +0.08em, all-caps
- 8pt bottom margin before first row

**Entity row (repeated for each of 10 SyncEntity types):**
- Height: 48pt (fits touch target)
- Layout: HStack with 8pt spacing
  - Leading: Status indicator circle -- 8pt diameter, filled `zoneCaution` (failed) or `zoneOptimal` (succeeded)
  - Center VStack (aligned leading):
    - Entity name: Alpino Regular, 15pt, `ColorTokens.text1` -- e.g. "Workouts", "Templates", "Personal Records"
    - Last synced timestamp: Alpino Regular, 13pt, `ColorTokens.text2` -- relative time format ("2 min ago", "3 hours ago", "Never")
  - Trailing (only when failed): Error summary text, Alpino Regular, 13pt, `ColorTokens.zoneCaution`, max 1 line, truncated
- Divider between rows: `ColorTokens.divider`, 0.5pt height, full width minus 16pt leading inset

**All synced state:**
- When all 10 entities show success, the section header row in ProfileView displays "All data synced" as the detail label in `ColorTokens.text2`, 15pt
- No badge on Profile tab

**Profile row (navigation entry point):**
- Follows existing ProfileView row pattern
- Leading icon: `arrow.triangle.2.circlepath` (SF Symbol) in `ColorTokens.text2`
- Text: "Sync Status" -- Alpino Regular, 17pt, `ColorTokens.text1`
- Detail: relative time of oldest successful sync OR "Issues" in `ColorTokens.zoneCaution` if any entity failed
- Trailing: chevron.right in `ColorTokens.text3`

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Profile row label | "Sync Status" |
| Profile row detail (healthy) | "All data synced" |
| Profile row detail (issues) | "Issues" |
| Section header | "SYNC STATUS" |
| Entity success timestamp | "{relative time}" (e.g. "2 min ago") |
| Entity never synced | "Never" |
| Entity failed timestamp | "Failed {relative time}" (e.g. "Failed 5 min ago") |
| Error detail (network) | "Network unavailable" |
| Error detail (auth) | "Authentication expired" |
| Error detail (server) | "Server error" |
| Error detail (decode) | "Data format error" |
| Error detail (unknown) | "Sync error" |
| Empty state heading | n/a (no empty state -- all 10 entities always listed) |
| Empty state body | n/a |
| Destructive confirmation | n/a (no destructive actions in this phase) |

---

## Entity Display Names

| SyncEntity enum case | Display Name |
|---------------------|--------------|
| workouts | "Workouts" |
| templates | "Templates" |
| personalRecords | "Personal Records" |
| recoverySnapshots | "Recovery" |
| wellnessCheckIns | "Wellness" |
| workloadSnapshots | "Training Load" |
| behaviorTags | "Behavior Tags" |
| coachRelationships | "Coach Links" |
| trainingProfiles | "Training Profile" |
| prescribedWorkouts | "Prescribed Workouts" |

---

## Interaction Contracts

| Interaction | Behavior |
|-------------|----------|
| Tap "Sync Status" row in Profile | Navigate to SyncStatusView (push on NavigationStack) |
| Pull-to-refresh on SyncStatusView | Trigger immediate foreground sync for all entities, update timestamps on completion |
| Sync completes in background | Dot badge appears/disappears with 150ms linear fade |
| App returns to foreground | Normal sync cycle runs; badge updates based on results |

---

## Accessibility

| Element | Accessibility Label | Trait |
|---------|-------------------|-------|
| Sync dot badge | "Sync issues detected" | .isStatusElement |
| Entity row (success) | "{name}, synced {time}" | .isStaticText |
| Entity row (failed) | "{name}, sync failed {time}, {error}" | .isStaticText |
| Sync Status row | "Sync Status, {detail}" | .isButton |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| n/a | n/a -- native iOS, no registry system | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
