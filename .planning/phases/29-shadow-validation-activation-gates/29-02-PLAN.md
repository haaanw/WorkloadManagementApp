---
phase: 29-shadow-validation-activation-gates
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
autonomous: false   # ends in a human review of the generated report artifact
requirements: [PRS-29-03]
files_modified:
  - WorkloadAppTests/ShadowValidationReportTests.swift
  - .planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md
must_haves:
  truths:
    - "A seeded deterministic shadow-validation report generator lives in the TEST target (ShadowValidationReportTests): it builds synthetic prediction/outcome traces with KNOWN ground truth, resolves them through the real harness path (CyclePredictionLog rows + ShadowAnalyticsService), runs ActivationGateEvaluator, and emits a markdown artifact."
    - "Determinism: NO Date.now (dates from a fixed anchor + day offsets), NO system RNG — any noise uses ShadowMetrics.SplitMix64(seed:) verbatim; a hash-equality test proves two runs with the same seed produce byte-identical markdown."
    - "Scenarios cover at least: PRS-clearly-wins (all 4 MAE CIs upper<0 + Spearman>=0.50 + slope in band + n>=60 -> recommends), PRS-clearly-loses (fails G-MAE), thin-data (n<60 -> insufficient data), and ambiguous (CI straddles 0 -> not a win)."
    - "Each scenario's gate verdict is XCTAsserted against its known ground truth (not just printed)."
    - "The report carries a prominent 'NO ACTIVATION THIS PHASE — master flag remains FALSE' banner; the generator XCTAsserts PRSMasterActivation.isEnabled == false AND PRSActivation.isEnabled == false at emit time."
    - "No report copy contains 'injury prediction' (no-prediction-copy grep guard extended to the report strings)."
    - "Artifact written to .planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md (FileManager + String.write), temp-dir fallback if unwritable (never hard-fails in sandboxed CI)."
    - "Live recovery score + live recommendation byte-unchanged; tier + Autoregulation + DualRun flag fences green; full suite green via real xcodebuild."
  artifacts:
    - path: "WorkloadAppTests/ShadowValidationReportTests.swift"
      provides: "seeded deterministic generator: synthetic traces + real-harness resolve + evaluator run + markdown emit + verdict asserts + hash-equality + flag-stays-false asserts"
      contains: "SplitMix64"
    - path: ".planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md"
      provides: "the human-review deliverable: per-scenario gate panels + NO-ACTIVATION banner"
      contains: "NO ACTIVATION"
  key_links:
    - from: "ShadowValidationReportTests"
      to: "ActivationGateEvaluator + ShadowAnalyticsService"
      via: "synthetic resolved rows -> metricsReport/pairedMAEDifferenceCI -> evaluate"
      pattern: "ActivationGateEvaluator"
    - from: "ShadowValidationReportTests"
      to: "29-shadow-validation-report.md artifact"
      via: "FileManager + String.write(to:atomically:encoding:)"
      pattern: "write\\(to:"
---

<objective>
Produce the Phase-29 result checkpoint: a deterministic, seeded shadow-validation report generator in the test target that builds synthetic PRS-vs-baseline prediction/outcome traces with KNOWN ground truth, resolves them through the REAL harness path, runs the Wave-1 `ActivationGateEvaluator`, emits a markdown artifact showing per-scenario gate panels (MAE-beat count + per-outcome CI, Spearman, calibration slope, n/data-maturity, overall verdict), and machine-asserts that the master activation flag stays FALSE and no live default flips. The same verdicts the report displays are also XCTAsserted against known ground truth, and a hash-equality test proves byte-reproducibility.

Purpose: give the user a reproducible, no-device artifact to JUDGE the activation-gate machinery on data with known answers, and to make the no-activation invariant legible (an unmissable banner + a machine-asserted FALSE flag). This is the user's gated result-checkpoint for the phase, so the final step is `autonomous: false` and pauses for human review of the report.
Output: `ShadowValidationReportTests.swift` (generator + verdict asserts + hash-equality + flag-stays-false asserts) and the generated `29-shadow-validation-report.md` artifact — build-green via real xcodebuild, then a human-review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/algorithm-moat-design.md
@.planning/phases/29-shadow-validation-activation-gates/29-RESEARCH.md
@.planning/phases/29-shadow-validation-activation-gates/29-01-SUMMARY.md
@WorkloadApp/Services/ActivationGateEvaluator.swift
@WorkloadApp/Services/ShadowAnalyticsService.swift
@WorkloadApp/Services/ShadowMetrics.swift
@WorkloadApp/Services/ShadowPredictor.swift
@WorkloadApp/Models/CyclePredictionLog.swift
@WorkloadAppTests/BaselineConvergenceReportTests.swift
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase + Wave 1 — do not re-explore the math. -->

Report-generator PRECEDENT to mirror VERBATIM in structure:
- WorkloadAppTests/BaselineConvergenceReportTests.swift — seeded generator: trace fixtures, deterministic loop, markdown emit, invariant XCTAsserts, hash-equality, artifact write with #filePath repo-root resolution + temp-dir fallback. Copy this skeleton; swap the subject from baseline-convergence to gate-evaluation.

Seeded PRNG (the ONLY allowed randomness, if any noise is needed):
- ShadowMetrics.SplitMix64(seed: UInt64) — nested struct RandomNumberGenerator. Fixed seed per scenario. NEVER SystemRandomNumberGenerator.

Evaluator API (from Wave 1 — see 29-01-SUMMARY for final signatures):
- ActivationGateEvaluator.<entry>(...) -> GateReport with per-gate passed + evidence + overall recommendsActivation. Constants: minMAEBeatCount=3, minSpearman=0.50, calibration [0.8,1.2], minResolvedRows=60.

Harness consumed (Phase 24/28 — DO NOT modify):
- ShadowAnalyticsService.metricsReport(resolvedRows:armId:"prs") and .pairedMAEDifferenceCI(resolvedRows:outcome:armA:"prs",armB:"baseline").
- CyclePredictionLog rows carry the generic ShadowArmPrediction store (armId "prs"/"baseline") + local-only *PRS columns. Build synthetic resolved rows the SAME way the harness writes them (read CyclePredictionLog.swift + ShadowAnalyticsService.recordPrediction for the row shape); set armPredictions + the outcome *Actual fields so metricsReport/pairedMAEDifferenceCI consume them.

Determinism rules: synthetic dates from a fixed anchor `Date(timeIntervalSince1970: 0)` + i*86400 (UTC calendar). Engine/metrics contain no time/RNG, so determinism is structural; the only RNG is seeded SplitMix64. Use a FIXED `seed:` arg to pairedMAEDifferenceCI so its bootstrap is reproducible.

Artifact path: `.planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md`. Resolve repo root from #filePath parent traversal (or env override); fallback to the test bundle temp dir if unwritable (print resolved path). NEVER hard-fail in sandboxed CI.

This plan adds NO app-target file (test-only) → NO pbxproj edit (WorkloadAppTests is synchronized).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Seeded shadow-validation report generator — synthetic traces, real-harness resolve, evaluator run, markdown emit, verdict asserts, hash-equality, flag-stays-false asserts</name>
  <files>WorkloadAppTests/ShadowValidationReportTests.swift, .planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md</files>
  <action>
Mirror `BaselineConvergenceReportTests` structure (read it). Build, in the TEST target, a deterministic seeded generator that:

1. Constructs synthetic `CyclePredictionLog` resolved rows for several SCENARIOS with KNOWN ground truth, each carrying `armPredictions` for the `"prs"` and `"baseline"` arms across the 4 continuous outcomes (.recovery, .wellness, .completion, .pain) plus the outcome `*Actual` fields, exactly as the harness writes them (read CyclePredictionLog.swift + ShadowAnalyticsService.recordPrediction for the row/child shape). Scenarios (minimum):
   - PRS-CLEARLY-WINS: n>=60, PRS predictions much closer to actuals on all 4 outcomes (every paired-MAE-difference CI upper<0), Spearman>=0.50, calibration slope in [0.8,1.2] → ground truth recommendsActivation == true.
   - PRS-CLEARLY-LOSES: n>=60, PRS worse on >=2 outcomes (G-MAE fails) → recommendsActivation == false.
   - THIN-DATA: n=10 with otherwise-perfect PRS → G-DATA-MATURITY forces recommendsActivation == false, reason "insufficient data".
   - AMBIGUOUS: n>=60 but PRS≈baseline so CIs straddle 0 (no win) → recommendsActivation == false.

2. For each scenario, runs `ShadowAnalyticsService.metricsReport(...armId:"prs")` + `pairedMAEDifferenceCI(armA:"prs",armB:"baseline", seed: <fixed>)` per outcome, feeds them to `ActivationGateEvaluator`, and captures the `GateReport`.

3. XCTAsserts each scenario's `recommendsActivation` + per-gate verdicts against its known ground truth (NOT just printed) — this is the regression guard.

4. Emits a markdown report to the artifact path with: a top banner "NO ACTIVATION THIS PHASE — master flag remains FALSE (this report demonstrates the gate machinery on synthetic data; a synthetic PASS is NOT an authorization to go live)"; per-scenario panels (per-outcome PRS-vs-baseline MAE + CI (lower,upper,point) + win/no-win, MAE-beat count X/4 with the raw-self-report-only sub-count X/3 per GA-2, Spearman per primary outcome vs 0.50, calibration slope vs [0.8,1.2], n vs minResolvedRows); the four-gate PASS/FAIL summary + overall recommendsActivation; and a closing line restating the master flag is FALSE. Use the product name "Tuwa". NO "injury prediction" copy — frame Strain-Risk context as load-tolerance/overreaching if mentioned (GA-10, GA-11 from Phase 28 lineage).

5. Writes via FileManager + String.write(to:atomically:encoding:) with #filePath repo-root resolution + temp-dir fallback (mirror BaselineConvergenceReportTests). 

6. A HASH-EQUALITY test runs the generation twice with the same seeds and asserts byte-identical markdown.

7. A FLAG-STAYS-FALSE test asserts `PRSMasterActivation.isEnabled == false` AND `PRSActivation.isEnabled == false` AND `CycleModifierActivation.isEnabled == false` at report-emit time.

8. A NO-PREDICTION-COPY test asserts the generated markdown contains no "injury prediction" substring (case-insensitive).

CRITICAL SCOPE FENCE: this plan FLIPS NO FLAG, adds NO app-target file, adds NO new persisted model/column, adds NO new statistic to ShadowMetrics. It only DRIVES + REPORTS the Wave-1 evaluator over synthetic data. The evaluator's recommendsActivation is rendered into the report and NEVER assigned to any flag.

Run the FULL WorkloadAppTests suite via real xcodebuild at the end.
  </action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/ShadowValidationReportTests 2>&1 | tail -12</automated>
  </verify>
  <done>ShadowValidationReportTests pass (per-scenario verdict asserts, hash-equality, flag-stays-false, no-prediction-copy). 29-shadow-validation-report.md generated with the NO-ACTIVATION banner. Full suite + all flag/tier fences green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A deterministic seeded shadow-validation report generator (test target) that drives the Wave-1 `ActivationGateEvaluator` over synthetic PRS-vs-baseline traces with known ground truth, and the generated `29-shadow-validation-report.md` artifact. NO flag was flipped; master activation flag remains FALSE (machine-asserted).
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/29-shadow-validation-activation-gates/artifacts/29-shadow-validation-report.md`.
2. Confirm the top banner reads "NO ACTIVATION THIS PHASE — master flag remains FALSE" and the closing line restates the flag is FALSE.
3. Confirm each scenario panel shows the four gates (G-MAE with X/4 + raw-self-report X/3 sub-count, G-SPEARMAN vs 0.50, G-CALIBRATION vs [0.8,1.2], G-DATA-MATURITY n vs 60) and an overall verdict consistent with the scenario name (clearly-wins → recommends; loses/thin/ambiguous → does not).
4. Confirm copy uses "Tuwa" and contains no "injury prediction" language.
5. Confirm the test run was green via real xcodebuild (TEST SUCCEEDED, 0 failures) and the tier/flag fences passed.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 29, or describe changes needed to the report/gates.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| synthetic report → reviewer | A synthetic "PASS" must never be read as an authorization to activate — banner + machine-asserted FALSE flag enforce this. |
| report generation → flag state | The generator renders recommendsActivation as text; it must never assign it to a flag. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Spoofing | synthetic PASS mistaken for go-live | mitigate | Unmissable NO-ACTIVATION banner + flag-stays-false XCTAssert at emit time (GA-10). |
| T-29-06 | Tampering | non-reproducible report | mitigate | Seeded SplitMix64 + fixed CI seed + fixed date anchor; hash-equality test. |
| T-29-07 | Repudiation | gate verdict provenance | mitigate | Every gate panel decomposes to metric value + threshold + per-outcome breakdown (glass-box). |
| T-29-SC | Tampering | package installs | mitigate | No new packages; test-target only; if any appear, halt + human verify. |
</threat_model>

<verification>
- `ShadowValidationReportTests` all green (verdict asserts, hash-equality, flag-stays-false, no-prediction-copy).
- `BaselineTierFenceTests` + `AutoregulationFlagFenceTests` + `DualRunFlagFenceTests` green.
- Full `WorkloadAppTests` suite green via real xcodebuild.
- `29-shadow-validation-report.md` generated with NO-ACTIVATION banner; committed/regenerable.
- Human reviews + approves the report artifact.
</verification>

<success_criteria>
Seeded shadow-validation report generator + the `29-shadow-validation-report.md` artifact exist; gate verdicts match known ground truth; report is byte-reproducible; master activation flag asserted FALSE. NO flag flipped. Live behavior unchanged. Committed atomically to main (no push). Human-approved.
</success_criteria>

<output>
Create `.planning/phases/29-shadow-validation-activation-gates/29-02-SUMMARY.md` and then `.planning/phases/29-shadow-validation-activation-gates/29-VERIFICATION.md` after the human approves.
</output>
