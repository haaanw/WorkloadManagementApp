---
phase: 29-shadow-validation-activation-gates
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PRS-29-01, PRS-29-02]
files_modified:
  - WorkloadApp/Services/PRSMasterActivation.swift
  - WorkloadApp/Services/ActivationGateEvaluator.swift
  - WorkloadAppTests/ActivationGateEvaluatorTests.swift
must_haves:
  truths:
    - "PRSMasterActivation.isEnabled exists and defaults to FALSE; documented as the milestone-level go-live gate that is NOT flipped this phase."
    - "ActivationGateEvaluator is a pure struct with static methods that consumes EXISTING ShadowAnalyticsService metric outputs and returns a structured GateReport; it NEVER assigns any *Activation.isEnabled."
    - "G-MAE counts the 4 continuous outcomes (.recovery, .wellness, .completion, .pain); passes when PRS beats baseline on >=3 with the paired-MAE-difference CI upper bound < 0 (PRS error strictly lower)."
    - "G-SPEARMAN uses fixed threshold 0.50; G-CALIBRATION uses fixed [0.8, 1.2]; both evaluated on the primary (raw self-report) outcomes."
    - "G-DATA-MATURITY: n < minResolvedRows (default 60) OR a nil gate metric forces recommendsActivation = false with reason 'insufficient data'."
    - "An activation-flag fence test asserts PRSMasterActivation + PRSActivation + CycleModifierActivation are all false."
    - "Live recovery score + live recommendation unchanged; BaselineTierFenceTests + AutoregulationFlagFenceTests + DualRunFlagFenceTests green."
  artifacts:
    - path: "WorkloadApp/Services/PRSMasterActivation.swift"
      provides: "Master activation flag, default false"
      contains: "isEnabled"
    - path: "WorkloadApp/Services/ActivationGateEvaluator.swift"
      provides: "Pure deterministic gate evaluation; report-only"
      contains: "struct ActivationGateEvaluator"
    - path: "WorkloadAppTests/ActivationGateEvaluatorTests.swift"
      provides: "Oracle/boundary/thin-data + activation-flag fence + no-mutation isolation tests"
      contains: "ActivationGateEvaluator"
  key_links:
    - from: "ActivationGateEvaluator"
      to: "ShadowAnalyticsService.OutcomeMetrics + pairedMAEDifferenceCI tuple"
      via: "consumes existing per-arm metric outputs (no new statistics)"
      pattern: "OutcomeMetrics|pairedMAEDifference"
---

<objective>
Create the activation-gate evaluation core: the NEW `PRSMasterActivation` master flag (default FALSE, stays FALSE), and the pure deterministic `ActivationGateEvaluator` that consumes the EXISTING Phase-24 `ShadowAnalyticsService` PRS-vs-baseline metric outputs and reports — report-only, never flipping a flag — whether the four ROADMAP activation gates pass. Plus the full evaluator test battery, an activation-flag fence test, and a no-mutation isolation grep guard.

Purpose: build the honest gate that any future live activation must clear, as auditable named-constant logic with an oracle test, while guaranteeing zero behavior change (every flag FALSE, evaluator cannot mutate any flag).
Output: PRSMasterActivation, ActivationGateEvaluator + GateReport types, oracle/boundary/thin-data tests, activation-flag fence test, no-mutation isolation grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/algorithm-moat-design.md
@.planning/phases/29-shadow-validation-activation-gates/29-RESEARCH.md
@WorkloadApp/Services/ShadowAnalyticsService.swift
@WorkloadApp/Services/ShadowMetrics.swift
@WorkloadApp/Services/ShadowPredictor.swift
@WorkloadApp/Services/PRSActivation.swift
@WorkloadApp/Services/CycleModifierGate.swift
@CLAUDE.md

<interfaces>
<!-- Executor: read ShadowAnalyticsService.swift + ShadowMetrics.swift for the EXACT tuple/struct shapes. Do NOT add new statistics — all gate math already exists. -->
<!--
ShadowAnalyticsService (Phase 24/28) ALREADY provides everything the evaluator needs:
  struct OutcomeMetrics { var n: Int; var mae: Double?; var calibrationSlope: Double?; var spearmanRho: Double?; var engineDerived: Bool }
  static func metricsReport(resolvedRows:[CyclePredictionLog], armId:String) -> [ShadowPredictor.Outcome: OutcomeMetrics]
  static func pairedMAEDifferenceCI(resolvedRows:..., outcome:..., armA:String="baseline", armB:String="cycleAware", blockLength:Int=7, resamples:Int=1000, alpha:Double=0.05, seed:UInt64=0x5EED) -> (lower:Double, upper:Double, point:Double)?
  static func aggregate(resolvedRows:) -> [ShadowPredictor.Outcome: OutcomeMAE]   (OutcomeMAE has prsMAE: Double?)

ShadowPredictor.Outcome cases: .recovery (engineDerived/secondary), .wellness, .completion (adherence), .pain (all raw self-report), .niggleSeverity (NO arm predicts in v1, always nil — EXCLUDE from gate counting).
ShadowPredictor.engineDerivedOutcomes == [.recovery].

"current algorithm" baseline arm id = "baseline"; PRS arm id = "prs" (GA-9).
Sign convention for pairedMAEDifferenceCI with armA="prs", armB="baseline": d = |err_prs| - |err_baseline|; PRS WINS when upper < 0 (PRS error strictly lower). Document this in the engine.
-->
<!-- PRSActivation (Phase 28) is the engineering swap gate — SEPARATE from the NEW PRSMasterActivation milestone go-live gate (GA-1). Do NOT reuse or modify PRSActivation. -->
<!-- CycleModifierActivation (Phase 20, in CycleModifierGate.swift) is the pattern to mirror for the flag-defaults-false posture. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRSMasterActivation master flag (default FALSE, stays FALSE)</name>
  <files>WorkloadApp/Services/PRSMasterActivation.swift</files>
  <action>
Create `PRSMasterActivation` as a NEW enum namespace mirroring the `PRSActivation` / `CycleModifierActivation` posture exactly (read PRSActivation.swift and CycleModifierGate.swift for the shape). Expose a static `isEnabled` defaulting to `false` (GA-1, per research §219 / §4.4). This is the SEPARATE milestone-level "gates passed, go live" gate — distinct from the Phase-28 `PRSActivation` engineering swap gate.

Add a doc comment stating: this is the master ACTIVATION flag for PRS-v1 (the milestone-level go-live gate, SEPARATE from PRSActivation the engineering swap gate); it DEFAULTS FALSE; Phase 29 builds + reports the activation gates but NEVER flips it; flipping to true is a FUTURE human-authorized decision after reviewing 29-shadow-validation-report.md (research Open Q6); NO code may assign this flag from `ActivationGateEvaluator.recommendsActivation` — that boolean is report-only (GA-7).

Provide a TEST-ONLY `withEnabled(_:_:)` override helper mirroring PRSActivation's pattern, so the flag-on branch is exercisable WITHOUT shipping an enabled default. Production code must never call it.

This is a new app-target Swift file — ensure it is added to the `workload management` target in the pbxproj (verify the .pbxproj references it, per the iOS file-registration convention). Run the FULL suite after adding it.
  </action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/BaselineTierFenceTests 2>&1 | tail -6</automated>
  </verify>
  <done>PRSMasterActivation.isEnabled exists, defaults false, in the app target. Tier fence green. Full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure ActivationGateEvaluator + GateReport (report-only, no flag mutation)</name>
  <files>WorkloadApp/Services/ActivationGateEvaluator.swift, WorkloadAppTests/ActivationGateEvaluatorTests.swift</files>
  <behavior>
    - Thresholds are FIXED named static constants matching the ROADMAP verbatim (GA-8): minMAEBeatCount = 3 (of 4 continuous outcomes), minSpearman = 0.50, calibrationLow = 0.8, calibrationHigh = 1.2, minResolvedRows = 60.
    - G-MAE oracle: given per-outcome paired-MAE-difference CIs (prs vs baseline), the gate counts an outcome as "PRS wins" iff its CI upper bound < 0 (PRS error strictly lower, GA-9). Passes iff wins >= 3 of the 4 continuous outcomes (.recovery, .wellness, .completion, .pain). .niggleSeverity is excluded.
    - G-SPEARMAN oracle: passes iff the designated primary self-report outcome (.wellness) has spearmanRho >= 0.50 AND no primary outcome (.wellness/.completion/.pain) with n>=minResolvedRows is below 0.50. Boundary: exactly 0.50 passes (>=).
    - G-CALIBRATION oracle: passes iff the primary outcome calibration slope is within [0.8, 1.2] inclusive AND no sufficiently-sampled primary outcome is outside. Boundary: exactly 0.8 and 1.2 pass.
    - G-DATA-MATURITY: if the resolved-row count n for the gating outcomes < minResolvedRows, OR any required gate metric is nil (ShadowMetrics returns nil on thin/degenerate data), recommendsActivation = false with reason "insufficient data" — REGARDLESS of any other gate (GA-4). Thin-data oracle: n=10 -> recommendsActivation false, reason insufficient data, even if synthetic metrics look perfect.
    - GateReport decomposition: every gate's PASS/FAIL carries its evidence (the metric value(s), the threshold, and per-outcome breakdown) so the report is glass-box. overall recommendsActivation == (all four gates passed). recommendsActivation is REPORT-ONLY.
    - No-mutation: ActivationGateEvaluator source contains no assignment to any `.isEnabled` (asserted by a grep test in ActivationGateEvaluatorTests).
  </behavior>
  <action>
Create `ActivationGateEvaluator` as a pure struct with static methods (Foundation only, deterministic). Define a `GateReport` result type carrying: per-gate results (G-MAE, G-SPEARMAN, G-CALIBRATION, G-DATA-MATURITY) each with `passed: Bool`, the evidence (metric values + thresholds + per-outcome breakdown), an `engineDerived` tag on `.recovery` evidence (GA-2 honesty caveat — also expose a "raw-self-report-only" MAE-win sub-count), a human-readable reason string, and an overall `recommendsActivation: Bool` (= all four gates passed).

The evaluator's entry point CONSUMES the existing `ShadowAnalyticsService` outputs — do NOT recompute statistics. Accept inputs as either (a) the resolved rows plus a thin wrapper that calls `ShadowAnalyticsService.metricsReport(resolvedRows:armId:"prs")` and `pairedMAEDifferenceCI(armA:"prs", armB:"baseline")` per continuous outcome, or (b) directly the per-outcome metric structs + CI tuples (the testable pure form). Prefer form (b) as the pure core (oracle-testable with hand-built inputs) and a thin form-(a) convenience that wires the existing service. Read ShadowAnalyticsService.swift for the exact `OutcomeMetrics` / `pairedMAEDifferenceCI` shapes; reuse them verbatim.

Constants are FIXED named statics (GA-8). The sign convention for the MAE CI is `armA="prs"` vs `armB="baseline"` so "PRS wins" ⇔ CI upper < 0 (GA-9) — document it on the constant/function.

CRITICAL: this engine is REPORT-ONLY. It returns a recommendation; it MUST NOT read or write any `*Activation.isEnabled` (GA-7). Do NOT import or reference `PRSMasterActivation`/`PRSActivation` for mutation. NO persisted SwiftData state (GA-5). NO new statistics in ShadowMetrics.

Write tests FIRST (RED): in `ActivationGateEvaluatorTests` cover the oracle for each gate, per-gate isolated failure, boundary values (Spearman exactly 0.50, slope exactly 0.8/1.2, CI upper exactly 0 = NOT a win), thin-data precondition. Add an ACTIVATION-FLAG FENCE test asserting `PRSMasterActivation.isEnabled == false && PRSActivation.isEnabled == false && CycleModifierActivation.isEnabled == false`. Add a NO-MUTATION isolation test that reads the ActivationGateEvaluator source file (via `#filePath` parent traversal to the repo, like BaselineConvergenceReportTests resolves its artifact path) and asserts it contains no `isEnabled =` / `.isEnabled =` substring. Then implement (GREEN). Run the FULL suite at the end (new type added).
  </action>
  <verify>
    <automated>cd "workload management" && xcodebuild test -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/ActivationGateEvaluatorTests 2>&1 | tail -10</automated>
  </verify>
  <done>All ActivationGateEvaluatorTests pass (oracle, per-gate fail, boundaries, thin-data, activation-flag fence, no-mutation grep). Full WorkloadAppTests suite green. Tier + Autoregulation + DualRun flag fences green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| evaluator output → flag state | The evaluator's recommendsActivation must be UNABLE to mutate any activation flag (enforced by no-mutation grep). |
| shadow metrics (thin/degenerate) → gate verdict | Thin/degenerate data must yield "insufficient data → no activation", never a fabricated pass. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Elevation of privilege | ActivationGateEvaluator → flag | mitigate | recommendsActivation is report-only; no-mutation grep proves the source assigns no *Activation.isEnabled (GA-7). |
| T-29-02 | Tampering | PRSMasterActivation default | mitigate | Default false asserted by the activation-flag fence test; flag never flipped this phase. |
| T-29-03 | Spoofing | Thin-data fabricated pass | mitigate | G-DATA-MATURITY hard precondition: n < 60 OR nil metric → insufficient data → no activation (GA-4). |
| T-29-04 | Information disclosure | Gate evidence | accept | On-device only, no PII; no sync; no new persisted state. |
| T-29-SC | Tampering | package installs | mitigate | No new packages added; if any appear, halt + human verify. |
</threat_model>

<verification>
- `ActivationGateEvaluatorTests` all green (oracle, per-gate fail, boundaries, thin-data, activation-flag fence, no-mutation grep).
- `BaselineTierFenceTests` + `AutoregulationFlagFenceTests` + `DualRunFlagFenceTests` green.
- Full `WorkloadAppTests` suite green via real xcodebuild (NOT SourceKit).
- No new package installs. No new persisted SwiftData model/column.
</verification>

<success_criteria>
PRSMasterActivation (default false) + a tested pure report-only ActivationGateEvaluator exist. No flag flipped. Live behavior unchanged. Committed atomically to main (no push).
</success_criteria>

<output>
Append a SUMMARY entry: create `.planning/phases/29-shadow-validation-activation-gates/29-01-SUMMARY.md` when done.
</output>
