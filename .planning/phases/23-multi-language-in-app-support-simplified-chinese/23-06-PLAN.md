---
phase: 23-multi-language-in-app-support-simplified-chinese
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: ["23-04"]
files_modified:
  - WorkloadApp/Views/Auth/LoginView.swift
  - WorkloadApp/Views/Auth/SignUpView.swift
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/Views/Profile/ProfileView.swift
  - WorkloadApp/Views/Workload/WorkloadView.swift
  - WorkloadApp/Views/Recovery/RecoveryView.swift
  - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
  - WorkloadApp/Resources/Localizable.xcstrings
autonomous: true
requirements:
  - D-01    # Localize core UI strings — every Text/Label/button/tab/alert/empty state/onboarding
  - D-03    # zh-Hans coverage (push notifications were closed in 23-02; this finishes the Text() literal sweep)
  - D-06    # Hybrid Chinese + (Latin acronym) format for technical terms
  - D-07    # First-occurrence-per-screen hybrid rule
  - D-09    # Single Localizable.xcstrings source of truth
  - D-10    # LocalizedStringKey via String(localized:) / Text(_:) literal-init
  - D-16    # Live-switch correctness propagates to migrated surfaces
  - D-17    # CJK font cascade still drives newly-migrated zh-Hans values (no view-side font override regressions)
  - D-22    # Marketing-tone pass for the LoginView tagline + SignUpView subhead

must_haves:
  truths:
    - "Zero capitalized hardcoded `Text(\"[A-Z]…\")` literals remain in any of the 6 listed view files (audit grep returns 0 matches for the migrated set)."
    - "Every newly-introduced catalog key exists in Localizable.xcstrings with both `en` and `zh-Hans` stringUnit at `state: \"translated\"`."
    - "Localizable.xcstrings parses as valid JSON after edits (jq pass)."
    - "Project compiles: `xcodebuild build` exits 0 for `workload management` scheme on iPhone 17 Pro Max simulator."
    - "Technical-term strings containing ACWR/HRV/RHR/TSS/EWMA/CTL/ATL/PR follow the hybrid `中文 (ACRONYM)` form in their zh-Hans values per D-06."
    - "Interpolated label `READINESS · \\(dateLabel)` migrates without breaking — uses `String(localized:)` with a `%@` placeholder OR keeps the interpolation while the static portion (`READINESS ·`) becomes a catalog key — executor picks whichever pattern matches the 23-04 catalog conventions."
    - "No fullwidth punctuation (U+FF08, U+FF09, U+3000) introduced in any new zh-Hans value (lint pass)."
  artifacts:
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "All Auth/Dashboard/Profile/Workload/Recovery/WorkoutLog Text() literal keys, en + zh-Hans translated"
    - path: "WorkloadApp/Views/Auth/LoginView.swift"
      provides: "Uses catalog keys for WORKLOAD wordmark, tagline, EMAIL, PASSWORD, Sign In, Create an account"
    - path: "WorkloadApp/Views/Auth/SignUpView.swift"
      provides: "Uses catalog keys for Create Account, subhead, PRIMARY SPORT, submit button"
    - path: "WorkloadApp/Views/Dashboard/DashboardView.swift"
      provides: "Uses catalog keys for READINESS · {date}, TRAINING LOAD, RECENT SESSIONS, EST, RPE n, Connect Health, empty/building-baseline copy"
    - path: "WorkloadApp/Views/Profile/ProfileView.swift"
      provides: "Uses catalog keys for all preference row labels, sheet bodies, button labels"
    - path: "WorkloadApp/Views/Workload/WorkloadView.swift"
      provides: "Uses catalog keys for ACWR header, LOAD TREND, RECENT PRS, empty state"
    - path: "WorkloadApp/Views/Recovery/RecoveryView.swift"
      provides: "Uses catalog keys for INSIGHTS, BEHAVIOR IMPACT, Morning Check-in, RECOVERY SCORE, WELLNESS CHECK-INS, empty state"
    - path: "WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift"
      provides: "Uses catalog keys for PRESCRIBED, No Workouts Yet, RPE prompt, RPE n labels, Easy/Maximal endpoints"
  key_links:
    - from: "WorkloadApp/Views/**/*.swift"
      to: "WorkloadApp/Resources/Localizable.xcstrings"
      via: "Text(\"namespaced.key\") LocalizedStringKey lookup"
      pattern: "Text\\(\"[a-z][a-zA-Z]+\\.[a-z]"
---

<objective>
Close VERIFICATION gap #2 (WR-01 deferred sweep). Migrate the ~33 capitalized hardcoded `Text("…")` literals (and any additional lowercase/button/sheet labels surfaced by the audit) across the six remaining view files into the namespaced Localizable.xcstrings catalog, with `en` source + `zh-Hans` translated values. After this plan, a zh-Hans user sees zero English fallback on Auth, Dashboard, Profile, Workload, Recovery, and WorkoutLog surfaces.

Purpose: D-01/D-03/D-09/D-10 require full string coverage; REVIEW WR-01 explicitly deferred this sweep into a follow-up; VERIFICATION gap #2 now blocks phase closure. This plan does NOT touch gaps #1 (Tuwa ASC rename — resolved 4302952), #3 (CR-05 HeroReadinessCard date locale — resolved), or #4 (WR-07/08/09 weight + weekday + AM/PM — resolved 11b0756, 9041ad6, a78eba9).

Output: 6 view files migrated; Localizable.xcstrings extended with ~50 new key/value pairs (en + zh-Hans); xcodebuild green; SUMMARY documents marketing-tone candidates for the LoginView tagline + SignUpView subhead and flags them for the native-reviewer human gate (23-HUMAN-UAT.md Test 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DESIGN.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-CONTEXT.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-VERIFICATION.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-04-PLAN.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-04-SUMMARY.md

<interfaces>
<!-- Established catalog + view patterns from 23-01..23-04. The executor uses these directly — no exploration needed. -->

Catalog entry shape (Localizable.xcstrings — `strings.<key>` value):
```json
{
  "extractionState": "manual",
  "localizations": {
    "en":      { "stringUnit": { "state": "translated", "value": "Sign Out" } },
    "zh-Hans": { "stringUnit": { "state": "translated", "value": "退出登录" } }
  }
}
```

Top-level file shape (DO NOT alter):
```json
{
  "sourceLanguage": "en",
  "strings": { "<key>": { ... }, ... },
  "version": "1.0"
}
```

View call-site pattern (already in use at ProfileView.swift:161, 480):
```swift
Text("profile.language.label")    // LocalizedStringKey literal-init resolves via catalog
Text("profile.signOut")
```

Interpolated values: prefer extracting the static portion into a catalog key and keeping the interpolation in Swift. Example for `Text("READINESS · \(dateLabel)")`:
```swift
// Option A (preferred — keeps the date interpolation in Swift, localizes only the static prefix)
Text("dashboard.hero.readiness") + Text(" · \(dateLabel)")
// Option B (single key with placeholder)
Text("dashboard.hero.readinessLabel \(dateLabel)")    // catalog en value: "READINESS · %@", zh-Hans: "准备度 · %@"
```
Executor picks whichever matches the convention established in DashboardView for already-localized strings (Text("dashboard.title"), etc.). If 23-04 used neither pattern for date-interpolated headers, Option B with `%@` is canonical Apple guidance for xcstrings.

Namespacing convention (from 23-01..23-04):
- `surface.element.purpose` — e.g. `auth.field.email`, `dashboard.section.trainingLoad`, `profile.row.weightUnit`
- Surface roots in use: `auth.`, `dashboard.`, `recovery.`, `workload.`, `workoutLog.`, `profile.`, `action.`, `term.`, `onboarding.`, `language.`, `notif.`
- Section headers preserve their uppercase styling in the catalog value (the source string is "TRAINING LOAD", not "Training Load") because the views do not apply `.textCase(.uppercase)` to these literals.
- zh-Hans values for ACRONYM-bearing labels follow D-06 hybrid: `训练负荷 (TRAINING LOAD)` — but per D-07 short section headers may omit the parenthetical acronym if the screen already introduced it (executor decides per-surface; document choices in SUMMARY).

zh-Hans terminology anchors (from 23-CONTEXT D-06 + 23-RESEARCH glossary — re-read 23-RESEARCH.md "Canonical zh-Hans Glossary" for definitive forms):
- TRAINING LOAD → 训练负荷
- RECENT SESSIONS → 最近训练
- RECOVERY SCORE → 恢复评分
- WELLNESS CHECK-INS → 状态自评
- LOAD TREND → 负荷趋势
- RECENT PRS → 最新个人最佳 (PR)
- INSIGHTS → 洞察
- BEHAVIOR IMPACT → 行为影响
- PRESCRIBED → 已布置
- EMAIL → 邮箱
- PASSWORD → 密码
- Sign In → 登录
- Create an account → 注册账号
- Create Account → 注册账号 (button/heading reuse permitted)
- PRIMARY SPORT → 主要运动项目
- Send Invite → 发送邀请
- INVITE CODE → 邀请码
- ATHLETE EMAIL → 运动员邮箱
- HealthKit Permissions → 健康数据权限
- Sync Status → 同步状态
- Enable Coach Mode → 启用教练模式
- Coach Only → 仅教练模式
- Hormonal Contraceptive → 激素避孕
- Pregnant → 怀孕
- Lactating → 哺乳期
- Weekly Summary → 每周总结
- Authorized → 已授权
- Authorize HealthKit Access → 授权访问健康数据
- WORKLOAD (wordmark on LoginView) → keep as "WORKLOAD" wordmark in zh-Hans value too (per D-CONTEXT brand handling — wordmark is a logotype; flag in SUMMARY for native-reviewer confirmation per D-22). Alternative zh-Hans value `训练负荷` is acceptable only if the reviewer prefers a translated wordmark.
- Train smarter. Recover better. (LoginView tagline, marketing-tone) → propose 2 zh-Hans candidates in SUMMARY per D-22, default value: `更聪明地训练，更有效地恢复。`

Lint (re-used from 23-04):
- No fullwidth U+FF08 `（`, U+FF09 `）`, U+3000 `　` in zh-Hans values.
- ASCII `(`, `)`, and single ASCII space U+0020 between Chinese and Latin acronyms.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit pass — enumerate all hardcoded Text() literals across the 6 view files and produce the migration table</name>
  <files>.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md</files>
  <read_first>
    - WorkloadApp/Views/Auth/LoginView.swift
    - WorkloadApp/Views/Auth/SignUpView.swift
    - WorkloadApp/Views/Dashboard/DashboardView.swift
    - WorkloadApp/Views/Profile/ProfileView.swift
    - WorkloadApp/Views/Workload/WorkloadView.swift
    - WorkloadApp/Views/Recovery/RecoveryView.swift
    - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
    - WorkloadApp/Resources/Localizable.xcstrings (read first 50 lines to confirm shape + sample 10 existing keys per surface)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md (search for "Canonical zh-Hans Glossary" and read the surrounding ~60 lines for terminology anchors not already in <interfaces>)
  </read_first>
  <action>Run a deterministic grep audit across the 6 view files. For each surface, capture three categories of literals: (a) capitalized strings `Text("[A-Z]…")`, (b) sentence-case button/label literals `Text("[A-Z][a-z]…")` already caught by (a), and (c) any `.navigationTitle("…")`, `Button("…", action:)`, `.alert("…", …)`, `Label("…", …)`, `Toggle("…", …)`, and `Picker("…", …)` first-argument literals that are bare English strings (not already a catalog key — catalog keys match the regex `^[a-z][a-zA-Z]+\.[a-zA-Z]`). Write the audit to `23-06-AUDIT.md` as a single Markdown table with columns: `surface | file:line | swift_construct | current_literal | proposed_key | en_value | zh-Hans_value | notes`. Use the namespacing convention from <interfaces>. Set zh-Hans values from the anchor list in <interfaces>; for any literal whose Chinese rendering is not anchored, draft a mainland-zh-Hans translation following the peer-coach tone described in UI-SPEC lines 298-302 (omit 您 where natural). For the LoginView wordmark and tagline, write both proposed values explicitly and mark them `MARKETING-PASS` in the notes column. For the `Text("READINESS · \(dateLabel)")` interpolation, propose key `dashboard.hero.readinessLabel` with en value `READINESS · %@` and zh-Hans value `准备度 · %@` (Option B from <interfaces>), unless the executor confirms an existing convention in DashboardView's already-localized strings — in which case mirror that. Use this command to seed the audit (executor extends manually for sheet/Button/navigationTitle): `grep -nE 'Text\("|\.navigationTitle\("|Button\("[A-Z]|Label\("[A-Z]|Toggle\("[A-Z]|Picker\("[A-Z]|\.alert\("[A-Z]' WorkloadApp/Views/Auth/LoginView.swift WorkloadApp/Views/Auth/SignUpView.swift WorkloadApp/Views/Dashboard/DashboardView.swift WorkloadApp/Views/Profile/ProfileView.swift WorkloadApp/Views/Workload/WorkloadView.swift WorkloadApp/Views/Recovery/RecoveryView.swift WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift | grep -v '"[a-z][a-zA-Z]*\.[a-z]'`. Exclude pure-interpolation literals like `Text("\(value)")` and SF Symbol Image strings. Final audit row count should be ≥ 33 (the capitalized lower bound) and is expected to reach ~50 with sheet/Button labels.</action>
  <verify>
    <automated>test -f .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md && [ "$(grep -cE '^\| WorkloadApp/Views' .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md)" -ge 33 ]</automated>
  </verify>
  <done>Audit file exists; contains ≥33 migration rows across all 6 surfaces; every row has a proposed key, en value, and zh-Hans value; marketing-pass items flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Auth + Dashboard surfaces (LoginView, SignUpView, DashboardView) — view edits + catalog entries</name>
  <files>WorkloadApp/Views/Auth/LoginView.swift, WorkloadApp/Views/Auth/SignUpView.swift, WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md (the migration table from Task 1)
    - WorkloadApp/Views/Auth/LoginView.swift (full file — already partially localized; do not regress existing catalog calls)
    - WorkloadApp/Views/Auth/SignUpView.swift (full file)
    - WorkloadApp/Views/Dashboard/DashboardView.swift (full file — already partially localized; confirm existing pattern for date-interpolated labels)
    - WorkloadApp/Resources/Localizable.xcstrings (load fully to know which keys already exist)
  </read_first>
  <action>For each row in the AUDIT table whose surface is `Auth` or `Dashboard`: (1) replace the bare-English literal in the Swift source with `Text("<proposed_key>")` (or the appropriate `Label`/`Button`/`navigationTitle` form), preserving the existing modifier chain (font, foregroundStyle, tracking, kerning, etc.). (2) Add the corresponding entry to `WorkloadApp/Resources/Localizable.xcstrings` under `strings.<proposed_key>` with the catalog shape shown in <interfaces> — both `en` and `zh-Hans` stringUnit at `state: "translated"`. For `Text("READINESS · \(dateLabel)")` at DashboardView.swift:268, replace with the pattern picked in the audit (default: `Text("dashboard.hero.readinessLabel \(dateLabel)")` with catalog en `"READINESS · %@"` and zh-Hans `"准备度 · %@"`). For `Text("RPE \(Int(rpe))")` at DashboardView.swift:612, use catalog key `dashboard.session.rpeValue` with en `"RPE %lld"` and zh-Hans `"RPE %lld"` (acronym kept Latin per D-06). For the LoginView wordmark `Text("WORKLOAD")` (line 43), use key `auth.brand.wordmark` and set both en and zh-Hans to `"WORKLOAD"` (logotype preservation — flag in SUMMARY per D-22 if reviewer prefers translated form). For the tagline `Text("Train smarter. Recover better.")` (line 46), use key `auth.brand.tagline` en `"Train smarter. Recover better."` zh-Hans `"更聪明地训练，更有效地恢复。"` and mark as MARKETING-PASS in SUMMARY (propose `"训练更聪明，恢复更彻底。"` as alternate candidate for native-reviewer per D-22). For the Cycle-Aware Recovery block at DashboardView.swift:69-90, note that `dashboard.cycleAware.title` and `dashboard.cycleAware.body` already exist in the catalog — only migrate `Text("Open Settings")` (use key `action.openSettings`, en `"Open Settings"`, zh-Hans `"打开设置"`). Add no fullwidth punctuation to any zh-Hans value. After all edits, run `xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build` and resolve any compile errors before moving on.</action>
  <verify>
    <automated>set -e; jq -e '.strings | keys | map(select(startswith("auth.") or startswith("dashboard."))) | length' WorkloadApp/Resources/Localizable.xcstrings; grep -cE 'Text\("[A-Z]' WorkloadApp/Views/Auth/LoginView.swift WorkloadApp/Views/Auth/SignUpView.swift WorkloadApp/Views/Dashboard/DashboardView.swift | awk -F: '{s+=$2} END {if (s>0) {print "FAIL: capitalized literals remain:", s; exit 1} else print "OK"}'; xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build -quiet</automated>
  </verify>
  <done>Auth + Dashboard views compile; zero `Text("[A-Z]…")` literals remain in those three files; every new catalog key has en + zh-Hans translated entries; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Profile + Workload + Recovery + WorkoutLog surfaces — view edits + catalog entries</name>
  <files>WorkloadApp/Views/Profile/ProfileView.swift, WorkloadApp/Views/Workload/WorkloadView.swift, WorkloadApp/Views/Recovery/RecoveryView.swift, WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md (rows for these 4 surfaces)
    - WorkloadApp/Views/Profile/ProfileView.swift (full file — largest surface; ~25+ literals across preference rows, invite sheet, HealthKit sheet, delete-account confirmation)
    - WorkloadApp/Views/Workload/WorkloadView.swift
    - WorkloadApp/Views/Recovery/RecoveryView.swift
    - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
    - WorkloadApp/Resources/Localizable.xcstrings (re-read after Task 2 to know full key set)
  </read_first>
  <action>Same migration pattern as Task 2, applied to the 4 surfaces. Key namespace assignments: `profile.row.*` for preference row labels (e.g. `profile.row.weightUnit`, `profile.row.acwrMethod`, `profile.row.loadMetric`, `profile.row.coachMode`, `profile.row.coachOnly`, `profile.row.hormonalContraceptive`, `profile.row.pregnant`, `profile.row.lactating`, `profile.row.weeklySummary`), `profile.section.*` for section headers, `profile.sheet.*` for invite/delete/HealthKit sheets, `profile.healthkit.*` for HealthKit-specific labels (`profile.healthkit.permissions`, `profile.healthkit.authorize`, `profile.healthkit.authorized`, `profile.healthkit.disclaimer`), `profile.sync.*` for sync status (`profile.sync.status`, `profile.sync.issues`, `profile.sync.allSynced`), `profile.invite.*` for invite flow (`profile.invite.code`, `profile.invite.athleteEmail`, `profile.invite.lookUp`, `profile.invite.send`, `profile.invite.sentConfirmation`, `profile.invite.codeBody`), `profile.delete.body`. For Workload: `workload.section.acwr`, `workload.section.loadTrend`, `workload.section.recentPRs`, `workload.empty.body`. For Recovery: `recovery.section.insights`, `recovery.section.behaviorImpact`, `recovery.section.recoveryScore`, `recovery.section.wellnessCheckIns`, `recovery.checkin.title` (`Morning Check-in` → `晨间自评`), `recovery.checkin.prompt` (`How are you feeling today?` → `今天感觉如何？`), `recovery.empty.body`. For WorkoutLog: `workoutLog.session.prescribed`, `workoutLog.empty.title` (`No Workouts Yet` → `还没有训练记录`), `workoutLog.empty.body`, `workoutLog.rpe.prompt` (`How hard was this session?` → `这次训练强度如何？`), `workoutLog.rpe.value` (en `"RPE: %lld"` zh-Hans `"RPE：%lld"` — note: D-06 keeps ACRONYM in Latin; the colon localizes to full-width for typographic correctness ONLY in this Chinese-prose context — but per the no-fullwidth-punctuation lint, use ASCII colon: en `"RPE: %lld"` zh-Hans `"RPE %lld"` matching dashboard.session.rpeValue). For RPE endpoints `Easy` → `轻松`, `Maximal` → `极限` use keys `workoutLog.rpe.easy`, `workoutLog.rpe.maximal`. Apply the no-fullwidth-punctuation lint to all new zh-Hans values. Run xcodebuild after each surface to keep the failure surface small.</action>
  <verify>
    <automated>set -e; grep -cE 'Text\("[A-Z]' WorkloadApp/Views/Profile/ProfileView.swift WorkloadApp/Views/Workload/WorkloadView.swift WorkloadApp/Views/Recovery/RecoveryView.swift WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift | awk -F: '{s+=$2} END {if (s>0) {print "FAIL: capitalized literals remain:", s; exit 1} else print "OK"}'; xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build -quiet</automated>
  </verify>
  <done>All 4 surfaces compile; zero `Text("[A-Z]…")` literals remain across all 6 phase-23-gap-closure files combined; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Catalog hygiene + lint + final verification</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings, .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-SUMMARY.md</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings (final state after Tasks 2 and 3)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-AUDIT.md
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-VERIFICATION.md (gap #2 closure requirements)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>(1) Run the lint script below against `Localizable.xcstrings` and resolve any failures: every key must have both `en` and `zh-Hans` stringUnit at `state: "translated"`; no fullwidth U+FF08/U+FF09/U+3000 in any zh-Hans value; JSON parses. (2) Run one final repo-wide audit grep to confirm zero capitalized `Text("[A-Z]…")` literals remain in the 6 view files (Workload + Recovery section headers like `RECENT PRS` were the most likely miss). (3) Write `23-06-SUMMARY.md` following the standard summary template, with explicit sections for: (a) gap #2 closure evidence (paste the lint script output + audit grep output showing 0 hits + xcodebuild exit 0); (b) marketing-pass candidates flagged for native-reviewer human gate (LoginView wordmark, LoginView tagline, SignUpView subhead) with both proposed zh-Hans values; (c) any literals deliberately left unmigrated (interpolation-only strings, SF Symbol names, debug-only logs) with rationale; (d) hand-off note to 23-HUMAN-UAT.md Test 4 (native-reviewer visual audit) listing the new surfaces now in scope.</action>
  <verify>
    <automated>python3 -c "
import json, sys, re
d = json.load(open('WorkloadApp/Resources/Localizable.xcstrings'))
fail = 0
fullwidth = re.compile(r'[（）　]')
for k, v in d.get('strings', {}).items():
    locs = v.get('localizations', {})
    en = locs.get('en', {}).get('stringUnit', {})
    zh = locs.get('zh-Hans', {}).get('stringUnit', {})
    if en.get('state') != 'translated':
        print(f'FAIL: {k} en state = {en.get(\"state\")}'); fail += 1
    if zh.get('state') != 'translated':
        print(f'FAIL: {k} zh-Hans state = {zh.get(\"state\")}'); fail += 1
    if fullwidth.search(zh.get('value', '')):
        print(f'FAIL: {k} zh-Hans contains fullwidth punctuation: {zh.get(\"value\")}'); fail += 1
print(f'Total keys: {len(d[\"strings\"])}, failures: {fail}')
sys.exit(1 if fail else 0)
" &amp;&amp; \
grep -REc 'Text\("[A-Z]' WorkloadApp/Views/Auth/LoginView.swift WorkloadApp/Views/Auth/SignUpView.swift WorkloadApp/Views/Dashboard/DashboardView.swift WorkloadApp/Views/Profile/ProfileView.swift WorkloadApp/Views/Workload/WorkloadView.swift WorkloadApp/Views/Recovery/RecoveryView.swift WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift | awk -F: '{s+=$2} END {if (s>0) {print "FAIL: residual capitalized literals:", s; exit 1} else print "OK: zero residual capitalized literals"}' &amp;&amp; \
test -f .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-SUMMARY.md</automated>
  </verify>
  <done>Lint passes (every key en+zh-Hans translated, no fullwidth punctuation); zero capitalized Text literals in the 6 files; SUMMARY exists with gap #2 closure evidence and marketing-pass hand-off; final xcodebuild green.</done>
</task>

</tasks>

<verification>
Phase 23 VERIFICATION gap #2 is closed when:
- Audit table at `23-06-AUDIT.md` enumerates ≥33 (expected ~50) migration rows across the 6 surfaces.
- Every audit row has a corresponding catalog entry in `Localizable.xcstrings` with `en` + `zh-Hans` at `state: "translated"`.
- Repo-wide grep `Text\("[A-Z]` against the 6 view files returns zero matches.
- `xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build` exits 0.
- The Python lint script in Task 4 exits 0.
- `23-06-SUMMARY.md` documents the marketing-pass candidates and hands them to `23-HUMAN-UAT.md` Test 4 (native-reviewer visual audit).

This plan does NOT re-verify gaps #1 (resolved 4302952), #3 (resolved), or #4 (resolved 11b0756/9041ad6/a78eba9) — see VERIFICATION gap audit in the planning context.
</verification>

<success_criteria>
- Six view files (LoginView, SignUpView, DashboardView, ProfileView, WorkloadView, RecoveryView, WorkoutLogView) contain zero capitalized `Text("…")` literals.
- `Localizable.xcstrings` grows by ~50 namespaced keys, all `en` + `zh-Hans` translated, no fullwidth punctuation.
- Project builds clean.
- SUMMARY.md flags marketing-pass items for native-reviewer follow-up so phase 23 closure does not regress on D-22.
- Live-switch behavior on the migrated surfaces will be confirmed in `23-HUMAN-UAT.md` Test 4 — this plan does not include the human gate (it is a deferred manual checkpoint, already tracked).
</success_criteria>

<output>
Create `.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-06-SUMMARY.md` with gap #2 closure evidence and marketing-pass hand-off when all tasks complete.
</output>
