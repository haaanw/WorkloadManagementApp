---
phase: 23-multi-language-in-app-support-simplified-chinese
plan: 04
type: execute
wave: 3
depends_on: ["23-02", "23-03"]
files_modified:
  - WorkloadApp/Resources/Localizable.xcstrings
  - WorkloadApp/Resources/InfoPlist.xcstrings
  - WorkloadApp/Components/MetricTile.swift
  - WorkloadApp/Components/ZoneBadge.swift
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/Views/Workload/WorkloadView.swift
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every key in Localizable.xcstrings has a zh-Hans stringUnit with state of translated or reviewed — no remaining new state for zh-Hans"
    - "Every zh-Hans value passes the hybrid-terminology lint — ASCII parens U+0028/U+0029 and ASCII space U+0020 only; no fullwidth punctuation"
    - "InfoPlist.xcstrings NSHealthShareUsageDescription zh-Hans value matches UI-SPEC line 291 verbatim"
    - "Technical training terms follow the hybrid format from UI-SPEC Surface 3 — Chinese term + ASCII space + parenthesized Latin acronym (per D-06/D-07)"
    - "Marketing-tone copy receives a translator-style second pass per D-22 (paywall headline, onboarding hero, App Store-bound marketing strings if in catalog)"
    - "Dashboard + MetricTile + ZoneBadge render at iPhone 17 Pro Max in zh-Hans without truncation; multi-line wrap permitted where labels exceed available width"
    - "Charts framework axis labels in WorkloadView render in zh-Hans after live switch (id locale modifier forces rebuild per RESEARCH Pitfall 4)"
    - "ZoneBadge zh-Hans horizontal padding equals 16 (vs 10 in en) — UI-SPEC line 78"
    - "All visual acceptance checklist items from UI-SPEC lines 340-354 pass in both dark and light mode"
    - "RevenueCat product display names (Athlete Pro / Coach) appear in English inside zh-Hans UpgradeSheet. Updating zh-Hans product titles in the RevenueCat dashboard is out of scope for Phase 23 — track as follow-up after ship."
  artifacts:
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "Fully translated zh-Hans catalog"
    - path: "WorkloadApp/Resources/InfoPlist.xcstrings"
      provides: "Localized NSHealthShareUsageDescription"
  key_links:
    - from: "WorkloadApp/Resources/Localizable.xcstrings"
      to: "WorkloadApp/Resources/Fonts/NotoSansSC-*.otf"
      via: "Cascade renders zh-Hans values via Font.Tokens cascade descriptor"
      pattern: "zh-Hans"
---

<objective>
Populate the zh-Hans column of Localizable.xcstrings and InfoPlist.xcstrings; apply the hybrid terminology format for technical training terms; run a marketing-tone second pass on paywall + onboarding hero + App Store-bound strings; audit dashboard + metric tile + zone badge density at zh-Hans on iPhone 17 Pro Max.

Purpose: D-21 (LLM-drafted, human-reviewed), D-22 (marketing-tone pass), D-06/D-07 (hybrid format with first-occurrence rule), D-20 (zh-Hans density audit). This plan brings the catalog from "en source only" to "fully translated and visually validated".

Output: All zh-Hans values committed in reviewed state; density audit captured in SUMMARY with before/after screenshots; visual acceptance checklist signed off. SUMMARY also notes the RevenueCat product display-name follow-up (English product titles inside zh-Hans UpgradeSheet — dashboard update deferred post-ship).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DESIGN.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-CONTEXT.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM-draft + hybrid-format zh-Hans translations for every key in Localizable.xcstrings + InfoPlist.xcstrings</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings, WorkloadApp/Resources/InfoPlist.xcstrings</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings (current — after P2 sweep, full set of en source values present; zh-Hans column empty/new)
    - WorkloadApp/Resources/InfoPlist.xcstrings (current — NSHealthShareUsageDescription en value present after P2)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md "Canonical zh-Hans Glossary" (lines 519-582) — anchor terms and recommended hybrid forms with catalog keys
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md "Copywriting Contract" (lines 274-303) — verbatim zh-Hans strings for picker + onboarding step; "Surface 3 Mixed-Script Typography" (lines 220-250); "Surface 4 Locale-Aware Formatting" (lines 254-270) — unit suffixes
  </read_first>
  <action>For every key in Localizable.xcstrings whose zh-Hans stringUnit is missing or in `new` state, populate a zh-Hans translation. Follow this priority for wording: (1) RESEARCH Canonical Glossary table for any term whose key matches term.*, sport.*, zone.*, recoveryZone.*, cyclePhase.*, frequency.*, experience.* — use the recommended hybrid form or standalone Chinese exactly as listed; (2) UI-SPEC Copywriting Contract (lines 274-303) for language.picker.*, profile.language.*, onboarding.language.*, onboarding.continue.toSetup — use the verbatim Chinese values; (3) for all other keys, LLM-draft an idiomatic mainland zh-Hans translation following the tone guideline from UI-SPEC lines 298-302 (peer-coach voice, omit 您 where natural, mainland fitness terminology). For any key containing a technical training term, apply the hybrid format from UI-SPEC Surface 3: Chinese term + ASCII space U+0020 + ASCII parenthesis U+0028 + Latin acronym + ASCII U+0029. First-occurrence rule (D-07): on any given screen, the first appearance uses the hybrid; subsequent same-screen references may drop the acronym IF layout demands AND meaning is preserved. Educational/glossary copy ALWAYS uses hybrid. For notif.weekly.body.template, use the RESEARCH-recommended zh-Hans format: "本周记录 %lld 次训练 — 已连续 %lld 周。新增 %lld 项个人最佳！" (PATTERNS line 487; adjust placeholder count to match the en source); leave English plural variations intact in the en column. For weight unit suffixes: when MeasurementFormatter renders the unit, the system already produces 公斤/磅 in zh-Hans automatically — DO NOT add a `weightUnit.kg.suffix` key with `公斤` overriding the formatter (UI-SPEC line 262 + PATTERNS line 409 caveat). For zh-Hans values, ensure NO fullwidth punctuation: scan for U+FF08 `(`, U+FF09 `)`, U+3000 ideographic space and reject (RESEARCH lint at lines 583-588). After populating, set each entry's zh-Hans state to "translated"; mark the small subset receiving the marketing pass (Task 2) as "needs_review" temporarily. Also populate `InfoPlist.xcstrings` NSHealthShareUsageDescription zh-Hans value verbatim from UI-SPEC line 291: "Tonus 读取您的心率、心率变异性和睡眠数据，用于计算每日恢复评分。这些原始数据不会离开您的设备。" Catalog edits can be made by hand-editing the JSON or via Xcode catalog editor; either path is acceptable provided the resulting JSON is well-formed and the lint passes. Run the hybrid-terminology lint as a final check. Build the project after the catalog change so Xcode compiles localized strings.</action>
  <verify>
    <automated>python3 -c "
import json, sys, re
d = json.load(open('WorkloadApp/Resources/Localizable.xcstrings'))
missing = []
fullwidth_bad = []
for k, v in d.get('strings', {}).items():
    loc = v.get('localizations', {})
    zh = loc.get('zh-Hans', {})
    su = zh.get('stringUnit', {})
    state = su.get('state', '')
    val = su.get('value', '')
    if not val or state == 'new':
        missing.append(k)
    if '（' in val or '）' in val or '\\u3000' in val:
        fullwidth_bad.append(k)
print('missing zh-Hans:', len(missing), missing[:5])
print('fullwidth violations:', len(fullwidth_bad), fullwidth_bad[:5])
sys.exit(0 if not missing and not fullwidth_bad else 1)
" && python3 -c "
import json
d = json.load(open('WorkloadApp/Resources/InfoPlist.xcstrings'))
hk = d['strings']['NSHealthShareUsageDescription']['localizations']['zh-Hans']['stringUnit']['value']
assert 'Tonus' in hk and '心率' in hk and '不会离开您的设备' in hk, 'HK consent zh-Hans missing required content'
" && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - Every key in Localizable.xcstrings has a zh-Hans stringUnit with non-empty value AND state not equal to "new".
    - Zero fullwidth parentheses (U+FF08/U+FF09) or ideographic spaces (U+3000) in any zh-Hans value.
    - InfoPlist.xcstrings NSHealthShareUsageDescription zh-Hans value contains "Tonus", "心率", and the phrase "不会离开您的设备" (UI-SPEC line 291).
    - Glossary key term.acwr value equals "训练负荷比 (ACWR)"; term.hrv equals "心率变异性 (HRV)"; verifying via `python3 -c "import json; d=json.load(open('WorkloadApp/Resources/Localizable.xcstrings')); print(d['strings'].get('term.acwr',{}).get('localizations',{}).get('zh-Hans',{}).get('stringUnit',{}).get('value',''))"` outputs the expected hybrid.
    - xcodebuild clean.
  </acceptance_criteria>
  <done>Catalog fully translated and lint-clean; HealthKit consent localized; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Marketing-tone second pass + zh-Hans density audit on Dashboard / MetricTile / ZoneBadge / Charts</name>
  <files>WorkloadApp/Resources/Localizable.xcstrings, WorkloadApp/Components/MetricTile.swift, WorkloadApp/Components/ZoneBadge.swift, WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/Views/Workload/WorkloadView.swift</files>
  <read_first>
    - WorkloadApp/Resources/Localizable.xcstrings (target — locate paywall.*, onboarding.hero.*, and any marketing.* keys for the second pass)
    - WorkloadApp/Components/MetricTile.swift (target — verify no .lineLimit(1) suppression per UI-SPEC line 75)
    - WorkloadApp/Components/ZoneBadge.swift (target — confirm P2's locale-conditional padding and textCase/tracking rules; tighten if needed)
    - WorkloadApp/Views/Dashboard/DashboardView.swift (target — hero readiness label; metric strip; recovery card phase context line; ACWR section label)
    - WorkloadApp/Views/Workload/WorkloadView.swift (target — Chart axis labels in zh-Hans live switch)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md "zh-Hans density audit" (lines 70-82) + "Typography" zh-Hans overrides (lines 92-99) + "Visual Acceptance Checklist" (lines 340-354)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md "Pitfall 4 Charts framework using its own locale" (lines 817-822)
  </read_first>
  <action>STEP A — Marketing-tone pass (D-22): identify the small set of marketing-tone keys — paywall.headline, paywall.subhead, paywall.cta, onboarding.hero.title, onboarding.hero.subtitle, onboarding.language.title, onboarding.language.subtitle, onboarding.continue.toSetup, and any keys under `marketing.*` or `appStore.*` (these are owned by Plan 05 if present in catalog; if not present here, skip them). For each, rewrite the zh-Hans value with attention to Chinese fitness culture conventions per UI-SPEC tone guideline lines 298-302 — never literal; prefer concise 3-5 character titles for onboarding hero pages; reference house styles like Keep / Codoon for sport-culture phrasing if helpful. Move each from `needs_review` to `translated` state after the rewrite. STEP B — Density audit on the actual rendered UI in zh-Hans on iPhone 17 Pro Max simulator: run the app under SCREENSHOT_MODE with `-AppleLanguages "(zh-Hans)"` and confirm: (1) MetricTile labels wrap to 2 lines where the hybrid term overflows; tile height remains 8pt-aligned; NEVER truncated with ellipsis (UI-SPEC line 75). If a metric tile is truncating, remove any `.lineLimit(1)` on `Text(title)`, and confirm the parent container can grow vertically along the 8pt grid. (2) ZoneBadge zh-Hans padding is 16pt horizontal (vs 10 in en) — confirm by reading the file; if missing, ensure the locale-conditional from P2 Task 2e is present (`@Environment(\.locale) private var locale; .padding(.horizontal, locale.identifier == "zh-Hans" ? 16 : 10)`). (3) zh-Hans micro labels do NOT apply `.textCase(.uppercase)` or `+0.08em` tracking — locate "READINESS" / "ACWR" / "HRV" style headers on Dashboard and confirm they read in zh-Hans without uppercase (Chinese has no case). (4) Dashboard ACWR section label uses the full hybrid `训练负荷比 (ACWR)` per UI-SPEC line 77; if width exceeds 60% of card it wraps above the value vertically — verify visually and patch the layout if it truncates. (5) WorkloadView Chart axis labels render in zh-Hans: in each `AxisValueLabel { ... }` closure, the formatting must receive the env locale (`Text(dateValue, format: .dateTime.month(.abbreviated).day().locale(locale))`); each Chart view must carry `.id(locale)` to force a rebuild on env-locale change (RESEARCH Pitfall 4). Add these if absent. (6) Test live switch: from a running zh-Hans session, tap the language row to switch to English; all dashboard labels, dates, weight strings, and chart axis labels re-render within 150ms crossfade. Capture before/after screenshots (en vs zh-Hans) of DashboardView, WorkloadView, RecoveryView, ProfileView, OnboardingView step 0, UpgradeSheet — save under `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots/` for SUMMARY documentation. (7) RevenueCat follow-up note: while screenshotting UpgradeSheet in zh-Hans, observe that the product display names ("Athlete Pro", "Coach") remain in English because RC dashboard zh-Hans product titles are out of scope for Phase 23. Record this verbatim in 23-04-SUMMARY.md under a "Follow-ups" heading. Sign off the UI-SPEC Visual Acceptance Checklist (lines 340-354) in the SUMMARY — every box must pass before this task is done. After all code-level adjustments, build the project clean.</action>
  <verify>
    <automated>grep -q '"zh-Hans" ? 16 : 10' WorkloadApp/Components/ZoneBadge.swift && ! grep -q '\.lineLimit(1)' WorkloadApp/Components/MetricTile.swift && grep -q '\.id(locale)' WorkloadApp/Views/Workload/WorkloadView.swift && grep -q '\.locale(locale)' WorkloadApp/Views/Workload/WorkloadView.swift && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - ZoneBadge.swift contains the locale-conditional horizontal padding string `"zh-Hans" ? 16 : 10`.
    - MetricTile.swift does NOT call `.lineLimit(1)` on the title Text (multi-line wrap permitted per UI-SPEC).
    - WorkloadView.swift Chart views carry `.id(locale)` and `.locale(locale)` (or equivalent) on date formatters.
    - Screenshots saved to `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots/` for at least: dashboard-en, dashboard-zh, workload-en, workload-zh, recovery-en, recovery-zh, profile-en, profile-zh, onboarding-step0-zh, paywall-zh — verifiable by `ls .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots/ | wc -l` >= 10.
    - 23-04-SUMMARY.md "Follow-ups" section records the RevenueCat product display name limitation: English titles inside zh-Hans UpgradeSheet, RC dashboard update deferred post-ship.
    - UI-SPEC Visual Acceptance Checklist signed off in 23-04-SUMMARY.md (every checkbox marked PASS).
    - xcodebuild clean.
  </acceptance_criteria>
  <done>Marketing copy re-toned; density audit complete with screenshots; visual acceptance checklist signed off; RC product-name follow-up documented; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Native zh-Hans reviewer sign-off on translations + density screenshots</name>
  <what-built>
    Task 1 produced a fully translated zh-Hans catalog (LLM draft + glossary application). Task 2 produced before/after screenshots of every primary surface in en + zh-Hans, plus marketing-tone rewrites of paywall + onboarding hero. All visual acceptance checklist items have been verified by automated build + visual inspection.

    Per D-21 (LLM-draft, human-reviewed) and D-22 (marketing-tone second pass), human review is mandatory before the catalog ships. This checkpoint surfaces the catalog + screenshots to the user for sign-off.
  </what-built>
  <how-to-verify>
    1. Open `WorkloadApp/Resources/Localizable.xcstrings` in Xcode catalog editor. Browse zh-Hans column. Flag any entries that read awkwardly, use wrong terminology, or feel mechanical. Edit in place and save.
    2. Specifically review marketing keys: `paywall.headline`, `paywall.subhead`, `paywall.cta`, `onboarding.hero.title`, `onboarding.hero.subtitle`, `onboarding.continue.toSetup`. These got the D-22 second pass — confirm tone is appropriate for mainland Chinese fitness audiences (Keep / Codoon style).
    3. Inspect HealthKit consent: open Settings -> Tonus -> Health on a zh-Hans simulator. The consent text must match UI-SPEC line 291 verbatim and read naturally.
    4. Open `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots/` and compare en vs zh-Hans for: dashboard, workload (chart axis labels), recovery, profile, onboarding step 0, paywall. Check for: truncation (none allowed on dashboard/onboarding per UI-SPEC line 82); MetricTile multi-line wrap appearing where needed; ZoneBadge padding looking visually balanced; no PingFang fallback on Chinese glyphs (compare weight to General Sans Latin glyphs alongside).
    5. Live-switch test: Profile -> Language -> tap "中文(简体)" -> back to Dashboard. Crossfade ~150ms. Dates, weights, chart axes all update without restart.
    6. Glossary spot-check: open any screen showing ACWR / HRV / RHR — the hybrid form (e.g., `训练负荷比 (ACWR)`) appears at first occurrence per UI-SPEC Surface 3 contract.
    7. Acknowledge RC follow-up: UpgradeSheet in zh-Hans will show English product titles ("Athlete Pro", "Coach") until the RevenueCat dashboard zh-Hans titles are added (deferred post-ship).

    If anything fails review, edit the catalog (Xcode handles state transitions) or file a follow-up task; otherwise approve.
  </how-to-verify>
  <resume-signal>Reply "approved" (or list specific keys/screens to revise; planner will respin Task 1/Task 2 fixes before proceeding to Plan 05 ASC metadata).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog value text | LLM-drafted then human-reviewed; final values are static at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Information Disclosure | HealthKit consent string (zh-Hans) | mitigate | UI-SPEC line 291 restates "data does not leave device" in zh-Hans; reviewed in Task 3 checkpoint to satisfy Apple HIG App Review 5.1.1 (RESEARCH lines 664-674) |
| T-23-10 | Tampering | Catalog injection via dynamic keys | mitigate (carried from P2) | This plan only edits values, never adds dynamic key construction |
</threat_model>

<verification>
```bash
# Hybrid-terminology lint
python3 -c "
import json
d = json.load(open('WorkloadApp/Resources/Localizable.xcstrings'))
bad = []
for k, v in d['strings'].items():
    val = v.get('localizations',{}).get('zh-Hans',{}).get('stringUnit',{}).get('value','')
    if '（' in val or '）' in val or '\\u3000' in val:
        bad.append((k, val))
print('Hybrid violations:', bad)
"
# expected: empty list

# Coverage check
python3 -c "
import json
d = json.load(open('WorkloadApp/Resources/Localizable.xcstrings'))
new = [k for k,v in d['strings'].items() if v.get('localizations',{}).get('zh-Hans',{}).get('stringUnit',{}).get('state','') == 'new']
missing = [k for k,v in d['strings'].items() if not v.get('localizations',{}).get('zh-Hans',{}).get('stringUnit',{}).get('value','')]
print('new state:', len(new), '  missing values:', len(missing))
"
# expected: zero, zero
```

Manual verification gated by Task 3 checkpoint.
</verification>

<success_criteria>
- All three task acceptance_criteria pass.
- Zero zh-Hans values in `new` state.
- Zero fullwidth punctuation in zh-Hans values.
- HealthKit consent zh-Hans matches UI-SPEC line 291.
- Density audit screenshots saved to phase directory.
- UI-SPEC Visual Acceptance Checklist signed off in SUMMARY.
- RevenueCat product display name limitation documented in SUMMARY follow-ups.
- Human reviewer (Task 3) explicit approval recorded.
</success_criteria>

<output>
Create `.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-04-SUMMARY.md` when done. Embed the Visual Acceptance Checklist sign-off AND a "Follow-ups" section noting the RevenueCat dashboard zh-Hans product title work as a post-ship task.
</output>
