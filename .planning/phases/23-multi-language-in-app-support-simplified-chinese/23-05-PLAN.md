---
phase: 23-multi-language-in-app-support-simplified-chinese
plan: 05
type: execute
wave: 4
depends_on: ["23-04"]
files_modified:
  - .planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md
  - .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/
  - workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme
autonomous: false
requirements: []
user_setup:
  - service: app_store_connect
    why: "Add Simplified Chinese (zh-Hans) App Localization, upload zh-Hans screenshots, and enter zh-Hans metadata for Tonus on the App Store"
    dashboard_config:
      - task: "Sidebar -> App Information -> Localizable Information -> + Add Language -> Simplified Chinese"
        location: "App Store Connect -> My Apps -> Tonus (com.tonus.app)"
      - task: "Sidebar -> App Store -> current version -> + Add Language -> Simplified Chinese (Promotional Text, Description, Keywords, Support URL, Marketing URL, What's New, Screenshots)"
        location: "App Store Connect -> My Apps -> Tonus -> App Store tab"
must_haves:
  truths:
    - "zh-Hans Localizable Information present in App Store Connect for Tonus: Name (<=30), Subtitle (<=30)"
    - "zh-Hans App Store version localization present: Description (<=4000), Keywords (<=100 comma-separated chars), Promotional Text (<=170), What's New (<=4000)"
    - "All zh-Hans field lengths validated locally against ASC limits BEFORE entry (Python length assertions in Task 1)"
    - "zh-Hans screenshots rendered via SCREENSHOT_MODE + AppleLanguages override at iPhone 17 Pro Max resolution 1320x2868"
    - "zh-Hans screenshots uploaded to ASC under the Simplified Chinese localization (uploaded by user under human-verify gate)"
    - "No 'Submit for Review' or 'Release' button is ever clicked autonomously — per memory feedback_asc_caution.md"
    - "All ASC actions are paused at human-verify checkpoints; user confirms each step in the dashboard"
  artifacts:
    - path: ".planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md"
      provides: "Drafted zh-Hans strings for ASC fields with verified character counts"
    - path: ".planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/"
      provides: "Rendered zh-Hans screenshots ready for ASC upload"
    - path: "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme"
      provides: "Xcode scheme that launches the app in zh-Hans with SCREENSHOT_MODE seeding"
  key_links:
    - from: "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme"
      to: "WorkloadApp/App/AppRouter.swift"
      via: "SCREENSHOT_MODE + -AppleLanguages (zh-Hans) launch arguments"
      pattern: "AppleLanguages"
---

<objective>
Add the Simplified Chinese (zh-Hans) storefront localization to Tonus in App Store Connect: enter zh-Hans App Name, Subtitle, Description, Keywords, Promotional Text, What's New, and upload zh-Hans screenshots rendered from the existing SCREENSHOT_MODE scheme with AppleLanguages override.

Purpose: D-04. Phase 23 cannot ship to Chinese-locale users without ASC metadata in zh-Hans. Per memory `feedback_asc_caution.md`, this plan is `autonomous: false` and every ASC action is gated behind `checkpoint:human-verify`. The Claude executor PREPARES strings + screenshots; the user PERFORMS the ASC entry/upload.

Output: zh-Hans App Store storefront entry for Tonus ready for the next version submission (submission button NOT clicked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-CONTEXT.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-04-PLAN.md
@WorkloadApp/App/AppRouter.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft zh-Hans ASC metadata with verified character counts</name>
  <files>.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md</files>
  <read_first>
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md "App Store Connect zh-Hans Metadata" (lines 590-648) — field limits + example strings
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md "Copywriting Contract" (lines 274-303) and tone guideline (lines 298-302)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-04-PLAN.md (Plan 04 produced final translated paywall + onboarding hero copy that informs ASC tone)
    - existing en App Store metadata if previously captured under .planning/ (search `git grep -l "App Store" .planning/phases/07-app-store-metadata 2>/dev/null` for prior English entries)
    - Memory: ~/.claude/projects/-Users-hanwen-Desktop-Tonus/memory/reference_asc_navigation.md (ASC sidebar URLs for Tonus)
    - Memory: ~/.claude/projects/-Users-hanwen-Desktop-Tonus/memory/feedback_asc_caution.md (never click submission buttons)
  </read_first>
  <action>Create `.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md`. For each ASC field draft a zh-Hans value following the tone guideline (peer-coach voice, mainland fitness culture conventions, no literal translation of English marketing copy). Open Question Q1 from RESEARCH lines 982-986 is decided by user during human-verify; default in the draft to keeping Tonus as Latin in App Name and adding a Chinese descriptor in Subtitle (the brand-purity-preserving default per memory project_rename_faros.md). Required fields with limits:
  - App Name (limit 30 chars): default draft `Tonus · 训练负荷管理` (approx 12 chars)
  - Subtitle (limit 30 chars): default draft `恢复 × 训练负荷 智能管理` (approx 14 chars)
  - Promotional Text (limit 170 chars)
  - Description (limit 4000 chars) — full app description in zh-Hans
  - Keywords (limit 100 chars, comma-separated): example `训练负荷,恢复评分,HRV,心率变异性,ACWR,运动表现,过度训练,周期化,跑步,力量训练`
  - What's New in This Version (limit 4000 chars)
  - Support URL + Marketing URL — reuse existing en URLs unless user provides zh-specific.
  After drafting each value, run a Python length check INSIDE the markdown file by embedding the values in a code block followed by a length assertion table. The check must compute `len(value)` for each (Chinese characters count as 1 each per ASC) and assert `<= limit`. If any value exceeds, shorten and re-check. Include the RESEARCH glossary anchors (训练负荷比 / 心率变异性 / etc.) where the marketing copy references technical terms — use the hybrid form on first occurrence per UI-SPEC Surface 3. Do NOT include keyword padding or repetition tricks; keep keywords on-topic. Note in the file that Tonus name choice (Latin-only vs hybrid) requires user confirmation under Task 2 checkpoint.</action>
  <verify>
    <automated>test -f .planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md && python3 -c "
import re
content = open('.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md').read()
# Extract values via fenced sections labelled name/subtitle/promo/description/keywords/whatsnew
patterns = {
  'App Name': 30,
  'Subtitle': 30,
  'Promotional Text': 170,
  'Description': 4000,
  'Keywords': 100,
  'What\\'s New': 4000,
}
errors = []
for label, limit in patterns.items():
    # Loose extraction — look for a labelled section followed by a fenced block
    m = re.search(r'###\\s+' + re.escape(label) + r'.*?\\n+\`\`\`(?:text)?\\n(.*?)\\n\`\`\`', content, re.DOTALL)
    if not m:
        errors.append(f'{label}: section missing')
        continue
    val = m.group(1).strip()
    if len(val) > limit:
        errors.append(f'{label}: {len(val)} > {limit}')
print('Errors:', errors)
assert not errors
"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md` exists.
    - File contains fenced sections labelled `App Name`, `Subtitle`, `Promotional Text`, `Description`, `Keywords`, `What's New`.
    - Every value passes its ASC character limit (App Name <=30, Subtitle <=30, Promotional Text <=170, Description <=4000, Keywords <=100, What's New <=4000).
    - Keywords field is comma-separated and contains no spaces after commas (ASC convention) — verifiable via `grep -E '^[^ ]+(,[^ ]+)*$'` on the extracted keyword line.
    - First-occurrence hybrid format applied: `训练负荷比 (ACWR)` appears in Description on first mention of ACWR (UI-SPEC Surface 3 + D-07).
    - File notes the App Name decision (Latin-only vs hybrid) is gated on user confirmation in Task 2.
  </acceptance_criteria>
  <done>zh-Hans ASC strings drafted with verified character counts; user can review before pasting.</done>
</task>

<task type="auto">
  <name>Task 2: Create Screenshots-zhHans scheme + render zh-Hans screenshots at iPhone 17 Pro Max resolution</name>
  <files>workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme, .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/</files>
  <read_first>
    - workload management.xcodeproj/xcshareddata/xcschemes/ (list existing schemes — the existing screenshot scheme is the analog; copy + adapt)
    - WorkloadApp/App/AppRouter.swift (existing SCREENSHOT_MODE branch; P1 added a setLocale call inside this branch when -AppleLanguages contains zh-Hans)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md "Integrating with SCREENSHOT_MODE" (lines 634-648) + "Screenshot device families" (lines 622-631)
    - Project STACK.md mentions xcparse as optional — confirm presence with `command -v xcparse`; if absent install via brew or use Xcode-native screenshot extraction from xcresult.
  </read_first>
  <action>Duplicate the existing screenshot scheme (find it under `workload management.xcodeproj/xcshareddata/xcschemes/`) to a new file `Screenshots-zhHans.xcscheme`. In the new scheme, edit Run -> Arguments -> Arguments Passed On Launch to include: `SCREENSHOT_MODE`, `-AppleLanguages`, `(zh-Hans)`, `-AppleLocale`, `zh_Hans_CN`. These four args mirror RESEARCH "Integrating with SCREENSHOT_MODE" lines 638-645. Confirm AppRouter's existing P1-added branch sets `container.localeManager.setLocale(Locale(identifier: "zh-Hans"))` when these args are present. Run the screenshot tests via `xcodebuild test -project "workload management.xcodeproj" -scheme "Screenshots-zhHans" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max'` (the existing en screenshot scheme already defines a ScreenshotTests UI test target — verify by inspecting xcscheme file; if not, planner adds an XCUITest entry that drives each tab). Extract screenshots from the resulting xcresult via xcparse (if available) or manually via Xcode -> Report Navigator. Save under `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/` with filenames like `01-dashboard.png`, `02-workload.png`, `03-recovery.png`, `04-workout-log.png`, `05-profile.png`, `06-paywall.png`, `07-onboarding-language-step.png`, `08-onboarding-frequency-step.png`. Each image MUST be 1320x2868 (iPhone 17 Pro Max portrait) per RESEARCH lines 627; verify via `sips -g pixelHeight -g pixelWidth screenshots-zhHans/*.png` or equivalent. Visually verify NO English fallback strings appear anywhere on the rendered screenshots (this is the visual confirmation the entire P1-P4 chain works end-to-end in zh-Hans). If any English string leaks through, capture which key + which screen and either patch the catalog (revisit Plan 04 Task 1) or add the missing key to Localizable.xcstrings and translate before rerunning the screenshot pass.</action>
  <verify>
    <automated>test -f "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme" && grep -q "AppleLanguages" "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme" && grep -q "zh-Hans" "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme" && grep -q "SCREENSHOT_MODE" "workload management.xcodeproj/xcshareddata/xcschemes/Screenshots-zhHans.xcscheme" && test -d .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/ && ls .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/*.png | wc -l | awk '$1 >= 6 {exit 0} {exit 1}' && for f in .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/*.png; do h=$(sips -g pixelHeight "$f" 2>/dev/null | awk '/pixelHeight/ {print $2}'); w=$(sips -g pixelWidth "$f" 2>/dev/null | awk '/pixelWidth/ {print $2}'); [ "$h" = "2868" ] && [ "$w" = "1320" ] || { echo "wrong size: $f $w x $h"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - Scheme file `Screenshots-zhHans.xcscheme` exists and contains `SCREENSHOT_MODE`, `-AppleLanguages`, `zh-Hans`.
    - Directory `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/` contains at least 6 PNG files (covering Dashboard, Workload, Recovery, Workout Log, Profile, Paywall at minimum; ideally 8 with the two onboarding steps).
    - Every PNG has dimensions 1320 x 2868 (iPhone 17 Pro Max portrait per ASC requirement).
    - Visual sanity (recorded in SUMMARY): no English fallback strings visible in any zh-Hans screenshot (audit performed during the screenshot pass and resolved by translating missing keys back in Plan 04 if found).
  </acceptance_criteria>
  <done>zh-Hans screenshot scheme + 1320x2868 PNGs rendered and saved; ready for human upload to ASC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (checkpoint): User enters zh-Hans App Localization in ASC + uploads screenshots</name>
  <what-built>
    Task 1 produced `.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md` with character-verified zh-Hans values for App Name, Subtitle, Promotional Text, Description, Keywords, What's New. Task 2 produced 1320x2868 PNG screenshots at `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/`.

    Per memory `feedback_asc_caution.md` — Claude does NOT click submission buttons or interact with ASC autonomously. This checkpoint hands control to the user.
  </what-built>
  <how-to-verify>
    Note: ASC UI evolves; if your screen does not match the steps below, pause and describe what you see — the planner will adjust.

    1. Log in to App Store Connect: https://appstoreconnect.apple.com -> My Apps -> Tonus (com.tonus.app).
    2. Sidebar -> **App Information** -> Localizable Information panel -> language dropdown at the top of the section -> **+ Add Language** -> select **Simplified Chinese (zh-Hans)**.
    3. Open `.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md`. Confirm the App Name choice (Latin-only `Tonus` vs hybrid `Tonus · 训练负荷管理`); the file's default is the hybrid. If you prefer brand purity, edit the file in place to `Tonus` and re-run the length check. Paste the chosen App Name + Subtitle into the form. Save.
    4. Sidebar -> **App Store** -> current version (e.g., v1.4) -> language dropdown -> **+ Add Language** -> Simplified Chinese.
    5. Paste Promotional Text, Description, Keywords, What's New from the metadata file. Reuse existing en Support URL + Marketing URL unless you have zh-specific ones. Save.
    6. Open the App Previews and Screenshots section for the new zh-Hans localization. Upload each PNG from `.planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/` in order (the iPhone 17 Pro Max 6.9" Display slot covers smaller display classes via scaling). Save.
    7. **Do NOT click "Submit for Review" or "Release"**. Saved metadata persists; submission ships with the next version submission triggered separately by you.
    8. Confirm in this session by replying "approved" with any notes.

    Caveats:
    - ASC sometimes asks for a Privacy Policy URL per-locale even though en is global — reuse the existing en privacy URL if so.
    - If any field rejects with a character-count error despite the local check, paste the exact field name and the error message back here so the planner can revise the draft.
    - If a screenshot rejects for resolution, confirm it is 1320x2868; if smaller, regenerate via the Screenshots-zhHans scheme.
  </how-to-verify>
  <resume-signal>Reply "approved" (with optional notes) once all zh-Hans metadata is saved in ASC and all screenshots are uploaded. Reply with the exact error text if ASC rejects any field. Phase 23 closes after this checkpoint approves.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local draft -> App Store Connect | User-mediated paste; no Claude automation against ASC |
| Screenshot files | Local-only artifacts; uploaded by user under blocking-human gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Tampering | ASC submission button | mitigate | Plan is `autonomous: false`; all ASC interaction gated by `checkpoint:human-verify gate="blocking-human"`; memory `feedback_asc_caution.md` reinforces never auto-clicking submission |
| T-23-12 | Information Disclosure | Marketing string content | accept | Marketing copy is public by definition once on the App Store; no PII; tone reviewed in Plan 04 Task 3 |
</threat_model>

<verification>
```bash
# Metadata length check
python3 -c "import re; c=open('.planning/phases/23-multi-language-in-app-support-simplified-chinese/asc-metadata-zhHans.md').read(); print(re.findall(r'###\\s+\\S+', c))"

# Screenshot resolution
sips -g pixelWidth -g pixelHeight .planning/phases/23-multi-language-in-app-support-simplified-chinese/screenshots-zhHans/*.png | head -40
```

Final verification = user checkpoint approval.
</verification>

<success_criteria>
- All three task acceptance_criteria pass.
- `asc-metadata-zhHans.md` exists with verified character counts.
- 6+ screenshots at 1320x2868 saved under phase directory.
- User has saved zh-Hans App Localization in App Store Connect AND uploaded screenshots.
- No "Submit for Review" or "Release" button was clicked autonomously.
</success_criteria>

<output>
Create `.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-05-SUMMARY.md` when done. Include the final character counts for each ASC field and the screenshot file list.
</output>
