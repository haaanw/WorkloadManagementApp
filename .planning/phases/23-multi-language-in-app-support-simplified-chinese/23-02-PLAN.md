---
phase: 23-multi-language-in-app-support-simplified-chinese
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - WorkloadApp/Models/Enums.swift
  - WorkloadApp/Services/AuthService.swift
  - WorkloadApp/Services/NotificationService.swift
  - WorkloadApp/Resources/Localizable.xcstrings
  - WorkloadApp/Resources/InfoPlist.xcstrings
  - WorkloadApp/Views/Dashboard/**/*.swift
  - WorkloadApp/Views/WorkoutLog/**/*.swift
  - WorkloadApp/Views/Recovery/**/*.swift
  - WorkloadApp/Views/Workload/**/*.swift
  - WorkloadApp/Views/Auth/**/*.swift
  - WorkloadApp/Views/Subscription/**/*.swift
  - WorkloadApp/Views/Onboarding/**/*.swift
  - WorkloadApp/Views/Profile/**/*.swift
  - WorkloadApp/Views/Coach/**/*.swift
  - WorkloadApp/Components/**/*.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every user-facing Text(\"...\") / Label(\"...\", ...) / Button(\"...\") / .alert / .navigationTitle / .toolbar literal uses a namespaced LocalizedStringKey (e.g., \"tab.home\", \"action.save\")"
    - "Every enum displayName in Models/Enums.swift uses String(localized: \"...\", defaultValue: \"...\")"
    - "AuthService.AuthError exposes a stable `localizationKey: String.LocalizationValue` (or equivalent key enum); LoginView/SignUpView render the user-visible message by re-resolving the key at the View boundary with the injected env locale — live-switch works without re-throw"
    - "Zero matches for `error.localizedDescription` in WorkloadApp/Views/ (auth error path explicitly re-resolves on env locale)"
    - "NotificationService.scheduleWeeklySummary composes content via .localizedUserNotificationString(forKey:arguments:) — deliver-time localization"
    - "NotificationService cancels + reschedules any legacy pending weekly-summary on first launch when a UserDefaults-tracked notificationSchemaVersion is less than the current version"
    - "InfoPlist.xcstrings carries NSHealthShareUsageDescription for en + zh-Hans value placeholder (zh-Hans value populated in P4)"
    - "git grep 'Locale.current' on WorkloadApp/Views, WorkloadApp/Components, WorkloadApp/Utilities returns zero matches"
    - "git grep -n 'String(localized:' WorkloadApp/Models/ filtered to exclude Enums.swift returns zero matches (D-05 boundary: only Enums may carry localized strings inside Models/)"
    - "Compiler-extracted Localizable.xcstrings contains entries for all sport.*, zone.*, recoveryZone.*, frequency.*, experience.*, cyclePhase.*, auth.error.*, notif.*, tab.*, action.*, profile.*, onboarding.*, language.picker.*, date.*, duration.*, paywall.*, term.* namespaces"
  artifacts:
    - path: "WorkloadApp/Models/Enums.swift"
      provides: "All ~15 displayName properties resolved via String(localized:, defaultValue:)"
    - path: "WorkloadApp/Services/AuthService.swift"
      provides: "AuthError carries a stable localizationKey + en defaultValue; errorDescription kept as debug fallback"
    - path: "WorkloadApp/Services/NotificationService.swift"
      provides: "Compose-time deferred localization + reschedule migration"
    - path: "WorkloadApp/Resources/Localizable.xcstrings"
      provides: "Populated catalog (en source values only; zh-Hans values placeholder — populated in P4)"
    - path: "WorkloadApp/Resources/InfoPlist.xcstrings"
      provides: "NSHealthShareUsageDescription key present"
  key_links:
    - from: "WorkloadApp/Models/Enums.swift"
      to: "WorkloadApp/Resources/Localizable.xcstrings"
      via: "String(localized:) compiler extraction"
      pattern: "String\\(localized:"
    - from: "WorkloadApp/Views/Auth/LoginView.swift, SignUpView.swift"
      to: "WorkloadApp/Services/AuthService.swift"
      via: "String(localized: error.localizationKey, locale: env.locale) at View boundary — re-resolves on env locale change"
      pattern: "error\\.localizationKey"
    - from: "WorkloadApp/Services/NotificationService.swift"
      to: "WorkloadApp/Resources/Localizable.xcstrings"
      via: ".localizedUserNotificationString(forKey:) — iOS resolves at deliver time"
      pattern: "localizedUserNotificationString"
---

<objective>
String sweep + catalog migration. Convert every hardcoded user-facing literal across enum displayNames, AuthError, notifications, and all Views/Components into `String(localized:)` or `LocalizedStringKey` forms. Move HealthKit consent string into `InfoPlist.xcstrings`. Reschedule existing pending weekly-summary notifications so they pick up the deferred-localization path. Auth errors live-switch via View-boundary re-resolution against the env locale (per RESEARCH Pitfall 7).

Purpose: Realize D-01, D-02, D-03, D-10, D-11, D-14. After P1 stood up the plumbing, every English string must move into the catalog so P4's translation pass has something to translate. Compiler extraction (enabled in P1) auto-populates the catalog on Build.

Output: Localizable.xcstrings populated with the full en source surface area; zh-Hans column still untranslated (P4 owns translation); notification migration logic deployed; auth errors re-render on live switch; build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-CONTEXT.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md
@.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-01-PLAN.md
@WorkloadApp/Models/Enums.swift
@WorkloadApp/Services/AuthService.swift
@WorkloadApp/Services/NotificationService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate enum displayNames + AuthError live-switch + non-View service strings to String(localized:)</name>
  <files>WorkloadApp/Models/Enums.swift, WorkloadApp/Services/AuthService.swift</files>
  <read_first>
    - WorkloadApp/Models/Enums.swift (target — 15 displayName properties; PATTERNS.md lists lines 16, 45, 66, 79, 105, 139, 180, 199, 224, 271, 289, 310, 327, 358, 390)
    - WorkloadApp/Services/AuthService.swift (target — AuthError lines 116-127)
    - WorkloadApp/Views/Auth/LoginView.swift (call sites at lines 191, 229, 264 use `error.localizedDescription`; local `AuthBootstrapError` at lines 270-279 also needs same treatment)
    - WorkloadApp/Views/Auth/SignUpView.swift (call sites at lines 197, 235, 270 use `error.localizedDescription`; local AuthBootstrapError at line 279)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md §"Models/Enums.swift" (lines 376-410) + §"Services/AuthService.swift" (lines 413-447)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md §"Canonical zh-Hans Glossary" (lines 519-582) AND Pitfall 7 (lines 835-839 — `String(localized:)` without explicit locale resolves against Bundle.main/AppleLanguages, NOT the env locale, so auth errors won't live-switch unless the View re-resolves)
  </read_first>
  <action>STEP A — Enums.swift: in each `var displayName: String` switch case across all enums (SportType, WeightUnit, ACWRMethod, LoadSource, ACWRZone, RecoveryZone, MuscleGroup, ExerciseCategory, SessionType, Frequency, Experience, CyclePhase, and any other enums with `displayName`), replace each `return "Lifting"`-style literal with `return String(localized: "<namespace>.<caseId>", defaultValue: "Lifting")`. Key namespacing from RESEARCH §"Canonical zh-Hans Glossary": `sport.lifting`, `sport.running`, `sport.cycling`, `sport.teamSport`, `sport.crossfit`, `sport.swimming`, `sport.custom`; `weightUnit.kg`, `weightUnit.lbs`; `zone.optimal`, `zone.caution`, `zone.danger`, `zone.low`; `recoveryZone.red`, `recoveryZone.yellow`, `recoveryZone.green`; `frequency.oneToTwo`, `frequency.threeToFour`, `frequency.fiveToSix`, `frequency.sevenPlus`; `experience.beginner`, `experience.intermediate`, `experience.advanced`; `cyclePhase.earlyFollicular`, `cyclePhase.lateFollicular`, `cyclePhase.ovulatory`, `cyclePhase.earlyLuteal`, `cyclePhase.lateLuteal`; `acwrMethod.*`, `loadSource.*`, `muscleGroup.*`, `exerciseCategory.*`, `sessionType.*`. The `defaultValue:` parameter MUST be the existing en literal verbatim. DO NOT use string interpolation in keys (RESEARCH Pitfall 1).

STEP B — AuthService.swift AuthError live-switch (RESEARCH Pitfall 7): add a stable `localizationKey: String.LocalizationValue` (or equivalent `var localizationKey: String.LocalizationValue` computed property) to `AuthError` that returns the catalog key per case: `.noUserReturned` → `"auth.error.noUserReturned"`, `.noIdentityToken` → `"auth.error.noIdentityToken"`, `.socialSignInFailed(let message)` → wrap the raw server message as a non-localizable verbatim value (the server string is opaque — return a `String.LocalizationValue` constructed from the message; this case does not live-switch by design). Add corresponding `var defaultValue: String` so the en source value can be passed to `String(localized:, defaultValue:)` at the call site: `.noUserReturned` → "Sign up succeeded but no user was returned. Please try again.", `.noIdentityToken` → "Apple sign in failed. Could not retrieve identity token.", `.socialSignInFailed(let message)` → message. Keep `errorDescription` as a debug fallback (it can continue to return the en defaultValue — no `locale:` argument needed there; this is used by NSError tooling/logging, not by SwiftUI views). DO NOT remove `errorDescription`.

For the local `AuthBootstrapError` enums in LoginView.swift (lines 270-279) and SignUpView.swift (line 279+), apply the same `localizationKey` + `defaultValue` pattern with keys `auth.error.noUserId` and `auth.error.athleteNotFound`. (View-boundary re-resolution code itself lives in Task 2c — this task only adds the surfaces.)

DO NOT add a `locale:` argument anywhere inside AuthService — the View re-resolves with env locale at render time (Task 2c). The compiler-extracted catalog still picks up the keys via `String.LocalizationValue` literals.</action>
  <verify>
    <automated>! grep -nE 'case \.[a-zA-Z]+:\s*"[A-Z]' WorkloadApp/Models/Enums.swift && grep -c 'String(localized:' WorkloadApp/Models/Enums.swift | awk '$1 >= 25' && grep -q 'localizationKey' WorkloadApp/Services/AuthService.swift && grep -q 'auth.error.noUserReturned' WorkloadApp/Services/AuthService.swift && grep -q 'auth.error.noIdentityToken' WorkloadApp/Services/AuthService.swift && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `Models/Enums.swift` contains NO bare `case .x: "English"` lines (`grep -nE 'case \.[a-zA-Z]+:\s*"[A-Z]'` returns zero).
    - `Models/Enums.swift` contains at least 25 occurrences of `String(localized:`.
    - Every `String(localized:)` call in `Models/Enums.swift` uses a literal key — `! grep -nE 'String\(localized: "[^"]*\\\(' WorkloadApp/Models/Enums.swift`.
    - `AuthService.swift` AuthError exposes `localizationKey` and `defaultValue` members; both `auth.error.noUserReturned` and `auth.error.noIdentityToken` literal keys are present.
    - `AuthService.swift` `errorDescription` is preserved as a debug fallback.
    - `git grep -n 'String(localized:' WorkloadApp/Models/ | grep -v Enums.swift` returns zero matches (D-05 boundary: only Enums.swift may carry localized strings inside Models/).
    - xcodebuild for scheme "workload management" exits 0.
  </acceptance_criteria>
  <done>Enum displayNames + AuthError surfaces use String(localized: defaultValue:) and expose stable keys; View-boundary re-resolution wiring is staged for Task 2c.</done>
</task>

<task type="auto">
  <name>Task 2a: Sweep Dashboard + WorkoutLog Views into LocalizedStringKey</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/Views/Dashboard/RecentSessionsCard.swift, WorkloadApp/Views/Dashboard/MetricsStrip.swift, WorkloadApp/Views/Dashboard/RecoveryHeroCard.swift, WorkloadApp/Views/Dashboard/ACWRSection.swift, WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift, WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift, WorkloadApp/Views/WorkoutLog/SessionDetailView.swift, WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift</files>
  <read_first>
    - Each target file under WorkloadApp/Views/Dashboard/ and WorkloadApp/Views/WorkoutLog/ (enumerate actual files on disk first; the list above is the expected set — adjust if files differ)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md §"Migration Strategy" Step 4 (lines 405-419) and Step 5 (lines 420-433); Pitfall 4 (lines 817-822) for Chart axis labels
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md §"Shared Patterns"
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md §"Copywriting Contract" (lines 274-303)
  </read_first>
  <action>For every `.swift` file in the file list above, replace each hardcoded user-facing literal in `Text("...")`, `Label("...", systemImage:)`, `Button("...")`, `.alert("...", ...)`, `.confirmationDialog("...")`, `.navigationTitle("...")`, `.toolbar { Text("...") }`, `TextField("...", text:)` placeholder, `Section { } header: { Text("...") }`, `Picker("...", selection:)` with a namespaced LocalizedStringKey literal. Use namespaces: `dashboard.*`, `workoutLog.*`, `tab.home`, `tab.log`, `action.save`, `action.cancel`, `action.continue`, `action.delete`, `action.done`. The literal English text becomes the en source value via compiler extraction. In each modified View, add `@Environment(\.locale) private var locale` and update every call to `DateHelpers.shortString` / `relativeString` / `WeightFormatter.display` / `displayVolume` to pass `locale:` argument. Build incrementally every 3-5 files per CLAUDE.md.</action>
  <verify>
    <automated>matches=$(git grep -l 'Locale.current' WorkloadApp/Views/Dashboard WorkloadApp/Views/WorkoutLog 2>/dev/null); test -z "$matches" || { echo "VIOLATIONS: $matches"; false; } ; xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'Locale.current' WorkloadApp/Views/Dashboard WorkloadApp/Views/WorkoutLog` returns ZERO matches.
    - `git grep -nE '\.shortString\b[^(]' WorkloadApp/Views/Dashboard WorkloadApp/Views/WorkoutLog` returns ZERO matches (every call passes a locale parameter).
    - DashboardView.swift contains `@Environment(\.locale)`.
    - No new `RoundedRectangle` or `.shadow(` in modified files.
    - xcodebuild succeeds.
  </acceptance_criteria>
  <done>Dashboard + WorkoutLog views fully localized and locale-aware; build green.</done>
</task>

<task type="auto">
  <name>Task 2b: Sweep Recovery + Workload + Charts Views into LocalizedStringKey (with Chart axis locale rebuild)</name>
  <files>WorkloadApp/Views/Recovery/RecoveryView.swift, WorkloadApp/Views/Recovery/WellnessCheckInSheet.swift, WorkloadApp/Views/Workload/WorkloadView.swift, WorkloadApp/Components/HRVTrendChart.swift, WorkloadApp/Components/SleepTrendChart.swift</files>
  <read_first>
    - Each target file (enumerate actuals — Recovery/, Workload/, and any chart files under Components/)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md §"Pitfall 4 Charts framework using its own locale" (lines 817-822) — Chart axis labels need explicit `.locale(locale)` AND `.id(locale)` on the Chart view to force a rebuild on env-locale change
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md §"Shared Patterns"
  </read_first>
  <action>Apply the same LocalizedStringKey sweep pattern as Task 2a across the Recovery + Workload + Charts file list. Namespaces: `recovery.*`, `workload.*`. In `WorkloadView.swift` and the chart Components, locate every `AxisValueLabel { Text(dateValue, format: ...) }` closure and pass the injected locale: `Text(dateValue, format: .dateTime.month(.abbreviated).day().locale(locale))`. Add `.id(locale)` modifier to each Chart view so it rebuilds on locale change (RESEARCH Pitfall 4). Add `@Environment(\.locale) private var locale` to each View if absent. Build incrementally every 3-5 files.</action>
  <verify>
    <automated>matches=$(git grep -l 'Locale.current' WorkloadApp/Views/Recovery WorkloadApp/Views/Workload 2>/dev/null); test -z "$matches" || { echo "VIOLATIONS: $matches"; false; } ; grep -q '\.id(locale)' WorkloadApp/Views/Workload/WorkloadView.swift && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'Locale.current' WorkloadApp/Views/Recovery WorkloadApp/Views/Workload` returns ZERO matches.
    - `WorkloadView.swift` contains `.id(locale)` on each Chart view AND `.locale(locale)` on date format inside AxisValueLabel.
    - HRVTrendChart + SleepTrendChart components also receive `.id(locale)`.
    - No new `RoundedRectangle` or `.shadow(` in modified files.
    - xcodebuild succeeds.
  </acceptance_criteria>
  <done>Recovery + Workload + chart components localized; charts respect injected locale on live switch.</done>
</task>

<task type="auto">
  <name>Task 2c: Sweep Auth + Subscription + Paywall Views with View-boundary AuthError re-resolution</name>
  <files>WorkloadApp/Views/Auth/LoginView.swift, WorkloadApp/Views/Auth/SignUpView.swift, WorkloadApp/Views/Subscription/UpgradeSheet.swift</files>
  <read_first>
    - WorkloadApp/Views/Auth/LoginView.swift (call sites at lines 191, 229, 264 use `error.localizedDescription` — these must be replaced; local AuthBootstrapError at lines 270-279)
    - WorkloadApp/Views/Auth/SignUpView.swift (call sites at lines 197, 235, 270 use `error.localizedDescription`; local AuthBootstrapError near line 279)
    - WorkloadApp/Views/Subscription/UpgradeSheet.swift (target — full sweep)
    - WorkloadApp/Services/AuthService.swift (post-Task-1 — AuthError exposes `localizationKey` and `defaultValue`)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md Pitfall 7 (lines 835-839) — explains why `String(localized:)` without explicit `locale:` does NOT live-switch
  </read_first>
  <action>STEP A — Standard LocalizedStringKey sweep across all three files using namespaces `auth.login.*`, `auth.signup.*`, `paywall.*`, `action.*`. Add `@Environment(\.locale) private var locale` to each View.

STEP B — Replace every `errorMessage = error.localizedDescription` (LoginView lines 191, 229, 264; SignUpView lines 197, 235, 270) with a View-boundary re-resolution path that handles both `AuthService.AuthError` and the local `AuthBootstrapError`. Pattern:
- Cast the caught error: `if let authError = error as? AuthService.AuthError { errorMessage = String(localized: authError.localizationKey, defaultValue: authError.defaultValue, locale: locale) } else if let bootstrap = error as? AuthBootstrapError { errorMessage = String(localized: bootstrap.localizationKey, defaultValue: bootstrap.defaultValue, locale: locale) } else { errorMessage = error.localizedDescription }`
- The final `else` branch (non-typed errors like Supabase SDK errors) keeps `.localizedDescription` only as last-resort fallback for server-originated strings that the catalog cannot translate.
- Adjust the local `AuthBootstrapError` enum (LoginView lines 270-279, SignUpView counterpart) to expose `localizationKey: String.LocalizationValue` and `defaultValue: String` per case (keys `auth.error.noUserId`, `auth.error.athleteNotFound`) — these surfaces were added in Task 1.

STEP C — Re-rendering on live switch: because `errorMessage` is `@State`, it does NOT automatically re-resolve when env locale changes mid-flight. Add a `.onChange(of: locale)` modifier on the error-bearing view container that, if `errorMessage` is non-empty AND a captured `lastAuthError: (any Error)?` `@State` is set, re-runs the cast-and-resolve block above to refresh `errorMessage` against the new env locale. Capture `lastAuthError = error` in each catch block alongside the message assignment.

STEP D — Build incrementally every 3-5 files; confirm zero `error.localizedDescription` remain in the auth typed-cast branches.</action>
  <verify>
    <automated>! git grep -nE 'errorMessage\s*=\s*error\.localizedDescription' WorkloadApp/Views/Auth/ ; grep -q 'authError.localizationKey' WorkloadApp/Views/Auth/LoginView.swift && grep -q 'authError.localizationKey' WorkloadApp/Views/Auth/SignUpView.swift && grep -q 'onChange(of: locale' WorkloadApp/Views/Auth/LoginView.swift && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'error.localizedDescription' WorkloadApp/Views/` returns ZERO matches in the typed-cast branches (last-resort fallback `else` for non-typed SDK errors is allowed — distinguish by inspecting surrounding code; if any literal call to `error.localizedDescription` appears OUTSIDE an `else` fallback for unmatched error types, it is a violation).
    - LoginView.swift and SignUpView.swift each contain at least one `String(localized: ..., locale: locale)` call resolving an auth error key.
    - LoginView.swift contains `.onChange(of: locale)` that re-resolves any pending `errorMessage` against the new env locale.
    - `git grep -n 'Locale.current' WorkloadApp/Views/Auth WorkloadApp/Views/Subscription` returns ZERO matches.
    - Live-switch acceptance proof (manual smoke documented in SUMMARY): trigger an auth error (bad credentials), confirm message text changes from English to Chinese when locale flips via LanguagePickerView with the error still on-screen — no re-throw required.
    - No new `RoundedRectangle` or `.shadow(` in modified files.
    - xcodebuild succeeds.
  </acceptance_criteria>
  <done>Auth + Subscription + Paywall localized; auth errors live-switch against env locale; UpgradeSheet copy in catalog. (Note: RevenueCat product display names — "Athlete Pro" / "Coach" — remain English inside zh-Hans UpgradeSheet; updating the RC dashboard zh-Hans titles is out of scope for Phase 23 per Plan 23-04 follow-up.)</done>
</task>

<task type="auto">
  <name>Task 2d: Sweep Onboarding + Profile + Coach + Templates Views into LocalizedStringKey</name>
  <files>WorkloadApp/Views/Onboarding/OnboardingView.swift, WorkloadApp/Views/Profile/ProfileView.swift, WorkloadApp/Views/Profile/LanguagePickerView.swift, WorkloadApp/Views/Coach/CoachRosterView.swift, WorkloadApp/Views/Coach/CoachAthleteDetailView.swift, WorkloadApp/Views/Templates/TemplateEditorSheet.swift, WorkloadApp/Views/Templates/TemplateListView.swift</files>
  <read_first>
    - Each target file (enumerate actuals on disk; LanguagePickerView already uses LocalizedStringKey from P1 — confirm and skip if clean)
    - OnboardingView already partly touched in P1 Task 3 — finish any remaining literals
    - ProfileView already partly touched in P1 Task 3 — finish remaining literals
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md §"Copywriting Contract"
  </read_first>
  <action>Standard LocalizedStringKey sweep across the file list. Namespaces: `onboarding.*`, `profile.*`, `coach.*`, `template.*`, `tab.profile`, `tab.roster`, `tab.templates`, `language.picker.*`. Add `@Environment(\.locale) private var locale` where dates/weights are formatted. Verify P1-touched files (LanguagePickerView, OnboardingView language step, ProfileView language row) are not regressed — keep their keys as defined in P1. Build incrementally every 3-5 files.</action>
  <verify>
    <automated>matches=$(git grep -l 'Locale.current' WorkloadApp/Views/Onboarding WorkloadApp/Views/Profile WorkloadApp/Views/Coach WorkloadApp/Views/Templates 2>/dev/null); test -z "$matches" || { echo "VIOLATIONS: $matches"; false; } ; xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'Locale.current' WorkloadApp/Views/Onboarding WorkloadApp/Views/Profile WorkloadApp/Views/Coach WorkloadApp/Views/Templates` returns ZERO matches.
    - P1 keys preserved (`grep -q "language.picker.title" WorkloadApp/Views/Profile/LanguagePickerView.swift`).
    - No new `RoundedRectangle` or `.shadow(` in modified files.
    - xcodebuild succeeds.
  </acceptance_criteria>
  <done>Onboarding, Profile, Coach, and Templates views fully localized; build green.</done>
</task>

<task type="auto">
  <name>Task 2e: Sweep reusable Components into LocalizedStringKey with locale-conditional styling</name>
  <files>WorkloadApp/Components/MetricTile.swift, WorkloadApp/Components/ZoneBadge.swift, WorkloadApp/Components/EmptyStateCard.swift, WorkloadApp/Components/SectionHeader.swift</files>
  <read_first>
    - Each target file (enumerate actuals under WorkloadApp/Components/; the list above is the expected reusable component set — adjust to match disk)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md §"Components/MetricTile.swift + ZoneBadge"
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-UI-SPEC.md lines 70-99 (zh-Hans density audit, typography overrides) and lines 76, 78, 94, 96 (locale-conditional textCase/tracking/padding)
  </read_first>
  <action>Standard LocalizedStringKey sweep across all reusable components. In `Components/MetricTile.swift`, confirm `Text(title).font(.Tokens.micro)` has no `.lineLimit(1)` (UI-SPEC line 75 — wrap permitted). In `Components/ZoneBadge.swift` apply UI-SPEC line 78 + line 94 + line 96 locale-conditional rules: `@Environment(\.locale) private var locale`; horizontal padding `locale.identifier == "zh-Hans" ? 16 : 10`; only apply `.textCase(.uppercase)` and tracking `.tracking(1.2)` when `locale.languageCode?.identifier == "en"` (Chinese has no case). Build incrementally.</action>
  <verify>
    <automated>matches=$(git grep -l 'Locale.current' WorkloadApp/Components 2>/dev/null); test -z "$matches" || { echo "VIOLATIONS: $matches"; false; } ; grep -q '"zh-Hans" ? 16 : 10' WorkloadApp/Components/ZoneBadge.swift && ! grep -q '\.lineLimit(1)' WorkloadApp/Components/MetricTile.swift && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'Locale.current' WorkloadApp/Components` returns ZERO matches.
    - `Components/ZoneBadge.swift` contains locale-conditional padding (`"zh-Hans" ? 16 : 10`) AND conditional `.textCase(.uppercase)` keyed on `languageCode?.identifier == "en"`.
    - `Components/MetricTile.swift` does NOT call `.lineLimit(1)` on the title Text.
    - No new `RoundedRectangle` or `.shadow(` in modified files.
    - xcodebuild succeeds.
    - PHASE-WIDE D-14 audit: `git grep -n 'Locale.current' WorkloadApp/Views WorkloadApp/Components WorkloadApp/Utilities` returns ZERO matches across the entire restricted-tier surface (this task is the last to touch Components — the audit is the closing gate).
    - PHASE-WIDE D-05 audit: `git grep -n 'String(localized:' WorkloadApp/Models/ | grep -v Enums.swift` returns ZERO matches (Models tier confined to Enums.swift).
    - PHASE-WIDE catalog audit: `Localizable.xcstrings` (after a Build run) contains entries for at least namespaces `tab.`, `action.`, `auth.error.`, `sport.`, `zone.`, `recoveryZone.`, `dashboard.`, `recovery.`, `workload.`, `profile.`, `onboarding.`, `paywall.`, `language.picker.` — verifiable by `python3 -c "import json,sys; d=json.load(open('WorkloadApp/Resources/Localizable.xcstrings')); keys=list(d['strings'].keys()); [sys.exit(1) for ns in ['tab.','action.','auth.error.','sport.','zone.','dashboard.','onboarding.','profile.','language.picker.'] if not any(k.startswith(ns) for k in keys)]"`.
  </acceptance_criteria>
  <done>Reusable components localized with zh-Hans-aware styling; phase-wide D-05/D-14/catalog audits pass.</done>
</task>

<task type="auto">
  <name>Task 3: NotificationService deferred localization + reschedule migration + InfoPlist HealthKit consent</name>
  <files>WorkloadApp/Services/NotificationService.swift, WorkloadApp/Resources/InfoPlist.xcstrings, WorkloadApp/Resources/Localizable.xcstrings</files>
  <read_first>
    - WorkloadApp/Services/NotificationService.swift (target — `scheduleWeeklySummary` lines 32-55; `buildNotificationBody` lines 66-90 if present)
    - workload management/workload-management-Info.plist (current en string for `NSHealthShareUsageDescription`)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-PATTERNS.md §"Services/NotificationService.swift" (lines 451-491)
    - .planning/phases/23-multi-language-in-app-support-simplified-chinese/23-RESEARCH.md §"Pattern 4" (lines 326-343), §"Pre-existing notification cleanup" (lines 729-738), §"HealthKit + Info.plist Localization" (lines 650-682)
  </read_first>
  <action>In `NotificationService.swift`, rewrite `scheduleWeeklySummary` per PATTERNS.md Pattern §"Services/NotificationService.swift": change the signature to take structured arguments — `func scheduleWeeklySummary(weekday: Int, hour: Int, minute: Int, sessionCount: Int, streak: Int, prCount: Int, volumeDelta: Double)`. Inside, `content.title = .localizedUserNotificationString(forKey: "notif.weekly.title", arguments: nil)` and `content.body = .localizedUserNotificationString(forKey: "notif.weekly.body.template", arguments: [sessionCount, streak, prCount, Int(abs(volumeDelta))])`. REMOVE the existing `buildNotificationBody` helper if its only use was composing `content.body` in Swift. Update every call site of `scheduleWeeklySummary` (search `git grep -nE 'scheduleWeeklySummary' WorkloadApp/`) to pass the structured arguments. Add a `notificationSchemaVersion` migration: define `private static let currentSchemaVersion = 2` in `NotificationService`; on `NotificationService.init()` (or in a method invoked from `AppContainer.init`), read `UserDefaults.standard.integer(forKey: "notificationSchemaVersion")`; if `< currentSchemaVersion` AND notifications are enabled (per existing `@AppStorage` flag — inspect the file to find it), call `cancelWeeklySummary()` then re-invoke `scheduleWeeklySummary` with the latest values; finally write `UserDefaults.standard.set(currentSchemaVersion, forKey: "notificationSchemaVersion")`. Idempotent — second launch sees version == current and no-ops. Add `notif.weekly.title` and `notif.weekly.body.template` keys to `Localizable.xcstrings` (en values: `"Your Week in Review"` and `"%lld sessions logged — %lld week streak. %lld new PRs!"`). Use Xcode plural-rule editor for English plural variations on session/streak/PR. Migrate `NSHealthShareUsageDescription` localization: in `InfoPlist.xcstrings`, add an entry with `extractionState: "manual"` and an en `stringUnit.value` equal to the existing Info.plist string verbatim, and an empty zh-Hans `stringUnit` (P4 populates the zh-Hans value from UI-SPEC line 291).</action>
  <verify>
    <automated>grep -q "localizedUserNotificationString" WorkloadApp/Services/NotificationService.swift && grep -q "notif.weekly.title" WorkloadApp/Services/NotificationService.swift && grep -q "notif.weekly.body.template" WorkloadApp/Services/NotificationService.swift && grep -q "notificationSchemaVersion" WorkloadApp/Services/NotificationService.swift && python3 -c "import json,sys; d=json.load(open('WorkloadApp/Resources/InfoPlist.xcstrings')); assert 'NSHealthShareUsageDescription' in d.get('strings',{}), 'missing key'" && python3 -c "import json,sys; d=json.load(open('WorkloadApp/Resources/Localizable.xcstrings')); ks=d.get('strings',{}); assert 'notif.weekly.title' in ks and 'notif.weekly.body.template' in ks" && xcodebuild -project "workload management.xcodeproj" -scheme "workload management" -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' build 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `NotificationService.swift` contains `localizedUserNotificationString` at least twice (title + body).
    - `NotificationService.swift` contains a `notificationSchemaVersion` UserDefaults read/write — `grep -c notificationSchemaVersion WorkloadApp/Services/NotificationService.swift` >= 2.
    - `Localizable.xcstrings` contains keys `notif.weekly.title` and `notif.weekly.body.template`.
    - `InfoPlist.xcstrings` contains a `NSHealthShareUsageDescription` entry with en source string equal to the current Info.plist en value.
    - xcodebuild for scheme "workload management" exits 0.
    - Manual smoke (documented in SUMMARY): cancel + reschedule path runs without exceptions on simulator first launch when `notificationSchemaVersion` is absent.
  </acceptance_criteria>
  <done>Notifications use deliver-time localization, legacy schedules cancelled + replayed on first launch after Phase 23 ships, HealthKit consent string is localizable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog key construction | Static literals only — never interpolated from user input |
| UNNotificationContent payload | Tested for English-only fallback leakage (notification fires correctly when device language flips post-scheduling) |
| AuthError → View error rendering | Error key resolved at View boundary using env locale — never burns localized string at throw time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | `String(localized:)` key interpolation | mitigate | Grep gate forbidding `String(localized: "..\(..)` patterns (see RESEARCH Pitfall 1 + Security V5) |
| T-23-06 | Information Disclosure | Push notification payload | mitigate | `.localizedUserNotificationString` resolves at deliver time; no English-only string burns into UNCalendarTrigger content; reschedule migration purges legacy English-baked payloads |
| T-23-07 | Repudiation | HealthKit consent localization | mitigate | `NSHealthShareUsageDescription` moved into `InfoPlist.xcstrings`; iOS Settings + first-prompt resolve from localized value per Apple HIG (RESEARCH §"HealthKit + Info.plist Localization") |
| T-23-08 | Information Disclosure | Auth error string locked to scheduling locale | mitigate | View-boundary re-resolution via `String(localized: error.localizationKey, locale: env.locale)` + `.onChange(of: locale)` refresh path defends RESEARCH Pitfall 7 |
</threat_model>

<verification>
```bash
# Locale.current audit — D-14 enforcement
git grep -n 'Locale.current' WorkloadApp/Views/ WorkloadApp/Components/ WorkloadApp/Utilities/
# expected: zero matches

# D-05 boundary lint — only Enums.swift may carry String(localized:) inside Models/
git grep -n 'String(localized:' WorkloadApp/Models/ | grep -v Enums.swift
# expected: zero matches

# Dynamic key audit — RESEARCH Pitfall 1
git grep -nE 'String\(localized: "[^"]*\\\(' WorkloadApp/
# expected: zero matches

# Auth error live-switch audit — RESEARCH Pitfall 7
git grep -n 'error.localizedDescription' WorkloadApp/Views/
# expected: zero matches (or only inside else-fallback branches for non-typed SDK errors)

# Catalog populated
python3 -c "import json; d=json.load(open('WorkloadApp/Resources/Localizable.xcstrings')); print('key count:', len(d['strings']))"
# expected: well over 100 keys after the sweep
```

Manual: Trigger an auth error on LoginView (bad creds), tap LanguagePickerView mid-error, confirm errorMessage text re-renders in the new locale without re-throwing. Schedule weekly summary on simulator, advance clock, confirm notification displays in current device language; change device language, advance clock again — same pending request fires in new language without re-scheduling.
</verification>

<success_criteria>
- All seven task acceptance_criteria pass.
- `git grep 'Locale.current' WorkloadApp/Views/ WorkloadApp/Components/ WorkloadApp/Utilities/` returns ZERO matches.
- `git grep 'String(localized:' WorkloadApp/Models/ | grep -v Enums.swift` returns ZERO matches.
- `git grep 'error.localizedDescription' WorkloadApp/Views/` returns ZERO matches in typed-cast branches.
- No dynamic key interpolation anywhere in the codebase.
- `Localizable.xcstrings` contains all expected namespaces.
- `InfoPlist.xcstrings` contains `NSHealthShareUsageDescription`.
- NotificationService uses `.localizedUserNotificationString` and reschedule migration.
- AuthError live-switches against env locale via View-boundary re-resolution.
- xcodebuild clean on iPhone 17 Pro Max simulator.
</success_criteria>

<output>
Create `.planning/phases/23-multi-language-in-app-support-simplified-chinese/23-02-SUMMARY.md` when done.
</output>
