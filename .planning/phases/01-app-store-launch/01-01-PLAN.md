---
phase: 01-app-store-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workload management/workload management.xcodeproj/project.pbxproj
  - WorkloadApp/Services/SubscriptionService.swift
  - WorkloadApp/Views/Subscription/UpgradeSheet.swift
  - WorkloadApp/App/AppRouter.swift
autonomous: true
requirements: [STORE-01, STORE-03]

must_haves:
  truths:
    - "All bundle identifiers in the Xcode project use the com.tonus.app prefix"
    - "GitHub Pages URLs for privacy, terms, and support return HTTP 200"
    - "Fallback subscription prices in UpgradeSheet match D-08 pricing"
    - "SCREENSHOT_MODE sets isPro=true so premium features display in screenshots"
  artifacts:
    - path: "workload management/workload management.xcodeproj/project.pbxproj"
      provides: "Updated test target bundle identifiers"
      contains: "com.tonus.app.ScreenshotTests"
    - path: "WorkloadApp/Services/SubscriptionService.swift"
      provides: "DEBUG-only screenshot subscription override"
      contains: "overrideForScreenshots"
    - path: "WorkloadApp/Views/Subscription/UpgradeSheet.swift"
      provides: "Updated fallback pricing per D-08"
      contains: "$59.99/yr"
    - path: "WorkloadApp/App/AppRouter.swift"
      provides: "Subscription override call in SCREENSHOT_MODE block"
      contains: "overrideForScreenshots"
  key_links:
    - from: "WorkloadApp/App/AppRouter.swift"
      to: "WorkloadApp/Services/SubscriptionService.swift"
      via: "container.subscriptionService.overrideForScreenshots call in SCREENSHOT_MODE block"
      pattern: "overrideForScreenshots\\(isPro: true"
---

<objective>
Fix all code-level prerequisites for App Store submission: clean up test target bundle identifiers (per D-01/D-03), update fallback subscription pricing (per D-08), fix SCREENSHOT_MODE to bypass subscription gating, and verify GitHub Pages URLs are live (STORE-03).

Purpose: Code must be submission-ready before screenshots can be captured or builds archived.
Output: All four source files updated, project builds cleanly, URLs verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-store-launch/01-CONTEXT.md
@.planning/phases/01-app-store-launch/01-RESEARCH.md
@.planning/phases/01-app-store-launch/01-PATTERNS.md
@DESIGN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from PATTERNS.md. -->

From WorkloadApp/Services/SubscriptionService.swift:
```swift
@MainActor
@Observable
final class SubscriptionService {
    private(set) var isPro: Bool = false
    private(set) var isCoach: Bool = false
    // ... existing methods ...
}
```

From WorkloadApp/Views/Subscription/UpgradeSheet.swift (SubscriptionTier enum):
```swift
enum SubscriptionTier: String, CaseIterable {
    case athletePro
    case coach

    var fallbackAnnualPrice: String { ... }
    var fallbackMonthlyPrice: String { ... }
    var monthlyEquivalent: String { ... }
}
```

From WorkloadApp/App/AppRouter.swift (SCREENSHOT_MODE block, lines 37-55):
```swift
#if DEBUG
if ProcessInfo.processInfo.arguments.contains("SCREENSHOT_MODE") {
    container.setMode(.athlete)
    // ... mock data seeding ...
    container.setAuthenticated(true)
    isCheckingSession = false
    return
}
#endif
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bundle identifiers and verify URLs</name>
  <files>workload management/workload management.xcodeproj/project.pbxproj</files>
  <read_first>
    - workload management/workload management.xcodeproj/project.pbxproj (current bundle ID values)
    - .planning/phases/01-app-store-launch/01-RESEARCH.md (verified current state)
    - .planning/phases/01-app-store-launch/01-PATTERNS.md (exact line references)
  </read_first>
  <action>
1. In `project.pbxproj`, find all occurrences of `PRODUCT_BUNDLE_IDENTIFIER = H.ScreenshotTests` and replace with `PRODUCT_BUNDLE_IDENTIFIER = com.tonus.app.ScreenshotTests` (2 occurrences: Debug and Release configurations). Per D-01 and D-03.

2. In `project.pbxproj`, find all occurrences of `PRODUCT_BUNDLE_IDENTIFIER = H.WorkloadAppTests` and replace with `PRODUCT_BUNDLE_IDENTIFIER = com.tonus.app.WorkloadAppTests` (2 occurrences: Debug and Release configurations). Per D-01 and D-03.

3. Verify no remaining `H.` bundle identifier prefixes exist in the pbxproj file by grepping for `PRODUCT_BUNDLE_IDENTIFIER = H.`.

4. Verify GitHub Pages URLs return HTTP 200 (STORE-03):
   - `curl -sL -o /dev/null -w "%{http_code}" https://haaanw.github.io/WorkloadManagementApp/privacy.html`
   - `curl -sL -o /dev/null -w "%{http_code}" https://haaanw.github.io/WorkloadManagementApp/terms.html`
   - `curl -sL -o /dev/null -w "%{http_code}" https://haaanw.github.io/WorkloadManagementApp/support.html`
   All must return 200.

5. Run `xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination "platform=iOS Simulator,name=iPhone 16 Pro" -quiet build` to verify the project still builds cleanly after bundle ID changes.
  </action>
  <verify>
    <automated>grep -c "PRODUCT_BUNDLE_IDENTIFIER = H\." "workload management/workload management.xcodeproj/project.pbxproj" | grep "^0$" && echo "PASS: No H. bundle IDs remain"</automated>
  </verify>
  <acceptance_criteria>
    - `project.pbxproj` contains `PRODUCT_BUNDLE_IDENTIFIER = com.tonus.app.ScreenshotTests` (2 occurrences)
    - `project.pbxproj` contains `PRODUCT_BUNDLE_IDENTIFIER = com.tonus.app.WorkloadAppTests` (2 occurrences)
    - `project.pbxproj` contains zero occurrences of `PRODUCT_BUNDLE_IDENTIFIER = H.`
    - All three GitHub Pages URLs return HTTP 200
    - Project builds without errors
  </acceptance_criteria>
  <done>All test target bundle identifiers use com.tonus.app prefix. No H. prefixes remain. GitHub Pages URLs verified live. Project builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update subscription pricing and screenshot mode bypass</name>
  <files>WorkloadApp/Services/SubscriptionService.swift, WorkloadApp/Views/Subscription/UpgradeSheet.swift, WorkloadApp/App/AppRouter.swift</files>
  <read_first>
    - WorkloadApp/Services/SubscriptionService.swift (current class structure, property declarations)
    - WorkloadApp/Views/Subscription/UpgradeSheet.swift (current SubscriptionTier enum, fallback prices, savings badge)
    - WorkloadApp/App/AppRouter.swift (current SCREENSHOT_MODE block)
    - .planning/phases/01-app-store-launch/01-PATTERNS.md (exact code patterns and line numbers)
  </read_first>
  <action>
**A. SubscriptionService.swift -- Add screenshot override (per D-09, enables STORE-02)**

Add the following method inside the `SubscriptionService` class body, after all existing methods, before the closing brace. Wrap in `#if DEBUG`:

```swift
    // MARK: - Screenshot Support

    #if DEBUG
    /// Force subscription state for screenshot capture.
    /// Call ONLY from SCREENSHOT_MODE bootstrap in AppRouter.
    func overrideForScreenshots(isPro override: Bool, isCoach coachOverride: Bool) {
        self.isPro = override
        self.isCoach = coachOverride
    }
    #endif
```

**B. UpgradeSheet.swift -- Update fallback pricing (per D-08)**

In the `SubscriptionTier` enum, update these computed properties:

1. `fallbackAnnualPrice`:
   - `.athletePro` case: change `"$39.99/yr"` to `"$59.99/yr"`
   - `.coach` case: change `"$79.99/yr"` to `"$89.99/yr"`

2. `fallbackMonthlyPrice`:
   - `.athletePro` case: change `"$4.99/mo"` to `"$6.99/mo"`
   - `.coach` case: stays `"$9.99/mo"` (unchanged)

3. `monthlyEquivalent`:
   - `.athletePro` case: change `"$3.33"` to `"$5.00"`
   - `.coach` case: change `"$6.67"` to `"$7.42"`

4. Add a new computed property `annualSavingsBadge` to the `SubscriptionTier` enum:
```swift
    var annualSavingsBadge: String {
        switch self {
        case .athletePro: return "SAVE 29%"
        case .coach: return "SAVE 25%"
        }
    }
```

5. Find the `planButton` call for the annual plan that has the hardcoded `badge: "SAVE 33%"` and replace with `badge: selectedTier.annualSavingsBadge`.

**C. AppRouter.swift -- Wire screenshot subscription bypass**

In the `SCREENSHOT_MODE` `#if DEBUG` block, add the following line after `container.setAuthenticated(true)` and before `isCheckingSession = false`:

```swift
                container.subscriptionService.overrideForScreenshots(isPro: true, isCoach: false)
```

**D. Build verification**

Run `xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination "platform=iOS Simulator,name=iPhone 16 Pro" -quiet build` to verify all changes compile.
  </action>
  <verify>
    <automated>grep -q "overrideForScreenshots" WorkloadApp/Services/SubscriptionService.swift && grep -q "overrideForScreenshots" WorkloadApp/App/AppRouter.swift && grep -q '"\$59.99/yr"' WorkloadApp/Views/Subscription/UpgradeSheet.swift && grep -q "annualSavingsBadge" WorkloadApp/Views/Subscription/UpgradeSheet.swift && echo "PASS: All pricing and screenshot changes applied"</automated>
  </verify>
  <acceptance_criteria>
    - `SubscriptionService.swift` contains `func overrideForScreenshots(isPro override: Bool, isCoach coachOverride: Bool)` wrapped in `#if DEBUG`
    - `UpgradeSheet.swift` contains `"$59.99/yr"` (athlete annual fallback)
    - `UpgradeSheet.swift` contains `"$89.99/yr"` (coach annual fallback)
    - `UpgradeSheet.swift` contains `"$6.99/mo"` (athlete monthly fallback)
    - `UpgradeSheet.swift` contains `"$5.00"` (athlete monthly equivalent)
    - `UpgradeSheet.swift` contains `"$7.42"` (coach monthly equivalent)
    - `UpgradeSheet.swift` contains `var annualSavingsBadge: String`
    - `UpgradeSheet.swift` contains `selectedTier.annualSavingsBadge` (dynamic badge, not hardcoded "SAVE 33%")
    - `AppRouter.swift` contains `container.subscriptionService.overrideForScreenshots(isPro: true, isCoach: false)` inside the SCREENSHOT_MODE block
    - Project builds without errors
  </acceptance_criteria>
  <done>Fallback prices match D-08, savings badges are per-tier, screenshot mode bypasses subscription gating, project compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEBUG vs Release | Screenshot override must only exist in DEBUG builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | SubscriptionService.overrideForScreenshots | mitigate | Wrap method in `#if DEBUG` so it cannot exist in production builds |
| T-01-02 | Information Disclosure | Fallback pricing in UpgradeSheet | accept | Fallback prices are publicly visible in App Store; no sensitive data |
</threat_model>

<verification>
1. `grep -c "PRODUCT_BUNDLE_IDENTIFIER = H\." "workload management/workload management.xcodeproj/project.pbxproj"` returns 0
2. `grep -c "com.tonus.app.ScreenshotTests" "workload management/workload management.xcodeproj/project.pbxproj"` returns 2
3. `grep -c "com.tonus.app.WorkloadAppTests" "workload management/workload management.xcodeproj/project.pbxproj"` returns 2
4. `grep "overrideForScreenshots" WorkloadApp/Services/SubscriptionService.swift` returns a match
5. `grep "overrideForScreenshots" WorkloadApp/App/AppRouter.swift` returns a match
6. `grep '59.99' WorkloadApp/Views/Subscription/UpgradeSheet.swift` returns a match
7. `grep "annualSavingsBadge" WorkloadApp/Views/Subscription/UpgradeSheet.swift` returns matches
8. Project builds cleanly with `xcodebuild build`
</verification>

<success_criteria>
- All bundle identifiers use com.tonus.app prefix (STORE-01)
- GitHub Pages URLs verified live (STORE-03)
- Fallback pricing matches D-08 decision
- Screenshot mode bypasses both auth AND subscription gating
- Project builds without errors or warnings related to these changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-store-launch/01-01-SUMMARY.md`
</output>
