---
phase: 42-plan-input-today-s-planned-session-adjustable-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/WorkoutTemplate.swift
  - WorkloadAppTests/TemplateSetVerdictSlotTests.swift
autonomous: true
requirements: [PLAN-11]
must_haves:
  truths:
    - "Each planned working set can carry a nullable suggestedAdjusted slot the verdict writes into later"
    - "Each planned set carries an accept/decline marker (athleteOverrode) the athlete confirms in Phase 44"
    - "Existing SwiftData rows decode unchanged after the additive fields are added (no migration crash)"
    - "TemplateSet's synced template payload is unchanged — new fields are NOT in SetDTO"
  artifacts:
    - path: "WorkloadApp/Models/WorkoutTemplate.swift"
      provides: "TemplateSet additive-nullable verdict-target slots (adjustedTargetWeightKg, adjustedTargetRPE, verdictReason, verdictAppliedAt, athleteOverrode)"
      contains: "var adjustedTargetWeightKg: Double?"
    - path: "WorkloadAppTests/TemplateSetVerdictSlotTests.swift"
      provides: "Persistence round-trip + default-nil/false fidelity + sync-omission proof for the new slots"
      contains: "func test"
  key_links:
    - from: "WorkloadApp/Models/WorkoutTemplate.swift (TemplateSet)"
      to: "Schema in WorkloadApp/App/WorkloadApp.swift (TemplateSet.self already registered)"
      via: "additive nullable fields with defaults — automatic lightweight migration"
      pattern: "adjustedTargetWeightKg"
    - from: "TemplateSet @Model new fields"
      to: "SyncService SetDTO (hand-rolled, line ~1137)"
      via: "SetDTO field list is NOT extended — new fields never serialize into groupsJson"
      pattern: "private struct SetDTO"
---

<objective>
Add the additive-nullable verdict-target slots that each planned set must carry so the Phase-43 verdict can later read the planned target and write a suggested adjusted value + an accept/decline marker. This plan builds the SLOTS ONLY — no engine populates them; no UI reads them.

Purpose: PLAN-11 — give every planned working set the data the verdict needs, with zero migration and zero sync-payload change.
Output: Five additive-nullable fields on the `TemplateSet` @Model + a test file proving round-trip persistence, default values, and sync-omission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-CONTEXT.md

# The file being modified (READ FIRST — TemplateSet is the per-set target carrier)
@WorkloadApp/Models/WorkoutTemplate.swift

# Test pattern to mirror (in-memory ModelContainer; schema must include TemplateSet.self)
@WorkloadAppTests/BaselineStateModelTests.swift

<interfaces>
<!-- Real fields verified by reading WorkloadApp/Models/WorkoutTemplate.swift. The verdict slots are ADDED ALONGSIDE these. -->

Existing TemplateSet (`@Model final class TemplateSet`) target fields the verdict READS (do NOT change):
```swift
var setIndex: Int
var targetReps: Int?
var targetWeightKg: Double?
var targetDurationSeconds: Int?
var targetDistanceMeters: Double?
var targetRPE: Double?
var targetRIR: Int?
var isWarmup: Bool
var exercise: TemplateExercise?
```

Schema registration (already present — TemplateSet.self IS in the schema; no schema edit needed):
`WorkloadApp/App/WorkloadApp.swift` line ~88: `TemplateSet.self,`

Sync boundary (verified): TemplateSet syncs ONLY through the hand-rolled `private struct SetDTO` in
`WorkloadApp/Services/SyncService.swift` (line ~1137), whose `init(from set: TemplateSet)` copies an
EXPLICIT field list. New @Model fields not added to SetDTO are never encoded into `groupsJson`.
`PrescribedWorkout` is NOT referenced anywhere in SyncService (no sync path at all).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the five additive-nullable verdict-target slots to TemplateSet</name>
  <files>WorkloadApp/Models/WorkoutTemplate.swift</files>
  <behavior>
    - A brand-new TemplateSet() defaults adjustedTargetWeightKg == nil, adjustedTargetRPE == nil, verdictReason == nil, verdictAppliedAt == nil, athleteOverrode == false.
    - Setting and reading each slot returns the exact value set (Optional fidelity preserved).
    - The existing init signature still compiles for all current call sites (deepCopyGroups in this file + SyncService.decodeGroups) — the new fields are NOT added as init parameters; they default at the property level so no caller changes.
  </behavior>
  <action>
    In the `TemplateSet` @Model (the class at the bottom of WorkoutTemplate.swift, currently ending at `isWarmup`), add a `// MARK: - Verdict Targets (PLAN-11, additive-nullable, local-only)` section with exactly five stored properties, each with a default so existing rows decode unchanged and the existing `init` is untouched:
    `var adjustedTargetWeightKg: Double? = nil` (verdict's suggested adjusted weight; nil = unadjusted),
    `var adjustedTargetRPE: Double? = nil` (verdict's RPE cap for this set's exercise; nil = unadjusted),
    `var verdictReason: String? = nil` (one-line plain-language why),
    `var verdictAppliedAt: Date? = nil` (when the verdict was computed),
    `var athleteOverrode: Bool = false` (true once the athlete confirms/edits — the accept/decline marker).
    Add a brief doc comment above the block stating: additive-nullable, default nil/false so existing SwiftData rows decode without migration; populated ONLY by a future verdict pass on a PRESCRIPTION'S FROZEN COPY (never the source template); excluded from the synced payload (SetDTO is not extended). Do NOT add these to the `init(...)` parameter list, do NOT change `deepCopyGroups()`, do NOT touch SetDTO/SyncService. Reference: per D (CONTEXT) "The verdict's read/write contract — build the slots only".
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&1 | tail -3</automated>
  </verify>
  <done>Build ends `** BUILD SUCCEEDED **`. TemplateSet has the five new fields with property-level defaults; the existing `init` and `deepCopyGroups()` are unchanged; SetDTO is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence + default-value + sync-omission tests for the new slots</name>
  <files>WorkloadAppTests/TemplateSetVerdictSlotTests.swift</files>
  <behavior>
    - test_newTemplateSet_verdictSlotsDefaultToNilAndFalse: a fresh TemplateSet() has all four optional slots nil and athleteOverrode false.
    - test_verdictSlots_persistAndFetchBack_allFieldsEqual: insert a TemplateSet (attached to an ExerciseGroup/TemplateExercise graph) into an in-memory container, set all five slots, save, refetch by id, assert each slot round-trips exactly.
    - test_existingRowShape_decodesWithoutMigration: build a TemplateSet WITHOUT setting any verdict slot (simulating a pre-existing row), insert + save + refetch — proves additive fields don't break decode (no crash, defaults applied).
    - test_setDTO_excludesVerdictSlots: assert (by reflection of the JSON, OR by the documented invariant) that encoding a group graph via SyncService.encodeGroups then decoding via decodeGroups yields a TemplateSet whose verdict slots are back to defaults — proving the verdict fields are NOT carried in the synced payload.
  </behavior>
  <action>
    Create `WorkloadAppTests/TemplateSetVerdictSlotTests.swift` mirroring `BaselineStateModelTests.swift`: `@MainActor final class TemplateSetVerdictSlotTests: XCTestCase`, a `makeContext()` helper building an in-memory `ModelContainer` whose `Schema([...])` includes the SAME model list used in BaselineStateModelTests (it already lists TemplateSet.self, ExerciseGroup.self, TemplateExercise.self, PrescribedWorkout.self — copy that exact schema array). Avoid optional-relationship `#Predicate` fetches (fetch-all + filter in Swift, per the iOS 26.1 in-memory trap note). For the sync-omission test, call `SyncService.encodeGroups([group])` → `SyncService.decodeGroups(from: json)` (both are non-private static methods on SyncService verified at SyncService.swift ~981/988) and assert the decoded set's `adjustedTargetWeightKg`/`adjustedTargetRPE`/`verdictReason`/`verdictAppliedAt` are nil and `athleteOverrode` is false even though the source set had them populated. Tests are in WorkloadAppTests (auto-discovered, NO pbxproj entry needed).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/TemplateSetVerdictSlotTests 2>&1 | tail -8</automated>
  </verify>
  <done>All TemplateSetVerdictSlotTests pass. Round-trip, default-value, no-migration, and sync-omission behaviors are proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SwiftData ↔ Supabase sync | Only fields enumerated in hand-rolled Row/DTO structs cross to the backend; everything else stays local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information disclosure | New verdict slots leaking to Supabase | mitigate | SetDTO field list is NOT extended; test_setDTO_excludesVerdictSlots proves omission; PrescribedWorkout has no sync path |
| T-42-02 | Tampering | SwiftData store fails to open after additive change (migration crash) | mitigate | All new fields nullable-with-default ⇒ automatic lightweight migration; build + persistence test prove the store opens |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure Swift model + test additions) |
</threat_model>

<verification>
- `xcodebuild build` succeeds on sim CAF84E71-BB64-491D-87C8-875A0143B26D.
- `TemplateSetVerdictSlotTests` all green.
- `git diff WorkloadApp/Services/SyncService.swift` is EMPTY (sync encoder untouched).
- No new app-target file created in this plan ⇒ no pbxproj edit required (test file auto-discovers).
</verification>

<success_criteria>
- TemplateSet carries `adjustedTargetWeightKg`, `adjustedTargetRPE`, `verdictReason`, `verdictAppliedAt`, `athleteOverrode`, all additive-nullable with defaults.
- Existing rows decode unchanged (no migration); synced payload unchanged.
- Tests prove round-trip, defaults, and sync-omission.
</success_criteria>

<output>
Create `.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-01-SUMMARY.md` when done.
</output>
