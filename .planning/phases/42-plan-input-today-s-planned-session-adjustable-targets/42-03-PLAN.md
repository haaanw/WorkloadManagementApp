---
phase: 42-plan-input-today-s-planned-session-adjustable-targets
plan: 03
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - WorkloadApp/Views/WorkoutLog/PlanTodaySheet.swift
  - WorkloadApp/Views/WorkoutLog/ManualLiftEntrySheet.swift
  - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
  - workload management/workload management.xcodeproj/project.pbxproj
autonomous: false
requirements: [PLAN-10]
must_haves:
  truths:
    - "From the Workout Log, the athlete can open a 'Plan Today' affordance"
    - "The athlete can designate today's session by loading an existing template (reusing the template picker) so a PrescribedWorkout is created for today"
    - "The athlete can instead enter a one-off manual lift (name + target weight/reps/RPE) so a PrescribedWorkout (templateId nil) is created for today"
    - "The plan-input UI follows DESIGN.md (0pt corners, no shadows, Font.Tokens, 8pt grid, accent NOT used here)"
  artifacts:
    - path: "WorkloadApp/Views/WorkoutLog/PlanTodaySheet.swift"
      provides: "Designation entry sheet: choose load-template (reuses TemplatePickerSheet) or manual one-off entry; calls PlannedSessionRepository"
      contains: "PlannedSessionRepository"
    - path: "WorkloadApp/Views/WorkoutLog/ManualLiftEntrySheet.swift"
      provides: "Minimal manual today-entry form (lift name + target weight/reps/RPE)"
      contains: "planManualLift"
    - path: "WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift"
      provides: "Entry point (toolbar menu) that presents PlanTodaySheet"
      contains: "PlanTodaySheet"
  key_links:
    - from: "PlanTodaySheet (load-template path)"
      to: "PlannedSessionRepository.planFromTemplate"
      via: "TemplatePickerSheet onSelectTemplate then repository create"
      pattern: "planFromTemplate"
    - from: "ManualLiftEntrySheet"
      to: "PlannedSessionRepository.planManualLift"
      via: "form submit then repository create"
      pattern: "planManualLift"
---

<objective>
Add a minimal, DESIGN.md-compliant athlete-facing affordance to designate "today's planned session" — load an existing template (reusing the existing `TemplatePickerSheet`) or enter a one-off manual lift — wired to the Plan-02 `PlannedSessionRepository`. Reuse the template picker rather than rebuilding it.

Purpose: PLAN-10 — the user-facing half of "designate today's planned session", two ways. This plan does NOT build the verdict, the verdict card, or any adjusted-number display (Phases 43/44) — it only lets the athlete create/choose the plan the verdict will later read.
Output: Two small sheets + one entry point in WorkoutLogView; pbxproj registration for the two new view files; a human-verify checkpoint for the visual + flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-CONTEXT.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-02-SUMMARY.md
@./DESIGN.md

# The entry-point view being modified + the picker being REUSED (READ FIRST)
@WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
@WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift

# Style reference for a small DESIGN-compliant sheet
@WorkloadApp/Views/WorkoutLog/PrescribedWorkoutCard.swift

<interfaces>
PlannedSessionRepository (from Plan 02 — construct at point of use; CONFIRM exact signatures against
42-02-SUMMARY.md / PlannedSessionRepository.swift before calling):
- init(modelContext: ModelContext)
- func planFromTemplate(_ template: WorkoutTemplate, athleteId: UUID, scheduledDate: Date = .now) -> PrescribedWorkout
- func planManualLift(athleteId: UUID, liftName: String, targetWeightKg: Double?, targetReps: Int?, targetRPE: Double?, setCount: Int = 3, scheduledDate: Date = .now) -> PrescribedWorkout
- func fetchTodaysPlannedSession(athleteId: UUID) -> PrescribedWorkout?

TemplatePickerSheet (REUSE — verified real init):
- TemplatePickerSheet(onSelectTemplate: (WorkoutTemplate) -> Void, onStartBlank: () -> Void, onCreateTemplate: () -> Void).environment(container)

Current athlete fetch pattern (used throughout WorkoutLog views):
- @Query private var athletes: [Athlete] then athletes.first?.id

WorkoutLogView toolbar entry point (verified): an ellipsis.circle Menu near line 137 with Buttons
(AI import / My Programs / text import), and a separate "+" Button near line 161 that sets
showTemplatePicker = true. Add the "Plan Today" item to the existing Menu; do NOT remove the "+" start flow.

DESIGN tokens available: ColorTokens.text1/text2/text3/surface/surfaceEl/background/divider;
Font.Tokens.pageTitle/sectionHead/body/bodyMedium/label/smallLabel/micro;
Spacing.xs(8)/sm(16)/md(24)/lg(32)/xl(48); .cardStyle() modifier; SharpTextFieldStyle (square field style).
HARD RULES (DESIGN.md): Rectangle() not RoundedRectangle; NO .shadow; NO .system() fonts; spacing on the
8pt grid; ColorTokens.accent MUST NOT appear in these views (accent = hero readiness number only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ManualLiftEntrySheet — minimal one-off today-entry form</name>
  <files>WorkloadApp/Views/WorkoutLog/ManualLiftEntrySheet.swift</files>
  <action>
    Create a minimal DESIGN-compliant form sheet ManualLiftEntrySheet with dismiss + modelContext environment, athletes Query, a completion closure onPlanned, and State for liftName (String), targetWeightKg/targetReps as String-to-numeric, targetRPE (Double via slider with an optional toggle), setCount (Int, default 3). Lay out as a NavigationStack with a square field group: use SharpTextFieldStyle and Rectangle() borders, ColorTokens.surfaceEl/.cardStyle(), Font.Tokens.*, Spacing.* on the 8pt grid — NO accent, NO shadow, NO RoundedRectangle. A confirmation toolbar button "Plan Today" (disabled until liftName non-empty and an athlete exists) calls PlannedSessionRepository(modelContext: modelContext).planManualLift(athleteId: athleteId, liftName: trimmedName, targetWeightKg: parsedWeight, targetReps: parsedReps, targetRPE: rpeEnabled ? rpe : nil, setCount: setCount), then calls onPlanned() and dismiss(). A cancellation button dismisses without creating. Localize the new strings in the en + zh-Hans String Catalog, following the existing String(localized:) / catalog convention used in WorkoutLogView/ImportRPESheet (per CLAUDE.md, both languages). Reference: PLAN-10 manual today-entry path.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&1 | tail -3</automated>
  </verify>
  <done>Build ends BUILD SUCCEEDED after pbxproj registration (Task 3). Submitting calls planManualLift then onPlanned + dismiss; cancel creates nothing.</done>
</task>

<task type="auto">
  <name>Task 2: PlanTodaySheet (reuses TemplatePickerSheet) + WorkoutLogView entry point</name>
  <files>WorkloadApp/Views/WorkoutLog/PlanTodaySheet.swift, WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift</files>
  <action>
    Create PlanTodaySheet: a small DESIGN-compliant chooser presenting two paths — "Load a template" and "Enter a lift" — each creating today's PrescribedWorkout, with athletes Query, modelContext + AppContainer + dismiss environment. For the load-template path, present the EXISTING TemplatePickerSheet via a showTemplatePicker State and in onSelectTemplate call PlannedSessionRepository(modelContext: modelContext).planFromTemplate(template, athleteId: athletes.first!.id) then dismiss() the whole flow; wire onStartBlank to open the manual path and onCreateTemplate to a no-op (or dismiss). For the manual path, present ManualLiftEntrySheet(onPlanned: { dismiss() }) via a showManual State. Style with Rectangle() borders, .cardStyle()/surfaceEl, Font.Tokens, 8pt grid, no accent, no shadow. Then in WorkoutLogView add a new Button setting showPlanToday = true with Label "Plan Today" + systemImage calendar.badge.plus to the existing ellipsis.circle Menu (around line 137), add a showPlanToday State (default false), and add .sheet(isPresented: $showPlanToday) presenting PlanTodaySheet().environment(container). Leave the existing "+" start-session button and all current sheets unchanged. Localize new strings (en + zh-Hans).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&1 | tail -3</automated>
  </verify>
  <done>Build ends BUILD SUCCEEDED. grep finds PlanTodaySheet in WorkoutLogView.swift. From the toolbar menu, "Plan Today" opens the chooser; both paths create today's PrescribedWorkout.</done>
</task>

<task type="auto">
  <name>Task 3: Register the two new view files in pbxproj</name>
  <files>workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    The MAIN APP TARGET is NOT a synchronized group (per CONTEXT build/test gate + 41-02 learning) — each new app file needs four explicit entries. For BOTH PlanTodaySheet.swift and ManualLiftEntrySheet.swift, add four pbxproj entries each, mirroring the existing CrossModalFatigueEngine.swift entry quad: (1) a PBXBuildFile in-Sources line, (2) a PBXFileReference with path set to the filename and sourceTree set to the group token, (3) an entry in the WorkoutLog views group children array, and (4) an entry in the app target Sources build phase. Use fresh unique 24-hex object id pairs (do not collide with existing ids). Confirm exactly ONE fileRef per new file and NO stray " 2.swift" duplicate files left on disk (delete any duplicate copy if the editor created one). The Task-1/Task-2 builds only pass once these entries exist.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&1 | tail -3</automated>
  </verify>
  <done>App builds with PlanTodaySheet and ManualLiftEntrySheet compiled in (proves both are registered). Exactly one fileRef each; no " 2.swift" duplicates remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — Plan Today affordance (visual + both creation paths)</name>
  <action>Claude has already implemented and built the "Plan Today" affordance (Tasks 1-3 are autonomous and complete before this checkpoint). This task only asks the human to visually verify the result on-device — no code is written here.</action>
  <what-built>A "Plan Today" affordance in the Workout Log toolbar menu that opens a chooser. The chooser offers two paths to designate today's planned session: (a) load an existing template (reusing the template picker) which creates a PrescribedWorkout for today via planFromTemplate, and (b) a minimal manual one-off lift form (name + target weight/reps/RPE) which creates a PrescribedWorkout (templateId nil) via planManualLift. No verdict, no adjusted numbers, no verdict card — those are Phases 43/44.</what-built>
  <how-to-verify>
    1. Run the app on the simulator (sim CAF84E71-BB64-491D-87C8-875A0143B26D).
    2. Go to the Workout Log tab; open the ellipsis (…) toolbar menu; tap "Plan Today".
    3. Path A: choose "Load a template", pick a template; confirm the sheet dismisses cleanly (a PrescribedWorkout for today now exists — spot-check via fetchTodaysPlannedSession in a debug print or a follow-up session).
    4. Path B: choose "Enter a lift", type a lift name + a target weight/reps/RPE, tap "Plan Today"; confirm it dismisses cleanly.
    5. Visual check against DESIGN.md: 0pt corners (no rounded rectangles), no shadows, General Sans fonts only, 8pt-grid spacing, and the accent color does NOT appear anywhere in these sheets. Verify in BOTH light and dark mode.
    6. Confirm the existing "+" start-session flow and all other Workout Log behavior are unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (visual deviations, flow breaks, or either creation path failing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (manual lift form) to local store | Free-text lift name + numeric targets parsed before persistence; no backend crossing (PrescribedWorkout is local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Tampering | New view files silently excluded from build (pbxproj gap) | mitigate | Task 3 adds 4 explicit pbxproj entries per file; build gate confirms compile-in |
| T-42-07 | Denial of service | Crash when no athlete exists (athletes.first force-unwrap) | mitigate | Disable the confirm button until athletes.first is non-nil; guard the create call |
| T-42-08 | Information disclosure | Manual-entry plan leaking to backend | mitigate | Reuses Plan-02 repository which has no sync path; PrescribedWorkout absent from SyncService |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Swift views + pbxproj + catalog strings) |
</threat_model>

<verification>
- App builds on sim CAF84E71-BB64-491D-87C8-875A0143B26D with both new view files compiled in.
- WorkoutLogView references PlanTodaySheet; the "+" start flow is unchanged.
- New strings present in both en and zh-Hans catalogs.
- No accent color, no shadows, no RoundedRectangle in the two new sheets (DESIGN.md grep-style check).
- Human-verify checkpoint approved (visual + both creation paths, light + dark).
</verification>

<success_criteria>
- Athlete can designate today's planned session two ways from the Workout Log (load template / manual lift), each creating a PrescribedWorkout for today.
- UI is DESIGN.md-compliant and reuses the existing template picker.
- No verdict/adjusted-number surfacing (correctly deferred to Phases 43/44).
</success_criteria>

<output>
Create `.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-03-SUMMARY.md` when done.
</output>
