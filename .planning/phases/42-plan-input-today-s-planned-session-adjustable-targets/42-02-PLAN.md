---
phase: 42-plan-input-today-s-planned-session-adjustable-targets
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - WorkloadApp/Repositories/PlannedSessionRepository.swift
  - WorkloadAppTests/PlannedSessionRepositoryTests.swift
autonomous: true
requirements: [PLAN-10, PLAN-11]
must_haves:
  truths:
    - "The app can create today's planned session from an existing template (frozen deep-copy, not the source template)"
    - "The app can create today's planned session from a one-off manual lift entry (name + target weight/reps/RPE), with templateId nil"
    - "The app can fetch the athlete's planned session for today"
    - "A planned session's frozen working sets carry the additive verdict slots (default nil/false) ready for the verdict to write"
  artifacts:
    - path: "WorkloadApp/Repositories/PlannedSessionRepository.swift"
      provides: "Create-from-template, create-manual-one-off, and fetch-today PrescribedWorkout persistence for the current athlete"
      contains: "func planFromTemplate"
    - path: "WorkloadAppTests/PlannedSessionRepositoryTests.swift"
      provides: "Tests for template-path frozen copy (source untouched), manual-path graph, fetch-today, and verdict-slot presence"
      contains: "func test"
  key_links:
    - from: "PlannedSessionRepository.planFromTemplate"
      to: "WorkoutTemplate.deepCopyGroups()"
      via: "frozen per-prescription copy so the source authored template is never mutated"
      pattern: "deepCopyGroups"
    - from: "PlannedSessionRepository.planManualLift"
      to: "PrescribedWorkout(templateId: nil) + hand-built ExerciseGroup→TemplateExercise→TemplateSet graph"
      via: "one-off manual today-entry path"
      pattern: "templateId: nil"
---

<objective>
Build the persistence layer that lets the athlete designate "today's planned session" two ways — load an existing template (frozen deep-copy) or enter a one-off manual lift — producing a `PrescribedWorkout` for the current athlete whose working sets already carry the Plan-01 verdict slots. This is reuse-first: the `PrescribedWorkout` model, `deepCopyGroups()`, and the `@MainActor final class …Repository(modelContext:)` pattern all already exist.

Purpose: PLAN-10 — give the app a notion of today's planned session expressed in the EXISTING types (PrescribedWorkout / TemplateSet), not a new parallel hierarchy. The verdict's read/write target is the frozen prescription copy (PLAN-11).
Output: `PlannedSessionRepository` (3 methods) + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-CONTEXT.md
@.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-01-SUMMARY.md

# Models the repository builds/queries (READ FIRST — verify real field names)
@WorkloadApp/Models/PrescribedWorkout.swift
@WorkloadApp/Models/WorkoutTemplate.swift

# Repository pattern to mirror exactly
@WorkloadApp/Repositories/TemplateRepository.swift

# Test pattern (in-memory ModelContainer, fetch-all-then-filter)
@WorkloadAppTests/BaselineStateModelTests.swift

<interfaces>
<!-- Verified by reading the model + repository files. Use these exact signatures. -->

`@Model final class PrescribedWorkout` init (real signature):
```swift
init(id: UUID = UUID(), coachId: UUID, athleteId: UUID, templateId: UUID? = nil,
     scheduledDate: Date, templateName: String, sportType: SportType = .lifting,
     sessionType: SessionType = .strength, notes: String? = nil)
```
Other PrescribedWorkout members: `var groups: [ExerciseGroup]` (cascade, inverse \ExerciseGroup.prescription),
`var status: PrescriptionStatus` (computed over statusRawValue; default .assigned), `var scheduledDate: Date`,
`var completedSessionId: UUID?`, `var allExercises: [TemplateExercise]`.

`WorkoutTemplate.deepCopyGroups() -> [ExerciseGroup]` — returns DETACHED copies (the proven frozen-snapshot
helper; preserves all TemplateSet target fields). Existing prescribe call site did exactly:
`prescription.groups = template.deepCopyGroups()` then `modelContext.insert(prescription); try? modelContext.save()`.

Graph builders (for the manual path, real inits from WorkoutTemplate.swift):
```swift
ExerciseGroup(id:, groupName:, orderIndex:)            // groupName default "Group A", orderIndex 0
TemplateExercise(id:, exerciseName:, exerciseCategory:, muscleGroup:, orderIndex:) // category default .compound
TemplateSet(id:, setIndex:, targetReps:, targetWeightKg:, targetDurationSeconds:, targetDistanceMeters:, targetRPE:, targetRIR:, isWarmup:)
```
TemplateSet now ALSO carries (from Plan 01, property-default, NOT init params): adjustedTargetWeightKg/adjustedTargetRPE/verdictReason/verdictAppliedAt/athleteOverrode.

Repository pattern (TemplateRepository): `@MainActor final class …Repository { private let modelContext: ModelContext; init(modelContext:) }`.
Construct at point of use (same as TemplatePickerSheet builds `TemplateRepository(modelContext: modelContext)`).

Sync note (verified): `PrescribedWorkout` is NOT referenced in SyncService — it has NO sync path. The repository
must NOT call any push/sync method (the stray `pushPrescribedWorkout` from dormant coach work does not exist in
the compiled SyncService — do not reintroduce it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlannedSessionRepository — plan-from-template, plan-manual, fetch-today</name>
  <files>WorkloadApp/Repositories/PlannedSessionRepository.swift</files>
  <behavior>
    - planFromTemplate(_:athleteId:scheduledDate:): creates a PrescribedWorkout with templateId == template.id, denormalized templateName/sportType/sessionType from the template, groups = template.deepCopyGroups(); the SOURCE template's groups/sets are unchanged (frozen copy). Inserts + saves; returns the prescription.
    - planManualLift(athleteId:liftName:targetWeightKg:targetReps:targetRPE:setCount:scheduledDate:): creates a PrescribedWorkout with templateId == nil, one ExerciseGroup → one TemplateExercise(named liftName) → setCount working TemplateSets carrying the given target weight/reps/RPE (isWarmup false). Inserts + saves; returns the prescription.
    - fetchTodaysPlannedSession(athleteId:): returns the most recent non-skipped PrescribedWorkout whose scheduledDate is within today (Calendar.startOfDay…end), or nil. Uses fetch-all + Swift filter (avoid optional-relationship #Predicate trap).
    - Each created prescription's working sets expose the verdict slots at default (nil/false) — verifiable, ready for the verdict to write.
  </behavior>
  <action>
    Create `WorkloadApp/Repositories/PlannedSessionRepository.swift` as a `@MainActor final class PlannedSessionRepository` taking `modelContext: ModelContext` in init, mirroring TemplateRepository exactly. Implement the three methods per the behavior block. For the manual path, build the graph with the real inits in the interfaces block and a default `coachId` of the athleteId (self-coached — there is no coach; reuse the athleteId as coachId exactly as the prescribe call site uses `coachId` denormalization). Use a sensible default `scheduledDate: Date = .now`, `setCount: Int = 3`, and make targetRPE optional. Do NOT call any sync/push method (PrescribedWorkout has no sync path). Keep methods lean (mirror TemplateRepository line counts). Reference: PLAN-10 (designate today's session two ways) + the CONTEXT "Reuse existing models" decision (express today's session as a PrescribedWorkout, no new hierarchy).
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild build -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -configuration Debug 2>&1 | tail -3</automated>
  </verify>
  <done>Build ends `** BUILD SUCCEEDED **`. The new file is registered with the 4 required pbxproj entries (see Task 2 note) and compiles into the app target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register PlannedSessionRepository in pbxproj + repository tests</name>
  <files>WorkloadAppTests/PlannedSessionRepositoryTests.swift</files>
  <behavior>
    - test_planFromTemplate_freezesCopy_sourceTemplateUntouched: plan from a template with known sets; assert the prescription's set targets equal the template's, the prescription.templateId == template.id, AND the source template's TemplateSet objects are the SAME count/values (mutating a prescription set does not change the template set).
    - test_planManualLift_buildsOneOffGraph: plan a manual lift "Back Squat" 100kg×5 @RPE8 with setCount 3; assert templateId == nil, one exercise named "Back Squat", 3 non-warmup sets each with targetWeightKg 100 / targetReps 5 / targetRPE 8.
    - test_fetchTodaysPlannedSession_returnsTodaysPlan: create a plan scheduled today and one scheduled yesterday; assert fetch returns today's.
    - test_plannedSets_carryVerdictSlotsAtDefault: assert a freshly planned working set has adjustedTargetWeightKg nil, adjustedTargetRPE nil, verdictReason nil, verdictAppliedAt nil, athleteOverrode false (proves the slots are present and unpopulated, ready for Phase 43).
  </behavior>
  <action>
    First register the new app-target file: because the MAIN APP TARGET is NOT a synchronized group (per CONTEXT build/test gate + 41-02 learning), add the FOUR pbxproj entries for `WorkloadApp/Repositories/PlannedSessionRepository.swift` to `workload management/workload management.xcodeproj/project.pbxproj`, mirroring the existing CrossModalFatigueEngine.swift entry quad (a PBXBuildFile `… in Sources`, a PBXFileReference with `path = PlannedSessionRepository.swift`, an entry in the Repositories group children array, and an entry in the target's Sources build phase). Use a fresh unique 24-hex object id pair. Then create `WorkloadAppTests/PlannedSessionRepositoryTests.swift` mirroring BaselineStateModelTests (same in-memory `Schema([...])` array including TemplateSet.self/PrescribedWorkout.self/ExerciseGroup.self/TemplateExercise.self/Athlete.self), implementing the four tests per the behavior block. Build the seed template with deepCopyGroups-friendly graph. Test file is auto-discovered (no pbxproj entry). Do NOT leave any " 2.swift" duplicate file behind.
  </action>
  <verify>
    <automated>cd "/Users/hanwen/Desktop/Tonus/workload management" && xcodebuild test -scheme "workload management" -destination "platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D" -only-testing:WorkloadAppTests/PlannedSessionRepositoryTests 2>&1 | tail -8</automated>
  </verify>
  <done>PlannedSessionRepository.swift compiles into the app (4 pbxproj entries present, exactly one fileRef). All PlannedSessionRepositoryTests pass. Frozen-copy isolation, manual-graph, fetch-today, and verdict-slot-default behaviors are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source template ↔ frozen prescription | The verdict writes only the prescription copy; the authored template must never be mutated ("never writes the program") |
| local SwiftData ↔ Supabase sync | PrescribedWorkout has no sync path — planned sessions stay local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | planFromTemplate mutating the source authored template | mitigate | Use deepCopyGroups() (detached copies); test_planFromTemplate_freezesCopy asserts source untouched |
| T-42-04 | Information disclosure | Planned session leaking to backend | mitigate | Repository calls NO sync/push; PrescribedWorkout absent from SyncService (verified) |
| T-42-05 | Tampering | New app file silently excluded from build (pbxproj gap) | mitigate | 4 explicit pbxproj entries added + Task-1 build gate confirms compile-in |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Swift + pbxproj edit) |
</threat_model>

<verification>
- `xcodebuild build` succeeds on sim CAF84E71-BB64-491D-87C8-875A0143B26D with PlannedSessionRepository compiled in.
- `grep -c 'path = PlannedSessionRepository.swift;' "workload management/workload management.xcodeproj/project.pbxproj"` returns exactly 1.
- `PlannedSessionRepositoryTests` all green.
- `git diff WorkloadApp/Services/SyncService.swift` EMPTY; no push call introduced.
- No stray " 2.swift" files in WorkloadApp/Repositories/.
</verification>

<success_criteria>
- Athlete can produce today's planned session from a template (frozen copy, source untouched) OR a one-off manual lift (templateId nil).
- Today's planned session is fetchable.
- Planned working sets carry the Plan-01 verdict slots at default, ready for the verdict.
</success_criteria>

<output>
Create `.planning/phases/42-plan-input-today-s-planned-session-adjustable-targets/42-02-SUMMARY.md` when done.
</output>
