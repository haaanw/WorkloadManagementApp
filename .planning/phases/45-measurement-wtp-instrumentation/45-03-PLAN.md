---
phase: 45-measurement-wtp-instrumentation
plan: 03
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - WorkloadApp/Views/Profile/VerdictMeasurementView.swift
  - WorkloadApp/Views/Profile/ProfileView.swift
  - WorkloadApp/Resources/Localizable.xcstrings
  - WorkloadAppTests/GreenLightSurfaceTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [METRIC-02]
user_setup: []

must_haves:
  truths:
    - "A quiet internal analytics view surfaces the green-light rate, activation rate, and Day-7 / Day-30 retention computed by GreenLightEngine over the athlete's logged VerdictEvents"
    - "The view reads asOf = .now and Calendar.current ONCE at the view boundary and passes them into the pure engine (the engine itself stays date-injected)"
    - "Empty / no-signal-yet states read honestly (no fabricated 0% / 100%) — they show a still-learning placeholder, never a hero number"
    - "The surface is reachable from Profile but is NOT a hero affordance (a quiet row, no accent)"
  artifacts:
    - path: "WorkloadApp/Views/Profile/VerdictMeasurementView.swift"
      provides: "Quiet green-light / activation / retention analytics surface"
      contains: "struct VerdictMeasurementView"
      min_lines: 40
  key_links:
    - from: "WorkloadApp/Views/Profile/VerdictMeasurementView.swift"
      to: "GreenLightEngine.compute"
      via: "events from VerdictEventRepository.fetchAll"
      pattern: "GreenLightEngine.compute"
    - from: "WorkloadApp/Views/Profile/ProfileView.swift"
      to: "VerdictMeasurementView"
      via: "NavigationLink"
      pattern: "VerdictMeasurementView"
---

<objective>
Surface METRIC-02. Build a quiet internal analytics view that runs `GreenLightEngine` over the
athlete's logged `VerdictEvent`s and shows the green-light rate alongside activation rate and Day-7 /
Day-30 retention, reachable from a low-key Profile row. This is the founder's-playbook readout that
proves whether the verdict earns its keep — deliberately quiet (not a hero affordance), honest about
no-signal-yet states.

Purpose: 45-01 built the compute engine; this plan gives it a face. The view is the only place the
green-light signal is read by a human; per 45-CONTEXT it must be small and quiet, never promoted.

Output: `VerdictMeasurementView.swift`, a quiet Profile NavigationLink, en+zh-Hans strings, and a test
proving the view's value-binding matches the engine for seeded events + empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-measurement-wtp-instrumentation/45-CONTEXT.md
@.planning/phases/45-measurement-wtp-instrumentation/45-01-SUMMARY.md

# Files this plan MODIFIES / mirrors (read current state first):
@WorkloadApp/Views/Profile/ProfileView.swift
@WorkloadApp/Views/Profile/SyncStatusView.swift
@DESIGN.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

From WorkloadApp/Services/GreenLightEngine.swift (Plan 45-01):
  struct GreenLightMetrics { let greenLightRate: Double?; let differingDays: Int; let activationRate: Double?; let totalEvents: Int; let day7Retention: Bool?; let day30Retention: Bool? }
  static func compute(events: [VerdictEvent], asOf: Date, calendar: Calendar) -> GreenLightMetrics

From WorkloadApp/Repositories/VerdictEventRepository.swift (Plan 45-01):
  func fetchAll(athlete: Athlete?) -> [VerdictEvent]

ProfileView quiet-row pattern to MIRROR (the SyncStatusView link, ProfileView.swift ~240):
  sectionHeader("profile.section.dataSync")
  NavigationLink { SyncStatusView() } label: { HStack { Image(systemName:...); Text(...); Spacer(); Text(quietStatus); Image(systemName:"chevron.right") }
      .padding(.horizontal,16).padding(.vertical,16).background(ColorTokens.surfaceEl) }

DESIGN tokens available: Font.Tokens.*, ColorTokens.text1/text2/text3/surfaceEl/divider/zoneCaution,
Spacing.*; SectionHeader / cardStyle in WorkloadApp/Components/CardStyle.swift. Accent FORBIDDEN here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VerdictMeasurementView — green-light / activation / retention readout</name>
  <files>WorkloadApp/Views/Profile/VerdictMeasurementView.swift, WorkloadApp/Resources/Localizable.xcstrings, WorkloadAppTests/GreenLightSurfaceTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    Create `struct VerdictMeasurementView: View`. It fetches the current athlete's events
    (`@Query private var athletes: [Athlete]` → `.first`; build a `VerdictEventRepository(modelContext:)`
    from `@Environment(\.modelContext)`; call `fetchAll(athlete:)`), then computes
    `GreenLightEngine.compute(events:, asOf: .now, calendar: .current)` ONCE at the view boundary (the
    only place `.now`/`.current` appear — the engine stays injected). Render a quiet vertical list of
    composite stat rows (DESIGN.md: 0pt corners via Rectangle, NO shadows, Font.Tokens.*, 8pt grid,
    dark+light via ColorTokens, accent FORBIDDEN):
      - Green-light rate: show as a percentage with the differing-days denominator as quiet context
        ("on N differing-verdict days"). When `greenLightRate == nil` (no differing days yet) show the
        still-learning placeholder string — never "0%".
      - Activation rate: percentage with the total-events context; nil ⇒ placeholder.
      - Day-7 / Day-30 retention: render true ⇒ "retained", false ⇒ "lapsed", nil ⇒ "too early" copy.
    Use a quiet label + monospacedDigit value layout (mirror existing metric rows); NO hero number, NO
    accent color, NO chart. Add a short honest caption that these are pre-validation internal signals.
    Add en + zh-Hans for every new key (e.g. `measurement.title`, `measurement.greenLight.label`,
    `measurement.greenLight.context`, `measurement.activation.label`, `measurement.retention.day7`,
    `measurement.retention.day30`, `measurement.retained`, `measurement.lapsed`, `measurement.tooEarly`,
    `measurement.learning`, `measurement.caption`). Register the new file with 4 pbxproj entries (fresh
    hex IDs, suggest AE4505xx).

    Test (`GreenLightSurfaceTests`): seed events through a STORED in-memory container + repository
    (setUp/tearDown props), then assert that `GreenLightEngine.compute(events:asOf:calendar:)` over the
    repository's `fetchAll` returns the metrics the view will bind (green-light rate for a known
    differing/acted/right fixture, and nil green-light rate for an empty store). This pins the
    view→engine contract without rendering SwiftUI.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/GreenLightSurfaceTests 2>&1 | tail -20</automated>
  </verify>
  <done>VerdictMeasurementView computes via GreenLightEngine at the boundary, renders quiet composite rows with honest nil-states, follows DESIGN (no accent/shadow/RoundedRectangle), ships en+zh-Hans; contract test green.</done>
</task>

<task type="auto">
  <name>Task 2: Quiet Profile entry point</name>
  <files>WorkloadApp/Views/Profile/ProfileView.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <action>
    In `ProfileView`, add a quiet `NavigationLink { VerdictMeasurementView() } label: { ... }` mirroring
    the SyncStatusView row treatment (label + chevron, `.background(ColorTokens.surfaceEl)`, 16pt padding,
    `divider()`/`sectionDivider()` separators). Place it under a new low-key `sectionHeader` (e.g.
    `profile.section.measurement` → "Validation signals" / zh equivalent) or fold it into the existing
    data section — keep it visually quiet, NO accent, NOT a promoted/hero row (per 45-CONTEXT "surface
    quietly, not a hero affordance"). Add en + zh-Hans for the new section header + row label keys
    (e.g. `profile.section.measurement`, `profile.measurement.row`). Do not reorder or restyle existing
    Profile rows.
  </action>
  <verify>
    <automated>xcodebuild build -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -5</automated>
  </verify>
  <done>Profile has a quiet NavigationLink into VerdictMeasurementView (no accent, not a hero row); new keys are en+zh-Hans; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logged VerdictEvents → analytics readout | Only composite metrics are derived/displayed; no raw biometric is ever read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07 | Information Disclosure | VerdictMeasurementView | mitigate | Reads only composite VerdictEvent fields through GreenLightEngine; no HealthKit access; renders rates/labels only |
| T-45-08 | Tampering | Date determinism | mitigate | asOf/calendar captured at the view boundary and passed into the pure engine; no `.now` inside the engine |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies this phase |
</threat_model>

<verification>
- App build green on sim CAF84E71-BB64-491D-87C8-875A0143B26D.
- `grep -c "GreenLightEngine.compute" WorkloadApp/Views/Profile/VerdictMeasurementView.swift` ≥ 1.
- `grep -c "VerdictMeasurementView" WorkloadApp/Views/Profile/ProfileView.swift` ≥ 1.
- VerdictMeasurementView contains no `ColorTokens.accent`, `.shadow`, `RoundedRectangle`, `.cornerRadius`, `.system(`.
- en + zh-Hans present for every new `measurement.*` / `profile.section.measurement` key; no `" 2.swift"` dupes.
</verification>

<success_criteria>
- METRIC-02 is surfaced: green-light rate + activation + Day-7/Day-30 retention readable from a quiet Profile row.
- Honest no-signal-yet states; no fabricated 0%/100%; not a hero affordance.
- Engine stays date-injected; the view binds exactly what the engine computes (contract test green).
</success_criteria>

<output>
Create `.planning/phases/45-measurement-wtp-instrumentation/45-03-SUMMARY.md` when done.
</output>
