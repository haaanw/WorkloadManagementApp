---
phase: 45-measurement-wtp-instrumentation
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - WorkloadApp/ViewModels/TodayVerdictViewModel.swift
  - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
  - WorkloadApp/Views/WorkoutLog/VerdictOutcomeSheet.swift
  - WorkloadApp/Resources/Localizable.xcstrings
  - WorkloadAppTests/VerdictEventLoggingTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [METRIC-01]
user_setup: []

must_haves:
  truths:
    - "Every accept / keep-plan / feel-override decision records exactly one composite VerdictEvent (verdict label + planned/adjusted kg + delta + differed + action + region + reason)"
    - "The production WorkoutLog surface wires verdictVM.onDecisionRecorded to the VerdictEventRepository — the seam is never left nil, so no validation user can reach the verdict surface without an event being logged (SC4 ordering guard)"
    - "After a planned-session day passes, the athlete is prompted once for a no-guilt post-session outcome (right / wrong / unsure) that is written onto the awaiting VerdictEvent"
  artifacts:
    - path: "WorkloadApp/Views/WorkoutLog/VerdictOutcomeSheet.swift"
      provides: "DESIGN-compliant right/wrong/unsure post-session outcome capture"
      contains: "struct VerdictOutcomeSheet"
    - path: "WorkloadApp/ViewModels/TodayVerdictViewModel.swift"
      provides: "Captured headline verdict label + region for the logged event"
      contains: "lastHeadlineVerdict"
  key_links:
    - from: "WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift"
      to: "VerdictEventRepository.log"
      via: "verdictVM.onDecisionRecorded closure"
      pattern: "onDecisionRecorded"
    - from: "WorkloadApp/Views/WorkoutLog/VerdictOutcomeSheet.swift"
      to: "VerdictEventRepository.recordOutcome"
      via: "right/wrong/unsure selection"
      pattern: "recordOutcome"
---

<objective>
WIRE the existing Phase-44 decision seam into the Plan-45-01 log, and add the no-guilt post-session
outcome capture. This makes METRIC-01 LIVE: a `VerdictEvent` is recorded for every planned-session
decision, and a `right/wrong/unsure` outcome is captured after the session day passes. This plan also
satisfies SC4 — the production surface wires the logger so instrumentation is provably present before
any validation user can hit the verdict.

Purpose: 45-01 built the log + repository but nothing writes to them yet. The accept/keep/feel event
already exists (`TodayVerdictViewModel.onDecisionRecorded`); we wire it (no surface re-plumbing) and
enrich the logged payload with the go/modify/hold verdict label + muscle region the VM already has
access to but currently discards.

Output: VM capture of the headline verdict/region, the `onDecisionRecorded → repository.log` wiring in
WorkoutLog, a `VerdictOutcomeSheet`, en+zh-Hans strings, and an SC4 guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-measurement-wtp-instrumentation/45-CONTEXT.md
@.planning/phases/45-measurement-wtp-instrumentation/45-01-SUMMARY.md

# Files this plan MODIFIES (read current state before editing):
@WorkloadApp/ViewModels/TodayVerdictViewModel.swift
@WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
@WorkloadApp/Services/TodayVerdictService.swift
@DESIGN.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

From WorkloadApp/Services/VerdictDecision.swift (the event the hook emits — DO NOT change its shape):
  enum FeelOverride: String { case feelingStrong; case feelingRough }
  enum VerdictAction: Equatable { case accepted; case keptPlan; case feel(FeelOverride) }
  struct VerdictDecision: Equatable {
      let action: VerdictAction; let plannedTopSetKg: Double; let adjustedTopSetKg: Double?
      let hadAdjustment: Bool; let reasonLine: String; let decidedAt: Date }

From WorkloadApp/ViewModels/TodayVerdictViewModel.swift:
  var display: TodayVerdictDisplay?
  var onDecisionRecorded: ((VerdictDecision) -> Void)?     // <-- the seam to wire (currently nil)
  func refresh(athlete: Athlete)
  // refresh() calls verdictService.evaluateAndWrite(...) and DISCARDS the returned [VerdictResult].
  // sessionHeadline(in:) selects the max-weight non-warmup top set across exercises.

From WorkloadApp/Services/TodayVerdictEngine.swift:
  enum Verdict { case go; case modify; case hold }
  struct VerdictResult: Equatable { let verdict: Verdict; let adjustedTopSetKg: Double; let volumeCutSets: Int?; let loadFactor: Double }

From WorkloadApp/Services/TodayVerdictService.swift (region resolution to MIRROR for the headline exercise):
  let region = exercise.muscleGroup?.region ?? .fullBody        // exercise: TemplateExercise, region: MuscleRegion
  func evaluateAndWrite(prescribedWorkout:decisionInput:crossModalResult:plateStepKg:) -> [TodayVerdictEngine.VerdictResult]

From WorkloadApp/Repositories/VerdictEventRepository.swift (Plan 45-01):
  func log(decidedAt:planDate:verdictKindRaw:plannedTopSetKg:adjustedTopSetKg:deltaKg:differed:actionRaw:regionRaw:reasonLine:confidenceNote:athlete:) -> VerdictEvent
  func recordOutcome(_ outcomeRaw: String, for event: VerdictEvent, at date: Date)
  func mostRecentAwaitingOutcome(athlete: Athlete?, before: Date) -> VerdictEvent?

WorkoutLogView currently builds the VM in `.task(id: athletes.first?.id)`:
  if verdictVM == nil { verdictVM = TodayVerdictViewModel(modelContext: modelContext) }
  if let athlete = athletes.first { verdictVM?.refresh(athlete: athlete) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture verdict+region in the VM and wire onDecisionRecorded → repository.log (SC4)</name>
  <files>WorkloadApp/ViewModels/TodayVerdictViewModel.swift, WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift, WorkloadAppTests/VerdictEventLoggingTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    (a) In `TodayVerdictViewModel`, STOP discarding the `evaluateAndWrite` return. Capture the headline
    exercise's verdict label and region into two new read-only stored properties:
      - `private(set) var lastHeadlineVerdictRaw: String?` — "go" / "modify" / "hold" from the
        `TodayVerdictEngine.Verdict` of the headline exercise's `VerdictResult`. Align the returned
        `[VerdictResult]` with `plan.allExercises` (results are produced per exercise in that order);
        select the result for the exercise that owns the session headline top set (the same exercise
        `sessionHeadlineName(in:)` already identifies). For the cold-start defer path (built == nil /
        deferredToPlan), set the raw label to "defer".
      - `private(set) var lastHeadlineRegionRaw: String?` — the headline exercise's
        `muscleGroup?.region.rawValue ?? MuscleRegion.fullBody.rawValue` (mirror TodayVerdictService:150).
    Set both inside `refresh(athlete:)` right after `evaluateAndWrite`. Do NOT change `VerdictDecision`'s
    shape and do NOT touch the card — this is a non-visual VM enrichment only.

    (b) In `WorkoutLogView`, assign the production logger seam where the VM is constructed (the existing
    `.task(id:)` block, immediately after `verdictVM = TodayVerdictViewModel(...)`). Build a
    `VerdictEventRepository(modelContext: modelContext)` and set
    `verdictVM?.onDecisionRecorded = { decision in ... }`. Inside the closure, map the `VerdictDecision`
    + the VM's captured `lastHeadlineVerdictRaw` / `lastHeadlineRegionRaw` to a `repository.log(...)` call:
      - `verdictKindRaw` = `vm.lastHeadlineVerdictRaw ?? "go"`
      - `plannedTopSetKg` = `decision.plannedTopSetKg`
      - `adjustedTopSetKg` = `decision.adjustedTopSetKg`
      - `deltaKg` = `(decision.adjustedTopSetKg ?? decision.plannedTopSetKg) − decision.plannedTopSetKg`
      - `differed` = `decision.hadAdjustment`
      - `actionRaw` = map `decision.action`: .accepted→"accepted", .keptPlan→"keptPlan",
        .feel(.feelingStrong)→"feelStrong", .feel(.feelingRough)→"feelRough"
      - `regionRaw` = `vm.lastHeadlineRegionRaw ?? MuscleRegion.fullBody.rawValue`
      - `reasonLine` = `decision.reasonLine`; `confidenceNote` = `vm.display?.confidenceNote`
      - `planDate` = today's planned-session scheduled date (use the athlete's today; start-of-day is applied in the model)
      - `decidedAt` = `decision.decidedAt`; `athlete` = `athletes.first`.
    Hold the repository as `@State` so the closure captures a stable instance.

    (c) SC4 guard test (`VerdictEventLoggingTests`): own a STORED in-memory ModelContainer/Context + VM +
    VerdictEventRepository in setUp (deinit-SIGABRT avoidance). Seed an athlete + a today PrescribedWorkout
    (via PlannedSessionRepository.planManualLift), refresh the VM, wire `onDecisionRecorded` to the
    repository exactly as WorkoutLogView does, invoke `vm.accept()` (then a separate case `vm.keepPlan()`
    and `vm.feelOverride(.feelingRough)`), and assert each produces exactly one VerdictEvent with the
    correct actionRaw, differed flag, region, and verdict label. ADD a source-grep assertion (mirror
    TodayVerdictCardGuardTests): WorkoutLogView.swift MUST contain "onDecisionRecorded" — proving the
    seam is wired in production (the structural SC4 ordering guard; the seam is never left nil).
    Register VerdictOutcomeSheet's pbxproj entries in Task 2; this task's only new file is the test (auto-discovered).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/VerdictEventLoggingTests 2>&1 | tail -25</automated>
  </verify>
  <done>Each decision logs exactly one composite VerdictEvent with the right action/differed/region/verdict; WorkoutLogView.swift wires onDecisionRecorded (SC4 source-grep guard passes); app build green.</done>
</task>

<task type="auto">
  <name>Task 2: Post-session outcome capture (right / wrong / unsure)</name>
  <files>WorkloadApp/Views/WorkoutLog/VerdictOutcomeSheet.swift, WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift, WorkloadApp/Resources/Localizable.xcstrings, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    Create `VerdictOutcomeSheet` — a small DESIGN.md-compliant sheet (0pt corners via Rectangle, NO
    shadows, `Font.Tokens.*`, 8pt grid, dark+light via ColorTokens, accent FORBIDDEN here). It asks one
    calm, no-guilt question: "Was the call right?" with three equal-weight choices — Right / Wrong / Not
    sure — built via ONE shared button builder (mirror TodayVerdictCard.decisionButton's equal-weight
    treatment). It surfaces the event's planned→adjusted number + reason as quiet context (read-only).
    The sheet takes the awaiting `VerdictEvent` and an `onSelect: (String) -> Void` callback emitting
    "right" / "wrong" / "unsure". NO guilt/coercion copy.

    In `WorkoutLogView`: add `@State private var outcomeEvent: VerdictEvent?`. In the existing
    `.task(id: athletes.first?.id)` (after refresh) AND on the existing `onChange(of: showPlanToday)`
    re-read points, query `VerdictEventRepository.mostRecentAwaitingOutcome(athlete: athletes.first,
    before: Calendar.current.startOfDay(for: .now))` and set `outcomeEvent` to it (so a PAST planned-day
    decision with no recorded outcome triggers the prompt — never mid-session, since markCompleted is not
    wired in production). Present `.sheet(item: $outcomeEvent)` rendering `VerdictOutcomeSheet`; on select,
    call `repository.recordOutcome(selection, for: event, at: .now)` then clear `outcomeEvent`.

    Add en + zh-Hans strings for every new key (e.g. `verdictOutcome.title`, `verdictOutcome.question`,
    `verdictOutcome.right`, `verdictOutcome.wrong`, `verdictOutcome.unsure`, `verdictOutcome.context`).
    Calm, no-guilt copy in both languages. Register VerdictOutcomeSheet.swift with 4 pbxproj entries
    (fresh hex IDs, suggest AE4504xx).
  </action>
  <verify>
    <automated>xcodebuild build -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -5</automated>
  </verify>
  <done>VerdictOutcomeSheet presents for a past un-resolved VerdictEvent, writes right/wrong/unsure via recordOutcome, follows DESIGN (Rectangle/no-shadow/Font.Tokens/no-accent), and ships en+zh-Hans keys; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verdict decision (UI) → VerdictEvent log | Only composite decision payload + derived verdict label + region crosses |
| Validation user ↔ verdict surface | Surface must not be reachable without the logger wired (SC4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Information Disclosure | onDecisionRecorded → log payload | mitigate | Logger maps ONLY composite VerdictDecision fields + derived verdict/region; no raw biometric is in scope at the wire point (VerdictDecision carries none) |
| T-45-05 | Repudiation / Coverage gap | Production seam left nil | mitigate | Source-grep guard asserts WorkoutLogView wires onDecisionRecorded; logging integration test proves an event is written per decision (SC4) |
| T-45-06 | Tampering | Outcome self-report value | accept | Free single-user self-report; only "right"/"wrong"/"unsure" accepted; low-value, local-only |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies this phase |
</threat_model>

<verification>
- App build green on sim CAF84E71-BB64-491D-87C8-875A0143B26D.
- `grep -c "onDecisionRecorded" WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift` returns ≥ 1 (SC4 seam wired).
- VerdictEventLoggingTests proves one event per decision with correct composite payload.
- VerdictOutcomeSheet contains no DESIGN-banned tokens (Rectangle only, no `.shadow`, no `ColorTokens.accent`, no `.system(`).
- en + zh-Hans present for every new `verdictOutcome.*` key; no `" 2.swift"` dupes.
</verification>

<success_criteria>
- METRIC-01 is LIVE: every planned-session decision logs a composite VerdictEvent.
- SC4 ordering guard holds: the production surface wires the logger (structurally proven).
- Post-session right/wrong/unsure outcome is captured onto the awaiting event, no-guilt, DESIGN-compliant, bilingual.
</success_criteria>

<output>
Create `.planning/phases/45-measurement-wtp-instrumentation/45-02-SUMMARY.md` when done.
</output>
