---
phase: 45-measurement-wtp-instrumentation
plan: 04
type: execute
wave: 4
depends_on: ["45-01", "45-02", "45-03"]
files_modified:
  - WorkloadApp/Services/SeanEllisStore.swift
  - WorkloadApp/Views/Profile/SeanEllisPromptSheet.swift
  - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
  - WorkloadApp/Resources/Localizable.xcstrings
  - WorkloadAppTests/SeanEllisStoreTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [METRIC-03]
user_setup:
  - service: revenuecat
    why: "WTP / card-on-file (trial→paid) validation signal reuses the existing RevenueCat paywall"
    dashboard_config:
      - task: "Configure the trial→paid offering (intro-trial product) on the athlete_pro offering so the reused UpgradeSheet can start a trial — DEFERRED-EXTERNAL (RevenueCatConfig is gitignored; real charges tested by human)"
        location: "RevenueCat Dashboard → Offerings → athlete_pro"

must_haves:
  truths:
    - "After N logged verdict sessions the athlete is shown the Sean-Ellis question ('How would you feel if you could no longer use Tuwa?') with very / somewhat / not-disappointed choices, answered once per trigger and stored locally"
    - "The Sean-Ellis trigger fires at most once per athlete until re-eligible (it does not nag on every appearance) — driven by an injectable store, deterministically testable"
    - "A 'very disappointed' answer routes the athlete into the existing RevenueCat paywall (UpgradeSheet trial→paid) — the WTP / card-on-file code path is buildable; the dashboard offering config is flagged deferred-external"
  artifacts:
    - path: "WorkloadApp/Services/SeanEllisStore.swift"
      provides: "Local-only Sean-Ellis persistence + injectable trigger eligibility"
      contains: "struct SeanEllisStore"
    - path: "WorkloadApp/Views/Profile/SeanEllisPromptSheet.swift"
      provides: "DESIGN-compliant very/somewhat/not-disappointed prompt"
      contains: "struct SeanEllisPromptSheet"
  key_links:
    - from: "WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift"
      to: "SeanEllisPromptSheet"
      via: "trigger gated on SeanEllisStore.shouldPrompt(verdictEventCount:)"
      pattern: "shouldPrompt"
    - from: "WorkloadApp/Views/Profile/SeanEllisPromptSheet.swift"
      to: "UpgradeSheet"
      via: "very-disappointed → present existing paywall (WTP/card-on-file)"
      pattern: "UpgradeSheet"
---

<objective>
Instrument METRIC-03: capture the Sean-Ellis disappointment signal and route revealed willingness-to-pay
through the existing RevenueCat paywall. After the athlete has logged enough verdict sessions, ask the
single Sean-Ellis question, store the answer locally, and — when they say they'd be VERY disappointed —
offer the existing trial→paid path (card-on-file is the revealed signal we actually care about per the
pressure-test, which names WTP as the genuine open risk). No new paywall code: reuse `UpgradeSheet` +
`SubscriptionService`.

Purpose: stated demand is unreliable for this buyer; the pressure-test says capture REVEALED intent
(card-on-file / trial→paid) over stated. Sean-Ellis is the cheap stated proxy; the paywall hop is the
revealed one. The RevenueCat offering (trial product) is external dashboard config — the CODE path is
built and tested here; the config is flagged deferred-external.

Output: `SeanEllisStore` (local, injectable), `SeanEllisPromptSheet`, the WorkoutLog trigger mount, the
very-disappointed→UpgradeSheet hop, en+zh-Hans strings, and a deterministic store test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-measurement-wtp-instrumentation/45-CONTEXT.md
@.planning/phases/45-measurement-wtp-instrumentation/45-01-SUMMARY.md
@.planning/phases/45-measurement-wtp-instrumentation/45-02-SUMMARY.md
@.planning/research/plan-aware-thesis-pressure-test.md

# Files this plan MODIFIES / reuses (read current state first):
@WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
@WorkloadApp/Views/Subscription/UpgradeSheet.swift
@DESIGN.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

From WorkloadApp/Views/Subscription/UpgradeSheet.swift (REUSE — do not rebuild the paywall):
  enum UpgradeTrigger { case history(lockedWeeks: Int); case coach; case athletePro; case export }
  struct UpgradeSheet: View { init(trigger: UpgradeTrigger) }    // present as UpgradeSheet(trigger: .athletePro)
  // The "Start 7-Day Free Trial" CTA already appears when the package has an introductoryDiscount.

From WorkloadApp/Services/SubscriptionService.swift:
  @MainActor @Observable final class SubscriptionService — fetchOffering(for:) / purchase(package:) / restorePurchases()

From WorkloadApp/Repositories/VerdictEventRepository.swift (Plan 45-01):
  func fetchAll(athlete: Athlete?) -> [VerdictEvent]     // .count drives the Sean-Ellis trigger threshold

WorkoutLogView already hosts the verdict VM + an outcome sheet (Plan 45-02); it is the natural trigger
host (the athlete has just been making verdict decisions). It exposes @Query athletes and @Environment(\.modelContext).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SeanEllisStore (local, injectable) + SeanEllisPromptSheet</name>
  <files>WorkloadApp/Services/SeanEllisStore.swift, WorkloadApp/Views/Profile/SeanEllisPromptSheet.swift, WorkloadApp/Resources/Localizable.xcstrings, WorkloadAppTests/SeanEllisStoreTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    Create `struct SeanEllisStore` — a local-only persistence + eligibility helper backed by an INJECTED
    `UserDefaults` (default `.standard`) so it is deterministically unit-testable (no SwiftData model, no
    network, no Codable-to-Supabase). Keys (namespaced, e.g. "seanEllis.lastAnswerRaw",
    "seanEllis.lastAnsweredAt", "seanEllis.lastPromptedAtCount"). API:
      - `enum Disappointment: String { case very; case somewhat; case not }`
      - `func recordAnswer(_ answer: Disappointment, atEventCount: Int, on date: Date)` — persists the raw
        answer + date + the event count at answer time.
      - `func shouldPrompt(verdictEventCount: Int, threshold: Int = 5) -> Bool` — true only when
        `verdictEventCount >= threshold` AND no answer has been recorded since the last time the threshold
        bracket was crossed (i.e., never answered yet, OR enough NEW events have accrued since the last
        answer to re-qualify). Pure read of the injected defaults; no `.now` inside the decision.
      - `var lastAnswer: Disappointment?` (read-back for any future surface).
    Document the local-only contract (answer never leaves the device; mirrors the project's never-sync
    posture).

    Create `struct SeanEllisPromptSheet: View` — a DESIGN.md-compliant sheet (0pt corners via Rectangle,
    NO shadows, Font.Tokens.*, 8pt grid, dark+light via ColorTokens, accent FORBIDDEN). It asks
    "How would you feel if you could no longer use Tuwa?" with three equal-weight choices —
    Very disappointed / Somewhat disappointed / Not disappointed — built via ONE shared button builder
    (mirror TodayVerdictCard.decisionButton equal-weight treatment). It takes an
    `onAnswer: (SeanEllisStore.Disappointment) -> Void` callback. Calm, neutral copy (no guilt, no
    upsell language in the question itself). Add en + zh-Hans for every key
    (`seanEllis.title`, `seanEllis.question`, `seanEllis.very`, `seanEllis.somewhat`, `seanEllis.not`).
    Register both new app-target files (SeanEllisStore.swift, SeanEllisPromptSheet.swift) with 4 pbxproj
    entries each (fresh hex IDs, suggest AE4506xx / AE4507xx).

    Test (`SeanEllisStoreTests`): use an isolated `UserDefaults(suiteName:)` injected into the store.
    Assert shouldPrompt is false below threshold; true at/after threshold when never answered; false
    immediately after recordAnswer; true again only after enough new events accrue. Fully deterministic
    (no .standard, no .now baked into the decision).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/SeanEllisStoreTests 2>&1 | tail -20</automated>
  </verify>
  <done>SeanEllisStore persists answers + gates the prompt deterministically (injected defaults); SeanEllisPromptSheet renders very/somewhat/not equal-weight per DESIGN with en+zh-Hans; store tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the trigger + route very-disappointed → existing paywall (WTP/card-on-file)</name>
  <files>WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift, WorkloadApp/Views/Profile/SeanEllisPromptSheet.swift, WorkloadApp/Resources/Localizable.xcstrings</files>
  <action>
    In `WorkoutLogView`, mount the Sean-Ellis trigger (this view is Wave-4-edited AFTER the Plan-45-02
    logging/outcome wiring already landed — re-read the current file first). Add
    `@State private var showSeanEllis = false`. In the existing `.task(id: athletes.first?.id)` (after the
    verdict refresh + outcome check), build a `VerdictEventRepository`, read
    `fetchAll(athlete: athletes.first).count`, construct a `SeanEllisStore()` (default `.standard`), and
    set `showSeanEllis = store.shouldPrompt(verdictEventCount: count)`. Present
    `.sheet(isPresented: $showSeanEllis) { SeanEllisPromptSheet(onAnswer: ...) }`. In `onAnswer`:
    `store.recordAnswer(answer, atEventCount: count, on: .now)`, dismiss, and when
    `answer == .very` set `@State private var showWTPUpgrade = true`.

    Add `.sheet(isPresented: $showWTPUpgrade) { UpgradeSheet(trigger: .athletePro) }` — REUSE the existing
    paywall; do NOT add a new trigger case or new paywall code. This is the WTP / card-on-file path: a
    very-disappointed answer routes the athlete into the existing RevenueCat trial→paid flow. The actual
    RevenueCat OFFERING config (intro-trial product on athlete_pro) is EXTERNAL dashboard setup
    (RevenueCatConfig is gitignored) — leave a `// DEFERRED-EXTERNAL: RevenueCat dashboard trial→paid
    offering config + real-charge testing (human)` comment at the presentation site so the deferral is
    explicit in source. The pressure-test names WTP as the genuine open risk; this captures the REVEALED
    intent hop without inventing a new paywall.

    Add any remaining en + zh-Hans strings used at the mount site. Do NOT modify UpgradeSheet.swift or
    SubscriptionService.swift.
  </action>
  <verify>
    <automated>xcodebuild build -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' 2>&1 | tail -5</automated>
  </verify>
  <done>WorkoutLog triggers the Sean-Ellis prompt after N verdict sessions, stores the answer, and routes very-disappointed into the existing UpgradeSheet; the deferred-external RevenueCat config is flagged in-source; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sean-Ellis answer → local storage | Stays on device (UserDefaults); never synced |
| Athlete → RevenueCat (third party) | Payment / card-on-file handled by RevenueCat SDK + StoreKit, not by app code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Information Disclosure | Sean-Ellis answer | mitigate | Stored only in local UserDefaults (no Codable-to-Supabase, no network); local-only contract documented |
| T-45-10 | Tampering | WTP / purchase flow | transfer | Reuses existing RevenueCat SDK + StoreKit purchase path (UpgradeSheet); app adds no custom payment handling; receipt validation is RevenueCat/Apple's responsibility |
| T-45-11 | Repudiation | Trigger nag / double-charge UX | mitigate | shouldPrompt gates to once-per-eligibility (deterministic test); WTP hop only presents the existing paywall, never auto-purchases |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies this phase (RevenueCat SDK already present) |
</threat_model>

<verification>
- App build green on sim CAF84E71-BB64-491D-87C8-875A0143B26D.
- `grep -c "shouldPrompt" WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift` ≥ 1.
- `grep -c "UpgradeSheet(trigger:" WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift` ≥ 1 (WTP hop present).
- `grep -ci "DEFERRED-EXTERNAL" WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift` ≥ 1 (RevenueCat config deferral flagged in-source).
- SeanEllisPromptSheet / SeanEllisStore contain no `ColorTokens.accent`, `.shadow`, `RoundedRectangle`, `.system(`.
- UpgradeSheet.swift + SubscriptionService.swift are UNCHANGED (reuse only).
- en + zh-Hans present for every new `seanEllis.*` key; no `" 2.swift"` dupes.
</verification>

<success_criteria>
- METRIC-03: Sean-Ellis disappointment is captured (very/somewhat/not), stored locally, gated to once-per-eligibility.
- Very-disappointed routes into the existing RevenueCat trial→paid paywall (WTP / card-on-file code path live).
- RevenueCat dashboard offering config + real charges are flagged deferred-external (not a blocker).
</success_criteria>

<output>
Create `.planning/phases/45-measurement-wtp-instrumentation/45-04-SUMMARY.md` when done.
</output>
