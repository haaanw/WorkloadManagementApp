---
phase: 45-measurement-wtp-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Models/VerdictEvent.swift
  - WorkloadApp/App/WorkloadApp.swift
  - WorkloadApp/Repositories/VerdictEventRepository.swift
  - WorkloadApp/Services/GreenLightEngine.swift
  - WorkloadAppTests/VerdictEventModelTests.swift
  - WorkloadAppTests/VerdictEventRepositoryTests.swift
  - WorkloadAppTests/GreenLightEngineTests.swift
  - "workload management/workload management.xcodeproj/project.pbxproj"
autonomous: true
requirements: [METRIC-01, METRIC-02]
user_setup: []

must_haves:
  truths:
    - "A VerdictEvent @Model exists, is in the SwiftData schema, and stores ONLY composite fields (verdict, planned/adjusted kg, delta, differed, action, region label, reason, outcome, dates) — no raw HRV/RHR/sleep/HealthKit field names"
    - "VerdictEvent is local-only: its type name never appears in SyncService.swift (never syncs to Supabase)"
    - "A VerdictEventRepository can log a decision, record a post-session outcome, and fetch events for an athlete"
    - "GreenLightEngine computes green-light rate, activation rate, and Day-7/Day-30 retention deterministically from [VerdictEvent] with an injected asOf date + calendar (no baked .now)"
  artifacts:
    - path: "WorkloadApp/Models/VerdictEvent.swift"
      provides: "Composite-only local @Model for per-planned-session verdict events"
      contains: "final class VerdictEvent"
    - path: "WorkloadApp/Repositories/VerdictEventRepository.swift"
      provides: "@MainActor persistence: log / recordOutcome / fetch"
      contains: "final class VerdictEventRepository"
    - path: "WorkloadApp/Services/GreenLightEngine.swift"
      provides: "Pure green-light / activation / retention computation"
      contains: "struct GreenLightEngine"
  key_links:
    - from: "WorkloadApp/App/WorkloadApp.swift"
      to: "VerdictEvent.self"
      via: "Schema array registration"
      pattern: "VerdictEvent.self"
    - from: "WorkloadApp/Services/GreenLightEngine.swift"
      to: "[VerdictEvent]"
      via: "static compute over events with injected asOf/calendar"
      pattern: "asOf"
---

<objective>
Build the measurement SUBSTRATE for the v2.0 validation loop: the composite-only `VerdictEvent`
local `@Model`, its `@MainActor` repository, and the pure `GreenLightEngine` that turns logged events
into the green-light signal. NO UI and NO wiring in this plan — those are Plans 45-02/03/04. This plan
delivers the data + compute layer all higher plans consume.

Purpose: METRIC-01 needs a privacy-safe place to record every planned-session verdict decision;
METRIC-02 needs a deterministic engine to compute green-light / activation / retention from those
records. Both are pure/data layers, so they are built and unit-tested in isolation first.

Output: `VerdictEvent.swift` (+ schema registration), `VerdictEventRepository.swift`,
`GreenLightEngine.swift`, and three test files proving composite-only privacy, sync omission,
repository round-trips, and green-light math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-measurement-wtp-instrumentation/45-CONTEXT.md

# Real types to MIRROR (read before writing — do not invent shapes):
@WorkloadApp/Models/SorenessLog.swift
@WorkloadApp/Repositories/SorenessLogRepository.swift
@WorkloadApp/Services/VerdictDecision.swift
@WorkloadApp/App/WorkloadApp.swift
@WorkloadAppTests/TodayVerdictCardGuardTests.swift

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

From WorkloadApp/Services/VerdictDecision.swift (the Phase-44 event the logger will map FROM in 45-02):
  enum FeelOverride: String, Codable, CaseIterable { case feelingStrong; case feelingRough }
  enum VerdictAction: Equatable { case accepted; case keptPlan; case feel(FeelOverride) }
  struct VerdictDecision: Equatable {
      let action: VerdictAction
      let plannedTopSetKg: Double
      let adjustedTopSetKg: Double?
      let hadAdjustment: Bool
      let reasonLine: String
      let decidedAt: Date
  }

From WorkloadApp/Services/TodayVerdictEngine.swift (the verdict label captured in 45-02):
  enum Verdict { case go; case modify; case hold }

From WorkloadApp/Models/Enums.swift:
  enum MuscleRegion: String, Codable, CaseIterable, Identifiable { case legs, back, chest, shoulders, arms, core, fullBody }

SorenessLog local-only @Model pattern (MIRROR this exactly): @Attribute(.unique) var id: UUID;
real Date fields; *Raw: String for enums; var athlete: Athlete? bare inverse (NO array on Athlete);
NO Codable, NO encoder, NO push/pull — the type name appears NOWHERE in SyncService.swift.

Source-grep guard pattern (MIRROR TodayVerdictCardGuardTests): resolve repo root from #filePath
(WorkloadAppTests/ → repo root), read a target .swift source with String(contentsOf:), assert it
does/does-not contain tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerdictEvent composite-only @Model + schema registration + privacy guards</name>
  <files>WorkloadApp/Models/VerdictEvent.swift, WorkloadApp/App/WorkloadApp.swift, WorkloadAppTests/VerdictEventModelTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <behavior>
    - Round-trip: insert a VerdictEvent through an in-memory ModelContainer, fetch it back, all composite fields equal what was written (mirror ShadowDataContractTests.test_sorenessLog_roundTripsThroughContainer).
    - Composite-only field-name guard: source-grep VerdictEvent.swift (via #filePath → repo root) and assert it contains NONE of the raw-biometric field-name tokens (case-insensitive): "hrv", "rhr", "restingHR", "hrvSDNN", "sleepDuration", "heartRate", "bodyTemp", "wristTemperature", "vo2", "healthKit", "hkQuantity". This proves no raw HealthKit value can be stored.
    - Sync-omission guard: source-grep WorkloadApp/Services/SyncService.swift and assert it does NOT contain the substring "VerdictEvent" (local-only by omission, mirroring SorenessLog).
    - Schema guard: source-grep WorkloadApp/App/WorkloadApp.swift and assert it contains "VerdictEvent.self".
  </behavior>
  <action>
    Create `VerdictEvent.swift` as a local-only `@Model final class VerdictEvent` mirroring SorenessLog's
    privacy posture (NO Codable, NO encoder, NO push/pull, bare `var athlete: Athlete?` inverse, NO
    `[VerdictEvent]` array on Athlete). Store ONLY these COMPOSITE fields per METRIC-01 / 45-CONTEXT
    "COMPOSITE-ONLY privacy" guardrail:
      - `@Attribute(.unique) var id: UUID`
      - `var decidedAt: Date` (when the athlete decided)
      - `var planDate: Date` (start-of-day of the planned session)
      - `var verdictKindRaw: String` (the go/modify/hold/defer label — stored as "go"/"modify"/"hold"/"defer")
      - `var plannedTopSetKg: Double`
      - `var adjustedTopSetKg: Double?` (the suggestion; nil when none)
      - `var deltaKg: Double` (adjusted − planned; 0 when no adjustment)
      - `var differed: Bool` (true when |deltaKg| exceeds a small epsilon — the suggestion differs from the plan)
      - `var actionRaw: String` ("accepted" / "keptPlan" / "feelStrong" / "feelRough")
      - `var regionRaw: String` (a `MuscleRegion.rawValue` — the muscle REGION label, never a raw biometric)
      - `var reasonLine: String` (the composite one-line reason text already produced by VerdictReasonBuilder — no raw numbers)
      - `var confidenceNote: String?` (optional honest-confidence band text)
      - `var outcomeRaw: String?` (post-session self-report: "right" / "wrong" / "unsure"; nil until reported)
      - `var outcomeRecordedAt: Date?`
      - `var updatedAt: Date`
      - `var athlete: Athlete?`
    Provide a memberwise `init` with sensible defaults (outcome nil, updatedAt = .now). Do NOT store ANY
    raw HRV/RHR/sleep/temperature/HealthKit value — only composite scores/labels/deltas. Add a doc-comment
    block explicitly stating the local-only + composite-only contract (mirror SorenessLog's comment) and
    WHY (the project's raw-HealthKit-never-syncs rule). Then register the model additively in
    `WorkloadApp.swift` Schema array (add `VerdictEvent.self` alongside `SorenessLog.self`) — additive only,
    NO migration. Register the new model file with 4 explicit pbxproj entries (PBXBuildFile,
    PBXFileReference, PBXGroup children, PBXSourcesBuildPhase) mirroring the AA44xx entries; use fresh
    unique hex IDs (suggest AE4501xx).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/VerdictEventModelTests 2>&1 | tail -20</automated>
  </verify>
  <done>VerdictEvent.swift compiles, is in the schema, round-trips through a container, contains no raw-biometric field names, and "VerdictEvent" is absent from SyncService.swift. App build green on sim CAF84E71.</done>
</task>

<task type="auto">
  <name>Task 2: VerdictEventRepository — log / recordOutcome / fetch</name>
  <files>WorkloadApp/Repositories/VerdictEventRepository.swift, WorkloadAppTests/VerdictEventRepositoryTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <action>
    Create `@MainActor final class VerdictEventRepository` taking `ModelContext` in init (project
    convention; mirror SorenessLogRepository — apply the date window in the FetchDescriptor and filter
    the optional `athlete` relationship in Swift AFTER the fetch, never via an `athlete?.id` #Predicate,
    to dodge the iOS 26.1 in-memory trap). Methods:
      - `@discardableResult func log(decidedAt:planDate:verdictKindRaw:plannedTopSetKg:adjustedTopSetKg:deltaKg:differed:actionRaw:regionRaw:reasonLine:confidenceNote:athlete:) -> VerdictEvent`
        — constructs, inserts, and saves a VerdictEvent (self-contained save, matching SorenessLogRepository.insert).
      - `func recordOutcome(_ outcomeRaw: String, for event: VerdictEvent, at date: Date = .now)` — sets
        outcomeRaw + outcomeRecordedAt + updatedAt, then saves. (outcomeRaw is the "right"/"wrong"/"unsure" self-report.)
      - `func fetchAll(athlete: Athlete?) -> [VerdictEvent]` — newest-first by decidedAt; Swift-side athlete filter.
      - `func fetchRecent(days: Int, athlete: Athlete?) -> [VerdictEvent]` — events with decidedAt within the start-of-day window, newest-first; Swift-side athlete filter.
      - `func mostRecentAwaitingOutcome(athlete: Athlete?, before: Date) -> VerdictEvent?` — the newest event with `outcomeRaw == nil` whose `planDate < before` (drives the 45-02 post-session prompt; "before" is normally startOfDay(today)).
    Tests (own ModelContainer/ModelContext/repository as STORED XCTestCase props set in setUp + cleared in
    tearDown — the documented @MainActor deinit-SIGABRT avoidance from 42-02/43-03): log creates a row;
    recordOutcome updates outcomeRaw + outcomeRecordedAt; fetchAll/fetchRecent return newest-first and
    respect the athlete filter; mostRecentAwaitingOutcome returns only un-resolved past-day events.
    Register the new file with 4 pbxproj entries (fresh hex IDs, suggest AE4502xx).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/VerdictEventRepositoryTests 2>&1 | tail -20</automated>
  </verify>
  <done>Repository logs, records outcomes, and fetches (newest-first, athlete-filtered); mostRecentAwaitingOutcome returns only past-day un-resolved events; all repository tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GreenLightEngine — green-light / activation / retention (injected dates)</name>
  <files>WorkloadApp/Services/GreenLightEngine.swift, WorkloadAppTests/GreenLightEngineTests.swift, workload management/workload management.xcodeproj/project.pbxproj</files>
  <behavior>
    - Green-light rate: with 3 differing-verdict days where 2 were acted-on (action != "keptPlan") AND outcome == "right", and 1 was acted-on but outcome == "wrong", rate == 2/3. Non-differing events are excluded entirely from numerator and denominator.
    - Differing-day collapse: two differed events on the SAME start-of-day count as ONE differing day (group by injected-calendar startOfDay(planDate)).
    - keptPlan excluded from "acted": a differing day where the only event has action == "keptPlan" counts in the denominator but never the numerator.
    - Empty / no-differing-days: greenLightRate is nil (not 0 — honest "no signal yet").
    - Activation rate: events where action != "keptPlan" divided by total events; 4 events, 3 acted ⇒ 0.75.
    - Retention: with firstEvent at day 0 and asOf at day 8, day7Retention is true iff an event exists with decidedAt on/after startOfDay(firstEvent)+7d; if asOf has not yet reached firstEvent+7d, day7Retention is nil (cannot know yet). Same logic for day30 at the 30-day horizon. All computed with the INJECTED calendar + asOf — no baked .now.
  </behavior>
  <action>
    Create `struct GreenLightEngine` (pure, static methods only — no state, no @MainActor, mirrors
    WorkloadCalculator/RecoveryScoreEngine). Define a result struct
    `GreenLightMetrics { let greenLightRate: Double?; let differingDays: Int; let activationRate: Double?;
    let totalEvents: Int; let day7Retention: Bool?; let day30Retention: Bool? }`. One entry point
    `static func compute(events: [VerdictEvent], asOf: Date, calendar: Calendar) -> GreenLightMetrics`:
      - GREEN-LIGHT RATE (METRIC-02 definition from 45-CONTEXT): collapse `differed == true` events to days
        via `calendar.startOfDay(for: event.planDate)`. denominator = number of distinct differing days.
        numerator = differing days where the day's representative event has `actionRaw != "keptPlan"` AND
        `outcomeRaw == "right"`. rate = numerator / denominator; nil when denominator == 0.
      - ACTIVATION RATE = (events where actionRaw != "keptPlan") / totalEvents; nil when totalEvents == 0.
      - RETENTION: let firstDay = startOfDay(min decidedAt). For N in {7, 30}: if `asOf` is before
        `firstDay + N days` ⇒ nil (too early to know). Else ⇒ true iff some event has
        `decidedAt >= firstDay + N days`, false otherwise.
    Inject `asOf` and `calendar` on EVERY date computation — do NOT call `.now` or `.current` inside the
    engine (deterministic + testable; the explicit anti-pattern called out in 45-CONTEXT). Do NOT read
    raw biometrics — the engine only ever touches the composite VerdictEvent fields. Register the new
    file with 4 pbxproj entries (fresh hex IDs, suggest AE4503xx).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=CAF84E71-BB64-491D-87C8-875A0143B26D' -only-testing:WorkloadAppTests/GreenLightEngineTests 2>&1 | tail -20</automated>
  </verify>
  <done>GreenLightEngine computes green-light rate (differing-day collapse, keptPlan excluded, nil on no signal), activation rate, and Day-7/Day-30 retention — all from injected asOf/calendar; every behavior test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthKit-derived signals → VerdictEvent | Only composite scores/labels/deltas may cross; raw biometrics must never be stored |
| Device SwiftData store → Supabase sync | VerdictEvent must NEVER cross this boundary (local-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | VerdictEvent @Model | mitigate | Composite-only fields; source-grep guard asserts no raw-biometric field names (hrv/rhr/sleepDuration/heartRate/etc.) on the model |
| T-45-02 | Information Disclosure | SyncService payloads | mitigate | VerdictEvent is local-only by omission; sync-omission test asserts "VerdictEvent" never appears in SyncService.swift; no Codable/encoder/push/pull |
| T-45-03 | Tampering | SwiftData schema migration | accept | Additive-nullable model registration only; no migration, existing rows unaffected (matches SorenessLog/CyclePredictionLog additive pattern) |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies added this phase (Swift/SPM unchanged) — no supply-chain surface |
</threat_model>

<verification>
- App build green on sim CAF84E71-BB64-491D-87C8-875A0143B26D ⇒ `** BUILD SUCCEEDED **`.
- `VerdictEvent.self` present in the Schema array in WorkloadApp.swift.
- `grep -i -E "hrv|rhr|restingHR|hrvSDNN|sleepDuration|heartRate|bodyTemp|wristTemperature|vo2|healthKit" WorkloadApp/Models/VerdictEvent.swift` returns NOTHING.
- `grep -c "VerdictEvent" WorkloadApp/Services/SyncService.swift` returns 0.
- No `" 2.swift"` duplicate files created.
</verification>

<success_criteria>
- VerdictEvent @Model is composite-only, local-only, in the schema, and round-trips.
- VerdictEventRepository logs decisions, records outcomes, and fetches (athlete-filtered, newest-first).
- GreenLightEngine computes green-light/activation/retention deterministically with injected asOf/calendar.
- All three test files green; app build green.
</success_criteria>

<output>
Create `.planning/phases/45-measurement-wtp-instrumentation/45-01-SUMMARY.md` when done.
</output>
