---
phase: 13-design-polish
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - WorkloadApp/Components/SpikeAlertBanner.swift
  - WorkloadApp/Components/FatigueAttentionBanner.swift
  - WorkloadApp/Components/ToastBanner.swift
  - WorkloadApp/Components/StalenessWarningBadge.swift
  - WorkloadApp/Components/DeltaIndicator.swift
  - WorkloadApp/Views/Profile/InviteConfirmationSheet.swift
  - WorkloadApp/Views/Subscription/UpgradeSheet.swift
  - WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift
  - WorkloadApp/Views/WorkoutLog/TextTemplateImportSheet.swift
  - WorkloadApp/Views/Profile/ProfileView.swift
  - WorkloadApp/Views/Profile/TrainingProfileSheet.swift
  - WorkloadApp/Views/Dashboard/WeeklySummaryCard.swift
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/Views/Coach/ContextSwitcher.swift
  - WorkloadApp/Views/WorkoutLog/TemplateCarouselSection.swift
  - WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift
  - WorkloadApp/Views/Auth/SignUpView.swift
autonomous: true
requirements:
  - DESGN-01

must_haves:
  truths:
    - "Zero raw Font.custom('DMSans-*') calls remain in any file outside FontTokens.swift"
    - "Zero .font(.system(size:)) calls remain for text rendering (SF Symbol icons exempted)"
    - "All text in the app renders in Alpino via Font.Tokens"
  artifacts:
    - path: "WorkloadApp/Components/SpikeAlertBanner.swift"
      provides: "Tokens-based font references"
      contains: ".Tokens."
    - path: "WorkloadApp/Views/Profile/ProfileView.swift"
      provides: "Tokens-based font references (no system fonts)"
      contains: ".Tokens."
  key_links:
    - from: "all 17 modified files"
      to: "WorkloadApp/Utilities/FontTokens.swift"
      via: "Font.Tokens.* references"
      pattern: '\\.Tokens\\.'
---

<objective>
Replace all 20 raw `Font.custom("DMSans-*")` calls across 8 files and all 20 `.font(.system())` calls across 10 files with `Font.Tokens.*` references. This eliminates every rogue font reference so the entire app renders in Alpino through the centralized token system.

Purpose: After Plan 01 established Alpino in FontTokens.swift, views using Font.Tokens already render correctly. But 40 rogue references bypass the token system and still render in DM Sans or San Francisco. This plan fixes all of them.

Output: 17 files updated with Font.Tokens references. Zero rogue font calls remain in the codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-polish/13-CONTEXT.md
@.planning/phases/13-design-polish/13-RESEARCH.md
@.planning/phases/13-design-polish/13-PATTERNS.md
@.planning/phases/13-design-polish/13-01-SUMMARY.md

<interfaces>
<!-- Font.Tokens after Plan 01 (extended with new tokens) -->
From WorkloadApp/Utilities/FontTokens.swift (post Plan 01):
```swift
extension Font {
    enum Tokens {
        static let heroScore       = Font.custom("Alpino-Regular", size: 64)
        static let pageTitle       = Font.custom("Alpino-Regular", size: 32)
        static let sectionHead     = Font.custom("Alpino-Medium",  size: 19)
        static let body            = Font.custom("Alpino-Regular", size: 17)
        static let bodyMedium      = Font.custom("Alpino-Medium",  size: 17)
        static let label           = Font.custom("Alpino-Regular", size: 15)
        static let labelMedium     = Font.custom("Alpino-Medium",  size: 15)
        static let smallLabel      = Font.custom("Alpino-Regular", size: 13)
        static let smallLabelMedium = Font.custom("Alpino-Medium", size: 13)
        static let micro           = Font.custom("Alpino-Regular", size: 12)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert raw Font.custom("DMSans-*") calls to Font.Tokens (8 files, 20 refs)</name>
  <files>
    WorkloadApp/Components/SpikeAlertBanner.swift,
    WorkloadApp/Components/FatigueAttentionBanner.swift,
    WorkloadApp/Components/ToastBanner.swift,
    WorkloadApp/Components/StalenessWarningBadge.swift,
    WorkloadApp/Views/Profile/InviteConfirmationSheet.swift,
    WorkloadApp/Views/Subscription/UpgradeSheet.swift,
    WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift,
    WorkloadApp/Views/WorkoutLog/TextTemplateImportSheet.swift
  </files>
  <read_first>
    WorkloadApp/Utilities/FontTokens.swift,
    WorkloadApp/Components/SpikeAlertBanner.swift,
    WorkloadApp/Components/FatigueAttentionBanner.swift,
    WorkloadApp/Components/ToastBanner.swift,
    WorkloadApp/Components/StalenessWarningBadge.swift,
    WorkloadApp/Views/Profile/InviteConfirmationSheet.swift,
    WorkloadApp/Views/Subscription/UpgradeSheet.swift,
    WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift,
    WorkloadApp/Views/WorkoutLog/TextTemplateImportSheet.swift
  </read_first>
  <action>
Replace every `Font.custom("DMSans-*", size: N)` and `.font(.custom("DMSans-*", size: N))` with the appropriate Font.Tokens reference. Use this mapping:

| Raw Call | Replacement |
|----------|-------------|
| `.custom("DMSans-Regular", size: 9)` | `.Tokens.micro` |
| `.custom("DMSans-Regular", size: 11)` | `.Tokens.micro` |
| `.custom("DMSans-Medium", size: 11)` | `.Tokens.micro` (keep weight loss -- 11pt Medium was already non-standard) |
| `.custom("DMSans-Regular", size: 13)` | `.Tokens.smallLabel` |
| `.custom("DMSans-Medium", size: 13)` | `.Tokens.smallLabelMedium` |
| `.custom("DMSans-Regular", size: 14)` | `.Tokens.label` |
| `.custom("DMSans-Regular", size: 15)` | `.Tokens.label` |
| `.custom("DMSans-Medium", size: 15)` | `.Tokens.labelMedium` |
| `.custom("DMSans-Medium", size: 9)` | `.Tokens.micro` |
| `.custom("DMSans-Medium", size: 28)` | `.Tokens.pageTitle` (32pt Regular -- closest standard token) |

File-by-file specifics:

**SpikeAlertBanner.swift** (6 refs):
- 11pt Medium -> `.Tokens.micro`
- 13pt Regular -> `.Tokens.smallLabel`
- 13pt Medium -> `.Tokens.smallLabelMedium`
- 11pt Regular -> `.Tokens.micro`

**FatigueAttentionBanner.swift** (3 refs):
- 11pt Medium -> `.Tokens.micro`
- 15pt Medium -> `.Tokens.labelMedium`
- 13pt Regular -> `.Tokens.smallLabel`

**ToastBanner.swift** (1 ref):
- 13pt Regular -> `.Tokens.smallLabel`

**StalenessWarningBadge.swift** (1 ref):
- 11pt Regular -> `.Tokens.micro`

**InviteConfirmationSheet.swift** (6 refs):
- 15pt Regular -> `.Tokens.label`
- 11pt Medium -> `.Tokens.micro`
- 28pt Medium -> `.Tokens.pageTitle`
- 13pt Regular -> `.Tokens.smallLabel`

**UpgradeSheet.swift** (1 Font.custom ref):
- 9pt Medium -> `.Tokens.micro`

**WorkoutLogView.swift** (1 ref):
- 13pt Regular/Medium conditional -> `.Tokens.smallLabel` / `.Tokens.smallLabelMedium` (preserve conditional logic)

**TextTemplateImportSheet.swift** (1 ref):
- 14pt Regular -> `.Tokens.label`
  </action>
  <verify>
    <automated>! grep -rn 'Font\.custom("DMSans' WorkloadApp/Components/SpikeAlertBanner.swift WorkloadApp/Components/FatigueAttentionBanner.swift WorkloadApp/Components/ToastBanner.swift WorkloadApp/Components/StalenessWarningBadge.swift WorkloadApp/Views/Profile/InviteConfirmationSheet.swift WorkloadApp/Views/Subscription/UpgradeSheet.swift WorkloadApp/Views/WorkoutLog/WorkoutLogView.swift WorkloadApp/Views/WorkoutLog/TextTemplateImportSheet.swift && echo "PASS: zero DMSans Font.custom remaining"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `Font.custom("DMSans` in any of the 8 files
    - SpikeAlertBanner.swift contains `.Tokens.micro` and `.Tokens.smallLabel`
    - FatigueAttentionBanner.swift contains `.Tokens.labelMedium`
    - All replacements use `Font.Tokens.*` syntax (not raw `Font.custom("Alpino-*")`)
  </acceptance_criteria>
  <done>All 20 raw Font.custom("DMSans-*") calls replaced with Font.Tokens references across 8 files</done>
</task>

<task type="auto">
  <name>Task 2: Convert .font(.system()) calls to Font.Tokens (10 files, 20 refs)</name>
  <files>
    WorkloadApp/Views/Profile/ProfileView.swift,
    WorkloadApp/Views/Profile/TrainingProfileSheet.swift,
    WorkloadApp/Views/Dashboard/WeeklySummaryCard.swift,
    WorkloadApp/Views/Dashboard/DashboardView.swift,
    WorkloadApp/Views/Coach/ContextSwitcher.swift,
    WorkloadApp/Views/WorkoutLog/TemplateCarouselSection.swift,
    WorkloadApp/Views/Subscription/UpgradeSheet.swift,
    WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift,
    WorkloadApp/Components/DeltaIndicator.swift,
    WorkloadApp/Views/Auth/SignUpView.swift
  </files>
  <read_first>
    WorkloadApp/Utilities/FontTokens.swift,
    WorkloadApp/Views/Profile/ProfileView.swift,
    WorkloadApp/Views/Profile/TrainingProfileSheet.swift,
    WorkloadApp/Views/Dashboard/WeeklySummaryCard.swift,
    WorkloadApp/Views/Dashboard/DashboardView.swift,
    WorkloadApp/Views/Coach/ContextSwitcher.swift,
    WorkloadApp/Views/WorkoutLog/TemplateCarouselSection.swift,
    WorkloadApp/Views/Subscription/UpgradeSheet.swift,
    WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift,
    WorkloadApp/Components/DeltaIndicator.swift,
    WorkloadApp/Views/Auth/SignUpView.swift
  </read_first>
  <action>
Replace every `.font(.system(size: N))` and `.font(.system(size: N, weight: .medium))` with the appropriate Font.Tokens reference, EXCEPT for SF Symbol icon sizing which should keep `.font(.system(size:))`.

**EXCEPTION RULE:** If `.font(.system(size: N))` is applied to an `Image(systemName:)` (SF Symbol), keep it as-is. System font is correct for SF Symbols. Only convert text rendering calls.

Mapping:

| System Call | Replacement |
|-------------|-------------|
| `.system(size: 10)` | `.Tokens.micro` |
| `.system(size: 11)` | `.Tokens.micro` |
| `.system(size: 11, weight: .medium)` | `.Tokens.micro` |
| `.system(size: 12)` | `.Tokens.micro` |
| `.system(size: 12, weight: .medium)` | `.Tokens.micro` |
| `.system(size: 13)` | `.Tokens.smallLabel` |
| `.system(size: 14)` | `.Tokens.label` |
| `.system(size: 15)` | `.Tokens.label` |
| `.system(size: 17)` | `.Tokens.body` |
| `.system(size: 20)` | `.Tokens.sectionHead` (19pt Medium -- closest token) |
| `.system(size: 24)` for SF Symbol | KEEP AS-IS (icon, not text) |

File-by-file specifics:

**ProfileView.swift** (4 refs):
- 12pt -> `.Tokens.micro`
- 10pt -> `.Tokens.micro`
- 14pt -> `.Tokens.label`
- Check context: if any are SF Symbol Image(), keep as .system()

**TrainingProfileSheet.swift** (4 refs):
- 10pt -> `.Tokens.micro`
- 14pt -> `.Tokens.label`

**WeeklySummaryCard.swift** (2 refs):
- 12pt -> `.Tokens.micro`
- 13pt -> `.Tokens.smallLabel`

**DashboardView.swift** (1 ref):
- 12pt medium -> `.Tokens.micro`

**ContextSwitcher.swift** (1 ref):
- 12pt -> `.Tokens.micro`

**TemplateCarouselSection.swift** (4 refs):
- 17pt -> `.Tokens.body`
- 15pt -> `.Tokens.label`
- 24pt -> CHECK: if this is on an Image(systemName:), KEEP as `.system(size: 24)`. If on text, use `.Tokens.pageTitle` (but research says it is for SF Symbol icons, so likely keep)
- Read the file carefully to distinguish text vs icon usage

**UpgradeSheet.swift** (1 system ref -- this file also had 1 Font.custom ref fixed in Task 1):
- 11pt medium -> `.Tokens.micro`

**TemplatePickerSheet.swift** (1 ref):
- 15pt -> `.Tokens.label`

**DeltaIndicator.swift** (1 ref):
- 11pt medium -> `.Tokens.micro`

**SignUpView.swift** (1 ref):
- 20pt -> `.Tokens.sectionHead`
  </action>
  <verify>
    <automated>grep -rn '\.font(\.system(size:' WorkloadApp/Views/Profile/ProfileView.swift WorkloadApp/Views/Profile/TrainingProfileSheet.swift WorkloadApp/Views/Dashboard/WeeklySummaryCard.swift WorkloadApp/Views/Dashboard/DashboardView.swift WorkloadApp/Views/Coach/ContextSwitcher.swift WorkloadApp/Views/WorkoutLog/TemplateCarouselSection.swift WorkloadApp/Views/Subscription/UpgradeSheet.swift WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift WorkloadApp/Components/DeltaIndicator.swift WorkloadApp/Views/Auth/SignUpView.swift 2>/dev/null | grep -v "systemName" | head -5; REMAINING=$(grep -rn '\.font(\.system(size:' WorkloadApp/Views/Profile/ProfileView.swift WorkloadApp/Views/Profile/TrainingProfileSheet.swift WorkloadApp/Views/Dashboard/WeeklySummaryCard.swift WorkloadApp/Views/Dashboard/DashboardView.swift WorkloadApp/Views/Coach/ContextSwitcher.swift WorkloadApp/Views/WorkoutLog/TemplateCarouselSection.swift WorkloadApp/Views/Subscription/UpgradeSheet.swift WorkloadApp/Views/WorkoutLog/TemplatePickerSheet.swift WorkloadApp/Components/DeltaIndicator.swift WorkloadApp/Views/Auth/SignUpView.swift 2>/dev/null | grep -cv "systemName" || true); echo "Remaining non-icon system font calls: $REMAINING"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.font(.system(size:` calls remain on text-rendering views in the 10 files (SF Symbol icons exempted)
    - ProfileView.swift contains `.Tokens.micro` and `.Tokens.label`
    - TrainingProfileSheet.swift contains `.Tokens.micro` and `.Tokens.label`
    - WeeklySummaryCard.swift contains `.Tokens.smallLabel`
    - SignUpView.swift contains `.Tokens.sectionHead`
    - TemplateCarouselSection.swift: any `.system(size: 24)` on Image(systemName:) is preserved
    - All text-rendering font calls use `.Tokens.*` syntax
  </acceptance_criteria>
  <done>All 20 system font calls for text rendering converted to Font.Tokens. SF Symbol icon sizing preserved. Zero rogue font references remain in the codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- purely visual token replacement.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information Disclosure | N/A | accept | No data flow changes; font rendering is presentation-only |
</threat_model>

<verification>
1. `grep -rn 'Font\.custom("DMSans' WorkloadApp/` returns zero results (excluding FontTokens.swift which was already updated in Plan 01)
2. `grep -rn '\.system(size:' WorkloadApp/Views/ WorkloadApp/Components/` returns only SF Symbol icon sizing
3. `grep -rn '\.Tokens\.' WorkloadApp/Components/SpikeAlertBanner.swift` shows token usage
</verification>

<success_criteria>
- Zero `Font.custom("DMSans-*")` calls remain anywhere in the codebase
- Zero `.font(.system(size:))` calls remain for text rendering (SF Symbol exempted)
- All 17 files updated to use `Font.Tokens.*` references exclusively for text
- App typography is 100% controlled through FontTokens.swift
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish/13-02-SUMMARY.md`
</output>
