---
phase: 13-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WorkloadApp/Resources/Alpino-Regular.otf
  - WorkloadApp/Resources/Alpino-Medium.otf
  - WorkloadApp/Utilities/FontTokens.swift
  - WorkloadApp/App/WorkloadApp.swift
  - WorkloadApp/Services/PDFReportEngine.swift
  - workload management/workload-management-Info.plist
  - workload management/workload management.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - DESGN-01

must_haves:
  truths:
    - "Font.Tokens references Alpino PostScript names in all 7 token definitions"
    - "DEBUG assertion at app launch validates Alpino-Regular and Alpino-Medium load correctly"
    - "PDFReportEngine renders PDFs using Alpino fonts"
    - "Info.plist UIAppFonts lists Alpino-Regular.otf and Alpino-Medium.otf"
    - "DM Sans font files no longer exist in WorkloadApp/Resources/"
  artifacts:
    - path: "WorkloadApp/Resources/Alpino-Regular.otf"
      provides: "Alpino Regular font file"
    - path: "WorkloadApp/Resources/Alpino-Medium.otf"
      provides: "Alpino Medium font file"
    - path: "WorkloadApp/Utilities/FontTokens.swift"
      provides: "Centralized Alpino font token definitions"
      contains: "Alpino-Regular"
    - path: "workload management/workload-management-Info.plist"
      provides: "Font registration for iOS"
      contains: "Alpino-Regular.otf"
  key_links:
    - from: "WorkloadApp/Utilities/FontTokens.swift"
      to: "WorkloadApp/Resources/Alpino-Regular.otf"
      via: "Font.custom() PostScript name"
      pattern: 'Font.custom\("Alpino-Regular"'
    - from: "WorkloadApp/App/WorkloadApp.swift"
      to: "UIFont"
      via: "DEBUG assertion"
      pattern: 'UIFont\(name: "Alpino-Regular"'
---

<objective>
Swap the font infrastructure from DM Sans to Alpino across all foundational files: copy font files, update build configuration, update the centralized Font.Tokens enum, update DEBUG assertions, and update PDFReportEngine UIFont references.

Purpose: Establish the Alpino font as the single typography source so all views that already use Font.Tokens automatically render in Alpino. This is the foundation for Plan 02 (rogue font cleanup) and Plan 03 (SharpTextFieldStyle).

Output: Alpino font files in Resources, updated FontTokens.swift, updated Info.plist, updated pbxproj, updated WorkloadApp.swift assertions, updated PDFReportEngine.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-polish/13-CONTEXT.md
@.planning/phases/13-design-polish/13-RESEARCH.md
@.planning/phases/13-design-polish/13-PATTERNS.md
@.planning/phases/13-design-polish/13-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- Current FontTokens.swift (lines 10-33) — the central file being modified -->
From WorkloadApp/Utilities/FontTokens.swift:
```swift
extension Font {
    enum Tokens {
        static let heroScore   = Font.custom("DMSans-Regular", size: 64)
        static let pageTitle   = Font.custom("DMSans-Regular", size: 32)
        static let sectionHead = Font.custom("DMSans-Medium",  size: 19)
        static let body        = Font.custom("DMSans-Regular", size: 17)
        static let bodyMedium  = Font.custom("DMSans-Medium",  size: 17)
        static let label       = Font.custom("DMSans-Regular", size: 15)
        static let micro       = Font.custom("DMSans-Regular", size: 12)
    }
}
```

From WorkloadApp/App/WorkloadApp.swift (lines 11-18) — DEBUG assertions:
```swift
#if DEBUG
assert(
    UIFont(name: "DMSans-Regular", size: 15) != nil,
    "DMSans-Regular font not found. Add DMSans-Regular.ttf to the project and UIAppFonts in Info.plist."
)
assert(
    UIFont(name: "DMSans-Medium", size: 15) != nil,
    "DMSans-Medium font not found. Add DMSans-Medium.ttf to the project and UIAppFonts in Info.plist."
)
#endif
```

From WorkloadApp/Services/PDFReportEngine.swift (lines 68-74) — UIFont:
```swift
private static func fontRegular(_ size: CGFloat) -> UIFont {
    UIFont(name: "DMSans-Regular", size: size) ?? .systemFont(ofSize: size)
}
private static func fontMedium(_ size: CGFloat) -> UIFont {
    UIFont(name: "DMSans-Medium", size: size) ?? .systemFont(ofSize: size, weight: .medium)
}
```

From workload management/workload-management-Info.plist (lines 26-30) — font registration:
```xml
<key>UIAppFonts</key>
<array>
    <string>DMSans-Regular.ttf</string>
    <string>DMSans-Medium.ttf</string>
</array>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Alpino font files to Resources and remove DM Sans files</name>
  <files>
    WorkloadApp/Resources/Alpino-Regular.otf,
    WorkloadApp/Resources/Alpino-Medium.otf
  </files>
  <read_first>
    Alpino_Complete/Alpino_Complete/Fonts/OTF/Alpino-Regular.otf,
    Alpino_Complete/Alpino_Complete/Fonts/OTF/Alpino-Medium.otf,
    WorkloadApp/Resources/
  </read_first>
  <action>
1. Copy `Alpino_Complete/Alpino_Complete/Fonts/OTF/Alpino-Regular.otf` to `WorkloadApp/Resources/Alpino-Regular.otf` (per D-01).
2. Copy `Alpino_Complete/Alpino_Complete/Fonts/OTF/Alpino-Medium.otf` to `WorkloadApp/Resources/Alpino-Medium.otf` (per D-01).
3. Remove `WorkloadApp/Resources/DMSans-Regular.ttf` (per D-03).
4. Remove `WorkloadApp/Resources/DMSans-Medium.ttf` (per D-03).
5. Verify PostScript names match `Alpino-Regular` and `Alpino-Medium` (confirmed by fc-scan in research phase).
  </action>
  <verify>
    <automated>ls -la WorkloadApp/Resources/Alpino-*.otf && ! ls WorkloadApp/Resources/DMSans-*.ttf 2>/dev/null && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `WorkloadApp/Resources/Alpino-Regular.otf` exists (non-zero size)
    - `WorkloadApp/Resources/Alpino-Medium.otf` exists (non-zero size)
    - `WorkloadApp/Resources/DMSans-Regular.ttf` does NOT exist
    - `WorkloadApp/Resources/DMSans-Medium.ttf` does NOT exist
  </acceptance_criteria>
  <done>Alpino font files in Resources, DM Sans files removed</done>
</task>

<task type="auto">
  <name>Task 2: Update build config, FontTokens, assertions, and PDFReportEngine</name>
  <files>
    WorkloadApp/Utilities/FontTokens.swift,
    WorkloadApp/App/WorkloadApp.swift,
    WorkloadApp/Services/PDFReportEngine.swift,
    workload management/workload-management-Info.plist,
    workload management/workload management.xcodeproj/project.pbxproj
  </files>
  <read_first>
    WorkloadApp/Utilities/FontTokens.swift,
    WorkloadApp/App/WorkloadApp.swift,
    WorkloadApp/Services/PDFReportEngine.swift,
    workload management/workload-management-Info.plist,
    workload management/workload management.xcodeproj/project.pbxproj
  </read_first>
  <action>
**FontTokens.swift** (per D-01, D-02):
- Replace all 4 occurrences of `"DMSans-Regular"` with `"Alpino-Regular"`
- Replace all 3 occurrences of `"DMSans-Medium"` with `"Alpino-Medium"`
- Update doc comment (line 3) from "DM Sans Regular (400) and Medium (500)" to "Alpino Regular and Medium"
- Update note (line 8) from "DMSans-Regular.otf and DMSans-Medium.otf" to "Alpino-Regular.otf and Alpino-Medium.otf"
- Add new tokens needed for Plan 02 rogue font cleanup:
  - `static let smallLabel = Font.custom("Alpino-Regular", size: 13)` with doc: "13pt -- Component labels, banner text"
  - `static let smallLabelMedium = Font.custom("Alpino-Medium", size: 13)` with doc: "13pt Medium -- Component emphasis"
  - `static let labelMedium = Font.custom("Alpino-Medium", size: 15)` with doc: "15pt Medium -- emphasized secondary info"

**WorkloadApp.swift** (assertions, lines 11-18):
- Change `UIFont(name: "DMSans-Regular", size: 15)` to `UIFont(name: "Alpino-Regular", size: 15)`
- Change assertion message to: `"Alpino-Regular font not found. Add Alpino-Regular.otf to the project and UIAppFonts in Info.plist."`
- Change `UIFont(name: "DMSans-Medium", size: 15)` to `UIFont(name: "Alpino-Medium", size: 15)`
- Change assertion message to: `"Alpino-Medium font not found. Add Alpino-Medium.otf to the project and UIAppFonts in Info.plist."`

**PDFReportEngine.swift** (lines 68-74):
- Change `UIFont(name: "DMSans-Regular", size: size)` to `UIFont(name: "Alpino-Regular", size: size)`
- Change `UIFont(name: "DMSans-Medium", size: size)` to `UIFont(name: "Alpino-Medium", size: size)`

**Info.plist** (lines 26-30):
- Replace `<string>DMSans-Regular.ttf</string>` with `<string>Alpino-Regular.otf</string>`
- Replace `<string>DMSans-Medium.ttf</string>` with `<string>Alpino-Medium.otf</string>`

**project.pbxproj**:
- Find all PBXBuildFile entries referencing DMSans .ttf files and update them to reference Alpino .otf files
- Find all PBXFileReference entries for DMSans files and update filenames, paths, lastKnownFileType (from file to file) to Alpino .otf
- Update PBXGroup children entries from DMSans filenames to Alpino filenames
- Update Resources build phase file references
- Keep the same UUID structure -- only change the filename/path strings
- IMPORTANT: If Xcode's project file format makes manual editing risky, use `ruby -e` or `sed` to do precise string replacements of `DMSans-Regular.ttf` -> `Alpino-Regular.otf` and `DMSans-Medium.ttf` -> `Alpino-Medium.otf` throughout the entire pbxproj file. Also replace `DMSans-Regular` -> `Alpino-Regular` and `DMSans-Medium` -> `Alpino-Medium` for any bare name references.
  </action>
  <verify>
    <automated>grep -c "Alpino-Regular" WorkloadApp/Utilities/FontTokens.swift && grep -c "Alpino-Medium" WorkloadApp/Utilities/FontTokens.swift && grep "Alpino-Regular" WorkloadApp/App/WorkloadApp.swift && grep "Alpino-Regular" WorkloadApp/Services/PDFReportEngine.swift && grep "Alpino-Regular.otf" "workload management/workload-management-Info.plist" && ! grep -r "DMSans" WorkloadApp/Utilities/FontTokens.swift WorkloadApp/App/WorkloadApp.swift WorkloadApp/Services/PDFReportEngine.swift "workload management/workload-management-Info.plist" && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - FontTokens.swift contains `"Alpino-Regular"` (5+ occurrences with new tokens) and `"Alpino-Medium"` (4+ occurrences)
    - FontTokens.swift contains zero instances of `DMSans`
    - FontTokens.swift contains `static let smallLabel = Font.custom("Alpino-Regular", size: 13)`
    - FontTokens.swift contains `static let smallLabelMedium = Font.custom("Alpino-Medium", size: 13)`
    - FontTokens.swift contains `static let labelMedium = Font.custom("Alpino-Medium", size: 15)`
    - WorkloadApp.swift contains `UIFont(name: "Alpino-Regular"` and `UIFont(name: "Alpino-Medium"`
    - WorkloadApp.swift contains zero instances of `DMSans`
    - PDFReportEngine.swift contains `UIFont(name: "Alpino-Regular"` and `UIFont(name: "Alpino-Medium"`
    - PDFReportEngine.swift contains zero instances of `DMSans`
    - Info.plist contains `Alpino-Regular.otf` and `Alpino-Medium.otf` under UIAppFonts
    - Info.plist contains zero instances of `DMSans`
    - pbxproj contains `Alpino-Regular.otf` and `Alpino-Medium.otf` references
    - pbxproj contains zero instances of `DMSans`
  </acceptance_criteria>
  <done>All font infrastructure points to Alpino. Views using Font.Tokens automatically render in Alpino. New tokens (smallLabel, smallLabelMedium, labelMedium) available for Plan 02 rogue cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this is a purely visual/config change with no user input, network calls, or data persistence changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Font files (.otf) | accept | Font files are bundled resources, not user-modifiable at runtime. iOS code signing prevents tampering. |
</threat_model>

<verification>
1. `grep -r "DMSans" WorkloadApp/Utilities/ WorkloadApp/App/WorkloadApp.swift WorkloadApp/Services/PDFReportEngine.swift "workload management/workload-management-Info.plist"` returns zero results
2. `ls WorkloadApp/Resources/Alpino-*.otf` shows both files
3. `grep "Alpino" WorkloadApp/Utilities/FontTokens.swift` shows all token definitions use Alpino
4. `grep "smallLabel" WorkloadApp/Utilities/FontTokens.swift` confirms new tokens exist
</verification>

<success_criteria>
- Alpino font files bundled in Resources
- DM Sans font files removed
- Font.Tokens enum uses Alpino PostScript names exclusively
- New tokens (smallLabel, smallLabelMedium, labelMedium) added for downstream rogue cleanup
- DEBUG assertion validates Alpino at launch
- PDFReportEngine uses Alpino for PDF generation
- Info.plist registers Alpino .otf files
- pbxproj references Alpino files (no DMSans references remain)
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish/13-01-SUMMARY.md`
</output>
