---
phase: 13-design-polish
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - WorkloadApp/Components/SharpTextFieldStyle.swift
  - WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift
  - WorkloadApp/Views/TemplateEditorSheet.swift
  - WorkloadApp/Views/PrescribeWorkoutSheet.swift
  - WorkloadApp/Views/Recovery/MorningCheckInSheet.swift
  - WorkloadApp/Views/WorkoutLog/FinishWorkoutSheet.swift
  - WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift
  - DESIGN.md
autonomous: true
requirements:
  - DESGN-02

must_haves:
  truths:
    - "SharpTextFieldStyle exists and conforms to TextFieldStyle protocol"
    - "All text fields in the app render with 0pt corners (no roundedBorder)"
    - "DESIGN.md references Alpino instead of DM Sans"
    - "DESIGN.md decisions log includes Alpino migration entry"
  artifacts:
    - path: "WorkloadApp/Components/SharpTextFieldStyle.swift"
      provides: "Custom TextFieldStyle with 0pt corners"
      contains: "struct SharpTextFieldStyle: TextFieldStyle"
    - path: "DESIGN.md"
      provides: "Updated design system documentation"
      contains: "Alpino"
  key_links:
    - from: "WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift"
      to: "WorkloadApp/Components/SharpTextFieldStyle.swift"
      via: ".textFieldStyle(SharpTextFieldStyle())"
      pattern: "SharpTextFieldStyle"
    - from: "WorkloadApp/Views/TemplateEditorSheet.swift"
      to: "WorkloadApp/Components/SharpTextFieldStyle.swift"
      via: ".textFieldStyle(SharpTextFieldStyle())"
      pattern: "SharpTextFieldStyle"
---

<objective>
Create SharpTextFieldStyle with 0pt corners and apply it across all 6 files (23 occurrences of `.roundedBorder`). Update DESIGN.md to reference Alpino and document the migration.

Purpose: Eliminate the last visual debt (rounded text input corners) and update the design system documentation to reflect the Alpino migration. After this plan, the design system is fully aligned for all future v1.3 phases.

Output: New SharpTextFieldStyle.swift component, 6 view files updated, DESIGN.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-polish/13-CONTEXT.md
@.planning/phases/13-design-polish/13-RESEARCH.md
@.planning/phases/13-design-polish/13-PATTERNS.md
@.planning/phases/13-design-polish/13-UI-SPEC.md
@.planning/phases/13-design-polish/13-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- ColorTokens referenced by SharpTextFieldStyle -->
From WorkloadApp/Utilities/ColorTokens.swift:
```swift
enum ColorTokens {
    static let text1: Color      // primary text
    static let text2: Color      // secondary text
    static let text3: Color      // tertiary text
    static let surface: Color    // card/field background
    static let divider: Color    // borders, hairline rules
}
```

<!-- Font.Tokens referenced by SharpTextFieldStyle -->
From WorkloadApp/Utilities/FontTokens.swift (post Plan 01):
```swift
extension Font {
    enum Tokens {
        static let body = Font.custom("Alpino-Regular", size: 17)
        // ... other tokens
    }
}
```

<!-- Card styling pattern from SpikeAlertBanner (reuse for SharpTextFieldStyle) -->
From WorkloadApp/Components/SpikeAlertBanner.swift (lines 70-76):
```swift
.background(ColorTokens.surface)
.overlay(
    Rectangle()
        .stroke(ColorTokens.divider, lineWidth: 0.5)
)
```

<!-- Current TextField pattern being replaced -->
From WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift:
```swift
TextField("Session Name (optional)", text: $sessionName)
    .font(.Tokens.body)
    .foregroundStyle(ColorTokens.text1)
    .textFieldStyle(.roundedBorder)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SharpTextFieldStyle and apply to all 6 view files</name>
  <files>
    WorkloadApp/Components/SharpTextFieldStyle.swift,
    WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift,
    WorkloadApp/Views/TemplateEditorSheet.swift,
    WorkloadApp/Views/PrescribeWorkoutSheet.swift,
    WorkloadApp/Views/Recovery/MorningCheckInSheet.swift,
    WorkloadApp/Views/WorkoutLog/FinishWorkoutSheet.swift,
    WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift
  </files>
  <read_first>
    WorkloadApp/Utilities/ColorTokens.swift,
    WorkloadApp/Utilities/FontTokens.swift,
    WorkloadApp/Components/SpikeAlertBanner.swift,
    WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift,
    WorkloadApp/Views/TemplateEditorSheet.swift,
    WorkloadApp/Views/PrescribeWorkoutSheet.swift,
    WorkloadApp/Views/Recovery/MorningCheckInSheet.swift,
    WorkloadApp/Views/WorkoutLog/FinishWorkoutSheet.swift,
    WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift
  </read_first>
  <action>
**Create `WorkloadApp/Components/SharpTextFieldStyle.swift`** (per D-05, UI-SPEC contract):

```swift
import SwiftUI

/// 0pt-corner text field style matching the design system.
/// Replaces .roundedBorder across all text inputs.
struct SharpTextFieldStyle: TextFieldStyle {
    func makeBody(configuration: Configuration) -> some View {
        configuration.label
            .padding(.horizontal, 16)
            .padding(.vertical, 12)
            .font(.Tokens.body)
            .foregroundStyle(ColorTokens.text1)
            .background(ColorTokens.surface)
            .overlay(
                Rectangle()
                    .stroke(ColorTokens.divider, lineWidth: 0.5)
            )
    }
}
```

NOTE on @FocusState: Research flagged that @FocusState may not work inside TextFieldStyle (Pitfall 4 in RESEARCH.md). Start with the simpler version above (no focus state). If focus border differentiation is desired later, it can be added by wrapping in a helper View. The simpler version is correct per DESIGN.md (hairline borders, no visual flair).

**Apply to all 6 view files** -- replace every `.textFieldStyle(.roundedBorder)` with `.textFieldStyle(SharpTextFieldStyle())`:

**ActiveWorkoutSheet.swift** (11 occurrences):
- Find all 11 instances of `.textFieldStyle(.roundedBorder)` and replace with `.textFieldStyle(SharpTextFieldStyle())`
- For each TextField that has `.font(.Tokens.body)` and `.foregroundStyle(ColorTokens.text1)` applied directly: remove those modifiers since SharpTextFieldStyle applies them internally. But ONLY remove if the values match the style defaults (body font, text1 color). If a TextField uses a different font/color, keep the override.

**TemplateEditorSheet.swift** (8 occurrences):
- Same replacement pattern as above

**PrescribeWorkoutSheet.swift** (1 occurrence):
- Same replacement pattern

**MorningCheckInSheet.swift** (1 occurrence):
- Same replacement pattern

**FinishWorkoutSheet.swift** (1 occurrence):
- Same replacement pattern

**ExercisePickerView.swift** (1 occurrence):
- Same replacement pattern

After creating SharpTextFieldStyle.swift, verify it is added to the Xcode project:
- Check that the .pbxproj file includes a PBXFileReference for SharpTextFieldStyle.swift
- If not automatically included (since it's a new file in an existing group), add it to the project file
  </action>
  <verify>
    <automated>grep -c "SharpTextFieldStyle" WorkloadApp/Components/SharpTextFieldStyle.swift && grep -rn "roundedBorder" WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift WorkloadApp/Views/TemplateEditorSheet.swift WorkloadApp/Views/PrescribeWorkoutSheet.swift WorkloadApp/Views/Recovery/MorningCheckInSheet.swift WorkloadApp/Views/WorkoutLog/FinishWorkoutSheet.swift WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift 2>/dev/null; REMAINING=$(grep -rc "roundedBorder" WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift WorkloadApp/Views/TemplateEditorSheet.swift WorkloadApp/Views/PrescribeWorkoutSheet.swift WorkloadApp/Views/Recovery/MorningCheckInSheet.swift WorkloadApp/Views/WorkoutLog/FinishWorkoutSheet.swift WorkloadApp/Views/WorkoutLog/ExercisePickerView.swift 2>/dev/null | awk -F: '{s+=$2}END{print s}'); echo "Remaining roundedBorder: $REMAINING"</automated>
  </verify>
  <acceptance_criteria>
    - `WorkloadApp/Components/SharpTextFieldStyle.swift` exists and contains `struct SharpTextFieldStyle: TextFieldStyle`
    - SharpTextFieldStyle.swift contains `Rectangle()` (not RoundedRectangle)
    - SharpTextFieldStyle.swift contains `ColorTokens.divider`
    - SharpTextFieldStyle.swift contains `ColorTokens.surface`
    - SharpTextFieldStyle.swift contains `.font(.Tokens.body)`
    - Zero occurrences of `.roundedBorder` remain in the 6 view files
    - ActiveWorkoutSheet.swift contains `SharpTextFieldStyle()` (11 times)
    - TemplateEditorSheet.swift contains `SharpTextFieldStyle()` (8 times)
    - pbxproj contains `SharpTextFieldStyle.swift` reference
  </acceptance_criteria>
  <done>SharpTextFieldStyle created and applied to all 23 text field occurrences across 6 files. Zero rounded borders remain in text inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Update DESIGN.md to reference Alpino (per D-06)</name>
  <files>DESIGN.md</files>
  <read_first>DESIGN.md</read_first>
  <action>
Update the following sections in DESIGN.md (per D-06):

**Typography section (around lines 23-31):**
- Change `DM Sans` to `Alpino` in the typeface description
- Update description: "Alpino -- geometric, neutral, precise. Sharp terminals, excellent numerics. Bundle as `.otf` from FontShare (ITF FFL license)."
- Update weight description to reference Alpino Regular and Alpino Medium
- Update hero score, page title, section header, body, secondary info, micro references from "DM Sans" to "Alpino"

**Font Loading section (around lines 44-48):**
- Change `DMSans-Regular.otf` to `Alpino-Regular.otf`
- Change `DMSans-Medium.otf` to `Alpino-Medium.otf`
- Change `Font.custom("DMSans-Regular", size: 15)` to `Font.custom("Alpino-Regular", size: 15)`
- Update download link: remove Google Fonts URL, add FontShare attribution: "Source: https://www.fontshare.com/fonts/alpino (ITF FFL license)"

**Implementation Rules (search for rule about typography/font):**
- Update any rule that references `DMSans` to reference `Alpino`
- Specifically rule #8 or similar: change `Font.custom("DMSans-Regular", size:)` to `Font.custom("Alpino-Regular", size:)` and `Font.custom("DMSans-Medium", size:)` to `Font.custom("Alpino-Medium", size:)`

**Decisions Log (at the end of the file or in a decisions section):**
- Add new row: `| 2026-05-10 | Migrated from DM Sans to Alpino | Geometric sans with sharper terminals -- aligns with International Style direction |`

Ensure zero instances of "DM Sans" or "DMSans" remain in DESIGN.md after edits (except possibly in the decisions log row describing the migration FROM DM Sans).
  </action>
  <verify>
    <automated>grep -c "Alpino" DESIGN.md && echo "---" && grep -c "DMSans" DESIGN.md | xargs -I{} echo "DMSans refs remaining: {}" && grep "Alpino-Regular.otf" DESIGN.md && grep "Migrated from DM Sans" DESIGN.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains "Alpino" in typography section
    - DESIGN.md contains "Alpino-Regular.otf" in font loading section
    - DESIGN.md contains "Alpino-Medium.otf" in font loading section
    - DESIGN.md contains `Font.custom("Alpino-Regular"` example
    - DESIGN.md contains FontShare attribution URL
    - DESIGN.md decisions log contains "Migrated from DM Sans to Alpino" entry with date 2026-05-10
    - Zero instances of "DMSans" remain in DESIGN.md (the string "DM Sans" may appear only in the migration decision log entry describing what was replaced)
  </acceptance_criteria>
  <done>DESIGN.md updated to reference Alpino as the current typeface with FontShare attribution and migration decision logged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- TextFieldStyle is a visual component with no data handling. DESIGN.md is documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | N/A | accept | TextFieldStyle renders input text visually; no new data exposure. Standard SwiftUI text field security applies. |
</threat_model>

<verification>
1. `grep -rn "roundedBorder" WorkloadApp/Views/ WorkloadApp/Components/` returns zero results
2. `test -f WorkloadApp/Components/SharpTextFieldStyle.swift` returns 0
3. `grep "Alpino" DESIGN.md | head -5` shows Alpino references
4. `grep -c "DMSans" DESIGN.md` returns 0 (or 1 if migration log mentions it)
</verification>

<success_criteria>
- SharpTextFieldStyle.swift exists with TextFieldStyle conformance, 0pt corners, ColorTokens styling
- All 23 `.roundedBorder` occurrences replaced with `SharpTextFieldStyle()` across 6 files
- DESIGN.md fully updated: Alpino references, FontShare attribution, migration logged
- Zero `.roundedBorder` text field styles remain in the codebase
- Zero "DMSans" references remain in DESIGN.md
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish/13-03-SUMMARY.md`
</output>
