---
phase: 28-readiness-fusion-explainable-decisions
plan: 05
type: execute
wave: 4
depends_on: [28-04]
autonomous: false   # human visual-review checkpoint (provisional Dashboard placement)
requirements: [PRS-28-04, PRS-28-05]
files_modified:
  - WorkloadApp/Services/PRSReadinessInputBuilder.swift
  - WorkloadApp/ViewModels/DashboardViewModel.swift
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadAppTests/DashboardViewModelDualRunTests.swift
must_haves:
  truths:
    - "With PRSActivation.isEnabled == false (default, production), DashboardViewModel.load() does NO new published-property work and leaves dualRunMessage == nil; the Dashboard is byte-unchanged (PRSDualRunCard renders EmptyView)."
    - "With the flag ON, dualRunMessage is non-nil and carries the LEGACY headline (recovery×ACWR) and the UPDATED headline (readiness×strain-risk) from REAL data — readiness/strain are recomputed with ReadinessFusionEngine + StrainRiskEngine + BaselineEngine over the athlete's real history, never fabricated."
    - "PRSDualRunCard is mounted directly below HeroReadinessCard in DashboardView; flag-off → EmptyView → layout byte-identical."
    - "The three fence tests (AutoregulationFlagFenceTests, DualRunFlagFenceTests, BaselineTierFenceTests) remain green AND unmodified."
  artifacts:
    - path: "WorkloadApp/Services/PRSReadinessInputBuilder.swift"
      provides: "Pure builder that recomputes a real AutoregulationEngine.ReadinessInput from data already on the Dashboard load path (real z's via BaselineEngine, real StrainRiskEngine.fuse)."
      contains: "enum PRSReadinessInputBuilder"
    - path: "WorkloadApp/ViewModels/DashboardViewModel.swift"
      provides: "Published dualRunMessage (nil default); flag-gated build after the legacy recommendation."
      contains: "var dualRunMessage"
    - path: "WorkloadApp/Views/Dashboard/DashboardView.swift"
      provides: "PRSDualRunCard mounted under HeroReadinessCard (provisional placement)."
      contains: "PRSDualRunCard(message:"
  key_links:
    - from: "DashboardViewModel.load()"
      to: "PRSDualRunSurface.dualRunMessage"
      via: "flag-gated build of legacy + recommendReadiness(updated)"
      pattern: "PRSDualRunSurface\\.dualRunMessage"
    - from: "DashboardView body"
      to: "viewModel.dualRunMessage"
      via: "PRSDualRunCard(message:)"
      pattern: "PRSDualRunCard\\(message:"
---

<objective>
Wire the FLAGGED dual-run "method updated" card into the live Dashboard: `DashboardViewModel` produces a real `DualRunMessage` (legacy recovery×ACWR headline + updated readiness×strain-risk headline) — but ONLY inside `if PRSActivation.isEnabled` — and `DashboardView` mounts `PRSDualRunCard` directly below `HeroReadinessCard`. With the flag OFF (the default, production) the VM does ZERO new published-property work, `dualRunMessage` stays nil, and the card renders `EmptyView` → the Dashboard is byte-unchanged.

Purpose: Complete the Dashboard surface of PRS-v1 (gated off) so Phase 29 can flip the master flag and immediately show the honest dual-run transition with REAL readiness/strain values — no placeholders, no re-architecture at activation time.
Output: A pure `PRSReadinessInputBuilder`, the flag-gated VM build + published `dualRunMessage`, the provisional card mount, and a new `DashboardViewModel` dual-run test. The three fence tests stay green and untouched.

CONSTRAINT — visuals are PROVISIONAL: the card placement below the hero is flagged for human visual review; it is NOT final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md

@WorkloadApp/ViewModels/DashboardViewModel.swift
@WorkloadApp/Views/Dashboard/DashboardView.swift
@WorkloadApp/Views/Dashboard/PRSDualRunCard.swift
@WorkloadApp/Services/PRSDualRunSurface.swift
@WorkloadApp/Services/AutoregulationEngine.swift
@WorkloadApp/Services/ReadinessFusionEngine.swift
@WorkloadApp/Services/StrainRiskEngine.swift
@WorkloadApp/Services/BaselineEngine.swift
@WorkloadApp/Services/StrengthLoadEngine.swift
@WorkloadApp/Services/LoadDistributionEngine.swift
@WorkloadApp/Services/FatigueIndexEngine.swift
@WorkloadApp/Services/PRSActivation.swift

<interfaces>
<!-- Real contracts the executor uses directly — no codebase exploration needed. -->

AutoregulationEngine.ReadinessInput(readinessZone: ReadinessZone, readiness: Double, strainRiskZone: StrainRiskZone, wellnessScore: Double?, daysSinceLastRest: Int, fatigueIndex: Double? = nil, acwrContextLabel: String)
AutoregulationEngine.recommendReadiness(input: ReadinessInput) -> TrainingRecommendation
PRSDualRunSurface.dualRunMessage(legacy: TrainingRecommendation, updated: TrainingRecommendation) -> DualRunMessage?   // returns nil when PRSActivation.isEnabled == false
struct PRSDualRunSurface.DualRunMessage { title; explanation; previousHeadline; updatedHeadline }   // Equatable
struct PRSDualRunCard: View { let message: PRSDualRunSurface.DualRunMessage? }   // message == nil → EmptyView

ReadinessFusionEngine.compute(_ input: ReadinessInput) -> ReadinessResult   // .readiness (0-100), .zone (ReadinessZone), .confidence
ReadinessFusionEngine.ReadinessInput(hrvZ: Double?, rhrZ: Double?, sleepZ: Double?, subjectiveTrendSlope: Double? = nil, confidence: Double = 0.0)

BaselineEngine.SignalState(mu:welfordMean:m2:count:madBuffer:lastBucketedDate:cvRatio:cvLevel:confidence:)  // all defaulted
BaselineEngine.SignalConfig.hrv / .rhr / .sleep   // .rhr is higherIsBetter:false → +z = better already
BaselineEngine.step(state: SignalState, observation: Double, config: SignalConfig, bucketedDate: Date? = nil) -> SignalState
BaselineEngine.score(state: SignalState, observation y: Double, config: SignalConfig) -> (z: Double?, innovation: Double)  // nil z while count<2 (cold-start) — NEVER fabricated
BaselineEngine.confidence(state: SignalState, daysSinceLastBucket: Int) -> Double

StrainRiskEngine.fuse(_ input: Input) -> StrainRiskResult   // .zone (StrainRiskZone)
StrainRiskEngine.Input(strengthLoad: StrengthLoadEngine.StrengthLoadResult, loadDistribution: LoadDistributionEngine.LoadDistributionResult, fatigue: FatigueIndexEngine.FatigueResult, enduranceLoadElevation: Double? = nil, baselineConfidence: Double? = nil)
StrengthLoadEngine.perMuscleStrengthLoad(sessions: [WorkoutSession], sorenessRegions: [MuscleRegion] = [], asOf: Date, calendar: Calendar, ...) -> StrengthLoadResult
LoadDistributionEngine.distribution(sessions: [WorkoutSession], asOf: Date, calendar: Calendar, ...) -> LoadDistributionResult
FatigueIndexEngine.FatigueResult   // ALREADY computed in load() as `fatigueResult` (loadElevation, sessionDensity, recoveryTrend, restDebt, wellnessTrend, softTissueRisk, index, zone)

PRSActivation.isEnabled -> Bool   // default false; PRSActivation.withEnabled(_:_:) is TEST-ONLY
</interfaces>

VERIFIED CODE FACTS (do not re-derive):
- `ReadinessFusionEngine.compute` / `StrainRiskEngine.fuse` are NOT called in any production file today — only in unit tests. There is NO existing live readiness/strain computation to "reuse"; this plan RECOMPUTES with the same engines + same real inputs (the only non-fabricating option). Record this as the central gray-area decision in the SUMMARY.
- `BaselineState` (@Model) is in the schema but is NEVER written in production (`DayBucketer`/`BaselineEngine.step` run only in tests). So persisted personal z-scores do NOT exist at runtime. The builder therefore folds `BaselineEngine.step` over the athlete's REAL `recentSnapshots` HRV/RHR/sleep history (ascending), then `score`s today's REAL raw value → real personal z. Cold-start (count<2) → nil z; the fusion engine renormalizes over present signals (honest, NOT mean-imputed, NOT fabricated).
- `DashboardViewModel.load()` already computes the legacy `recommendation` (~L298), the real `fatigueResult` (~L278, in the non-cold-start branch), `allSessions` (~L182), raw `latestHRV/latestRHR/latestSleepMinutes`, `recentSnapshots` (28d), `daysSinceRest`, and an ACWR context (`acwr`,`acwrZone`). All inputs the builder needs are already on the load path.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure PRSReadinessInputBuilder — recompute a REAL ReadinessInput from load() data</name>
  <files>WorkloadApp/Services/PRSReadinessInputBuilder.swift</files>
  <action>
Create `enum PRSReadinessInputBuilder` — a pure, Foundation-only, deterministic helper (mirrors the engine convention: static methods, no state, no SwiftData fetch, no `.now` baked in — accept `asOf` + `calendar`). It builds a REAL `AutoregulationEngine.ReadinessInput` from values already available in `DashboardViewModel.load()`. NO fabricated numbers anywhere — every field traces to a real engine output or real datum (per PRS-28-04/05).

Provide one entry point:
`static func build(recentSnapshots: [RecoverySnapshot], latestHRV: Double?, latestRHR: Double?, latestSleepMinutes: Double?, allSessions: [WorkoutSession], fatigueResult: FatigueIndexEngine.FatigueResult?, daysSinceRest: Int, wellnessScore: Double?, acwr: Double, acwrZone: ACWRZone, asOf: Date, calendar: Calendar) -> AutoregulationEngine.ReadinessInput?`

Readiness side (real personal z via BaselineEngine — same engine the moat uses):
  - For each of HRV / RHR / sleep: from `recentSnapshots` sorted ascending by date, take the real per-day series (`compactMap` the signal so gap days don't inflate counts — `hrvSDNN`, `restingHR`, `sleepDurationMinutes`). Fold `BaselineEngine.step(state:observation:config:)` over the series with the matching `SignalConfig` (.hrv/.rhr/.sleep) starting from a fresh `SignalState()`, then call `BaselineEngine.score(state:observation:config:)` with TODAY'S real raw value (`latestHRV`/`latestRHR`/`latestSleepMinutes`). Use the returned `.z` (Optional — nil while cold-start; pass nil straight through, the fusion engine excludes+renormalizes, it does NOT impute). If a signal's raw value is nil, its z is nil.
  - Compute a real `confidence` via `BaselineEngine.confidence(state:daysSinceLastBucket:)` (use the HRV state, daysSinceLastBucket 0 for today). Pass the SAME `confidence` into `ReadinessFusionEngine.ReadinessInput.confidence`.
  - Call `ReadinessFusionEngine.compute(...)` → real `readiness` (0-100) + real `zone` (ReadinessZone).

Strain side (real StrainRiskEngine.fuse — same engine):
  - `strengthLoad = StrengthLoadEngine.perMuscleStrengthLoad(sessions: allSessions, asOf: asOf, calendar: calendar)`.
  - `loadDistribution = LoadDistributionEngine.distribution(sessions: allSessions, asOf: asOf, calendar: calendar)`.
  - `fatigue`: use the passed `fatigueResult`. If nil (cold-start suppressed it), the strain channel cannot be honestly computed → return nil from `build(...)` (the VM then leaves dualRunMessage nil — no fabrication). Do NOT synthesize a zero FatigueResult.
  - `StrainRiskEngine.fuse(StrainRiskEngine.Input(strengthLoad:loadDistribution:fatigue:))` → real `StrainRiskZone`.

ACWR demotion (GA-4 — ACWR is a context LABEL only, never a decision input): derive `acwrContextLabel` as a short human string from `acwrZone` (e.g. .optimal → "Load Steady", .caution → "Load Building", .danger → "Load High", .undertrained → "Building Base", .noData → "" empty). Map this in a small private switch; keep copy "Tuwa"-voice, NEVER "injury prediction".

Assemble and return `AutoregulationEngine.ReadinessInput(readinessZone:, readiness:, strainRiskZone:, wellnessScore:, daysSinceLastRest:, fatigueIndex: fatigueResult?.index, acwrContextLabel:)`.

This helper is flag-agnostic (pure); it is only ever CALLED inside the flag guard in Task 2, so flag-off cost is zero. Header doc must state: recomputes with the real engines + real inputs (no live source exists to reuse); cold-start → nil z (excluded, never imputed); returns nil when the real strain channel is unavailable.
  </action>
  <verify>
    <automated>xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,id=$KNOWN_ALIVE_SIM_ID' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
    <automated>grep -q "enum PRSReadinessInputBuilder" WorkloadApp/Services/PRSReadinessInputBuilder.swift</automated>
  </verify>
  <done>PRSReadinessInputBuilder compiles; builds a real ReadinessInput from real engine outputs; returns nil (no fabrication) when fatigueResult is nil; no "injury prediction" / dead-name copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardViewModel — publish dualRunMessage, build it flag-gated after the legacy recommendation</name>
  <files>WorkloadApp/ViewModels/DashboardViewModel.swift</files>
  <behavior>
    - Flag OFF (default): after load(), `dualRunMessage == nil`; NO call to PRSReadinessInputBuilder / ReadinessFusionEngine / StrainRiskEngine / recommendReadiness occurs (the build is entirely inside `if PRSActivation.isEnabled`). Existing published properties are unchanged.
    - Flag ON: after load() (with real data present), `dualRunMessage != nil`, `dualRunMessage.previousHeadline == recommendation.headline`, and `dualRunMessage.updatedHeadline == recommendReadiness(...).headline`.
  </behavior>
  <action>
Add a published property: `var dualRunMessage: PRSDualRunSurface.DualRunMessage?` defaulting to nil, near the existing `recommendation` declaration (~L30).

In `load()`, IMMEDIATELY AFTER the legacy `recommendation = AutoregulationEngine.recommend(...)` assignment completes (~L298-302), insert a SINGLE flag guard so flag-off does literally nothing new:

```
if PRSActivation.isEnabled {
    // FLAG ON ONLY — recompute readiness/strain with the real engines (no live source exists to reuse).
    if let legacy = recommendation,
       let readinessInput = PRSReadinessInputBuilder.build(
           recentSnapshots: recentSnapshots,
           latestHRV: latestHRV, latestRHR: latestRHR, latestSleepMinutes: latestSleepMinutes,
           allSessions: allSessions,
           fatigueResult: <the real FatigueResult computed above>,
           daysSinceRest: daysSinceRest,
           wellnessScore: nil,           // matches the legacy autoInput.wellnessScore source today
           acwr: acwr, acwrZone: acwrZone,
           asOf: .now, calendar: .current
       ) {
        let updated = AutoregulationEngine.recommendReadiness(input: readinessInput)
        dualRunMessage = PRSDualRunSurface.dualRunMessage(legacy: legacy, updated: updated)
    }
}
```
NOTE the action text above names identifiers only; do NOT paste a fenced code block into the implementation comments — write directive prose.

Plumbing detail: the real `FatigueResult` is currently a local (`fatigueResult`) declared inside the non-cold-start `else` branch (~L278). Hoist a `var fatigueResultForReadiness: FatigueIndexEngine.FatigueResult? = nil` above the cold-start `if isColdStartActive` block and assign it in the else branch where `fatigueResult` is computed (set the new var = fatigueResult). Cold-start branch leaves it nil → builder returns nil → dualRunMessage stays nil. This hoist must NOT change any existing published property or the existing fatigueIndex/fatigueZone assignments (verify by the fence + full-suite run).

CRITICAL flag-off invariant: with `PRSActivation.isEnabled == false`, the `if` body is skipped entirely — no builder call, no engine call, `dualRunMessage` remains nil. This is the byte-identical guarantee. Do NOT compute the ReadinessInput unconditionally and discard it; the WHOLE build must be inside the guard.

Set `dualRunMessage = nil` is the default (declared) — do not reset it elsewhere; load() only ever ASSIGNS non-nil inside the guard.
  </action>
  <verify>
    <automated>xcodebuild -scheme "workload management" -destination 'platform=iOS Simulator,id=$KNOWN_ALIVE_SIM_ID' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
    <automated>grep -q "var dualRunMessage" WorkloadApp/ViewModels/DashboardViewModel.swift</automated>
    <automated>grep -c "if PRSActivation.isEnabled" WorkloadApp/ViewModels/DashboardViewModel.swift | grep -qv '^0$'</automated>
  </verify>
  <done>dualRunMessage published (nil default); the entire readiness/strain build + dualRunMessage assignment sit inside `if PRSActivation.isEnabled`; flag-off leaves dualRunMessage nil and changes no other property; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Mount PRSDualRunCard under HeroReadinessCard (provisional) + DashboardViewModel dual-run test</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadAppTests/DashboardViewModelDualRunTests.swift</files>
  <action>
View mount (DashboardView body, ~L52): directly BELOW `HeroReadinessCard(viewModel: viewModel)` and INSIDE the existing `VStack(spacing: 0)`, add:
`PRSDualRunCard(message: viewModel.dualRunMessage)`
With the flag off, `dualRunMessage` is nil → `PRSDualRunCard` renders `EmptyView()` → the VStack layout is byte-identical to today (no spacer, no padding shift). Follow the EXISTING card conventions in this file (PRSDualRunCard already applies its own `.padding(16)` + hairline `Rectangle().stroke` border + `ColorTokens.surface` — DESIGN-compliant; do NOT add corner radius, shadow, or accent). Add a one-line comment above the mount: `// Phase 28 Wave 4 — FLAGGED dual-run card; placement PROVISIONAL, flagged for human visual review.` Do NOT touch HeroReadinessCard or any other card.

ViewModel test (NEW file WorkloadAppTests/DashboardViewModelDualRunTests.swift), `@MainActor final class DashboardViewModelDualRunTests: XCTestCase`. Use an in-memory ModelContainer + a seeded Athlete with enough real RecoverySnapshot history + WorkoutSessions that the non-cold-start branch runs (so fatigueResult is non-nil and real data is present). Mirror the seeding style of existing @MainActor SwiftData tests in WorkloadAppTests (in-memory `ModelConfiguration(isStoredInMemoryOnly: true)`).
  - test_flagOff_dualRunMessage_nilAfterLoad: assert `PRSActivation.isEnabled == false` (precondition), run `await vm.load(...)`, assert `vm.dualRunMessage == nil`. This is the byte-identical guard at the VM level.
  - test_flagOn_dualRunMessage_nonNil_withLegacyAndUpdatedHeadlines: run load() inside `PRSActivation.withEnabled(true) { ... }` (await the async load via a wrapper that keeps the override in scope for the full load — if `withEnabled` is sync-only, instead toggle via the same helper pattern used by DualRunFlagFenceTests; the override must be active during load()). Assert `vm.dualRunMessage != nil`, `vm.dualRunMessage?.previousHeadline == vm.recommendation?.headline`, and that `updatedHeadline` is non-empty. If the async-override interaction is awkward, factor the build into a synchronous VM method `buildDualRunMessage()` called at the end of load() and unit-test THAT method directly under `withEnabled(true)` (still proves the flag-gated real build).

Do NOT touch AutoregulationFlagFenceTests, DualRunFlagFenceTests, or BaselineTierFenceTests. Ensure the new test file is added to the WorkloadAppTests target membership in the .pbxproj (per CLAUDE.md: verify new files are in the Xcode project).
  </action>
  <verify>
    <automated>grep -q "PRSDualRunCard(message: viewModel.dualRunMessage)" WorkloadApp/Views/Dashboard/DashboardView.swift</automated>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=$KNOWN_ALIVE_SIM_ID' -only-testing:WorkloadAppTests/DashboardViewModelDualRunTests 2>&1 | tail -8 | grep -q "Test Suite.*passed\|** TEST SUCCEEDED"</automated>
    <automated>xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=$KNOWN_ALIVE_SIM_ID' -only-testing:WorkloadAppTests/AutoregulationFlagFenceTests -only-testing:WorkloadAppTests/DualRunFlagFenceTests -only-testing:WorkloadAppTests/BaselineTierFenceTests 2>&1 | tail -8 | grep -q "** TEST SUCCEEDED"</automated>
  </verify>
  <done>Card mounted under hero with provisional-review comment; new DashboardViewModelDualRunTests pass (flag-off nil, flag-on non-nil with legacy headline match); the three fence tests pass UNMODIFIED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Flagged dual-run "method updated" card wired into the live Dashboard below the hero readiness card. With PRSActivation OFF (default) it renders EmptyView — the Dashboard is unchanged. The card visuals + placement are PROVISIONAL.</what-built>
  <how-to-verify>
1. Confirm flag-off (production default): launch the app normally; the Dashboard must look byte-identical to before (NO dual-run card visible, no layout shift).
2. To preview the flagged visuals (developer-only, DO NOT ship): temporarily set PRSActivation via a test/preview harness (NEVER flip the shipped default) and confirm the card appears directly below the hero, showing "Previous" vs "Updated" headlines, with 0pt corners, hairline border, no shadow, General Sans, accent NOT used on the card.
3. Judge placement: is "directly below the hero readiness number" the right home for this transition card, or should it sit lower (e.g. below the metrics strip / training-load section)? Placement is PROVISIONAL.
  </how-to-verify>
  <resume-signal>Type "approved" to keep the provisional placement, or describe the placement/visual change you want.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag → live UI | PRSActivation.isEnabled gates whether new computation/UI runs; flag-off MUST be byte-identical |
| engines → display | recomputed readiness/strain feed only display copy this phase; never persisted, never synced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | DashboardViewModel.load() flag-off path | mitigate | Entire readiness/strain build + dualRunMessage assignment INSIDE `if PRSActivation.isEnabled`; dualRunMessage default nil; fence tests + new VM flag-off test pin byte-identical behavior |
| T-28-02 | Information disclosure | recomputed z-scores / strain | accept | Values are display-only, pure recompute, NOT written to SwiftData and NOT added to SyncService (no model/Codable change); HealthKit-derived state stays on device |
| T-28-03 | Repudiation | fabricated readiness/strain | mitigate | Builder uses ONLY real engines (BaselineEngine/ReadinessFusionEngine/StrainRiskEngine) over real history; cold-start → nil z (excluded, not imputed); returns nil when real fatigue unavailable → no placeholder numbers |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (Swift-only, no new SPM dependency) |
</threat_model>

<verification>
- Flag-off byte-identical: `DashboardViewModelDualRunTests.test_flagOff_dualRunMessage_nilAfterLoad` green; the three fence tests green AND unmodified (`git diff --stat` shows no change to AutoregulationFlagFenceTests / DualRunFlagFenceTests / BaselineTierFenceTests).
- No fabrication: PRSReadinessInputBuilder traces every field to a real engine output; returns nil rather than synthesizing inputs.
- No new sync: `git diff` touches no `@Model`, no `Codable`, no `SyncService.swift`.
- DESIGN: card already 0pt/hairline/General Sans/no-accent; mount adds no corner radius/shadow.
- Copy: no "injury prediction"; "Tuwa" only — `grep -ri "injury prediction\|faros\|tonus\|tutrice" WorkloadApp/Services/PRSReadinessInputBuilder.swift` returns nothing.
- FULL suite after the shared VM change: `xcodebuild test -scheme "workload management" -destination 'platform=iOS Simulator,id=$KNOWN_ALIVE_SIM_ID' -only-testing:WorkloadAppTests` → 0 failures.
- DISCARD any .xcstrings build churn; commit serially to main, no branch, never push.
</verification>

<success_criteria>
- `dualRunMessage` published on DashboardViewModel (nil default).
- Flag-off: load() does no new published-property work, dualRunMessage stays nil, Dashboard byte-unchanged (PRSDualRunCard → EmptyView).
- Flag-on: dualRunMessage non-nil with real legacy + updated headlines from real recomputed readiness/strain.
- PRSDualRunCard mounted below HeroReadinessCard (provisional, flagged for human review).
- Three fence tests green and UNMODIFIED; full WorkloadAppTests suite green.
- PRSActivation + PRSMasterActivation defaults stay FALSE.
</success_criteria>

<output>
Create `.planning/phases/28-readiness-fusion-explainable-decisions/28-05-SUMMARY.md` when done. Record the central gray-area decision: no live readiness/strain source existed to reuse, so it was RECOMPUTED with the same engines over the athlete's real history (BaselineEngine-driven personal z; StrainRiskEngine.fuse over real sessions + the real FatigueResult), cold-start → nil z (excluded, never imputed), builder returns nil rather than fabricate. Then update `28-VERIFICATION.md` (use Write, not Edit) with the verified-green HEAD + the fence-tests-unmodified evidence.
</output>
