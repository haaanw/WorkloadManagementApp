---
phase: 28-readiness-fusion-explainable-decisions
plan: 04
type: execute
wave: 4
depends_on: [28-03]
autonomous: false   # human visual-review checkpoint
requirements: [PRS-28-04, PRS-28-05]
files_modified:
  - WorkloadApp/Views/Dashboard/DashboardView.swift
  - WorkloadApp/ViewModels/DashboardViewModel.swift
  - WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift
  - WorkloadAppTests/DualRunFlagFenceTests.swift
must_haves:
  truths:
    - "When PRSActivation.isEnabled == false (default), the UI is byte-identical to today — no dual-run/method-updated copy renders (flag-fence UI test)."
    - "When the flag is on, a 'method updated' / dual-run message renders per DESIGN.md, showing both old and new recommendations during the dual-run window."
    - "When the flag is on, the new recommendation adjusts a REAL planned/logged workout (volume%/RPE cap applied to an actual PrescribedWorkout or the next logged session context), not a fabricated one."
    - "No copy says 'injury prediction'; Strain-Risk framed as load-tolerance/overreaching-caution context."
    - "Visuals are FLAGGED for human review (not final); checkpoint approved before phase close."
  artifacts:
    - path: "WorkloadApp/Views/Dashboard/DashboardView.swift"
      provides: "Flagged dual-run 'method updated' surface (invisible with flag off)"
  key_links:
    - from: "flagged recommendation"
      to: "PrescribedWorkout / logged session"
      via: "applied volume% + RPE cap"
---

<objective>
Add the dual-run "method updated" messaging and wire the flagged recommendation to adjust a REAL planned/logged workout — both entirely behind PRSActivation (invisible with flag off, so live UI is byte-identical). Then a human reviews the visuals (NOT final).

Purpose: Complete the user-facing surface of PRS-v1 (gated off) so the recommendation "becomes true prescription" by modifying an actual workout, with honest dual-run transition messaging — ready for Phase 29 activation.
Output: Flagged dual-run copy, real-workout adjustment wiring, flag-fence UI test, human visual-review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/competitive-algorithm-analysis.md
@.planning/phases/28-readiness-fusion-explainable-decisions/28-RESEARCH.md
@WorkloadApp/Services/AutoregulationEngine.swift
@WorkloadApp/Services/PRSActivation.swift
@WorkloadApp/ViewModels/DashboardViewModel.swift
@WorkloadApp/Views/Dashboard/DashboardView.swift
@WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift
@DESIGN.md
@CLAUDE.md

<interfaces>
<!-- Read DashboardViewModel.swift + DashboardView.swift + ActiveWorkoutSheet.swift fully first. -->
<!-- DESIGN.md is mandatory: 0pt corners (Rectangle not RoundedRectangle), no shadows, General Sans (Font.Tokens.*), 8pt grid, accent only on hero number. -->
<!-- All copy must use "Tuwa", localized strings; honor zh-Hans catalog (Phase 23). -->
<!-- PrescribedWorkout @Model is the planned-workout target for the adjustment. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flagged dual-run "method updated" surface + real-workout adjustment</name>
  <files>WorkloadApp/Views/Dashboard/DashboardView.swift, WorkloadApp/ViewModels/DashboardViewModel.swift, WorkloadApp/Views/WorkoutLog/ActiveWorkoutSheet.swift, WorkloadAppTests/DualRunFlagFenceTests.swift</files>
  <action>
Read the three views/VM fully. Behind `PRSActivation.isEnabled` ONLY (GA-6/GA-8): add a DESIGN.md-compliant "method updated" / dual-run surface that, when active, shows both the legacy and the new recommendation during a dual-run window and a short explanation that the recommendation method changed (research §289 dual-run + "method updated"). With the flag off (default), render NOTHING new — the existing UI must be byte-identical.

Wire the flagged recommendation to adjust a REAL planned/logged workout (GA-9, competitive §218): apply the new volume%/RPE-cap to an actual PrescribedWorkout if one exists for the day, else to the next logged session context. Do NOT fabricate a workout. The legacy (flag-off) recommendation rendering and the live recovery score are untouched.

Follow DESIGN.md exactly (0pt corners, no shadows, Font.Tokens.*, 8pt grid, accent only on hero number). Use localized "Tuwa" copy; never "injury prediction". Discard any `.xcstrings` build-churn before commit. Add DualRunFlagFenceTests asserting: with flag off, the dual-run surface is absent and the recommendation output is the legacy output; with flag on (test override), the surface data is present and the adjustment targets a real workout. Run the FULL suite.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -only-testing:WorkloadAppTests/DualRunFlagFenceTests 2>&1 | tail -8</automated>
  </verify>
  <done>With flag off, UI + recommendation byte-identical (fence test green). With flag on, dual-run copy renders and adjusts a real workout. No-prediction-copy holds. Full suite + tier fence green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A flagged (default-off) dual-run "method updated" surface on the Dashboard + the recommendation adjusting a real planned/logged workout, all behind PRSActivation. With the flag off the UI is unchanged; visuals are NOT final and need human eyes.
  </what-built>
  <how-to-verify>
1. Confirm with the flag OFF (default) the app looks byte-identical to today (no new copy, recovery score + recommendation unchanged).
2. Temporarily enable PRSActivation in a DEBUG build and verify: the "method updated"/dual-run surface appears, shows both old and new recommendations, follows DESIGN.md (0pt corners, no shadows, General Sans, 8pt grid, accent only on hero number), uses "Tuwa", and never says "injury prediction".
3. Confirm the new recommendation visibly adjusts a real planned/logged workout (volume %, RPE cap).
4. Judge whether the visual treatment is acceptable as a STARTING point (visuals are explicitly NOT final).
  </how-to-verify>
  <resume-signal>Type "approved" (visuals accepted as starting point) or describe required changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| flag-off UI → new surface | New surface MUST NOT render with flag off (UI byte-identical). |
| recommendation → real workout | Adjustment must target an actual planned/logged workout, never fabricate. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Tampering | flag-off UI | mitigate | Flag-fence UI test asserts no new surface + legacy recommendation. |
| T-28-11 | Repudiation | dual-run copy | mitigate | Copy decomposes to decision factors; no-prediction-copy grep. |
| T-28-SC | Tampering | package installs | mitigate | No new packages; halt + human verify if any appear. |
</threat_model>

<verification>
- Flag-off UI byte-identical (DualRunFlagFenceTests green).
- Flag-on dual-run surface + real-workout adjustment verified by human.
- DESIGN.md compliance + Tuwa + no-prediction-copy.
- `BaselineTierFenceTests` + full `WorkloadAppTests` suite green via real xcodebuild.
- Human visual-review checkpoint approved.
</verification>

<success_criteria>
Dual-run messaging + real-workout adjustment shipped behind the flag (invisible with flag off); human approved visuals as a starting point. Phase 28 complete and ready for Phase 29. Committed atomically to main (no push).
</success_criteria>

<output>
Create `.planning/phases/28-readiness-fusion-explainable-decisions/28-VERIFICATION.md` and append SUMMARY entry (28-04 done).
</output>
