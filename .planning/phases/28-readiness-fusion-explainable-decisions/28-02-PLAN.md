---
phase: 28-readiness-fusion-explainable-decisions
plan: 02
type: execute
wave: 2
depends_on: [28-01]
autonomous: true
requirements: [PRS-28-02, PRS-28-03]
files_modified:
  - WorkloadApp/Services/AutoregulationEngine.swift
  - WorkloadApp/Services/ReasoningEngine.swift
  - WorkloadAppTests/AutoregulationFlagFenceTests.swift
  - WorkloadAppTests/ReasoningDecisionExplanationTests.swift
must_haves:
  truths:
    - "With PRSActivation.isEnabled == false, AutoregulationEngine.recommend is byte-identical to the pre-Phase-28 legacy path (golden snapshot test)."
    - "With the flag on, the recommendation is keyed on (readinessZone × strainRiskZone); ACWR appears only as a context label, never in a decision or warning."
    - "ReasoningEngine can explain the DECISION (ranked factors + confidence), additively, without changing its existing summarize signature."
    - "New Readiness/decision path is not referenced by any live flag-off call site (isolation grep)."
    - "No user-facing copy says 'injury prediction' (no-prediction-copy grep)."
  artifacts:
    - path: "WorkloadApp/Services/AutoregulationEngine.swift"
      provides: "Flagged readiness×strain-risk path; legacy recovery×ACWR path untouched"
    - path: "WorkloadApp/Services/ReasoningEngine.swift"
      provides: "Additive decision-explanation entry point"
  key_links:
    - from: "AutoregulationEngine flagged path"
      to: "ReadinessFusionEngine + StrainRiskEngine"
      via: "(readinessZone × strainRiskZone) matrix"
---

<objective>
Swap the AutoregulationEngine decision axes from (recoveryZone × acwrZone) to (readinessZone × strainRiskZone) BEHIND the PRSActivation flag, demote ACWR to a context-only label in the new path, and upgrade ReasoningEngine to explain the DECISION (not just the recovery score). The legacy path is left untouched and is the DEFAULT when the flag is off, guaranteeing byte-identical live behavior.

Purpose: Make the "honest about ACWR" + "explainable prescription" differentiators real in code, while the master flag keeps the live recommendation byte-unchanged until Phase 29.
Output: Flagged Autoregulation swap, decision-explanation upgrade, byte-identical golden fence test, isolation grep, no-prediction-copy grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/algorithm-moat-design.md
@.planning/research/competitive-algorithm-analysis.md
@.planning/phases/28-readiness-fusion-explainable-decisions/28-RESEARCH.md
@WorkloadApp/Services/AutoregulationEngine.swift
@WorkloadApp/Services/ReasoningEngine.swift
@WorkloadApp/Services/ReadinessFusionEngine.swift
@WorkloadApp/Services/StrainRiskEngine.swift
@WorkloadApp/Services/PRSActivation.swift
@CLAUDE.md

<interfaces>
<!-- Read AutoregulationEngine.swift FULLY before editing. The legacy recommend(...) keyed on (recoveryZone × acwrZone) returning cap/volume%/sessionType/headline/detail + warnings + cycle double-gate MUST remain byte-identical when flag off. -->
<!-- ReadinessZone (Wave 1, Enums.swift), StrainRiskZone (Phase 27) are the two new axes. -->
<!-- ReasoningEngine.summarize EXISTING signature must be preserved; add a NEW method for decision explanation. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flagged AutoregulationEngine matrix swap + byte-identical fence</name>
  <files>WorkloadApp/Services/AutoregulationEngine.swift, WorkloadAppTests/AutoregulationFlagFenceTests.swift</files>
  <behavior>
    - Flag-off byte-identical: for a fixed battery of inputs, recommend(...) with PRSActivation.isEnabled == false returns output equal field-for-field to a captured golden snapshot of the pre-Phase-28 legacy output.
    - Flag-on path: recommend(...) with flag on selects a NEW branch keyed on (readinessZone × strainRiskZone); the returned cap/volume%/sessionType are produced by the new matrix.
    - ACWR demotion: in the flag-on path, ACWR is NOT an input to the decision or to any warning; it appears only as a separate context-label field/string (GA-4). Assert no warning references ACWR in the flag-on path.
    - Cycle double-gate and existing warning shell preserved in the new path (research §3 "keep the explainable recommendation shell").
  </behavior>
  <action>
Read AutoregulationEngine.swift fully. Add a NEW recommendation branch selected by `PRSActivation.isEnabled` (GA-5) — do NOT rewrite the existing (recoveryZone × acwrZone) matrix in place. The legacy branch must be reached verbatim when the flag is off.

New branch: author a `(readinessZone × strainRiskZone)` decision matrix (3×3, mirroring the legacy matrix authoring style) producing the same output shape (RPE cap, volume %, session type, headline, detail, warnings). Consume ReadinessZone (from ReadinessFusionEngine result) and StrainRiskZone (from Phase-27 StrainRiskEngine) — do NOT modify StrainRiskEngine math (GA-10). Demote ACWR: remove it from the new decision/warnings; emit it only as a context-label string (GA-4). Keep the cycle double-gate.

Before changing the dispatch, capture the legacy output for a fixed input battery as a golden snapshot in AutoregulationFlagFenceTests (RED → record), then implement the branch so the flag-off path stays byte-identical (GREEN). Add the isolation assertion: the new branch's engines are only reached via the flag.

Run the FULL suite (shared decision-path change).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -only-testing:WorkloadAppTests/AutoregulationFlagFenceTests 2>&1 | tail -8</automated>
  </verify>
  <done>Byte-identical-with-flag-off golden test passes. Flag-on path uses (readiness × strain-risk); ACWR is context-label only. Full suite + tier fence green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReasoningEngine decision-explanation upgrade + grep guards</name>
  <files>WorkloadApp/Services/ReasoningEngine.swift, WorkloadAppTests/ReasoningDecisionExplanationTests.swift</files>
  <behavior>
    - New method explains the DECISION: given Readiness factors + Strain-Risk ranked factors + the recommendation, returns ranked human-readable reasons WITH confidence (e.g. "volume cut because HRV −x%, sleep debt, high per-muscle hard sets, no rest day").
    - Decomposability: every reason maps back to a named pre-update factor (signal, z/deviation, contribution) — glass-box (research §2.7 / §253 / codex MINOR).
    - Existing summarize(...) output unchanged (regression test on existing behavior).
    - No-prediction-copy: produced strings never contain "injury prediction"/"injury risk" (GA-11). Extend the Phase-27 grep guard to ReasoningEngine + AutoregulationEngine new strings.
  </behavior>
  <action>
Add a NEW entry point to ReasoningEngine (e.g. `explainDecision(...)`) that consumes the ReadinessFusionEngine factors + StrainRiskEngine ranked factors + the recommendation and returns ranked, confidence-annotated decision reasons. Do NOT change the existing `summarize` signature (GA additive). Replace the fixed 7h sleep target ONLY inside the new decision explanation with the personal sleep baseline if available (research §2.7); leave the legacy summarize untouched.

Add/extend a grep-based no-prediction-copy guard test asserting zero occurrences of injury-prediction phrasing in the new copy (use `grep -v '^//' | grep -ci` style, comment-filtered). Add the isolation grep test: the new decision-explanation + Readiness path appears only behind the flag or in shadow/test code, never in a live flag-off call site.

Run the FULL suite.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -only-testing:WorkloadAppTests/ReasoningDecisionExplanationTests 2>&1 | tail -8</automated>
  </verify>
  <done>Decision-explanation method produces decomposable, confidence-annotated reasons. Legacy summarize unchanged. No-prediction-copy + isolation grep guards green. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| flag-off live recommendation → new matrix | The single most important boundary: flag-off MUST be byte-identical (golden fence). |
| ACWR value → decision | ACWR must NOT cross into the new decision; context-label only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | flag-off recommendation | mitigate | Byte-identical golden snapshot test gates the wave. |
| T-28-05 | Spoofing | ACWR re-entry | mitigate | Assert no warning/decision in flag-on path references ACWR. |
| T-28-06 | Repudiation | decision reasons | mitigate | Decomposability test: each reason ↔ named pre-update factor. |
| T-28-SC | Tampering | package installs | mitigate | No new packages; halt + human verify if any appear. |
</threat_model>

<verification>
- Byte-identical golden fence test green.
- Decision-explanation + legacy-summarize-unchanged tests green.
- Isolation grep + no-prediction-copy grep green.
- `BaselineTierFenceTests` + full `WorkloadAppTests` suite green via real xcodebuild.
</verification>

<success_criteria>
Autoregulation swap + decision explanation live behind the flag; flag-off byte-identical; ACWR demoted; honesty guards green. Committed atomically to main (no push).
</success_criteria>

<output>
Append SUMMARY entry (28-02 done).
</output>
