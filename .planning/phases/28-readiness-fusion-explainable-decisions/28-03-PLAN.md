---
phase: 28-readiness-fusion-explainable-decisions
plan: 03
type: execute
wave: 3
depends_on: [28-02]
autonomous: true
requirements: [PRS-28-06]
files_modified:
  - WorkloadApp/Services/ShadowPredictor.swift
  - WorkloadApp/Models/CyclePredictionLog.swift
  - WorkloadApp/Services/ShadowAnalyticsService.swift
  - WorkloadAppTests/PRSShadowArmTests.swift
must_haves:
  truths:
    - "ShadowPredictor emits a third competing prediction (PRS Readiness) alongside baseline and cycle-aware arms."
    - "CyclePredictionLog gains parallel *PRS fields that are LOCAL-ONLY and absent from any sync mapping (sync-omission test)."
    - "PRS predictions are written with correct predictionDate/targetDate/feature-cutoff/outcome-window (Phase-24 date contract), resolved against RAW self-report/soreness/adherence labels (never engine-derived recovery)."
    - "ShadowAnalyticsService reports PRS metrics alongside baseline/cycle-aware; no activation logic added (that is Phase 29)."
    - "The PRS arm runs unconditionally for shadow logging and is never user-facing; live recommendation unaffected."
  artifacts:
    - path: "WorkloadApp/Services/ShadowPredictor.swift"
      provides: "PRS-v1 third prediction arm (shadow only)"
    - path: "WorkloadApp/Models/CyclePredictionLog.swift"
      provides: "Local-only *PRS columns"
  key_links:
    - from: "ShadowPredictor PRS arm"
      to: "CyclePredictionLog *PRS columns"
      via: "prequential write keyed on predictionDate/targetDate"
---

<objective>
Wire the PRS-v1 predicting arm into the existing shadow harness: ShadowPredictor emits a third prediction (PRS Readiness from ReadinessFusionEngine), CyclePredictionLog stores parallel local-only *PRS fields, and ShadowAnalyticsService reports PRS metrics. Shadow-only, never user-facing. No activation gate logic (Phase 29).

Purpose: Begin collecting the prequential shadow data that Phase 29 will use to decide activation — using the Phase-24 date contract and RAW self-report labels (not circular engine-derived recovery).
Output: PRS arm in ShadowPredictor, local-only *PRS columns, PRS metrics in ShadowAnalyticsService, sync-omission + date-contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/algorithm-moat-design.md
@.planning/phases/28-readiness-fusion-explainable-decisions/28-RESEARCH.md
@WorkloadApp/Services/ShadowPredictor.swift
@WorkloadApp/Services/ShadowAnalyticsService.swift
@WorkloadApp/Models/CyclePredictionLog.swift
@WorkloadApp/Services/ReadinessFusionEngine.swift
@WorkloadApp/Services/SyncService.swift
@CLAUDE.md

<interfaces>
<!-- Read ShadowPredictor.swift / CyclePredictionLog.swift / ShadowAnalyticsService.swift fully first. -->
<!-- Existing arms: baselinePrediction (no cycle) and cycleAwarePrediction; aggregate computes per-outcome metrics with Phase-24 predictionDate/targetDate/cutoff/window. -->
<!-- CyclePredictionLog is ALREADY local-only/never-synced (D-13 posture) — verify it is absent from SyncService and keep the new *PRS columns the same. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PRS arm in ShadowPredictor + local-only *PRS columns</name>
  <files>WorkloadApp/Services/ShadowPredictor.swift, WorkloadApp/Models/CyclePredictionLog.swift, WorkloadAppTests/PRSShadowArmTests.swift</files>
  <behavior>
    - ShadowPredictor produces a PRS Readiness prediction (via ReadinessFusionEngine) alongside the existing baseline + cycle-aware arms for the same predictionDate/targetDate.
    - CyclePredictionLog *PRS fields store the PRS prediction parallel to *Baseline/*CycleAware; they are LOCAL-ONLY (sync-omission test: not present in SyncService's row mapping / not Codable-synced).
    - Date contract: PRS row written with predictionDate = feature-cutoff day, targetDate = outcome day; resolved against the outcome window (Phase-24 contract), never same-day self-resolution.
    - Labels: resolved against RAW self-report/wellness/soreness/adherence (NiggleInjuryDeriver / WellnessCheckIn), NOT engine-derived recovery score (codex §315).
    - Runs unconditionally (shadow logging) — independent of PRSActivation.isEnabled; produces no user-facing output.
  </behavior>
  <action>
Read ShadowPredictor + CyclePredictionLog + ShadowAnalyticsService + SyncService fully. Add a third PRS arm to ShadowPredictor that calls ReadinessFusionEngine for the predictionDate's prequential z inputs (yesterday's baseline state → today's deviation, no leakage — research §313). Add parallel `*PRS` fields to CyclePredictionLog mirroring the existing `*Baseline`/`*CycleAware` fields; keep them LOCAL-ONLY and absent from SyncService (GA-7). Resolve against RAW labels only.

Do NOT add any user-facing surface here. Do NOT touch AutoregulationEngine. The arm logs regardless of the flag (shadow discipline, research §219). Run the FULL suite (model + shared-type change).
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -only-testing:WorkloadAppTests/PRSShadowArmTests 2>&1 | tail -8</automated>
  </verify>
  <done>PRS arm logs a third prediction with correct date contract + raw labels. *PRS columns local-only (sync-omission test green). Full suite + tier fence green.</done>
</task>

<task type="auto">
  <name>Task 2: PRS metrics in ShadowAnalyticsService</name>
  <files>WorkloadApp/Services/ShadowAnalyticsService.swift, WorkloadAppTests/PRSShadowArmTests.swift</files>
  <action>
Extend ShadowAnalyticsService.aggregate to report PRS metrics (per-outcome MAE, plus the Phase-24 calibration/Spearman/blocked-CV machinery) alongside the existing baseline/cycle-aware metrics. Do NOT add activation-gate decision logic (that is Phase 29 — GA / research §4.4); only REPORT the numbers. Ensure PRS metrics are computed only over resolved rows with a valid date contract.

Add assertions to PRSShadowArmTests: aggregate returns PRS metrics; metrics are nil/empty (not crashing) when no resolved PRS rows exist; no activation flag is read or flipped by the analytics path. Run the FULL suite.
  </action>
  <verify>
    <automated>xcodebuild test -scheme "workload management" -only-testing:WorkloadAppTests/PRSShadowArmTests 2>&1 | tail -8</automated>
  </verify>
  <done>ShadowAnalyticsService reports PRS metrics; no activation logic added; graceful with zero resolved rows. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| shadow log → Supabase sync | *PRS columns MUST NOT cross to sync (local-only invariant). |
| prediction write → resolution | Date-contract boundary: feature cutoff vs outcome window (no same-day leak). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Information disclosure | *PRS columns | mitigate | Sync-omission test asserts absence from SyncService mapping. |
| T-28-08 | Tampering | date contract | mitigate | predictionDate/targetDate/window asserted; no same-day self-resolution. |
| T-28-09 | Repudiation | outcome labels | mitigate | Labels are raw self-report/soreness/adherence, not engine-derived. |
| T-28-SC | Tampering | package installs | mitigate | No new packages; halt + human verify if any appear. |
</threat_model>

<verification>
- PRS arm logs with correct date contract; sync-omission test green.
- ShadowAnalyticsService PRS metrics present; no activation logic.
- `BaselineTierFenceTests` + full `WorkloadAppTests` suite green via real xcodebuild.
</verification>

<success_criteria>
PRS-v1 arm collecting shadow data via the upgraded harness, local-only, no user-facing effect, no activation logic. Committed atomically to main (no push).
</success_criteria>

<output>
Append SUMMARY entry (28-03 done).
</output>
