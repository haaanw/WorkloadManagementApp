# Orchestration Plan — v1.6 Full UI/UX Polish ("Tuwa Editorial")

Date opened: 2026-07-14. Orchestrator: Claude (session). Workers: serial agent lanes in canonical checkout (NO worktrees, NO branches — standing rule). Orchestrator verifies every build claim and commits per lane.

## User decisions (2026-07-13/14)
- D1: Active milestone = full UI design + animation polish; ships as **App Store v1.6** (live store = v1.5). Color system UNCHANGED.
- D2: Taste reference = contralabs.com. Direction = "Serif + soften corners" (new design language, "Tuwa Editorial"). **Treatment PICKED 2026-07-14: "Ink & Grain"** (`.planning/design-reference/tuwa-c3-vs-d1.html`, middle column) —
  - Serif = **Source Serif 4** (bundle variable font), DISPLAY ONLY: readiness score + verdict headline; app-authored strings only, NEVER user content (avoids CJK mixing). Weight 400, tracking ≈ -0.03em, line-height ≈ 0.95. Everything else stays General Sans.
  - Corners: cards/plates **12pt**, CTA **pill (999)**; full CornerTokens scale defined in Stage 0. 0pt-everywhere rule RETIRED.
  - Halftone signature: accent dots r≈1.2 @ 8px grid, opacity ≈0.45, 135° fade mask — **HERO PLANE ONLY** (the one-sentence rule; must be fenced).
  - CTA: filled accent pill. NOTE: this + dots requires rewriting the accent rule in DESIGN.md v3 (accent = hero number + verdict CTA + halftone signature; still never decorative elsewhere).
  - Hero scale ≈ C3 (88px-mock score; on-device pt tuning in Stage 1). Metrics stay on standard plane. Palette/light-only/no-shadows/8pt grid all unchanged.
- D3: **REHOST the SwiftUI tree as the live app; retire the UIKit shell.** Shell code stays in target until v1.6 is validated on-device, then deleted in a cleanup commit. (Basis: delta audit 2026-07-14 — motion system, v1.5.2 logging redesign, 4-way picker, import suite all SwiftUI-only; shell exclusives mostly have existing SwiftUI twins.)
- D4: 5-phase polish plan GREEN-LIT (see Stages 0–4), applied to the SwiftUI tree post-rehost.
- D5: Audience re-affirmed: amateur hybrid athletes (lift + basketball + occasional other sports); founder = reference user.

## Ground truth (verified this session)
- Live mount point: `WorkloadApp/App/AppRouter.swift:33` → `AppShell()` (UIKit). SwiftUI Views/ tree compiles in target but is unmounted (zero UIHostingController).
- ALL FIVE commits of the 2026-07-10 round touched only the SwiftUI tree + Services — invisible in the running app. Rehost makes them live.
- Tuwa is **light-only** (ColorTokens.swift forces light appearance). CLAUDE.md dark-mode line is stale.
- Build gate: `xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination "id=8E872500-703D-4292-9758-38ADFCCFB126" -derivedDataPath ~/.tonus-dd build` (NEVER in-repo build/).
- New APP source files MUST be registered in project.pbxproj. Test files under WorkloadAppTests/ auto-sync.
- Strings: `Localizable.xcstrings` additive edits only; never regenerate the catalog.
- NSHealthUpdateUsageDescription removed from pbxproj 2026-07-14 (app is HealthKit read-only, `toShare: []`).

## Stage sequence

### Stage R — Rehost SwiftUI tree (prerequisite for all polish stages)
Mount the SwiftUI app in AppRouter; make every v1.5 shell capability reachable in SwiftUI. Delta audit findings (session 2026-07-14) drive the checklist:
1. AppRouter: replace `AppShell()` with SwiftUI MainTabView flow (auth → onboarding → tabs state machine).
2. Foreground sync: port shell's scenePhase-active sync trigger into the SwiftUI flow.
3. Onboarding: wire OnboardingView routing (post-signup → questionnaire → tabs).
4. Mount existing-but-orphaned SwiftUI twins: TodayVerdictCard (already in WorkoutLogView — verify reachable), NextMatchSection, VerdictMeasurementView.
5. Verify shell-exclusive deltas: plan-input surface, measurement read/record, verdict persistence (VerdictEvent WRITE path).
6. Coach mode: OUT (self-coached reset is the product intent; shell carrying coach UI was the deviation). Do NOT port coach surfaces.
7. Shell stays compiled in target (unused) until v1.6 ships.
Gate: build green + full unit suite + orchestrator on-sim smoke of all tabs.

### Stage 0 — DESIGN.md v3 ("Tuwa Editorial")
Rewrite corner + type rules per treatment pick; palette unchanged; light-only affirmed; update CLAUDE.md design constraints + design-fence tests; add CornerTokens; extend FontTokens with serif display styles; bundle chosen serif font.

### Stage 1 — Token/primitive layer
Serif display heads; corner radii on buttons/cards/inputs; unified press-feedback; type-scale updates in CardStyle/FontTokens.

### Stage 2 — Motion pass
Unified timing grammar; hero count-up live; VC/tab/section transitions; field-focus feedback; skeleton loading; haptics coverage. (SwiftUI Motion system is the substrate — extend, don't rebuild.)

### Stage 3 — Per-screen craft
Dashboard hierarchy, section rhythm, editorial whitespace, halftone signature (if picked), empty/loading/error states.

### Stage 4 — Final gate
Full test suite + design-fence + /codex cross-review + on-device dogfood pass. Then shell-deletion cleanup commit AFTER v1.6 validated.

## Known debt
- **ScreenshotTests (XCUITest, 49 tests) assert the retired shell's identifiers** — all fail post-rehost by design ("UIKit loading surface missing" etc.). Rewrite against the SwiftUI surface in Stage 4 (they also power the ASO screenshot harness, which needs redoing for v1.6 visuals anyway). Unit suite: 758/758 green post-rehost.
- Lane workers must NEVER run `xcodebuild test` — orchestrator runs the suite (2026-07-14 incident: three concurrent xcodebuild runs thrashing one DerivedData).

## Status log
- [x] Stage R dispatched (2026-07-14)
- [x] Stage R verified + committed (2026-07-14: AppRouter rehost — LaunchLoadingView/LoginView/OnboardingView/MainTabView, scenePhase sync mirrored, coach mode intentionally absent; unit suite 758 green / 49 expected ScreenshotTests failures / 2 skipped; orchestrator-verified after killing 3-way concurrent xcodebuild contention)
- [x] Treatment picked: **Ink & Grain** (see D2)
- [x] Stage 0 dispatched (brief: `stage-0-brief.md`)
- [x] Stage 0 verified + committed (2026-07-14, `bdbee4f`: DESIGN.md v3 + Source Serif 4 bundle + CornerTokens + HalftoneField + fences flipped; unit suite 761/0/2, orchestrator-run with -skip-testing:ScreenshotTests)
- [x] Stage 1 dispatched (token/primitive adoption)
- [x] Stage 1 verified + committed (2026-07-14, `0a3c95b`: primitives on CornerTokens, serif hero 88pt + halftone, accent pill CTAs, verdict card serif voice w/ nocebo guard preserved; sim renders orchestrator-reviewed; suite 762/0/2)
- [x] Stage 2 dispatched (motion pass)
- [x] Stage 2 verified + committed (2026-07-14, `842a8af`: route/tab crossfades, verdict settle, count-up verified on video, 15 RM bypasses fixed, Motion Law fences, SkeletonBlock; suite 764/0/2)
- [x] Stage 3 dispatched (per-screen craft; first dispatch 2026-07-14 died on subagent usage-credit exhaustion — zero work; re-dispatched 2026-07-17 after orchestrator did the 4 attention banners inline + added shared `attentionBannerStyle` to CardStyle)
- [x] Stage 3 verified + committed (2026-07-17: 54 files — square-residue sweep across import/share/export/profile/onboarding/upgrade surfaces, Dashboard hero hierarchy (periodization demoted to compact inline-ring row), recent-sessions grouped plate, Auth fields+pill CTAs + LocalizedStringKey-placeholder autolink bugfix, section rhythm, em-dash nits; xcstrings 1 value-only edit; suite 764/0/2 orchestrator-run via xcresulttool; 11 screenshots reviewed, 5 re-reviewed by orchestrator)
- [ ] **Stage 3.5 (found during Stage 3 verify): instrument-voice font resolution broken** — `FontTokens.cascaded` requests PS names `GeneralSans-Regular`/`GeneralSans-Medium`, but GeneralSans-Variable.ttf (since phase 14) has NO instance postscriptNameIDs and its own PS name is `GeneralSansVariable-Bold` (fvar default wght=700). Plain-DEBUG launch traps on the WorkloadApp.swift font assert; body type silently falls back to system font. Fix = resolve via registered PS name + kCTFontVariationAttribute (mirror Stage 0's serifDisplay pattern), update assert names, re-verify visually + full gate.
