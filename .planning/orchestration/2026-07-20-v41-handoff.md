# v4.1 Polish Round — Handoff Plan (3 parallel sessions)

Date: 2026-07-20. Orchestrator: the main session (verifies + commits everything). Workers: 3 user-dispatched Claude Code sessions, ALL in the canonical checkout `/Users/hanwen/dev/Tonus` (no worktrees, no branches).

## User decisions (this round)
- **D12:** Tab bar → **B "Console"** (11px title-case Medium labels, weight-shift on select, sliding well + springy red tick, per-item press-down 0.94). Demo: `.planning/design-reference/tuwa-v4-polish-demos.html` §1-B.
- **D13:** Five-primitive interaction law ratified WITH adjustments: (a) haptics reduced — stepper taps silent, haptic only at min/max limits + toggle flips + the Home hero count-up detents; (b) digit roll faster/subtler (~100ms, small travel); (c) **fixed-width dial value cells** — a value container NEVER resizes when digit count changes (reserve width for the widest realistic reading); (d) **needles/numbers never return to zero** — value changes animate current→new directly (79→42 goes straight down); sweep-from-zero only on first appearance; (e) sweeps only on real moments (screen load / explicit re-measure) — minor updates settle instantly; (f) sheet entrances simple — surface moves as one unit, NO content stagger.
- **D14:** Layout recomposition rolls across ALL five tabs + key sheets (demo §3 "After" direction: baseline-aligned readouts, metric grids with unit superscripts + delta lines, ruled section headers, varied density).
- **D15:** 3 parallel sessions, disjoint file ownership, orchestrator verifies + commits per workstream.

## Coordination rules (ALL sessions — violations break parallel safety)
1. **NEVER run any `git` command.** Leave all work uncommitted in the tree. The orchestrator commits.
2. **NEVER run `xcodebuild test`** — build only: `xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination "id=8E872500-703D-4292-9758-38ADFCCFB126" -derivedDataPath ~/.tonus-dd build`
   Before EVERY build/launch: `pgrep -fl xcodebuild` — if another build is running, wait and retry. Never build concurrently with another session.
3. **Touch ONLY the files your workstream owns** (lists below). `project.pbxproj` is owned by NOBODY — the stubs `Components/LayoutPrimitives.swift` and `Components/SheetChrome.swift` are already registered. `Localizable.xcstrings`: do not edit; prefer existing keys; if a new label is unavoidable use `String(localized:defaultValue:)` in code and note it — if the catalog auto-churns during your build, leave it, the orchestrator reconciles.
4. **AppShell*.swift and Views/Coach/ are dead code — never touch.**
5. Fences (orchestrator runs them): corners only via `CornerTokens`; `IBMPlexMono` named only in FontTokens.swift; `SourceSerif4` banned; no `.shadow()`; no `.system(` fonts in Views/Components; structural spacing on the 8pt grid; animation/timing literals ONLY in `Components/CardStyle.swift` (elsewhere use `Motion.*` tokens); max ONE `panelStyle(` per Views/ file.
6. **Verify visually**: SCREENSHOT_MODE launch on sim `8E872500-703D-4292-9758-38ADFCCFB126`; screenshot every touched surface; for motion work, record video (`xcrun simctl io <sim> recordVideo out.mp4`, SIGINT to stop; `axe` CLI taps — tab items at y≈920, x=44/132/220/308/396). LOOK at your output and iterate.
7. **Report**: write your final report to `.planning/orchestration/ws<N>-report.md` (files touched per item, screenshots/video paths, build tail verbatim, deferrals). Do not self-assess "done" without renders.
8. These a11y IDs MUST survive: `dashboard.hero`, `recovery.scoreCard`, `workload.acwr`, `workoutLog.verdict.reason`, `workoutLog.verdict.strikeZone`, `workoutLog.startWorkout`, `activeWorkout.addExercise`, `profile.movementBank`, `tab.home/log/recovery/load/profile`, `tabbar.ink`, `export.workoutData`, `templatePicker.startBlank`, `app.loading.view`. Never place a container accessibilityIdentifier above a leaf ID (SwiftUI clobbers descendants).
9. The verdict card's anti-nocebo rules are law (TodayVerdictCardGuardTests): Accept/Keep-plan equal weight, no red gates, no accent on verdict state or number.

## File ownership matrix
| | WS1 Fabric | WS2 Layout | WS3 Sheets/Controls |
|---|---|---|---|
| Components/CardStyle.swift | ✅ | ❌ | ❌ |
| Components/TickScale.swift | ✅ | ❌ | ❌ |
| Components/InkTabBar.swift | ✅ | ❌ | ❌ |
| DESIGN.md + DesignSystemFenceTests.swift | ✅ | ❌ | ❌ |
| Components/LayoutPrimitives.swift (stub) | ❌ | ✅ | ❌ |
| Views/Dashboard/*, Views/Recovery/RecoveryView, Views/Workload/WorkloadView, Views/Profile/ProfileView, Views/WorkoutLog/WorkoutLogView + TodayVerdictCard | ❌ | ✅ | ❌ |
| Components/SheetChrome.swift (stub) | ❌ | ❌ | ✅ |
| Sheets: ActiveWorkoutSheet, MorningCheckInSheet, NiggleLogSheet, TrainingProfileSheet, UpgradeSheet, Workout/Text/Share import sheets, FinishWorkoutSheet, TemplatePickerSheet, PlanTodaySheet | ❌ | ❌ | ✅ |
| Controls: SetStepper, WeightBlockPicker, RepScrubber, SessionStartPicker, TimeRangeSegmentedControl, RadialPicker | ❌ | ❌ | ✅ |
| Utilities/ColorTokens.swift (chart* tokens ONLY) | ❌ | ❌ | ✅ |

Anything not listed: don't touch it; if your work genuinely requires it, note it as a deferral instead.

---

## PROMPT — WS1 · Interaction Fabric + Console Tab Bar
*(paste verbatim into session 1)*

You are workstream WS1 of the Tuwa v4.1 polish round, repo /Users/hanwen/dev/Tonus. Read FIRST: `.planning/orchestration/2026-07-20-v41-handoff.md` — obey the Coordination rules and your file-ownership row exactly. Then read DESIGN.md (v4 "Instrument") and the interactive demo `.planning/design-reference/tuwa-v4-polish-demos.html` (§1-B Console bar + §2 fabric are your ground truth; open in a browser via `open`).

Your mission — the app-wide interaction fabric (files: Components/CardStyle.swift, Components/TickScale.swift, Components/InkTabBar.swift, DESIGN.md, WorkloadAppTests/DesignSystemFenceTests.swift):
1. **DESIGN.md v4.1 amendment**: write the Five-Primitive Interaction Law with the user's adjustments (handoff doc D13) — Key (scale 0.97 + brighten, 120ms), Row (touch-down well ~6% ink, 110ms, no scale), Detent control (mechanical snap; digit roll ~100ms subtle; haptics ONLY at min/max limits + toggle flips; fixed-width value cells that never resize with digit count), Needle (sweep only on load/re-measure; changes animate current→new, NEVER via zero; small updates settle instantly), Surface (one-unit entrance, no stagger, origin-aware, interruptible).
2. **CardStyle.swift**: implement the vocabulary as reusable styles — extend PressableButtonStyle for the Key spec; add a `RowWellButtonStyle` (background well on isPressed, no scale); add a `tickSpring` Motion token (the ONE sanctioned overshoot curve, for the tab tick only); a `DialValueCell`-style helper that reserves max-width via a hidden template reading so value width never changes; retune digit-roll/`contentTransition` guidance constants. Haptic policy helpers per D13.
3. **InkTabBar.swift → Console** (demo §1-B): labels 11pt title-case Medium (tracking modest, zh-aware), selected = text1 + Medium→Semibold-feel weight shift (stay within Regular/Medium law — use color+size, not fake bolding, if semibold violates the two-weight law; judgment call, document it), sliding "well" behind the active item + the red tick sliding on `tickSpring`, per-item press-down 0.94 via your new style. Bar architecture/IDs unchanged.
4. **TickScale.swift**: add sweep policy — `sweepOnAppear` (first render 0→value allowed) vs value-change interpolation current→new (Animatable already does this — VERIFY no path resets to zero, including the Dashboard count-up's re-load path); detent haptics restricted to the Home hero per D13 (make detents opt-in via parameter, default off).
5. **Fences**: extend the animation fence if you add tokens; add a fence for the fixed-width rule if mechanically checkable (else document in DESIGN.md only).
Verify per Coordination rule 6 (screenshots + a short video of: tab walk with the new bar, a key press, a row press, stepper roll on any existing stepper). Report to `.planning/orchestration/ws1-report.md`.

---

## PROMPT — WS2 · Layout Recomposition (5 tabs)
*(paste verbatim into session 2)*

You are workstream WS2 of the Tuwa v4.1 polish round, repo /Users/hanwen/dev/Tonus. Read FIRST: `.planning/orchestration/2026-07-20-v41-handoff.md` — obey the Coordination rules and your file-ownership row exactly. Then DESIGN.md (v4 "Instrument") and the demo `.planning/design-reference/tuwa-v4-polish-demos.html` §3 (Before/After — the After column is the ratified direction, decision D14).

Your mission — give every tab a considered composition (files: Components/LayoutPrimitives.swift stub + the five tab view files per the ownership matrix; do NOT touch CardStyle/TickScale/InkTabBar — WS1 owns them; use existing shared styles and Motion tokens as-is):
1. **LayoutPrimitives.swift**: build `MetricCell` (micro-caps label, dial numeral with unit superscript, delta line; FIXED width behavior — the cell must not resize when digit count changes) and `RuledSectionHeader` (micro-caps + trailing 0.5pt hairline rule), plus any shared composition pieces you need. Tokens only.
2. **Home (DashboardView)**: the demo-§3 After composition — panel readout baseline-aligned (score left, zone stack right), tick numerals under the hero scale, RHR/HRV/Sleep as a 3-cell MetricCell grid, ruled section headers, recent sessions as two-line rows (name + meta line, mono value right). Keep: count-up, skeleton, `dashboard.hero` ID, PRSDualRunCard's quiet line, cold-start guards, all render conditions — this is composition, NOT behavior change.
3. **Log (WorkoutLogView + TodayVerdictCard)**: verdict card internal grid — baseline-align the dial weight with its caption, state label optically aligned to the header line; filter rail + template/history sections onto ruled headers + varied density. Nocebo law: Accept/Keep equal weight (guard tests will run).
4. **Load (WorkloadView)**: ACWR panel readout composed like Home's; ATL/CTL/TSB as the MetricCell grid; ruled headers.
5. **Recovery (RecoveryView)**: give it a designed peak WITHOUT a panel (Panel Law: Home owns it) — e.g. the score card gets the baseline readout treatment + a micro TickScale strip; factor rows to MetricCell grid where it reads better; ruled headers.
6. **Profile (ProfileView)**: ruled headers, grouped rows with meta lines where data exists; quiet, consistent.
Preserve every a11y ID in Coordination rule 8. Verify per rule 6 (screenshot all five tabs, top + scrolled). Report to `.planning/orchestration/ws2-report.md`.

---

## PROMPT — WS3 · Sheet Chrome + Controls + Charts
*(paste verbatim into session 3)*

You are workstream WS3 of the Tuwa v4.1 polish round, repo /Users/hanwen/dev/Tonus. Read FIRST: `.planning/orchestration/2026-07-20-v41-handoff.md` — obey the Coordination rules and your file-ownership row exactly. Then DESIGN.md (v4 "Instrument") and the demo `.planning/design-reference/tuwa-v4-polish-demos.html` (§2 fabric spec; sheet law = decision D13-f: one-unit entrance, no content stagger).

Your mission — kill the last stock-iOS leaks (files per the ownership matrix: Components/SheetChrome.swift stub, the listed sheet files, the listed control files, chart* tokens in ColorTokens.swift):
1. **SheetChrome.swift**: an instrument sheet header — micro-caps title (13pt Medium, wide tracking, uppercase, zh-aware), quiet caps Cancel/primary-action slots (Row/Key press behavior via existing shared styles), 0.5pt bottom hairline, flat opaque background — replacing stock `navigationTitle` + glass buttons on sheets. Detents/`presentationDragIndicator` judgment per sheet.
2. **Apply** to the owned sheets (ActiveWorkoutSheet first — the most-used flow; then MorningCheckInSheet, NiggleLogSheet, TrainingProfileSheet, UpgradeSheet, the import sheets, FinishWorkoutSheet, TemplatePickerSheet, PlanTodaySheet). Keep every action, binding, and a11y ID; this is chrome, not behavior.
3. **Controls detent pass** (SetStepper, WeightBlockPicker, RepScrubber, SessionStartPicker, TimeRangeSegmentedControl, RadialPicker): press states per the fabric (§2), value changes roll ~100ms subtle via `contentTransition(.numericText)` where numeric, FIXED-width value areas (never resize with digit count — reserve max width), haptics ONLY at min/max limits (strip any per-tap haptics; keep existing `Haptics.select` on discrete pickers' detents only if they are true detents — document each choice). Use `Motion.*` tokens only — NO animation literals outside CardStyle (fenced; if you need a new token, note it as a deferral for WS1/orchestrator instead of adding it).
4. **Chart palette** (ColorTokens chart* tokens ONLY): retune the olive/moss chart hues to aluminum-compatible instrument hues (cool-neutral inks + one muted supporting hue; keep ≥3:1 distinguishability and current semantic pairing; document hex choices). Do not touch non-chart tokens.
Verify per Coordination rule 6 (screenshots of every restyled sheet + a video of ActiveWorkoutSheet present/dismiss and one stepper interaction). Report to `.planning/orchestration/ws3-report.md`.

---

## Orchestrator verification protocol (after each WS reports)
1. Read `wsN-report.md`, review screenshots/videos myself, spot-check diffs (ownership compliance first: `git status` paths vs matrix).
2. Full gate: `xcodebuild … test` → xcresulttool summary (783 baseline; fences + guard tests + 13 UI tests must stay green).
3. Path-scoped commit per workstream (`feat(v4.1-wsN): …`). xcstrings churn reconciled by me.
4. All three landed → combined visual review + report to user → user re-dogfood.

## Status
- [x] Demos built + user decisions D12–D15 captured
- [x] Stubs pre-registered in pbxproj (LayoutPrimitives, SheetChrome) — build green
- [x] WS1 / WS2 / WS3 executed by user-dispatched parallel sessions (all ownership-compliant, zero pbxproj/xcstrings churn, no collisions)
- [x] Orchestrator verification: reports read, renders reviewed (Console bar, Home baseline readout, Recovery peak, ActiveWorkout instrument header), one cross-boundary integration fix applied (Home hero `detents: true` per WS1's note), /tmp artifacts preserved+trimmed into repo
- [x] Combined gate: **784 tests — 782 passed / 0 failed / 2 skipped** → landed as three path-scoped commits `03331d3` (WS1) / `0a960ce` (WS2) / `8d6da07` (WS3)
- **→ AWAITING USER RE-DOGFOOD** (detent haptics + press feel need hardware). Open threads: optional 11pt-Regular tab token (FontTokens); TemplateCarouselSection/NextMatchSection 19pt headers (unowned in this round); ShareImportPreviewSheet distinct title key (xcstrings); RowWellButtonStyle/DialValueCell delivered but adoption beyond WS scopes is follow-up.
