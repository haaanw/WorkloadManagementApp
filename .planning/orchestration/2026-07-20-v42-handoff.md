# v4.2 "Machined" Round — Handoff Plan (2 parallel sessions)

Date: 2026-07-21. Orchestrator: main session (verifies + commits). Workers: 2 user-dispatched Claude Code sessions in the canonical checkout `/Users/hanwen/dev/Tonus` (no worktrees/branches).

## User decisions
- **D16 (motion):** FULL spring refit — all Motion tokens to non-bouncy springs; the dip-crossfade is banned (no content may pass through full invisibility); asymmetric press (fast in, spring settle out); rise+stagger entrances. Demo: `.planning/design-reference/tuwa-v42-premium-demos.html` §1 (B variants).
- **D17 (forms):** Machined form system APP-WIDE, at the ultra bar: Relief Law surfaces, readout wells, inline machined option cells replacing every stock iOS `Menu`, focus wells, machined toggle, press-inversion keys. Demos: `tuwa-v42-ultra-forms.html` + tuning-board picks (1-B milled / 2-B debossed / 3-B round machined toggle / 4-A press inversion / 5-A machined option cells).
- **D18 (execution):** 2 handoff prompts, disjoint ownership, orchestrator verifies + commits.

## Pre-work (already committed by the orchestrator — build green)
- `ColorTokens`: `reliefHighlight`, `reliefHighlightSoft`, `reliefShade`, `wellTop`, `wellBottom`.
- `CardStyle.swift`: **`.raised(cornerRadius:)` / `.debossed(cornerRadius:)`** Relief modifiers (gradients + inner stroke lines — zero `.shadow()`). USE THESE — never hand-roll relief.
- `Components/InstrumentForm.swift` stub, pbxproj-registered (nobody touches pbxproj).
- DESIGN.md **v4.2 "Machined" amendment** — the law text both workstreams implement. Read it first.

## Coordination rules (BOTH sessions)
1. NEVER run any `git` command. Leave work uncommitted; write your report to `.planning/orchestration/wsA-report.md` / `wsB-report.md`.
2. NEVER run `xcodebuild test`. Build only: `xcodebuild -project "workload management/workload management.xcodeproj" -scheme "workload management" -destination "id=8E872500-703D-4292-9758-38ADFCCFB126" -derivedDataPath ~/.tonus-dd build` — and check `pgrep -fl xcodebuild` first; never build while the other session is building.
3. Touch ONLY files in your ownership list. `project.pbxproj`, `Localizable.xcstrings`, `ColorTokens.swift`, AppShell*, Views/Coach/ → NOBODY. Prefer existing localized keys; unavoidable new labels via `String(localized:defaultValue:)`, noted in the report.
4. Fences: corners via CornerTokens only; animation/timing literals ONLY in CardStyle.swift (Motion tokens elsewhere); `IBMPlexMono` only in FontTokens.swift; `SourceSerif4` banned; no `.shadow()`; no `.system(` in Views/Components; 8pt-grid spacing; ≤1 `panelStyle(` per Views file; `tickSpring` referenced only by CardStyle+InkTabBar.
5. Verify visually: SCREENSHOT_MODE on sim `8E872500-703D-4292-9758-38ADFCCFB126`; screenshot every touched surface; RECORD VIDEO for anything that moves (`xcrun simctl io <sim> recordVideo out.mp4`, SIGINT; `axe` taps — tab row y≈920, x=44/132/220/308/396). Look and iterate.
6. a11y IDs must survive: `dashboard.hero`, `recovery.scoreCard`, `workload.acwr`, `workoutLog.verdict.reason/.strikeZone`, `workoutLog.startWorkout`, `activeWorkout.addExercise`, `profile.movementBank`, `tab.*`, `tabbar.ink`, `export.workoutData`, `templatePicker.startBlank`, `app.loading.view`. Never a container ID above a leaf.
7. Verdict card anti-nocebo rules are law (TodayVerdictCardGuardTests).

## Ownership matrix
| | WS-A Motion | WS-B Forms |
|---|---|---|
| Components/CardStyle.swift (Motion tokens, press styles, crossfades, EntranceReveal) | ✅ | ❌ |
| Components/InkTabBar.swift, Components/TickScale.swift | ✅ | ❌ |
| App/AppRouter.swift (route/tab transitions only) | ✅ | ❌ |
| WorkloadAppTests/DesignSystemFenceTests.swift | ✅ | ❌ |
| Components/InstrumentForm.swift (stub) | ❌ | ✅ |
| Views/Profile/* (ProfileView, TrainingProfileSheet, LanguagePickerView, SyncStatusView) | ❌ | ✅ |
| Views/Onboarding/OnboardingView.swift | ❌ | ✅ |
| Views/Recovery/MorningCheckInSheet.swift + NiggleLogSheet.swift (form guts) | ❌ | ✅ |
| Components/SetStepper.swift, WeightBlockPicker.swift (readout-well adoption) | ❌ | ✅ |
| Views/Dashboard/*, Views/Workload/*, Views/WorkoutLog/* | ❌ (motion via shared styles only) | ❌ except WorkoutLog sheets' `Menu` replacements — list each in report |
| DESIGN.md | ❌ (law already written) | ❌ |

---

## PROMPT — WS-A · Spring Motion Refit
*(paste verbatim into session 1)*

You are workstream WS-A of the Tuwa v4.2 "Machined" round, repo /Users/hanwen/dev/Tonus. Read FIRST: `.planning/orchestration/2026-07-20-v42-handoff.md` (Coordination rules + your ownership row) → DESIGN.md's "v4.2 Machined amendment" (the Spring Motion Law you implement) → the demo `.planning/design-reference/tuwa-v42-premium-demos.html` §1 (open via `open`; the B variants are your spec) → `tuwa-v42-tuning-board.html` row 4 (press pick = A · Inversion).

Mission — make the app's motion premium-smooth (files: CardStyle.swift, InkTabBar.swift, TickScale.swift, AppRouter.swift, DesignSystemFenceTests.swift):
1. **Motion tokens → non-bouncy springs** (CardStyle only): `state`/`entrance`/`screen`/`exit`/`tabSwitch` etc. become `.spring(duration:…, bounce:0)`-class (interruptible, velocity-preserving); keep durations in the current fast band (≤0.4s settle); `tickSpring` keeps its overshoot (sole exception); `scoreCountUp`/`digitRoll` may stay eased ramps. Document each with the law reference.
2. **Kill the dip-crossfade**: rebuild `TabCrossfadeModifier` so NO frame renders content fully invisible — incoming content rises ~6px + fades IN OVER the previous frame (demo §1-B). Same for the AppRouter route `.transition(.opacity)` if it dips. Verify by frame-extracting your video (ffmpeg) — zero empty-card frames.
3. **Asymmetric press everywhere**: press-in ~85–120ms fast curve, release ~300ms spring settle, via the shared styles (PressableButtonStyle/.key/.rowWell). **Press-inversion for keys** (pick 4-A): pressed state swaps the raised relief for a debossed look (use the existing `.raised`/`.debossed` grammar — highlight dies, inner shade appears, 0.5px y-offset, brightness lift). KeyRow CTA cells, PrimaryActionButton, SheetHeaderButton emphasis slots inherit.
4. **Entrances**: EntranceReveal → 7px rise + fade, spring settle, 45ms stagger (cap kept). No scale-pop.
5. **Glides**: InkTabBar well + tick, TickScale needle — confirm they ride springs and decelerate into place (no hard arrival).
6. **Fence**: keep animation literals CardStyle-only (update the fence if the spring syntax needs allowing there; it must still ban literals elsewhere).
Verify per rule 5 (video of: tab walk, key press-and-hold, list entrance, verdict accept; watch frame-by-frame). Report → `.planning/orchestration/wsA-report.md` (token table before→after, dip-kill proof frames, files touched, build tail, deferrals).

---

## PROMPT — WS-B · Machined Form System
*(paste verbatim into session 2)*

You are workstream WS-B of the Tuwa v4.2 "Machined" round, repo /Users/hanwen/dev/Tonus. Read FIRST: `.planning/orchestration/2026-07-20-v42-handoff.md` (Coordination rules + your ownership row) → DESIGN.md's "v4.2 Machined amendment" (the law you implement) → the demos via `open`: `.planning/design-reference/tuwa-v42-ultra-forms.html` (THE bar — your output must feel like this) and `tuwa-v42-tuning-board.html` (picks: 1-B milled, 2-B debossed, 3-B ROUND machined toggle, 5-A machined option cells).

Mission — build the machined form system and deploy it app-wide (files per your ownership row; the `.raised`/`.debossed` Relief modifiers ALREADY EXIST in CardStyle.swift — use them, never hand-roll relief, never edit CardStyle):
1. **InstrumentForm.swift** (stub is pbxproj-registered): `InstrumentFormRow` (56pt, label + trailing content, `.rowWell` press, optional drawn 1.5px tick-chevron rotating on `Motion.state`); `ReadoutWell` (fixed-width debossed pocket: mono `dialSmall` value + micro-caps unit; reserve the widest realistic reading — never resizes); `InlineOptionList` (debossed channel expanding on `Motion.state`, holding butted `.raised` option cells with drilled selection dots — ink dot ring; selecting updates the row's readout and collapses after ~200ms); `FormField` (right-aligned text field that grows a debossed well + 1pt ink border on focus); `MachinedToggle` (ROUND polished knob riding a debossed channel; channel turns ink when on + engraved `panelInk` index tick appears; one `Haptics.tap()` per flip); destructive row helper (zone-danger text, quiet well press).
2. **ProfileView**: replace `editableTextField`/`editablePicker`(stock `Menu`)/notification day–time rows/language row/Sign-out/Delete with the system. EVERY stock `Menu` dies. Keep all bindings, save paths, `profile.movementBank` ID.
3. **TrainingProfileSheet + OnboardingView + MorningCheckInSheet + NiggleLogSheet**: same sweep — selects → `InlineOptionList` rows, fields → `FormField`, toggles → `MachinedToggle`, values → `ReadoutWell`.
4. **SetStepper + WeightBlockPicker**: adopt `ReadoutWell` for their value displays (keep their step/detent logic and haptic policy exactly as-is).
5. **Remaining `Menu` audit**: `grep -rn "Menu {" WorkloadApp/Views WorkloadApp/Components` — replace in files you own; list any in files you DON'T own as deferrals (do not touch).
Motion: only `Motion.*` tokens (no literals — fenced). Verify per rule 5 (screenshots of Profile top+scrolled, an open option list, focused field, toggle both states, TrainingProfile sheet, Onboarding step, MorningCheckIn; short video of open→select→collapse + toggle flip). Report → `.planning/orchestration/wsB-report.md`.

---

## Orchestrator protocol
Reports in → ownership check (`git status` vs matrix) → visual review → full gate (784 baseline) → path-scoped commits (WS-A first) → combined report → user re-dogfood.

## Status
- [x] Demos + tuning board; D16–D18 captured
- [x] Pre-work committed: relief tokens + `.raised`/`.debossed` + InstrumentForm stub + DESIGN.md v4.2 law (build green)
- [ ] WS-A / WS-B dispatched by user · reports pending
